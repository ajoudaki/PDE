# Neutral independent integration review: signed fixed-model risk edition

Review only this assignment, the frozen manifest, complete proposed edition,
before/after guides and numerical correspondence inputs, and required skills.
Do not read study history, prior reports/verdicts, selection findings, another
reviewer's findings, or perform author startup. Be fresh and distinct from
all authors, assemblers, selector and the paired scientific reviewers. Root
alone may write Git. Write only your assigned full report and generated scratch.

The proposed edition adds one signed C.4 subsection to global_nonlinear.md,
one fixed-certificate tool with private kernel and focused checks, and minimal
corresponding guide changes. The complete new content must be inspected,
including every new proof/code/test/guide line. Read the full notation and
guides, the complete provided operative dependency units and adjacent old
material needed to judge integration. State the precise older read scope and
unread complement; this is not a whole-book proof audit. Check interfaces,
canonical notation, references, summaries, placement, duplication, theorem
scope, scientific assumptions and preservation of all unselected material.

Independently verify the frozen manifest, full old chapter prefix, exact
unchanged base files, before/after guide scope, source-to-destination mapping,
code API and output/failure/resource contracts. The maintained certificate
must regenerate from its own inputs without study paths, chat/history, Git,
or archived arrays. Inspect the original numerical producer and verify direct
mathematical correspondence with the maintained source. Runtime/provenance
plumbing may differ; a numerical algorithm/error/input change cannot inherit
the old execution evidence silently.

From the standalone edition, run relevant focused exact/supplied-state checks,
new guide import/help/interface examples where they do not evaluate the full
coefficient, local-link/fragment checks and failure boundaries. The two full
coefficient calculations in the scientific budget are already complete.
Do not run another coefficient grid, resolution, training or witness search.
Both full raw runs and original numerical source are supplied for independent
execution/correspondence checks. Exact arithmetic, saved-bit reconstruction
and supplied-rule tests are allowed, at most one CPU minute per check. The
new result is a computer-assisted inequality, not an empirical training claim.
Report missing evidence or a required fresh calculation as a specific gap.

No established live file may be edited. Use only the assigned fresh scratch,
and never overwrite frozen numerical inputs. Retain unique verification
source in the report or an assigned flat study source, plus exact commands,
input/output hashes, environment, outcomes and limitations. Use the required
rigor skills yourself. Any required correction blocks acceptance; preserve
the old packet/report. Scientific changes reopen the paired scientific gate.
The report must distinguish scientific acceptance, integration acceptance,
and the separate final user approval. Neither a prior PASS nor an output
flag is evidence for this independent review.
