# Neutral complete review: proposed C.4 and guide sentence

This is one of two fresh complete isolated scientific reviews required by
RESEARCH_WORKFLOW Part 2. It is not author startup, relevance selection or
an integration review. Read only this assignment, the complete frozen packet,
needed frozen edition files and required skills. Do not read study history,
author reports, internal verdicts, the selector's report or the other reviewer.
Do not change inputs, run training/quadrature, or mutate Git.

The packet contains `candidate.md` (the entire proposed C.4 addition),
`dependencies.md` (complete operative existing proof units), `NOTATION.md`,
and full `docs_README_before.md`/`docs_README_after.md`. The accompanying
manifest records all authors/assemblers, selector, exact source hashes and
the standalone proposed edition. Read every scientific line of the candidate,
its dependency proof bodies and the guide/notation contract; repair truncated
reads. If a necessary dependency is missing, report it rather than assuming it.

The candidate concerns the specific two-hidden-layer tanh network and fixed
three-angle circle design stated in its theorem. Its desired scope is unique
local matching of training loss, a controlled cubic expansion for actual
population test-risk difference, an explicit coefficient retaining both
matrix response directions, and the qualified finite-width/GD consequences.
It explicitly does not determine the coefficient's sign or nonvanishing.
Judge the actual claims, including this limitation. No empirical claim or
maintained implementation is proposed; numerical study material is excluded.

Audit the model and loss factors, data degeneracy, Gaussian response and
conditional query chronology, all fixed fourth-moment estimates, uniform
circle control, actual-flow integral remainders, inverse matching, raw-GD
quantifiers, notation, and the guide's correspondence to the theorem. A
finite formal jet is not a population trajectory argument. A positive
training contraction is not a test-risk sign. A finite small random readout
must not be set to zero in the finite-network corollary. Check all boundary
cases used by the fixed circle, including repeated and antipodal passive slots.

Read `solve-math-rigorously/SKILL.md` and
`investigate-conjectures/SKILL.md` plus its applicable adversarial-audit
reference. No external theorem is intended; all operative dependencies are
contained in the packet. Scientific instructions here are the applicable
independent-review scope of the root workflow; author startup/history is not.

Write only the report path assigned in the dispatch and scratch in the
assigned generated review namespace. Retain exact read coverage, input hashes,
independence/completion evidence, actual attacks and outcomes, component
verdicts and every unresolved objection. Separate required corrections from
optional suggestions. Accept only if the complete exact scoped claims survive
the audit. A required correction prevents acceptance of this packet; original
inputs and report will be preserved and corrected inputs need fresh reviews.
