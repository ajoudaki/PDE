# Signed-risk certificate: concrete maintained-code assembly

Assembler: `sign_code_assembly`, 2026-09-10. This is author assembly and
internal checking of a proposed edition, not an independent scientific or
integration review and not promotion approval. No live book or maintained
code file was edited, no original study source was changed, and no Git
operation wrote the index. The coordinator remains the sole Git writer.
The permitted full Gaussian coefficient calculations were already exhausted
by production and its independent byte-identical reproduction. This assembly
performed **zero additional coefficient runs** and selected no new resolution,
model, teacher or design.

## Concrete destination and source correspondence

Each destination below is relative to `code/tools/two_layer_risk/`.
Copy the indicated source bytes exactly; there is no templating or editing
step. The folder is an optional tool, outside the `pde` public API and its
ordinary import path. `certify(output,target=26)` is its compact reusable
interface. The proposed maintained guide is complete in
[PROMOTION_TOOL_GUIDE.md](PROMOTION_TOOL_GUIDE.md).

| Study source | Destination | SHA-256 |
|---|---|---|
| `PROMOTION_certificate.py` | `certificate.py` | `36dd0b21dea51647dcbb9aef7c958d8f2ddadc397529e629ada5b122af0d2810` |
| `certificate_kernel.cpp` | `certificate_kernel.cpp` | `9d7bbcd743e465ae0e4caacd0f1e670d78283e1388a2fe48bda6193ef0e66ad9` |
| `PROMOTION_check_driver.py` | `check_driver.py` | `1f70dda734e117dca6ed25e1f2656242fbb085ee8cd0fd6f7ee9b7de8da9a4e2` |
| `PROMOTION_check_kernel.py` | `check_kernel.py` | `cc7f3fded02082eee118b5eecd0f947f39486eef27ea47d8095a828d62790be1` |
| `angle_error_bound.py` | `angle_error_bound.py` | `cc3d750d096212016534056d35d221d6e7840a4a9f192379d283c093b0f94149` |
| `PROMOTION_TOOL_GUIDE.md` | `README.md` | `6578bf5f1df4b5eaf525824075d7466b26cb7e2cd171310e377f06c7c8422571` |

The copied C++ kernel and angular helper remain exactly their original
bytes, including the kernel's frozen proof-comment text; no comment filename
is a runtime input. The driver grid docstring now points directly to the
canonical C.4.E/C.4.D equations. The two check scripts keep their
original exact and supplied-state oracles. `PROMOTION_check_kernel.py`
is byte-identical to `check_certificate_kernel.py`;
`PROMOTION_check_driver.py` changes only
`with_name('certificate_driver.py')` to `with_name('certificate.py')`.
No general cubature or training API has been introduced.

Original producer SHA-256:
`a2e49e1c635b347e6542372bbdb4fb8ad3438e6c923e4292dc79964a000d48e1`.
The assembly makes precisely these nonmathematical changes:

1. Replace study/root discovery by the executing source and sibling kernel.
   Runtime hashes contain only those two actually executed sources. Remove
   all study-document and Git calls. Preserve all primitive bits, exact
   constants/error fields, contractions, coefficient rows and final output.
2. Add explicit public input validation, before creating output: integer
   target 26 or 30, nonoptimized Python, Linux and Python binary64, nonempty
   string-valued path, and nonexistent nonsymlink output. The atomic fresh
   directory creation is repeated in the worker. Missing parent directories
   may be created; caller-selected output is unrestricted by study paths.
3. Expose `certify` as a subprocess boundary. It uses the current Python
   executable with `-B`, sets the four numerical thread variables in a copied
   child environment, and returns a freshly decoded `result.json`. Module
   import and API execution do not change the caller's environment. The
   child imports NumPy only after receiving its environment. CLI parsing
   dispatches to this public entry point or its hidden private worker.
4. Keep all mathematical assertions active by rejecting `-O`/`-OO` explicitly
   in both public validation and worker validation. Every mathematical
   helper remains private, with unchanged mathematical operations.
5. Record child thread settings and move the compiler-version query inside
   the existing failure-recording block. Compilation errors then retain
   failed metadata after its initialization. This moves a small version-query
   cost into the run timer; it changes no numerical operation or budget gate.
6. Before the promotion freeze, replace only the grid docstring's historical
   proof filename by `Dyadic grid satisfying (C4.E1)--(C4.E5) and
   (C4.D2)--(C4.D3).` and remove the guide's final historical-filename
   explanation. The strict equivalence check permits exactly this single
   known docstring substitution and verifies every grid operation unchanged.

The original 900-second cumulative CPU gates, kernel per-call soft CPU limit
of at most 60 seconds (hard limit one second above), and 120-second kernel
wall timeout are unchanged. These are primitive-call gates, not a hard total
wall limit or compiler timeout. A fresh worker starts separate accounting for
each API call. The guide specifies this distinction.

## Mathematical equivalence and scope of the checks

The retained checker below parses the original and candidate and then compares
complete source segments, rather than merely comparing numerical answers.
Of the **20** preexisting definitions other than `run`, **19** are byte-identical.
The remaining definition, `grid`, differs only in the exact documentation
sentence specified above. The checker requires byte equality after that one
known substitution and independently requires AST equality of its complete
body after the docstring. It does not discard arbitrary code or general
string differences. Thus all interval operations, exact constants/trigonometry,
grid choices, primitive calls/limits, covariance enclosures, floating root
proposal and response contraction retain their original operations.
The complete `run` tail starting at `build=subprocess.run` and
ending after its `finally` body is byte-identical. Its SHA-256 is
`93d8d8b443be17787aeb32987f143f1553e8ec2aeb6958622ea376e6d023a155`.
The five mathematical constant assignments are AST-identical. The compiler
assignment is AST-identical after replacing only its source-path expression.
The C++ source, angular helper and kernel check are byte-identical; the driver
check is identical after its one path substitution. Per-definition hashes
are retained in the initial and revised `assembly_checks.json` files. The
original and revised grid definition hashes are respectively
`70d699bcc30c4bf525f5fb282cfcebf93acda6a61c74a846afc4dd51f4058b06` and
`093bd2c700980284a0a7259185ab7f62882969029cd5995dd2d204bceecdfd97`.

Thus the mathematical producer, arithmetic order, grid algorithm, default
inputs and all error semantics correspond to the already executed producer.
This is a static correspondence argument plus deterministic component and
interface checks, **not** a new full maintained-command reproduction. No
byte-identical *full output file* is claimed: source and process metadata are
necessarily different. The two prior full executions remain attached to their
original versions; the coordinator supplies their raw evidence and complete
scientific proof in the frozen promotion packet. Neither author checks nor
this report substitutes for the two fresh complete scientific audits or the
separate independent integration review.

All maintained mathematical inputs are contained in the module/kernel or
proved in the canonical chapter. No archived coefficient is loaded. The
supported target 30 uses its existing proved error envelope but has not been
executed here; selecting it is not permission for another run in this study.
The guide retains the exact fixed model, normalization, sign convention, small
size of the leading effect, unevaluated time endpoint/remainder constant,
finite-network convergence on each fixed `[delta,t0]`, and absence of a
quantitative width threshold or uniform finite-width sign down to zero.

## Executed internal checks

Working directory: `/home/amir/Codes/PDE`. Generated namespace:
`data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/`.
The standalone tree contains only the six tool files under
`standalone/code/tools/two_layer_risk/`, with no `.git`, study source, prior
array or installed `pde` package. The mathematical tests ran against those
relocated sources. Import and CLI rejection checks ran from an unrelated
scratch working directory. This checks independence from study/Git inputs;
it is not OS filesystem confinement. Each of the four test commands had
`ulimit -t 60`, and was wrapped in `/usr/bin/time -v` with explicit one-thread
OpenBLAS, OMP, MKL and NumExpr environment variables and bytecode disabled.
All four initial commands exited zero. Their source snapshots, stdout/stderr
and timing files are retained unchanged. These component tests ran before the
strict docstring-only correction described below.

| Check | Observed result | CPU seconds (user + system) |
|---|---|---:|
| Source equivalence and synthetic interface | PASS; all equalities and rejection cases below | 1.05 |
| Existing exact driver oracle | PASS; longer rational series, grids, synthetic probability-table response contraction and symmetry weights | 3.07 |
| Existing kernel oracle | PASS; 19 exact primitive checks, direct small finite-tensor sums and omitted-root branch | 0.94 |
| Existing exact angular majorant | PASS, conditional on `abs(beta)<=1/10` | 0.02 |

Kernel maximum supplied-rule contraction discrepancy:
`5.88418203051333e-15`, below `1e-9`.
Kernel long-double mantissa: 64 bits. Compiled binary SHA-256:
`8c5b241801eaa4b8912989b9e404eb9693e63e94487661071c3bfa7044c189c7`,
the same as the original kernel component check.
Angular rational bound:
`20636262723469937491/127677049435953561600000000`, about
`1.6162859977291121e-7`, strictly below `1e-6`.
The angle check is only the rational derivative-majorant calculation.

Environment: Python 3.10.12, NumPy 1.26.4, Linux x86_64 with glibc 2.35,
GCC/g++ 11.4.0. This records the tested environment, not a performance
promise on other supported systems.

The interface test rejects ten invalid targets, five invalid path types or
values, an existing directory, existing file and dangling output symlink.
None starts a worker or changes existing content. The help command exits
zero, an unsupported CLI target exits 2, and an existing CLI output exits 1.
An optimized-Python subprocess explicitly rejects execution before output
creation. Synthetic worker transport for each supported target checks path,
arguments, one-thread environment, return value and return ownership without
executing a worker. A simulated failed compiler retains failed metadata with
only the two maintained-source hashes, no result and no angle folders.
Synthetic outputs are named and explicitly marked as such; none is a
scientific certificate.

The guide's namespace import was separately executed with only the standalone
`code/` on `PYTHONPATH`; its displayed signature was `(output, target=26)`.
The full producer CLI and successful API examples were deliberately not run,
because that would violate the remaining full-calculation budget. Their
underlying computation has the exact correspondence above. The initial guide
snapshot in the test tree has SHA-256
`651d75f121bfd190aa936dc5048a61f55dbbcfad66ab66ddbc644d124c69c1c7`.
After testing, one guide sentence was tightened to state the timer start
precisely. The later canonical-reference correction is separately checked
below. The original tested source and guide snapshots remain unchanged;
the final candidate hashes are in the mapping table. No guide link validation
is claimed here; the complete chapter and cross-links are assembled by the
coordinator.

Commands, before the common time/environment wrapper, were:

```sh
python -B data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/assembly_check.py studies/two_layer_test_risk data/generated/two_layer_test_risk/sign_code_assembly_20260910_01
python -B data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/standalone/code/tools/two_layer_risk/check_driver.py --output data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/driver_check
python -B data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/standalone/code/tools/two_layer_risk/check_kernel.py --output data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/kernel_check
python -B data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/standalone/code/tools/two_layer_risk/angle_error_bound.py --output data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/angle_check
```

| Evidence relative to the generated namespace | SHA-256 |
|---|---|
| `assembly_checks.json` | `d49d23ae4f537438be55269ba60f9c6c5bd1473e73c2462ec9b84b8ef340d8b4` |
| `driver_check/result.json` | `b18b877264538979ae705644396db788c3dba0884ed236e43a6371a3112154f6` |
| `kernel_check/result.json` | `32c62d10b7a06b3797540f15a32d74d8ce210403223e8482c2b8e9c1ab1963c9` |
| `angle_check/result.json` | `2a1d5ec88889cbe2f9dd80cd9b4faa7274b727db33a6c37d8c122122662c5f0a` |

## Bounded canonical-reference correction and fresh check

Before promotion freeze, the coordinator requested the exact grid docstring
correction and removal of the guide's final historical-filename sentence.
The corrected producer SHA-256 is
`36dd0b21dea51647dcbb9aef7c958d8f2ddadc397529e629ada5b122af0d2810`;
the corrected guide SHA-256 is
`6578bf5f1df4b5eaf525824075d7466b26cb7e2cd171310e377f06c7c8422571`.
All other maintained candidate bytes are unchanged. The prior report and
immediately preceding guide are preserved as
`docstring_revision/previous_PROMOTION_CODE_ASSEMBLY.md` and
`docstring_revision/previous_PROMOTION_TOOL_GUIDE.md`; the original standalone
source/check tree and every original result remain untouched.

Only the necessary equivalence/interface check was rerun, in fresh
`data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/docstring_revision/`.
It copied the six current tool files into its own standalone tree and used
the complete revised checker retained below. It again passed all strict
source/AST comparisons, import environment checks, synthetic transport,
failed-build retention, path/target rejection, CLI help/rejections, and
optimized-Python rejection. It exited zero in 1.05 CPU seconds (0.92 user,
0.13 system), with the same 60-second per-check CPU ceiling and explicit
single-thread environment. No Gaussian primitive, coefficient integral,
angular majorant or component-oracle test was repeated in this correction.
The original exact/supplied-state component checks remain evidence for the
unchanged mathematical operations, kernel and check scripts.

The exact check command before the common time/environment wrapper was:

```sh
python -B data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/docstring_revision/assembly_check.py studies/two_layer_test_risk data/generated/two_layer_test_risk/sign_code_assembly_20260910_01/docstring_revision
```

Revised check source SHA-256:
`4eadab1bf900652e8ed5a09c4372bc5fccb434c9db20b629a8b76ce6fb5bfd54`.
Revised `docstring_revision/assembly_checks.json` SHA-256:
`64b87bab98b6c62b6703fe1c118aa048b2b3407362924a2d588399d849fb437a`.
Its source manifest agrees with every final maintained candidate byte,
including the final guide. The only relaxed equality is the explicitly
listed `grid` docstring sentence; every operation is compared strictly.
Root instructions and workflow were reread before this correction and were
unchanged. Shared HEAD/index and concurrent modifications were rechecked;
no Git or unowned-file writes occurred.

## Reading, authorship and remaining gates

Read completely: root AGENTS and both workflow parts; study README;
SIGN_RELEVANCE and SIGN_THEOREM; code README; docs README and NOTATION;
certificate driver/kernel and both dedicated check scripts; CERTIFIED_ERROR,
CERTIFICATION_ENGINE, DRIVER_CERTIFICATION, ANGULAR_CERTIFICATE and the
angular source. Truncated tool views were repaired by complete bounded reads.
The README still showed historical open-sign text at startup; the coordinator
owns its current-state correction. The explicit task assignment, completed
signed theorem and selector supplied the current assembly scope and exhausted
calculation budget. No old review verdict was imported as a new acceptance.

Applied `solve-math-rigorously` and `investigate-conjectures`, including
`evidence-ledger` and `adversarial-audit` references. This task assembles code
and checks source equivalence; it does not independently re-prove C.1--C.4.
Startup HEAD was `df1117948764a984e7fd2d28949a3c87bc284f84`; the shared index
was empty and unrelated dirty files were preserved. Owned writes are only
this report, the four other assigned candidate sources/guides, and the
assigned generated namespace. The assembler must be excluded from the fresh
independent scientific and integration reviewer roles.

The candidate is internally checked and ready to freeze for those reviews.
The coordinator assembles the canonical proof, complete guides/dependencies,
review packet and integration edition. User approval is still required before
any live maintained-file promotion; this report neither requests nor supplies
that approval.

## Retained exact assembly/interface checker source

This is the complete source used for the author-only correspondence and
interface check. It is retained here so no essential test source exists only
under generated data. Save the code below verbatim as `assembly_check.py`
inside a fresh study-generated scratch directory. Prepare its standalone
six-file tool tree using the mapping above, then call it with the study and
scratch directories as its two arguments. Its output tree must be fresh for
this check; it never invokes the full coefficient producer.

```python
#!/usr/bin/env python3
"""Author-only relocation/interface checks; never evaluate the coefficient.

Usage: python -B assembly_check.py STUDY_DIR EXISTING_FRESH_ASSEMBLY_SCRATCH
Scratch must contain standalone/code/tools/two_layer_risk with mapped sources.
"""
import ast
import hashlib
import importlib.util
import json
import os
from pathlib import Path
import subprocess
import sys
from unittest.mock import patch

study = Path(sys.argv[1]).resolve()
scratch = Path(sys.argv[2]).resolve()
tool = scratch/'standalone/code/tools/two_layer_risk'
report = {'scope':'static equivalence and synthetic interface checks; no coefficient run'}


def sha_bytes(body):
    return hashlib.sha256(body).hexdigest()


def definitions(source):
    return {n.name: n for n in ast.parse(source).body
            if isinstance(n, (ast.FunctionDef, ast.ClassDef))}


def segment(source, node):
    return ast.get_source_segment(source, node)


old = (study/'certificate_driver.py').read_text()
new = (tool/'certificate.py').read_text()
old_defs, new_defs = definitions(old), definitions(new)
definition_checks = {}
docstring_checks = {}
for name, node in old_defs.items():
    if name == 'run':
        continue
    before, after = segment(old,node), segment(new,new_defs[name])
    if name == 'grid':
        old_doc = 'Rational variant of CERTIFIED_ERROR E5; no transcendental choices.'
        new_doc = 'Dyadic grid satisfying (C4.E1)--(C4.E5) and (C4.D2)--(C4.D3).'
        assert before.count(old_doc) == 1
        assert before.replace(old_doc,new_doc) == after
        # The explicit single docstring substitution is the only permitted change.
        before_node, after_node = ast.parse(before).body[0], ast.parse(after).body[0]
        assert ast.get_docstring(before_node) == old_doc
        assert ast.get_docstring(after_node) == new_doc
        assert ast.dump(ast.Module(body=before_node.body[1:],type_ignores=[])) == ast.dump(
            ast.Module(body=after_node.body[1:],type_ignores=[]))
        docstring_checks[name] = {'old_sha256':sha_bytes(before.encode()),
                                 'new_sha256':sha_bytes(after.encode()),
                                 'old_docstring':old_doc,'new_docstring':new_doc,
                                 'all_operations_identical':True}
    else:
        assert before == after, name
        definition_checks[name] = sha_bytes(before.encode())
report['byte_identical_definitions'] = definition_checks
report['exact_docstring_only_changes'] = docstring_checks
old_run = segment(old,old_defs['run'])
new_run = segment(new,new_defs['run'])
marker = '        build=subprocess.run'
assert old_run[old_run.index(marker):] == new_run[new_run.index(marker):]
report['byte_identical_execution_tail_sha256'] = sha_bytes(old_run[old_run.index(marker):].encode())
for name in ('BITS', 'SCALE', 'P', 'ARITHMETIC', 'ANGLE_ERROR'):
    def assignment(source):
        return next(n for n in ast.parse(source).body if isinstance(n,ast.Assign)
                    and any(isinstance(t,ast.Name) and t.id == name for t in n.targets))
    assert ast.dump(assignment(old)) == ast.dump(assignment(new)), name
report['unchanged_math_constants'] = ['BITS','SCALE','P','ARITHMETIC','ANGLE_ERROR']

def compile_assignment(source):
    return next(n for n in ast.walk(ast.parse(source)) if isinstance(n,ast.Assign)
                and any(isinstance(t,ast.Name) and t.id == 'compiler' for t in n.targets))

expected = segment(old,compile_assignment(old)).replace("str(STUDY/'certificate_kernel.cpp')",'str(KERNEL)')
assert ast.dump(ast.parse(expected)) == ast.dump(ast.parse(segment(new,compile_assignment(new))))
assert (tool/'certificate_kernel.cpp').read_bytes() == (study/'certificate_kernel.cpp').read_bytes()
assert (tool/'angle_error_bound.py').read_bytes() == (study/'angle_error_bound.py').read_bytes()
assert (tool/'check_kernel.py').read_bytes() == (study/'check_certificate_kernel.py').read_bytes()
assert (tool/'check_driver.py').read_text() == (study/'check_certificate_driver.py').read_text().replace(
    "with_name('certificate_driver.py')", "with_name('certificate.py')")
report['kernel_build_and_check_correspondence'] = 'PASS'

# Load only the relocated module. No old producer import mutates this process.
before = dict(os.environ)
spec = importlib.util.spec_from_file_location('relocated_certificate',tool/'certificate.py')
drv = importlib.util.module_from_spec(spec)
spec.loader.exec_module(drv)
assert dict(os.environ) == before
assert not (scratch/'standalone/studies').exists()
assert not (scratch/'standalone/.git').exists()
report['relocated_import_environment_unchanged'] = True

tests = scratch/'interface'
tests.mkdir(exist_ok=False)
for i, target in enumerate((True, False, 26.0, '26', None, -1, 0, 25, 27, 31)):
    output = tests/f'bad_target_{i}'
    with patch.object(drv.subprocess,'run',side_effect=AssertionError('unexpected worker')):
        try:
            drv.certify(output,target)
        except (TypeError,ValueError):
            pass
        else:
            raise AssertionError(('target accepted',target))
    assert not output.exists()
for output in ('', b'path', None, 42, 'a\x00b'):
    with patch.object(drv.subprocess,'run',side_effect=AssertionError('unexpected worker')):
        try:
            drv.certify(output)
        except (TypeError,ValueError):
            pass
        else:
            raise AssertionError(('output accepted',repr(output)))
existing = tests/'existing'
existing.mkdir()
(existing/'marker').write_text('owned by caller\n')
file = tests/'existing_file'
file.write_text('owned by caller\n')
link = tests/'dangling_link'
link.symlink_to(tests/'missing_target')
for output in (existing,file,link):
    with patch.object(drv.subprocess,'run',side_effect=AssertionError('unexpected worker')):
        try:
            drv.certify(output)
        except FileExistsError:
            pass
        else:
            raise AssertionError('nonfresh output accepted')
assert (existing/'marker').read_text() == 'owned by caller\n'
assert file.read_text() == 'owned by caller\n'
assert not (tests/'missing_target').exists()
report['boundary_rejections_before_creation'] = 'PASS: 10 targets, 5 path types/values, 3 nonfresh paths'

# Synthetic transport checks the public worker argument/environment boundary.
# This manufactured payload is explicitly not an arithmetic certificate.
for target in (26,30):
    output = tests/f'synthetic_transport_{target}'
    payload = {'synthetic_only':True, 'target':target}
    def fake_worker(command,env,check):
        assert command[:3] == [sys.executable,'-B',str(tool/'certificate.py')]
        assert command[-1] == '--_worker'
        assert command[command.index('--target')+1] == str(target)
        assert Path(command[command.index('--output')+1]) == output
        assert check is True
        assert env is not os.environ
        for name in ('OPENBLAS_NUM_THREADS','OMP_NUM_THREADS','MKL_NUM_THREADS','NUMEXPR_NUM_THREADS'):
            assert env[name] == '1'
        assert not output.exists()
        output.mkdir()
        (output/'result.json').write_text(json.dumps(payload))
        return subprocess.CompletedProcess(command,0)
    with patch.object(drv.subprocess,'run',side_effect=fake_worker):
        result = drv.certify(output,target)
    assert result == payload
    result['mutated'] = True
    assert json.loads((output/'result.json').read_text()) == payload
assert dict(os.environ) == before
report['synthetic_worker_transport_and_return_ownership'] = 'PASS for 26 and 30; no worker executed'

# A failed build cannot enter any Gaussian primitive or create a result.
output = tests/'synthetic_build_failure'
with patch.object(drv.subprocess,'check_output',return_value='synthetic compiler\n'), \
     patch.object(drv.subprocess,'run',return_value=subprocess.CompletedProcess([],1,'','synthetic build failure\n')), \
     patch.object(drv,'call_kernel',side_effect=AssertionError('forbidden coefficient call')):
    try:
        drv.run(type('Args',(),{'output':output,'target':26})())
    except AssertionError:
        pass
    else:
        raise AssertionError('build failure accepted')
metadata = json.loads((output/'metadata.json').read_text())
assert metadata['status'] == 'failed' and metadata['exit_status'] == 1
assert set(metadata['source_sha256']) == {'certificate.py','certificate_kernel.cpp'}
assert not (output/'result.json').exists()
assert not list(output.glob('angle_*'))
report['failed_build_retention'] = 'PASS: synthetic failed metadata, no result or angle folders'

env = dict(os.environ, OPENBLAS_NUM_THREADS='1', OMP_NUM_THREADS='1', PYTHONDONTWRITEBYTECODE='1')
commands = [
    [sys.executable,'-B',str(tool/'certificate.py'),'--help'],
    [sys.executable,'-B',str(tool/'certificate.py'),'--target','27','--output',str(tests/'bad_cli_target')],
    [sys.executable,'-B',str(tool/'certificate.py'),'--output',str(existing)],
]
outcomes = []
for i, command in enumerate(commands):
    proc = subprocess.run(command,cwd=tests,env=env,text=True,capture_output=True,timeout=30)
    (tests/f'cli_{i}_stdout.txt').write_text(proc.stdout)
    (tests/f'cli_{i}_stderr.txt').write_text(proc.stderr)
    assert proc.returncode == 0 if i == 0 else proc.returncode != 0
    outcomes.append({'command':command,'exit_status':proc.returncode})
assert not (tests/'bad_cli_target').exists()
assert (existing/'marker').read_text() == 'owned by caller\n'
code = '''import importlib.util, pathlib, sys
spec=importlib.util.spec_from_file_location("cert",sys.argv[1])
mod=importlib.util.module_from_spec(spec);spec.loader.exec_module(mod)
try:
    mod.certify(pathlib.Path(sys.argv[2]))
except RuntimeError as e:
    print(str(e))
else:
    raise RuntimeError("optimized execution was accepted")
'''
optimized = tests/'optimized_output'
command = [sys.executable,'-O','-B','-c',code,str(tool/'certificate.py'),str(optimized)]
proc = subprocess.run(command,cwd=tests,env=env,text=True,capture_output=True,timeout=30)
assert proc.returncode == 0 and 'without -O or -OO' in proc.stdout
assert not optimized.exists()
(tests/'optimized_stdout.txt').write_text(proc.stdout)
(tests/'optimized_stderr.txt').write_text(proc.stderr)
report['cli_checks'] = outcomes
report['optimized_python_rejection'] = 'PASS'
report['source_sha256'] = {p.name:sha_bytes(p.read_bytes()) for p in sorted(tool.iterdir()) if p.is_file()}
report['check_source_sha256'] = sha_bytes(Path(__file__).read_bytes())
report['status'] = 'PASS'
(scratch/'assembly_checks.json').write_text(json.dumps(report,indent=2)+'\n')
print(json.dumps(report,indent=2))
```
