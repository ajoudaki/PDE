# Neutral assignment: complete independent audit of the signed risk theorem

Review the exact frozen mathematical and executable inputs in this packet.
The candidate is SIGN_THEOREM.md: it claims that the specified two-hidden-layer
tanh network has lower uniform-circle test risk than its frozen-hidden-feature
comparison at equal training loss on a width-independent positive local time
interval. The new decisive premise is the rigorously enclosed positive cubic
coefficient. The candidate's exact quantitative and finite-width scope is part
of the review; do not replace it by a practical or universal claim.

This is a fresh independent scientific audit of the complete study result,
not author startup and not yet a book integration review. Read only this
assignment, the complete frozen inputs and the required skills. Do not read
study history, the study README, prior verdicts, component reviews, or the
other reviewer's findings. Do not stage/commit or modify inputs. Report any
necessary missing input rather than importing an old verdict. All author and
assembler identities and exact source/packet/evidence hashes are in the
manifest. The separate task message assigns report ownership and scratch.

Read solve-math-rigorously and investigate-conjectures SKILL.md, with the
applicable adversarial-audit reference. Read every scientific line in the
frozen theorem, baseline flow/coefficient/remainder, complete operative
dependencies, notation, new Gaussian/angular/arithmetic/assembly proofs,
kernel, driver and tests. Repair truncated reads. Numerical evidence may be
checked by programs that load every input/output; report that distinction
from human reading of the source and proof. Every scientific input needed
for the claimed implication is included; administrative endings were removed
from some copies and are enumerated in the manifest. No mathematical content
was intentionally removed. PROMOTION_C4.md is the complete baseline reduction
whose unresolved coefficient is addressed by the new certificate; its old
open-sign sentences do not supply or refute the new inequality.

Reconstruct all of the following:

1. Exact fixed model, correlated rank-two Gram, loss factors, frozen baseline,
   all trained blocks and both directions of the reused matrix.
2. Operative positive-time population theorem hypotheses and proofs; unique
   matching, actual-flow uniform fourth-order remainder and limit scope.
3. Complete scalar coefficient and every response contraction.
4. Gaussian strip/tail/tensor-mass estimates, covariance perturbation at
   singular inputs, analytic constants and their computer arithmetic.
5. Gaussian/angular differentiability, eighth-derivative majorants and
   the 256-node symmetry reduction, without differentiating passive roots.
6. IEEE/long-double/compiler hypotheses, elementary function and accumulated
   rounding bounds, dyadic constants and bit transport; all primitive radii.
7. Exact interval propagation, denominator and clock checks, raw projection
   versus clock subtraction, complete recorded numerical enclosure and the
   strict rational sign bound. A flag or positive midpoint is insufficient.
8. The finite-time inference, first-nonzero order, hidden-movement/nonaffinity
   scope and the finite-random-readout/GF/raw-GD boundary.

Both reviewers must audit the actual producer and meaningful deterministic
tests. Reviewer A is additionally assigned the single reserved fresh full
reproduction at target26, using the exact checked original driver bytes at
`/home/amir/Codes/PDE/studies/two_layer_test_risk/certificate_driver.py` and a
fresh output directory named in the task message. Before executing, compare
every scientific/code source hash to this packet's original-source map. The
driver hashes original study documents internally for provenance; that does
not authorize reading their administrative history. This reproduction is
explicitly authorized, at most900 CPU seconds, no other Gaussian coefficient
run and no new resolution. Reviewer B must not repeat Gaussian integration;
independently reassemble/check all 64 saved primitive outputs and intervals
or otherwise fully verify the saved numerical certificate. Small exact
arithmetic or supplied-state tests are allowed for both reviewers, each at
most60 CPU seconds, in fresh study-generated scratch. No training, random
sampling, parameters, teacher or dataset changes are allowed.

Save the original full report with exact read coverage, hashes, independent
derivations and hostile checks, actual commands/results, component verdicts,
unresolved objections, independence and completion evidence. Classify any
required correction precisely; it blocks acceptance of this version. No
message from the coordinator or another reviewer supplies mathematical
evidence. If accepted, state the exact conclusion and its limitations.
