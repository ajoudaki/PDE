# Neutral complete scientific review of a fixed-model signed risk addition

Read this assignment, the frozen manifest, every scientific line of the
packet and its complete operative dependencies, and the required skills.
Do not perform author startup, read a study README, historical verdicts,
selection reports, or any other reviewer's findings. Report missing inputs;
repair truncated reads. The manifest identifies all authors and assemblers.
The two scientific reviewers must be fresh and distinct from those people
and from the selector. Root is the only Git writer.

The candidate adds global-nonlinear C.4, a private fixed-certificate tool,
focused checks, and corresponding guide changes. Its claim is a positive
cubic test-risk difference at equal training loss for the exact fixed
two-hidden-layer tanh model and design stated in C.4, with an actual-flow
fourth-order remainder and a unique matching time. Its coefficient has a
computer-assisted finite certificate, including all approximation and
arithmetic errors. No iid-average, global-time, quantitative-width, practical
effect-size, or universal feature-learning claim is proposed.

Both reviewers must read the entire new chapter subsection, all included
dependency proof bodies, notation contract, complete before/after guides,
tool guide, all code/test/helper sources, and the numerical correspondence
inputs. Each independently audits the actual producer, not just its flag.
Attack normalization, the actual moving residual, both directions of the
reused matrix, all trained blocks, singular passive covariances, the
strong-flow remainder, unique loss matching, and finite GF/GD limit scope.
Check the Gaussian strip/tail proof, nonunit rule masses, Hessian constants,
angular Gaussian-field derivative argument, symmetry weights, exact
intervals, input constants, elementary functions, arithmetic operation
counts, root-factor discrepancy and all error propagation.

The supplied evidence includes the two completed full coefficient runs and
the exact original numerical source. The maintained candidate must be
self-contained, independent of studies/history/Git/archived outputs, and
mathematically identical to the already executed producer. Verify that
correspondence directly; do not adopt an author's equivalence verdict.
Inspect wrapper failure/input/output/resource semantics and exercise the
focused checks, import, CLI help and relevant edge cases from the standalone
edition. Both full coefficient calculations in the authorized budget have
already been used. Do not run another Gaussian coefficient integration,
resolution, training run or search. Exact arithmetic, supplied-state checks
and reconstruction from supplied primitive bits are allowed, capped at one
CPU minute per check. Report an algorithm/evidence mismatch as a gap; do
not repair it with an unauthorized third run.

The input packet intentionally contains raw evidence and source versions,
not prior reports. An executed reproduction matters because the sign proof
uses finite arithmetic; it is not an empirical training conclusion. Check
all saved node inputs/output contracts and exact final rational bounds,
and independently reconstruct the contraction/clock/risk interval. A
separate exact implementation or complete termwise comparison should make
independence substantive. No numerical resolution agreement can replace
the error argument.

Use `solve-math-rigorously` and applicable `investigate-conjectures`
instructions/references. No external result is required beyond the
contained proofs. Do not edit inputs. Write only the assigned full report
and scratch directory under the frozen run, retaining any unique check
source in the report or an assigned flat study file. Do not stage/commit.

The report must give precise read coverage and repaired truncations, input
and report hashes, original commands/results and evidence paths, isolation,
component verdicts, limitations and every unresolved objection. Any required
correction blocks acceptance and requires a new frozen complete packet and
two fresh complete scientific reviews. A narrowly described nonblocking
editorial suggestion must not silently become an accepted code or proof
change after freezing. This review is not the separate integration review
or user approval.
