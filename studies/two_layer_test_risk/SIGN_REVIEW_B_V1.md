# Fresh isolated scientific review B — signed matched-loss theorem, v1

Reviewer: `/root/sign_review_b`. Date: 2026-09-10.

**Verdict: PASS for the exact frozen scientific statement.** I found no required correction, missing operative scientific input, or unresolved objection that blocks the stated local, fixed-model conclusion. This is an independent study-result audit, not approval of a canonical/book addition. I did not participate in authoring or assembling the candidate, and did not read author startup, the live study README/history, prior reviews, or reviewer A's findings. No coordinator message supplied mathematical evidence.

The accepted assertion is precise: for the prescribed two-hidden-layer tanh model and the prescribed three-point design, the population matched-loss risk difference has a first nonzero cubic coefficient satisfying

\[
27/100000<\chi<273/1000000,
\qquad 35309/1000000<\beta<35311/1000000.
\]

The proved actual-flow remainder consequently yields a positive width-independent local interval with the lower bound in SIGN_THEOREM (S3). The interval and remainder constant are existential, not evaluated. Finite GF and every deterministic vanishing raw-GD step inherit the comparison in probability on each fixed closed interval bounded away from zero. There is no width rate or finite-width sign statement uniform down to zero.

## Scope, frozen identity, and complete coverage

I read the neutral assignment and both required skills, including the investigate-conjectures adversarial-audit reference. I read every line of every frozen proof/code/notation file listed below. Long reads were split into complete contiguous sections; truncations affecting the skill and reading guide were repaired. The packet contains 6,031 lines in these 18 files. Numerical records were consumed programmatically, rather than claiming visual inspection of each printed numeric entry: all 588 evidence files were byte-read and hash-verified; all 330 JSON files were parsed; the other 258 text files were read. All 64 production input/output/audit/enclosure sets were reconstructed numerically.

The assignment source hash is `40774ddaadd44abaa626ad2c4e7ffa63d878cdd1eb186e09afff470e0feb4208`. The manifest hash is `133358ebec642bfec1af8b46a2d239e7ef93877a36825c21d08260a58b350bbc`. Its recorded source head is `df1117948764a984e7fd2d28949a3c87bc284f84`; I did not query Git. The manifest names the author/assembler participants `root`, `cubic_derivation`, `matching_remainder`, `quadrature_check`, `sign_structure`, `certified_error`, and `certification_engine`. I checked the administrative-excision map and did not import any omitted historical verdict. In particular the old open-sign statements in PROMOTION_C4 were treated as the baseline's unresolved premise, which the new certificate must establish.

| Frozen file, complete read | Lines | SHA-256 |
|---|---:|---|
| SIGN_THEOREM.md | 173 | `e78046a220ed618adacaf05348762a5a33cce66c88a388df11f7b181c6182a88` |
| PROMOTION_C4.md | 642 | `b807efbd793b5b6ebc67e7f673efbcae4a234eacd28e667c304597c51d7435d4` |
| CUBIC_DERIVATION.md | 399 | `faf23db85ac2a03f00dd9bd2aa5bf918631982042649b4b84d7cd75721604742` |
| MATCHING_AND_REMAINDER.md | 538 | `6b7e9efe4ab6b2c4224df237886e9d573cc9272e6a5830d99201e6732d8bd282` |
| RESULT.md | 56 | `91fa906514233ed42123e24e634da0d8bce59a6f7f6cbff9e7e099c37ae3580d` |
| CERTIFIED_ERROR.md | 325 | `f3b73f701fb0ce53974b858942ce324f4b62ac002272b93b8273189d7c9742e1` |
| CERTIFICATION_ENGINE.md | 220 | `1e6fe519f1b69580dbd16d5f6502b9c98ed7ce268b2b27cc318ad4fda640db81` |
| ANGULAR_CERTIFICATE.md | 214 | `a60fb1a63e060c2b9fc7dc3b211a8bc0e71ce51195364c9a681127dcf0adb4d2` |
| DRIVER_CERTIFICATION.md | 244 | `5a16e2e78da438c3b7645d7f014ee3837bc84f20ad8702fa5084df322cd27000` |
| certificate_driver.py | 382 | `a2e49e1c635b347e6542372bbdb4fb8ad3438e6c923e4292dc79964a000d48e1` |
| certificate_kernel.cpp | 236 | `9d7bbcd743e465ae0e4caacd0f1e670d78283e1388a2fe48bda6193ef0e66ad9` |
| angle_error_bound.py | 88 | `cc3d750d096212016534056d35d221d6e7840a4a9f192379d283c093b0f94149` |
| check_certificate_driver.py | 140 | `3313df40cbf8170c423d4951bf06aff9733c3260c6d76e01cb3f3d8cee6de989` |
| check_certificate_kernel.py | 161 | `cc7f3fded02082eee118b5eecd0f947f39486eef27ea47d8095a828d62790be1` |
| dependencies.md | 1777 | `8378046bc80abc06077b34182141d282cf0ba6ac6d7fde6c84e2c6bab5b19db7` |
| docs_NOTATION.md | 98 | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| docs_README.md | 265 | `4d3cf63cf09e2effb3342f96272a754e8f8f127aada44179b893f6e1a36df453` |
| assignment.md | 73 | `40774ddaadd44abaa626ad2c4e7ffa63d878cdd1eb186e09afff470e0feb4208` |

The complete dependency proof read includes Section 2, Sections 3.1–3.4, A.1–A.4, C.1, the complete weighted response/tail proof C.2, C.3, and its final weighted-loss correction. Earlier shifted-arctangent constants in Section 2 were not substituted for the tanh hypotheses. Contextual literature links in the reading guide were not theorem premises; no external search was necessary to fill a proof gap.

Every packet and evidence hash matched. The full individual evidence-hash list is preserved in `data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/verified_input_hashes.json`. The frozen production result hash is `89815a7b16fb69f51683834049b370256fd32aba26528630f4c4f6b427fe406d`. Its recorded source hashes agree with the original-source map for all overlapping scientific/code entries. The production metadata's README hash was read as provenance only; no live README was opened.

## Independent reconstruction of the mathematics

### Exact model and operative population theorem

The stored predictor is `W3^T tanh(W2 tanh(W1 x/sqrt(2)))/n`. Initialization variances are `1,1/n,1/n^2`; mobilities are `(n,1,n)`; mean loss gives the factor `2/3` in the residual-driven updates. The labels are `(1,-q,-q)` with `q=(sqrt(5)-1)/4`, and the off-diagonal Gram entries are `(1+sqrt(5))/4,(1+sqrt(5))/4,q`. This is rank two. No argument in the candidate divides by a positive input-Gram eigenvalue. Lower root sampling is the actual two-dimensional Gaussian direction map.

C.1 applies with `L=2`, fixed `m=3,d=2`, weights `omega_a=1/3`, all mobilities one, Gaussian input/connector, and limiting zero readout. Tanh has the required bounded first and Lipschitz first derivative. The finite stored readout is covered by C.1's vanishing-RMS perturbation clause. Freezing the hidden blocks is admitted by its nonnegative-mobility statement.

I checked the construction rather than treating the C.1 theorem label as an axiom. Fixed finite Gaussian programs use the exact conditional projection after observing both directions of a matrix. The innovation covariance is a full input second moment; it is not reduced by the response's variance. At singular Grams, adding fresh small input roots makes fixed-program Grams invertible, finite same-array operator bounds control the perturbation, and continuous positive-semidefinite square roots plus bounded source derivatives remove the perturbation. The joint countable program construction and the limiting operator inequalities give actual mutually adjoint bounded actions.

The local positive-time bridge is C.2's response/tail argument. The entrywise forward-response pulse retains `Delta omega_b`; the reverse-response row has bounded absolute sum. The two cap families are selected in opposing layer orders from their zero-time prefactors, before shrinking time, which avoids a circular cap choice. The exponential derivative-growth estimates use weighted time sums and Jensen, not the maximum of a growing Gaussian history. Their subGaussian marginal bounds supply the one-reference cutoff tail in C.1. Localization then yields a comparison of the form `exp(C(1+R)T)[(1+R)(Delta+Delta')+exp(-cR^2)]`. Sending meshes to zero before the cutoff tends to infinity constructs the strong flow and proves uniqueness against any competing norm-bounded strong solution. The finite proxy comparison has the corresponding order `n -> infinity`, coarse mesh to zero, cutoff to infinity; it does not infer a growing-program theorem from a fixed-program limit.

The diagonal finite-dimensional Euler argument for GF and the passive-input probe/net argument preserve the stated local topology. First-layer passive fields are exact linear combinations of two independent training directions; bounded actions and tanh give a uniform angular Lipschitz bound. A finite angular net followed by its mesh limit supplies whole-circle prediction and risk convergence. No growing training set is introduced.

### Matching and the fourth-order trajectory remainder

Bounded ridge-function independence gives `Q>0` for pairwise nonparallel directions even though `G` is singular. Full support of `Y~N(0,Q)` gives the readout Gram `K>0`. Thus the frozen residual is `-exp(-2Kt/3)y`, with strictly decreasing loss from `L(0)` to zero. On the actual local norm ball, positivity of the kernel and its bounded operator norm keep actual loss positive and nonincreasing. The exponential comparison gives `tau(t) <= B_K t/lambda_min(K)`, so both clocks fit inside the established interval after shortening it. Its derivative at zero is `-4 B0`, where `B0=p^T Kp>0`.

With weighted labels `p=y/3`, the exact initial readout coefficient is `2S`, where `S=sum p_a H_a`. Both hidden changes start at order `t^2`. Their two upper contributions are the connector increment `M_x` and the forward action `W A_x`; the lower term includes the actual transpose `W* U_b`. I verified the normalization conversion from the unweighted notation in MATCHING_AND_REMAINDER.

The uniform polynomial remainder uses fourth moments only for these fixed initial directions. The reused transpose is bounded response plus Gaussian innovation; hence each `P_b` has finite fourth moment. The next conditioned forward action is a linear combination of the old Gaussian forward fields, bounded reverse inputs, and a Gaussian innovation with bounded variance. The fixed training Grams `Q,V` suffice for this step. This supplies uniform fourth moments of the initial `T_x,A_x,R_x^hid`, including passive inputs.

The flow's readout is pointwise `O(t)`, so initial field/operator changes are `O(t^2)` in the stated norms. The bounded-gate estimate converts an `O_L2(t^2)` argument change into an `O_L4(t)` gate change; multiplying by the fixed `L4` transpose direction then improves the velocity error to `O_L2(t^2)`. Integration gives `O(t^3)` errors in the hidden onset. Scalar Taylor along a fixed `L4` direction gives the needed activation estimate without assuming ambient Frechet smoothness on `L2`. Subtracting the two readout equations keeps the moving residual. It gives

\[
f_t(x)-g_t(x)=t^3J_x+O(t^4),\quad
J_x=4E[S E_x]+\frac43\sum_b p_bE[H_xE_b]
\]

uniformly on the circle. The readout difference starts at `t^3`; its residual-difference forcing contributes only at order four, while the common individual residual dynamics already have a quadratic term.

Adjunction gives `p^T J=16 A/3` and `p^T a=2B0`. The inverse-loss mean-value calculation therefore yields `beta=8 A/(3B0)` and the normalization identity `p^T(J-beta a)=0`. Subtracting squared risks supplies `chi=2 integral cos(3alpha)(J-beta a) dmu` with an actual uniform `Mt^4` remainder. The sign convention is consistent with frozen risk minus trained risk.

### Full scalar response contraction

For four formal upper slots, including `p_x=0`, I reconstructed

\[
W^*F=\sum_i h_i E[\partial_iF]+\Gamma_F,
\qquad E[\Gamma_F\Gamma_G]=E[FG].
\]

Conditioning on the lower roots then gives `Lambda_ab(F,G)=L_ab E[FG]+sum_ij T_abij E[partial_iF]E[partial_jG]`. The passive response derivative of `U_x` has both the three training terms `p_i E[d_i d_x]` and the passive term `E[S dd_x]`. The derivative of `H_x d_a` has both `E[d_x d_a]` and `E[H_x dd_a]`. Adjoining the next forward call yields the two displayed `C` terms for `J`.

I checked all contractions in the producer against these four-slot identities. The `M`, `D`, `A`, `F`, and `B` formulas retain both response directions and the response mean products. Formal coincident slots are legitimate: a null covariance combination is a zero lower-source `L2` combination. The implementation's conditioning over the fourth root is only Fubini over a finite tensor sum. Training moments use three roots without a spurious fourth-root mass; passive moments use the fourth root through the three conditional functions `E H_x,E d_x,E dd_x`.

## Analytic and arithmetic enclosure audit

The Gaussian proof shifts one coordinate at a time into its strip, controls the Fourier aliases by a geometric series, bounds deleted positive weights by the Gaussian tail integral, and telescopes the tensor rule with the finite rule masses. It does not require an unstated simultaneous complex polydisc bound. Tanh, its first derivative, and its second derivative have strip bounds `1,2,4`; repeated factors and all signed label sums are accounted for.

The covariance perturbation proof integrates the Gaussian density derivative along the convex covariance segment, regularized by `delta I`. Bounded Hessians make the limit uniform and permit removing `delta`. Hence it remains valid at singular passive inputs. The lower rule charges the discrepancy between the true two-root Gram and its dyadic direction Gram. The upper rule charges the exact rational discrepancy between the lower-Q enclosure and the dyadic factor product. An ordinary floating solve or conditional-variance clip is not asserted to be an exact square root. In the saved angle-zero case the chosen passive variance is `5.551115123125783e-17`; its artificial positive root is covered by that covariance discrepancy.

The Hessian sum constants `4,6,10,18`, respectively for `HH,dd,Hdd,HHdd`, give the Price radii in both tables. I checked their product-rule counts including repeated slots. Both exact and supplied dyadic label absolute sums are below `27/50`, so the separate label error is bounded by `2P epsilon_p` for squared S and `epsilon_p` for linear S. Every primitive receives arithmetic, strip/tail, covariance, and label contributions as applicable.

The 256-node angular proof realizes the upper angular process as an isonormal map of the lower feature curve. It differentiates that curve in finite `Lp`, not a passive root factor. Gaussian Holder/Bell-polynomial bounds justify expectation differentiation through order eight. Expanding all alpha-dependent factors in F and B gives the stated derivative profile; the identity `tanh' tanh=-tanh''/2` accounts for the improved last F term. Reflection and antipodal parity make the integrand even and pi-periodic, with zero teacher factor at pi/2, giving precisely the weight `2/256` at zero and `4/256` at indices 1–63. Eight integrations by parts and the alias sum give `2 zeta(8) D/256^8`.

My separate recursive Bell calculation reproduced

\[
D=41272525446939874982/31640625,
\quad e_{\rm angle}=20636262723469937491/127677049435953561600000000
<10^{-6}.
\]

The beta hypothesis was checked from every nodal beta interval, not a midpoint. Exact Machin/alternating-series constants, interval square roots, and real Taylor remainders enclose pi, labels, density normalization, directions, and teacher factors. The dyadic grid inequalities imply both strip exponent and Gaussian mass bounds without relying on floating transcendental grid choices. Exact partial exponential sums verified the claimed lower bounds at 26, 30, and 32, and I checked the resulting tensor error envelopes directly.

For the engine I checked binary64, at least 64 long-double significand bits, nearest rounding, no fast-math, and no fused contraction. The fresh test build used the required flags and reproduced the recorded binary SHA `8c5b241801eaa4b8912989b9e404eb9693e63e94487661071c3bfa7044c189c7`. The engine uses no assumed-accurate library exp or tanh. The 25-operation Horner expansion includes coefficient rounding in its longest dependency chain. Exact arithmetic gives its initial relative error below `44.474u<46u`; eight rounded squarings have upper error approximately `1.335709321e-12<2e-12`. The tanh ratio, saturation, node/dot-product perturbations, gate errors, positive weights, long-double accumulation, and final cast fit strictly within the `10^-9` primitive radius. I checked the range and count bounds used in that estimate, and the tiny underflow contributions are absorbed by its explicit slack.

Input and output transport is checked at binary64 bit level. Every saved real input, chosen grid, echoed bit pattern, finite output, node count, mass diagnostic, and per-call input/output hash was validated. This remains a computer-assisted proof relying on the declared floating arithmetic/compiler execution model; it is not a formal verification of the compiler or physical hardware. That is an explicit scope of the supplied certificate, not an extra assumption silently introduced by this review.

## All-64 reconstruction and independent numerical check

I ran no new Gaussian coefficient integration. The saved primitive data comprise 21,120 scalar outputs: 288 lower and 42 upper values at each of 64 nodes. Their recorded work totals are 443,840 lower and 86,101,134 upper tensor nodes. These counts describe the original evidence, not computations rerun by this reviewer.

First, importing the inspected frozen driver without invoking its production entry point, I regenerated constants, directions, grid selections, factors, primitive radii, all seven component intervals, teacher factors, weights, cumulative sums, and beta intersections from the saved primitive bits. Every field in each of the 64 enclosure records matched exactly, including its duplicate in the final result. The exact final raw, clock, nodal, beta, and chi intervals matched the frozen result.

Second, a separate interval implementation using a 128-bit outward rational grid rebuilt covariance errors and primitive enclosures and assembled the four-slot Lambda formulas directly. It did not call the producer's contraction routine. This checks the scientific formulas by a different grouping. Its component intervals all overlapped the corresponding producer intervals. Its B and J intervals need not be nested because distributive regrouping changes dependency overestimation; this is recorded for all 128 such component occurrences. Independently it yielded

\[
\frac{92060102708380688999533330032301939}{340282366920938463463374607431768211456}
\le\chi\le
\frac{11595060190589432443145806593383677}{42535295865117307932921825928971026432},
\]

approximately `[0.0002705403266745527,0.0002725985550296455]`, also strictly inside the theorem's rational bounds.

The exactly reproduced producer enclosure is

\[
\frac{5358604107658561212253567}{19807040628566084398385987584}
\le\chi\le
\frac{21597479156841685713774185}{79228162514264337593543950336}.
\]

Its pre-angular nodal interval is approximately `[0.00027154037037366817,0.0002715985113305291]`. The separately reproduced raw projection is `[0.0004387881644736982,0.0004388054963340112]`; the clock subtraction is `[0.0001672069850034821,0.00016724779410003002]`. These are explanatory decimals; strict rational comparisons determine the sign. The positive raw projection alone was never used as the conclusion.

The beta intersection is exactly

\[
\left[
\frac{2797504526179671494928101665}{79228162514264337593543950336},
\frac{2797556156441557459457527739}{79228162514264337593543950336}
\right].
\]

All B0 lower endpoints were positive; their minimum was `590222226635016925987767005/79228162514264337593543950336`. The maximum upper covariance discrepancy was `5199379552312523209/4951760157141521099596496896`. Integer/Fraction comparisons verified both rational theorem intervals and the angular beta premise. No `POSITIVE` flag supplied evidence.

## Commands, tests, and retained provenance

All scratch is under `/home/amir/Codes/PDE/data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/`. Frozen inputs were not changed. Python bytecode writing was disabled. Each executed check was constrained to at most 60 CPU seconds. No training, random sampling, new resolution, teacher/design change, or Git operation occurred.

The exact commands were the following, with the stated absolute packet and scratch paths expanded from this checkout:

```text
PYTHONDONTWRITEBYTECODE=1 python data/generated/two_layer_test_risk/sign_review_v1/packet/angle_error_bound.py --output data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/angle
PYTHONDONTWRITEBYTECODE=1 python data/generated/two_layer_test_risk/sign_review_v1/packet/check_certificate_driver.py --output data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/driver
PYTHONDONTWRITEBYTECODE=1 python data/generated/two_layer_test_risk/sign_review_v1/packet/check_certificate_kernel.py --output data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/kernel
PYTHONDONTWRITEBYTECODE=1 python data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/reassemble.py
PYTHONDONTWRITEBYTECODE=1 python data/generated/two_layer_test_risk/sign_review_v1/reviewer_b/scalar_checks.py
```

The first three were launched by a retained-result wrapper with `RLIMIT_CPU=(60,60)` and a 60-second timeout. Both independent scripts also set that CPU limit themselves. All completed with exit code zero. Reported CPU times were approximately `0.042`, `3.140`, `2.475` seconds for the three supplied tests, `25.423` seconds for the successful all-64 reconstruction, and `0.070` seconds for the scalar/evidence audit. Compile child accounting is included in the wrapper's test CPU figures.

The kernel test checks 19 fixed elementary-function inputs against an exact rational oracle, every lower tensor entry, all upper output groups against direct supplied finite tensor sums, and the omitted-zero-passive-root branch. Its maximum finite-rule discrepancy was `5.88418203051333e-15`. I did not treat its NumPy finite-rule oracle as a rigorous quadrature theorem; the analytic and arithmetic proofs provide that theorem. The driver test independently evaluates finite rational probability tables through explicit response means, checks constants/trigonometric widths/grid inequalities, and verifies the symmetry rule's cos3-squared mean. Its purpose is meaningful indexing/assembly validation. Archived elementary/lower/upper/singular raw test outputs exactly matched fresh outputs, and the archived exact driver/angular test values also matched.

Two initial reviewer-harness attempts failed and are preserved rather than suppressed. The first mistakenly classified the scalar metadata field `long_double_mantissa_bits` as an output array. The second assumed independent regrouped intervals must be nested in producer intervals; that is not a valid interval-arithmetic invariant. The final check uses complete producer-record equality plus independently valid reconstructed enclosures and their exact sign, while recording non-nesting explicitly. Neither event indicated a candidate scientific/code error or involved Gaussian integration. Error traces are retained as `reassemble_first_attempt_stderr.txt` and `reassemble_second_attempt_stderr.txt`.

The successful reconstruction source hash is `0d63f4de6cbb4e6d049409b4217b079a03e89b82826c67ee55d2117e56a30d3c`; its result hash is `1cd7d35094d411d7af2a80ef232c09b8922ef3bdac5e6500f64d60b1dfb3a47c`. The scalar-check source hash is `7ac50eff1ccbdb017c1e00a33ea6666b69fef9dd018f0b122ee495ba8041beb6`. Its final edit only removed an unused helper; no executed calculation changed. Detailed outputs are `fresh_tests.json`, `reassembly_result.json`, `independent_components.json`, `independent_non_nested_intervals.json`, `scalar_result.json`, and the corresponding stdout/stderr files.

## Hostile checks, component verdicts, and conclusion boundaries

| Attack / possible failure | Discriminator and result | Verdict |
|---|---|---|
| Wrong stored-weight/loss clock or hidden blocks missing | Re-derived raw updates, weighted onset, `p^T J=16 A/3`, and `p^T(J-beta a)=0`; all agree. | PASS |
| Singular original data or passive covariance invalidates a proof | `G` never inverted; `Q,V` training-only positivity proved; Price regularization and source null-space argument cover passive rank loss. | PASS |
| Reuse response replaced by a fresh matrix | Inspected both Gaussian conditioning formulas and all derivative mean products; separate four-slot Lambda assembly retains them. | PASS |
| Fixed-program algebra promoted to a positive-time trajectory | Complete C.2 tail/cap construction, one-reference stability, limit order, and actual-flow remainder checked. | PASS |
| Ambient L2 differentiability or trained fourth moments smuggled in | Fixed-direction Taylor and initial finite-query fourth moments suffice; no such unproved hypothesis is used. | PASS |
| Quadrature convergence trend mistaken for a certificate | Separate strip aliases, tails, tensor mass, covariance, label, arithmetic and angular errors are propagated into rational endpoints. | PASS |
| Singular Cholesky derivatives invalidate angle error | Isonormal angular construction and Gaussian derivative bounds avoid differentiating the chosen factor. | PASS |
| Unproved elementary-library accuracy or bit transport | Inspected polynomial primitives and accumulation bounds; exact scalar inequalities, echoed inputs, compiler contract, fresh binary/test identity verified. | PASS |
| Positive raw projection hides an adverse loss-matching term | Reassembled raw and clock contributions separately; their corrected interval plus angular error remains strictly positive. | PASS |
| Saved summary is inconsistent with primitive evidence | All 64 records, 21,120 outputs, exact final fractions, and all frozen hashes checked. | PASS |
| Hidden movement/nonaffinity inferred solely from the positive sign | Independently checked nonzero conditional innovation coefficients, initial full-support affine-fit gap, and continuity. | PASS |
| Finite-width or GD claim made too close to time zero | Actual finite random readout retained; positive-definite frozen kernel gives strict interpolated loss decay and inverse-clock convergence only on fixed intervals away from zero. | PASS |

The strongest ordinary limitation is that this is one specially specified fixed teacher/design with a very small local effect. The theorem openly retains that scope. The strongest structural concern was the population positive-time bridge; its complete response/tail and stability proof is supplied and checked. The most plausible numerical artifact was loss-clock cancellation combined with singular-root and angular errors; exact reassembly and the independent enclosure resolve it for this certificate. An interval containing zero would have left the sign unproved, regardless of raw projection or midpoint; the strict rational endpoints pass that predeclared criterion.

Both hidden preactivation and activation displacements have nonzero order-`t^2` onset, hence squared displacement order `t^4` and squared integrated speed order `t^3`. Conditional transpose covariance makes the lower coefficient nonzero; its lower-root-conditional variance survives projection on the initial forward span, leaving a noncancellable Gaussian forward innovation for the second layer. Everywhere-positive tanh gates preserve activation movement. The initialized Gaussian marginal affine-fit gap is strictly positive by strict Cauchy–Schwarz and remains positive locally by continuity. These facts support genuine local hidden movement and nonaffinity, not a practical magnitude or universal feature-learning claim.

Finally, `chi>27/100000` and `Mt<=27/200000` give `Delta(t)>=27 t^3/200000>0` on the stated positive interval. This makes the cubic term the first nonzero matched-risk term. The proof transfers a positive minimum on each fixed `[delta,t0]` to finite GF/raw GD in probability; it does not supply one finite width that works uniformly as delta tends to zero. No global training, later-time dominance, iid sample-complexity, growing-depth, or useful numerical time-window statement is accepted by this report.

**Required corrections: none. Unresolved acceptance-blocking objections: none. Review B is complete.** Reviewer A's separately assigned fresh full reproduction is outside this report; I neither repeated it nor used its result.
