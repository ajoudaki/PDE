# Migration-interface review

**Verdict: NOT CLEAN.** Four actionable interface findings remain. This verdict concerns routing, retained inputs, callable entrypoints, and source/data separation only; it does not establish or reject scientific correctness or full historical reproducibility.

## Findings

1. **P1 — The linear coefficient-map producer fails before argument parsing.** In [full_l2_paired_transition.py](/home/amir/Codes/PDE/studies/mfp_linear_growth_uniform_counterexample/full_l2_paired_transition.py:236), `main()` calls `PATHS.parse()`. The routing object assigned at line 32 has already been replaced by the mathematical path tuple at line 157. From the private external working directory, `python -B /home/amir/Codes/PDE/studies/mfp_linear_growth_uniform_counterexample/full_l2_paired_transition.py --help` raises `AttributeError: 'tuple' object has no attribute 'parse'`. The independent instrumented call recorded zero compiler calls. Consequently the generated map expected by the correctly routed immediate readers cannot be produced through this entrypoint. Keep the routing object and mathematical paths under distinct names.

2. **P1 — Several executable Gaussian entrypoints still overwrite retained source payloads and seals.** [forward_contraction.py](/home/amir/Codes/PDE/studies/mfp_gaussian_calculus/depth_order5_scalar/independent/forward_contraction.py:403) defaults `emit()` to its source directory, overwrites `FROZEN_FORWARD_RECURRENCE.json`, and regenerates its `.sha256`; its command calls `emit()` without a refusal at line 440. The reverse and moving contraction commands and the scalar multi-observable route-A Gamma04 command have the same source/seal behavior. The observables Gamma04 emitter also overwrites its retained recurrence and formula source. [build_full_report.py](/home/amir/Codes/PDE/studies/mfp_gaussian_calculus/depth_order5_scalar/independent/build_full_report.py:46) similarly overwrites `FULL_SCALAR_RECURRENCE.md` and its hash from its executable entrypoint. These are reachable commands, not code behind archive refusals. Executing the actual emitter/build bodies against tiny mocked transitions and an in-memory filesystem confirmed the destinations; no real transitions, reports, or seals were generated. Retire the historical writers before work, or provide a separate generated-output interface that does not recreate retained seals. Keep the existing proof payloads in source.

3. **P2 — The maintained package checker bypasses an archive-only command guard.** [order5/run_checks.py](/home/amir/Codes/PDE/studies/mfp_gaussian_calculus/order5/run_checks.py:3) imports and calls `audit_hostile.run()`. The refusal in `audit_hostile.py:770` protects only direct execution, so the package command still enters the retired mixed-input audit after its finite-width and compiler checks. Its callable tests read `order5/SINE_REGRESSION_RESULT.json` and `order5/independent/Q0_SPOT_COMPARISON.json` beside source, although the retained files are under historical data. A probe executing both actual `run()` bodies with scientific checks mocked reached the original spot-result reader and raised `FileNotFoundError` at that former source path. Handle the package consumer as well as the leaf command; it needs explicit routed inputs or an early archive refusal.

4. **P2 — The independent scalar comparison still reads coefficient maps from source.** [_read_accepted()](/home/amir/Codes/PDE/studies/mfp_gaussian_calculus/depth_order5_scalar/independent/depth_assembler.py:311) constructs the H2/H3/H4 map paths beneath `studies/mfp_gaussian_calculus`. All three reads fail there after separation. The live `--compare` branch calls this loader through `compare_accepted()`, after compiling the candidate, and offers no input selector. Direct loader probes reproduced all three missing paths without coefficient work. This loader is separate from the newly corrected shared reference loader. Give the comparison an explicit retained-input or generated-input route, or retire it before computation.

## Bounded evidence and coverage

- Independent probes followed actual first reads for the identity Hankel/spectral consumers, the linear map loader, and the three routed D3 gauge/tail/weighted analyzers. Defaults resolve under each study's generated data; historical and scratch selection propagate to those reads. D3 numerical work stopped before loading arrays or analyzing results.
- The newly frozen scalar audit routes selected coefficient maps and its sine-result consumer through the chosen study-data root. Its shared historical reference loader retains its logical labels, expected digests, schema checks, and counts. The two newly frozen replay commands refuse before imports/work; both were also checked directly from the external working directory.
- Five representative external-directory module/script help checks passed. Module checks explicitly supplied the repository on `PYTHONPATH`. The supplied Gaussian `test_migration_paths.py` was inspected only after independent source inspection: **9 tests passed** using private mocked fixtures. It does not cover findings 1–4.
- Cubic retained C++/Python inputs resolve in source, while its accepted result JSON resolves in historical data. The two C++ sources and several accepted inputs match their embedded hashes. Sine exact readers resolve their retained formula text and coefficient JSON correctly, with the checked hashes matching. Stieltjes retained proof/certificate JSON remains in source; its selected historical hidden-result paths exist.
- Causal default-producing probes route through the shared helper. Explicit-path experiment/analyzer interfaces and stdout-only exact tools were not classified as defective merely for lacking a generated-file default. Quadratic imports were traced within the allowed groups; no mathematical computations ran.
- The program-history PDF builder keeps maintained source separate and places generated Markdown, build files, and the PDF beneath `data/generated/mfp_program_history/report`. Its compiler was not run. Root `.gitignore` excludes generated data and transient products.

The cubic fixed-rho source does **not** match its retained embedded source hash. That unchanged provenance gate remains an honest reproducibility limitation, not a seal to refresh or waive. Dependencies into the out-of-scope quadratic-compiler study were identified from allowed source declarations but not opened. Missing optional packages were not installed; an inert SymPy import stub was used only for the identity first-read probe. No scientific, empirical, GPU, compilation, campaign, or resealing work ran.

## Input integrity and review boundary

The separate [input manifest](/tmp/pde-gaussian-routing-final.bJLzzv91/input_manifest.json) records exact start/end SHA-256 values for **485 scoped snapshot files**; all matched, with no additions to that enumerated set. This includes hash-only inventory entries, not a claim that every payload received substantive review. No repository or data files were written.

**Boundary deviation retained:** one early search used ineffective exclusions and exposed matching lines from the four pending files before confirmation. The user was informed; those excerpts were excluded from final evidence. A subsequent diagnostic boundary successfully blocked a transitive attempt to import the still-withheld reference loader. After the user supplied freeze confirmation, all four actual baseline hashes matched the supplied values exactly, the complete current files were read afresh, and their interfaces/dependencies were independently checked. Their final hashes also match. See [boundary note](/tmp/pde-gaussian-routing-final.bJLzzv91/read_boundary_note.md).

Evidence is in [diagnostics.json](/tmp/pde-gaussian-routing-final.bJLzzv91/diagnostics.json), [supplemental.json](/tmp/pde-gaussian-routing-final.bJLzzv91/supplemental.json), and [CLI checks](/tmp/pde-gaussian-routing-final.bJLzzv91/cli_checks.json), with small private reproduction scripts beside them. Prior review reports, other tasks, chats, and repository history were not used as evidence.
