# Shallow accepted-edition dependency check

The 27-file scientific edition was assembled from caf975a with only the accepted Gaussian chapter replaced. No studies, data, historical arrays or exporter files were included. The source fragment occurs exactly once; the previous chapter proof body is unchanged. This is a dependency check, not a re-audit of older proofs.

```text
Library boundary and local links checked: 25 files.
```

No new code or tests were introduced by this proof package. Both complete independent mathematical reports include direct exact cubic/fifth coefficient and dyadic-tail checks. The earlier 86-test implementation is unchanged in this edition.

The raw incomplete B1 report is retained byte-for-byte, including its extra blank line at EOF. The staged whitespace check reports that artifact-only warning; accepted mathematical and maintained files are clean.
