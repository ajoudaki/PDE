Read-boundary deviation: the search in exec cell returning chunk aade98 used directory-relative exclusions that did not match the searched full paths. It exposed matching lines from all four pending Gaussian files before freeze confirmation. Earlier initial SHA-256 manifest and the first grouped search used full-path exclusions and excluded all four. No execution/import of the four occurred. The excerpts are not accepted as final review evidence. They require post-confirmation baseline hashes and fresh inspection. The user was informed immediately. Subsequent searches must use the exact full-path excluded list, or a prefiltered manifest of allowed filenames.

Resolution: user explicitly confirmed all four frozen and supplied SHA-256 hashes. All four actual post-confirmation hashes matched exactly; they were added to input_manifest.json with baseline=post_freeze_confirmation. Full current interfaces are inspected afresh. Before confirmation, the private diagnostic audit hook also successfully refused a transitive import attempting to open reference_maps.py; that attempted read did not access its content. Earlier search excerpts are not final evidence. The deviation remains recorded and is not erased by the new baseline.
