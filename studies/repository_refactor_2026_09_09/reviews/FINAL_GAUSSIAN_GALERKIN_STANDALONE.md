# Gaussian/source transport standalone check

Library boundary and local links checked: 25 files.

The selected accepted scientific files contain no studies, generated data, exporter or unaccepted live API changes. Both complete reviewed proof fragments occur exactly once; removing them recovers the preceding committed chapters. No production code changed in this package and no new deterministic test run is claimed for it. Complete testing of the new APIs is recorded separately.
