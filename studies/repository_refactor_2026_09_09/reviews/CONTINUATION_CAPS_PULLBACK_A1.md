CLEAN

Independent isolated review A1 of the supplied given-space capped-flow/conditional-continuation and finite loss-Euler pullback candidates. No required correction was found in the requested mathematical, implementation, interface, or test scope. There are no severity-ranked findings.

The only candidate source was `/tmp/pde_continuation_review_a1`, as enumerated by its `INPUTS.json`. I read all 11 manifest files completely, including all dependencies and tests, totaling 2,498 lines. I also read `/etc/codex/skills/solve-math-rigorously/SKILL.md`. I did not read the project checkout, history, studies, prior reviews, other tasks, or other reviewers' work; I did not delegate or communicate with other reviewers. No candidate input was edited. The before/after checks below match every manifest hash, size, and line count.

**Given-space capped flows and conditional continuation.**

- `docs/given_space_caps.md:15–49`: the half-sum loss, mean-loss factor `2/3`, Hilbert metric, three unit input directions, genuine adjoint, and zero readout initialization are consistent. The supplied state membership includes the required finite first-block norm. Singular input Grams and coincident inputs cause no division or inverse. The loss at initialization is exactly `3/2`. The bounded initial middle operator is assumed on the specified spaces, without being identified as a Gaussian operator or assumed Hilbert–Schmidt.
- Lines 51–137 supply the Hilbert–Schmidt identities, boundedness and continuity of every field, and the chain rule needed for these equations. The gate-product continuity argument uses bounded continuous gates and strong `L²` convergence correctly. The curvewise chain rule uses coordinatewise absolute continuity from the time-integrable Hilbert velocity and Fubini, then obtains continuous `L²` derivative fields. It does not rely on an invalid general Fréchet differentiability claim for the activation superposition map. All adjoint and rank-one contractions have the stated types.
- Lines 141–162 correctly construct smooth scalar and radial caps. Their radial and tangential derivative eigenvalues give the asserted 1-Lipschitz bounds; their magnitude, identity region, positivity, and defect inequalities hold, including at zero.
- Theorem 12.1, lines 165–252, is supported by a complete local construction and extension argument. The auxiliary cap acts only in backward fields. Its top-gate estimate has coefficient `M`, because the amplitude bound is `2M` and the gate Lipschitz constant is at most `1/2`. The lower-gate estimate has coefficient `R`. The lower cap's input Lipschitz constant is one, so the combined ball-wise constant is of order `1+R`, not a product of cap sizes. The readout integral bound makes the auxiliary cap inactive before the chosen horizon. One common scalar cap multiplies the whole first-row gradient, preserving dissipation even with cancellation among sample directions. The energy inequality uses `chi² <= chi`, and gives the stated displacement bound. Bounded fields on the resulting energy ball give a strong endpoint limit and extension. The argument establishes uniqueness among all strong solutions of the capped equations from the specified initialization.
- Theorem 12.2, lines 256–407, remains explicitly conditional on (12.17). The theorem does not establish that premise. The conversion of its readout-direction tails to `L^j` bounds, integration in time, integer-moment Markov estimate, and square-tail integration correctly produce (12.19), with possibly changed constants and exponential rate.
- In the stability argument, the reference readout tail controls the top gate and the reference incoming-field tail controls the lower gate. The first term of the lower-gate difference multiplies an incoming-field difference by a bounded gate. Therefore (12.21) has one cutoff factor and yields the required `s log(B/s)` modulus. Only the reference state needs quantitative tails. The first-block cap defect constant `sqrt(3)` follows from the three unit input directions; there is no middle-block defect.
- The positive scalar majorant and its first-exit argument correctly show Cauchy convergence of the full real-indexed cap family on each compact horizon. The uniform convergence of continuous field maps is proved using compactness of the time interval and the limit curve, without invoking compactness of Hilbert balls. The strong limit solves the uncapped integral equation; its energy identity follows from the established chain rule. The raw kernel blocks have the correct Hilbert-metric contractions and converge by strong field convergence and Cauchy–Schwarz.
- Fatou's argument with threshold `a/2` transfers the tails to the limiting reference curve. The resulting one-sided comparison proves uniqueness against every strong competitor with the same start on a common compact interval. Applying it to a time-shift of the constructed curve gives exactly the asserted restart domain: states reached by this initialized curve. It does not assert existence from every arbitrary Hilbert state. Lines 409–417 correctly retain the separate obligations concerning Gaussian action construction, finite GF/GD identification, fitting, and feature motion.

**Finite loss-Euler pullback calculus.**

- `docs/loss_pullback.md:11–56`: the transformation `x=D^{-1/2} theta` gives the exact stated Euler map for a fixed symmetric positive-definite mobility. The full square-loss residual is recomputed with the state. The batch generalization uses the gradient of the mean loss. For fixed update count, an open step interval around zero exists, and the stated `C^6` hypotheses, or `P in C^7`, ensure the needed scalar step regularity. No change from nonlinear coordinates is used to turn GD into a different Euler map.
- Lines 60–134 correctly distinguish freezing the direction inside a single derivative from differentiating the resulting field under an outer operator. The composition recurrence exhausts all ordered words. The binomial identity uses powers of the single exact difference operator, so it does not require commutation of different `T_k`. The finite target-degree argument is justified at the stated regularity. The paired weights are `binom(2N,q)-2^j binom(N,q)`, and their degree bound follows by cancellation of the leading term for `q=j`.
- Every row of the degree-five table at lines 138–155 is correct. The sixth-derivative integral remainder at lines 157–169 has the correct factor `eta^6/5!`, and is confined to a fixed update count and a valid step interval.
- Lines 173–260 correctly derive the cubic formula. I separately checked the Euler-state cubic coefficients, the contractions `T_P`, `S_P`, `U_P`, and their conversion to an arbitrary residual and predictor derivatives. In particular, the coefficient `11 r^3 K B` includes the contribution from the third derivative of the square loss. The stationary cases and the exact affine-predictor formula agree. An independent exact two-coordinate nonlinear example described below verifies the full quadratic and cubic coefficients for three update counts.
- Lines 264–321 state enough tube inclusion for the telescoping proof: every required hybrid endpoint and each intermediate fine step lies in the convex set. The local defect, propagation factor, and final `R_T L_T M_T T^2 exp(L_T T)/(4N)` scalar bound are correct, including zero-bound cases. The dyadic Cauchy assertion requires the common bounds and tube inclusions it states. The text correctly distinguishes this estimate from the algebraic fixed-order degree bound and does not infer a uniform analytic remainder, arbitrary-partition result, or width limit.
- The contracts and proofs at lines 325–363 agree with `code/pde/exact_calculus.py:44–99`. Word order, exact `Fraction` weights, zero-order and zero-step outputs, omitted zero weights, coefficient ordering, output sizes, integer input restrictions, fresh return ownership, and the stated operation/storage bounds were checked. Neither interface evaluates derivatives or neural coefficients.

**Supplied dependencies and remaining implementation.**

`docs/finite_dynamics.md` supplies the finite gradient, kernel, energy, existence, and norm arguments used by the normalization contract. I checked the endpoint mobilities, factors of `n`, `d`, and `m`, the residual-free backward recursion, and the dissipation identity. Its global finite-flow argument uses the constant positive metric and finite energy to obtain a Cauchy endpoint; it requires no bounded activation and makes no unconditional GD stability assertion. Its width-uniform norm induction is under the separately stated bounded-first-derivative and initial-norm hypotheses. The Gaussian net estimate has the correct bound `2*9^(2n)*exp(-n*M^2/8)` and admits a fixed sufficiently large `M` at fixed depth.

`docs/gaussian_dependencies.md` supplies the needed proofs rather than requiring an unprovided specialized theorem. Its integration-by-parts recurrence permits singular covariance, and the rational Schur-complement validator handles negative, positive, and zero pivots correctly. In the forest expectation argument, every Wick pairing has `v <= p+c <= p+r`; a surviving pairing cannot connect original components. Extra equalities among free labels cost one power of width. This proves the stated expectation factorization, including odd-edge components and isolated vertices, without claiming concentration. The tree/forest key argument correctly establishes equivalence with color-preserving isomorphism and the code validates simple bipartite forests.

The separate quadratic certificate explicitly freezes the first block and uses the different order-one readout initialization and feature-ascent metric. Its finite polynomial ODE, conditional Gaussian law, moment convergence, monomial recurrence, formal inversion, six coefficients, shifted determinant, and direct polynomial witness are consistent. The formal construction is not used as an analytic series or a positive-time population equation. The moment obstruction has precisely the metric scope stated at lines 294–301. The finite reversion and determinant proofs cover the needed cases. No missing nontrivial specialized dependency was found in the requested scope; standard elementary calculus, finite-dimensional linear algebra, measure, and Hilbert facts were used at the stated hypotheses. Contextual references to other chapters do not serve as unstated proof steps here.

I read every function in `finite_network.py`, `gaussian_moments.py`, and `exact_calculus.py`, the package exports, numerical contract, and all test helpers and assertions. The finite model's array shapes, raw stored-weight convention, callback copying, revalidation, simultaneous updates, exact rational validators, local cache behavior, and fresh outputs are consistent with the supplied contracts. The numerical contract explicitly limits its robustness claims to the described float64 scaling and permits intermediate overflow/underflow in raw contractions; the implementation does not promise more.

**Executed validation.**

1. Deterministic supplied target, from the isolated root:

   `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=code python -m unittest discover -s code/tests -p test_exact_calculus.py -v`

   Result: 11 tests passed, including exact slot enumeration, every displayed temporal polynomial, nonlinear moving-field word evaluation through degree six, rational domains and ownership, reversion in both directions, independent determinant evaluation, forest invariance/validation, and the complete displayed certificate and witness.

2. Independent bounded checks, kept outside the candidate inputs in `/tmp/pde_caps_pullback_A1_checks.py`:

   `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/tmp/pde_continuation_review_a1/code python /tmp/pde_caps_pullback_A1_checks.py`

   All passed: exact paired full-square-loss coefficients through cubic degree for a two-coordinate cubic predictor at nonzero residual for `N=1,2,3`; temporal-row evaluation against binomial values for orders 1–8 and `N=0..6`; six Gaussian moment or singular/indefinite validation cases; and a fixed two-layer, two-sample finite gradient/metric/energy/simultaneous-step check. The finite gradient comparison used centered differences with tolerance `2e-10`; the rational checks used exact equality. These were small deterministic algebraic checks, with no random initialization, training path, sampling, or coefficient campaign.

Tests support the finite computations; the mathematical proofs above carry the analytic claims. No neural concentration or width-limit claim is inferred from these checks.

**Full read coverage and integrity.**

Each row was read in full, from line 1 through the listed final line. The listed SHA-256 matched `INPUTS.json` both before and after review; byte and line counts also matched. No bytecode was written to the input bundle.

| File | Full line coverage | Bytes | SHA-256, before = after |
|---|---:|---:|---|
| `code/NUMERICAL_CONTRACT.md` | 1–91 | 4890 | `3791adea2ec3a4713a265ab1aa2b00e9993b51a957415060bebc88fe2399de22` |
| `code/pde/__init__.py` | 1–26 | 611 | `65eb96a5dd455041d2ad1f32652e3e26e8c15192eb41bdffba27980f0f69e8c3` |
| `code/pde/exact_calculus.py` | 1–249 | 10108 | `d7cd27b3bffed6152bb9fad40514a8e2848561fa6d44e0cb1aa0fd652d7e5aa3` |
| `code/pde/finite_network.py` | 1–363 | 15525 | `efe694b200b8af2603cbf1bb9d0249f49636e874088c2bafd98f402b5bfbe551` |
| `code/pde/gaussian_moments.py` | 1–114 | 4464 | `6c2a21a9f3d101c433b5f06cb3f6e1dcdeca23a814891ea7bd81e73960e854ae` |
| `code/tests/test_exact_calculus.py` | 1–217 | 10214 | `58881bee416ba4b5db9e7688003bfcbef985c4fc82e006c29dbe8a25c32ce242` |
| `docs/NOTATION.md` | 1–98 | 5110 | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| `docs/finite_dynamics.md` | 1–214 | 8355 | `486a2864738d23c21e14e3830ee4cd9555ef2e909d2054366890a958a3dc626c` |
| `docs/gaussian_dependencies.md` | 1–346 | 17119 | `e52365e7ed2afaf3d48272b913caf60e1aa59f55d846019527f84ae15592cf84` |
| `docs/given_space_caps.md` | 1–417 | 18663 | `cdda914e481b46d51366a5d80661cd192b0f319d6507bc8b29eba69d87f9f95d` |
| `docs/loss_pullback.md` | 1–363 | 15809 | `3e14b0633a891b98c2ed25f3e1147e7d5e90b193a021b18e9d2875acb502575f` |

Total: 11 files, 2,498 lines. Required corrections: none. Verdict: CLEAN within the supplied scope and its explicit hypotheses.
