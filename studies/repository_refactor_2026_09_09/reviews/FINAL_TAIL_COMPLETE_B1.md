**Separate verdicts: LAST A–I — passes within its stated scopes. SCOPE A–E — passes within its stated scopes. SHALLOW — passes within its stated scope.**

I found no mathematical error requiring correction in the six supplied scientific files. These verdicts include checking the supplied dependencies themselves; they do not rely on an external theorem citation, presumed dependency verdict, experiment, or prior audit. The conditional and formal statements remain conditional and formal. None establishes a broader nonlinear limit theorem than it explicitly states.

### LAST A–I

Reviewed [FINAL_LAST_ADDITION.md](/tmp/pde_final_tail_b_glhw1wpf/FINAL_LAST_ADDITION.md), lines 1–2229.

**A — trained affine-reference coupling, lines 19–262.** The calculation uses three hidden layers, canonical Gaussian initialized actions, zero population readout, the raw product metric, and half-summed loss.

The parameter expansions retain changes in both hidden actions and the first layer. Their rooted Gaussian-word contractions give the marginal-variance coefficients \(1,3,6\). The conditional fourth-moment calculation for
\(\tau((A^*B^*BA)^2)\) gives \(3\), consistently with the supplied Wick construction.

The null identity \(v^Tf_e=eN_{v,e}\) holds on the entire raw state space because \(\sum_i v_i u_i=0\). Gaussian integration by parts on the affine trajectory therefore gives
\[
N_v(t)=10m'(1)\sum_i v_i(\Gamma y)_i^3t^3+O(t^4).
\]
For the displayed realizable triple, the cubic contraction is \(7/2+5/\sqrt2>0\). The gradient argument correctly obtains the null-row derivative without asserting twice Fréchet differentiability of an \(L^2\)-valued activation map. The strong/weak kernel block can consequently be first order in \(e\) at a trained affine-reference state despite being second order initially.

This is a reference-state calculation. It neither constructs the corresponding nonlinear trajectory nor disproves a Schur argument that retains first-order mixing and the moving-constraint derivative.

**B — conditional construction, lines 265–627.** The implication is valid under the stated uniform \(Kp\) bounds for the *actual stopped capped incoming fields*. The forward and backward difference estimates use only the reference multiplier’s moments. Optimizing the Hölder exponent produces the Osgood modulus \(D\log(e^2/D)\), whose reciprocal integral diverges.

The approximate energy identity is correctly derived for the capped nongradient field. It supplies the displacement bound needed to close the stopped-ball argument. Comparing two capped recursions directly avoids a cap-dependent exponential multiplying merely exponential tails. Both states and derivatives become uniformly Cauchy; strong multiplier continuity identifies the limiting raw gradient. Fatou transfers the reference moments and supplies uniqueness against bounded-primal competitors and reached-state restart.

The premise is not proved here. Finite-width GF/GD identification, fitting, and trained nonaffinity are explicitly separate obligations.

**C — strong Gaussian-norm obstruction, lines 630–698.** For each fixed positive perturbation,
\[
x\bigl[g(1+\varepsilon x)-1/2\bigr]/x\longrightarrow-1/2.
\]
The Gaussian integral therefore diverges for every proposed Orlicz scale below \(1/\sqrt2\). This proves the asserted discontinuity, while dominated convergence gives convergence in every fixed finite \(L^p\). Multiplication by the fixed nonlinear amplitude gives the stated gate obstruction.

The weaker \(\psi_2\times\psi_2\to\psi_1\) product estimate is valid without independence. It supplies no same-space contraction. The conclusions concern a norm and construction method, not failure of population dynamics.

**D — response focusing and raw-ball curvature, lines 701–968.**

- The smooth query construction has exactly orthonormal instantaneous sample queries, bounded values and bounded temporal \(H^1(L^2)\) norms. Its causal observation is uniformly sub-Gaussian. The signed response respects the covariance-weighted derivative bound yet concentrates on an interval of probability \(1/(3N)\), producing the stated divergent \(L^p\) and \(\psi_1\) norms and failure of square-tail uniform integrability. Writing a response coefficient as a time step times a density does not bound that density.
- The Hessian example uses genuine canonical initialized hidden actions and modifies only the readout. The rank-one direction has raw norm one and isolates one sample. The positive predictor-gradient term stays bounded, whereas residual curvature tends to \(-\infty\). Differentiation is justified separately for each bounded concentrated direction.

Both are correctly delimited: the first is a designed causal query program; the second is an ambient raw-ball state. Neither claims canonical reachability.

**E — stationary obstruction for smooth positive metrics, lines 971–1280.** The scalar transform is valid for the stated toy system. The nonzero real eigenvalues of the bracket derivative exclude a common metric making all the rowwise control fields Killing; the inverse-metric calculation separately identifies the Hessian-integrability obstruction.

The equal-elasticity lemma follows from opposite signs of its small- and large-argument asymptotics. Applying it three times gives all four exact stationarity cancellations. The finite-rank modifications of the canonical actions are Hilbert–Schmidt with bounds independent of concentration. The resulting states have nonzero residual and loss strictly below \(3/2\).

On the invariant finite-dimensional submanifold, the unit-direction negative curvature diverges, so the GF linearization has positive eigenvalues tending to infinity. At a stationary point the derivative-of-metric-along-flow term vanishes. Positivity of any restricted metric then forces its differential growth bound to exceed each such eigenvalue. Uniform equivalence to the raw metric is unnecessary.

The obstruction is to one finite differential bound on the entire specified ball. It does not establish that these stationary states are reached from canonical initialization.

**F — order-one-readout two-hidden-layer theorem, lines 1282–1734.** The effective first-coordinate/readout metric and the connector Frobenius metric give the displayed feature equations and all three kernel terms. The natural-coordinate transformation changes coordinates, not the optimizer.

The source construction meets III.F’s hypotheses: the non-Gaussian tuple \((u_0,\Theta(u_0))\) is an allowed finite-second-moment root tuple; clipped readout instructions have bounded derivatives; scalar feedback is causal. The trace-class state space and its rank-one estimates are supplied by the complete trace-space dependency.

Bounded activation gives the pointwise readout increment envelope. The smaller-cap readout controls the asymmetric gate comparison; Gaussian initial tails dominate its \(e^{C_SM}\) stability cost. This constructs the global feature flow in both time directions. The Gaussian-envelope Osgood estimate supplies uniqueness and restart with the immutable source retained.

The two mesh arguments are logically distinct and correctly ordered: first obtain uniform empirical square-tail control, then pass the kernel’s varying multiplier. Cap removal and the monotone scalar clock give the stated physical-time limits. The initial finite prediction is controlled using its actual conditional variance, rather than being set to zero.

The arctangent inverse formula, \(E\Theta(U)^2=14/3\), and reciprocal-polynomial examples satisfy the hypotheses. The theorem proves compact-time continuous-GF prediction, loss, kernel and specified current-field convergence. It asserts no raw-GD theorem, full hidden-velocity bundle or asymptotic fitting result.

**G — equal-label sech-gate transfer, lines 1736–1984.** The replacement activation satisfies every value, slope and curvature bound used by II.B–II.C. The two formula-dependent activity steps are also checked:

- Its feature separation exceeds the original \(\pi/20\) threshold.
- Its top gate-ratio rectangles give ratio below \(1/4\) and the required positive lower gate bound.

Thus the response bootstrap, reference-tail comparison, cap removal, full residual-vector finite comparison and ordered observation transfer apply. The positive readout feature gives \(g_s\ge25/36\), yielding the stated physical decay rates. The antipodal input case uses its one-field metric rather than a singular Gram inverse.

The positive backward Grams, sample-exchange argument and initialized directional chain rule establish the claimed positive-time speeds and nonzero quadratic feature/kernel terms. Scope remains two equal binary labels, three hidden layers, fixed activation, small stored readout, summed loss and compact-time width convergence.

**H — prescribed-control characteristic, lines 1987–2122.** The integrable Lipschitz bounds construct the characteristic and its derivative with the control held fixed. In the \(C_\rho^{-1}\) energy, the positive growth is bounded by the displayed \(h\). The truncated potential controls the nonnegative-correlation case directly. For negative correlation, monotonicity of \(x+y\), including its flat level sets, bounds time spent in the central strip. The remaining contribution is exponentially small.

Choosing \(Q=2\log(e+U)\) gives the stated polynomial tangent bound. The relative-gate inequality follows from \(|\phi''/\phi'|\le1\). Random-control moments require only the stated pathwise sign condition and moment of \(U\), not independence. Feedback variations are outside this result.

**I — first-Euler neighborhood obstruction, lines 2125–2229.** The auxiliary state correctly uses exactly zero initial readout and one fixed Euler step. Conditional Gaussian projection identifies a nondegenerate reverse innovation and positive probability of arbitrarily large query values while the middle preactivation lies in \([1,2]\).

The rank-one perturbation changes one preactivation by \(A^{-1}\) and has operator norm \(1/(A\|h_0^{(1)}\|)\). The old query in the gate difference supplies a fixed coordinate effect; the remaining query and residual errors are \(O(A^{-1})\) at the corresponding scale. The velocity/state ratio is therefore at least \(cA-C\).

The threshold is chosen before width tends to infinity. This proves the stated width-uniform neighborhood obstruction in the displayed distance, not a continuous-trajectory obstruction or failure of uniqueness.

### SCOPE A–E

Reviewed [FINAL_SCOPE_ADDITION.md](/tmp/pde_final_tail_b_glhw1wpf/FINAL_SCOPE_ADDITION.md), lines 1–1256.

**A — tanh vanishing-time continuity, lines 11–257.** The order-one stored readout is essential and is consistently retained. Odd truncation and independent centered readout make the initialized transpose-response coefficients vanish. Ordered truncation identifies the backward variances and the four-block \(K_0\).

Conditional Gaussian exponential averages and maxima require no coordinate independence. The entropy/Jensen multiplier estimate is correct. In the backward comparison, each new gate difference multiplies a fixed initialized field, so the logarithm is not iterated. This proves the vanishing-time field/kernel bounds and the immutable-query maximum estimate.

For zero label, the actual initial residual is \(O_{\mathbb P}(n^{-1/2})\); its decreasing magnitude makes the whole compact physical interval correspond to a vanishing feature interval. The stationary limiting conclusion follows.

Both column-balance identities are exact, with the factor \(n/2\) matching the raw connector update. They yield the stated inverse-gate RMS bound, but do not imply uniform square tails of the ungated query. No positive-time nonzero-label limit or GD conclusion is inferred.

**B — sin-plus-cosine initialization, lines 259–413.** The Gaussian product identities give all three Gram recursions with diagonal one. Tensor-power expansion and polynomial interpolation prove strict positive definiteness for distinct normalized inputs, including singular input Grams.

The two transpose-return matrices \(J_3,J_2\) contain the correct curvature terms. The reverse innovations use full second moments. The top nonzero-factor argument and the tensor-product Gram argument prove strict positivity of all three hidden coefficient matrices. The one unbounded reverse product is justified by clipping, moment domination and bounded-action passage.

The degree-two hidden blocks and doubled total label-mode coefficient have the correct mean-loss factor \(\gamma=2/m\). They are formal initialization coefficients, not an analytic trajectory expansion. The trigonometric Gaussian integral and regression residual \(1-2/e\) are correct. The first-node Gaussian query disproves precisely the proposed uniform \(Kh e^{Khp}\) estimate.

**C — fixed-offset affine reference, lines 415–595.** The given-space raw field is polynomial and locally Lipschitz. Its exact energy identity gives a strong Cauchy endpoint at any finite maximal time, proving global existence and restart.

The canonical initialized Grams are \(G+\ell\mathbf1\mathbf1^T\). The complete kernel, augmented-Gram lower bound and all three offset defects in the homogeneous balances are correct. Compressing by the constant-orthogonal projections removes exactly those defects.

For three distinct unit inputs, augmented independence justifies the stationary-predictor classification. The mixed-label loss threshold \(4/3\) is correct. Under the additional positive raw-Gram and sub-threshold-entry assumptions, the residual-length clock gives bounded total clock, a strong endpoint and exponential physical-time fitting. Neither additional assumption is silently obtained from pairwise separation.

**D — same-array source estimate, lines 598–795.** The Volterra inverse bounds handle a lower-triangular \(B\) with concentrated old columns. The proof does not incorrectly assign a column-density bound to \(AB\). Both Gaussian parts use the same frozen coefficient arrays and covariance as the actual recursion.

The actual second-moment bounds control those Gaussian parts. Discrete Grönwall then proves the displayed \(C\sqrt p\) bounds without a small-\(\varepsilon S\) assumption. The result remains conditional on the coefficient and actual-field bounds.

The eigenvalue argument gives \(\lambda_2(G)\ge\delta\). Gaussian regression gives the exact initialized kernel decomposition and strong/weak block estimates. The residual-coordinate calculation correctly retains \(\dot L-L\sigma\); the constrained-readout decomposition retains its orthogonal component. Neither provides a trained Schur floor or autonomous scalar closure.

**E — reached-state non-Lipschitzness, lines 797–1256.** The local feature construction applies to both label modes because II.B–II.C.2 do not use equal-label fitting. Symmetry in law followed by deterministic limits gives \(f_a=y_ag\); choosing the initial interval small enough makes the physical clock positive.

The lower-tail proof supplies the missing joint statement rather than assuming independence of trained fields. Its main steps are valid:

1. Uniform primal estimates give the top-delta temporal \(L^2\) modulus and hence the reverse-source covariance modulus.
2. Dyadic Gaussian bounds control the residual source path after scalar regression.
3. A large terminal reverse source forces a large actual query regardless of the independent forward source.
4. Scalar regression of the forward source, bounded displacement and a controlled Lipschitz constant put the terminal preactivation in any prescribed interval.
5. Closed interior events pass through the mesh and cap limits.

The first-feature Gram dual isolates one sample with a raw unit rank-one direction. Each test direction is individually bounded, which justifies its second directional derivative. The upper-layer remainder is uniformly bounded using the reached readout’s pointwise bound. The middle curvature multiplier has both unbounded essential tails. Substituting the residual symmetry only at the base state gives the correct full-loss identity
\[
-D^2\mathcal L[v,v]
=4(1-g)D^2g[v,v]-2\sum_a(Df_a[v])^2.
\]
Consequently the full loss has both signs of unbounded unit-direction curvature and its raw gradient is not locally Lipschitz at these reached states.

This is a local population result at fixed positive times. It asserts neither a finite-width positive-time Hessian limit nor opposite-label global continuation.

### SHALLOW

Reviewed [FINAL_SHALLOW_RATE_ADDITION.md](/tmp/pde_final_tail_b_glhw1wpf/FINAL_SHALLOW_RATE_ADDITION.md), lines 1–161, and its complete 215-line dependency.

**Verdict: passes.** The independent order-one Gaussian marks, full-square loss and common mobility match the characteristic equations and effective raw metric.

Bounded \(\phi'\) gives linear growth and global characteristics for both signs of feature time. The quadratic envelopes justify \(F'=EJ\ge0\). The integral empirical bound reduces the continuum supremum to centered sample means; the second-, fourth- and eighth-moment expansions have the required \(n^{-1},n^{-2},n^{-4}\) scales.

On \(\{|F_n(0)|\le1\}\), both clocks stay in a deterministic compact interval. Clock stability uses the deterministic limiting \(F\), avoiding a random Lipschitz constant. On the complement, actual residual monotonicity controls prediction error directly. This proves both prediction rates, the variance bound and the loss rate.

For bounded activation, the explicit characteristic displacement bounds and Cauchy–Schwarz prove the coupled particle-path estimate. Exchangeability transfers the average estimate to each fixed particle, and the deterministic limiting clock makes the limiting paths independent. No independence between an empirical discrepancy and an individual mark is assumed.

The conclusions concern fixed-horizon continuous GF, with label one and the stated initialization. They do not include GD rates, kernel rates, growing depth or fitting.

### Dependency audit

The complete [FINAL_LAST_DEPENDENCIES.md](/tmp/pde_final_tail_b_glhw1wpf/FINAL_LAST_DEPENDENCIES.md) was checked, including portions not operative in a particular candidate.

- **Finite model, lines 8–213:** the gradient factors, mobility-weighted kernel, dissipation identity and global finite-GF continuation are consistent. Width-uniform estimates require the stated initial RMS/operator bounds, bounded activation derivatives and fixed depth.
- **Special-data II, lines 219–1629:** the metric change \(W^{(1)}=\sqrt d\,V^{(1)}\), summed-loss clock, response induction, cap removal, nonsymmetric competitor comparison, actual finite residual control and observation transfer are consistent. Singular root covariances retain their formal source slots. I also checked the numerical bootstrap: \(Q_*=24829/19200\), and the displayed \(U_*\) fraction equals \(7895890947/8192000000<97/100\). The nonaffinity, positive-speed and initial quadratic claims use separate valid arguments.
- **Special-data III.M/F, lines 1635–2453:** adaptive conditioning adds the actual linear constraints to the queried matrix and preserves conditional independence of residual matrix factors. Projection errors vanish in normalized square norm. Gaussian integration by parts yields the stated response terms. Fresh query perturbations remove singular-Gram restrictions without assuming pseudoinverse continuity. The countable construction, density argument, action bounds and transpose pairings produce genuine bounded adjoints. Scalar Fréchet differentiation is distinguished correctly from the strong curve chain rule.
- **III.S/G/V, lines 2455–3579:** the controlled source induction uses only previously constructed rows, retains current transpose returns and keeps the actual arrays in its Gaussian decomposition. The gain estimates close at every separately fixed finite depth. Geometry, total control time and regression stability supply the claimed bounds. Cap removal uses tails only on the reference. Finite algorithms are compared with fixed auxiliary transcripts; observation caps are removed in dependency order, and compact \(L^2\) tails support uniform velocity and path-law passage.
- **III.N/A, lines 3581–4510:** forward Gram positivity, reverse covariance positivity, upper-layer innovations and derivative-valid clipping justify every initial-motion assertion. The physical coefficient \(18\) follows from two contributions of \(9\|\mathscr V\|^2\). The regression-margin arguments correctly distinguish broad depth-dependent bounds, compact-support obstructions and the stronger open function class with a depth-uniform margin.
- **Trace-space dependency, lines 4514–4696:** singular expansion, approximation numbers, trace-norm completeness, ideal estimates, trace identities and finite-Gram singular-value reduction are proved internally, including singular Grams.
- **Gaussian-word dependency, lines 4699–4861:** Wick expansion, quotient-graph counting, surviving noncrossing pairings, variance decay and rooted probes establish the required fixed-word limits. The doubled word space preserves orthogonality of the entire two rooted subspaces.

The [shallow dependency](/tmp/pde_final_tail_b_glhw1wpf/FINAL_SHALLOW_RATE_DEPENDENCIES.md), lines 1–215, independently proves global characteristics, scalar-clock uniqueness, consistent restart, compact-time moment bounds and the uniform empirical comparison. Its almost-sure conclusion is correctly restricted to the nested iid coupling.

**Required mathematical corrections: none identified.** In particular, these verdicts do not promote conditional moment hypotheses to established bounds, formal coefficients to trajectory theorems, ambient counterexamples to reachable counterexamples, or fixed-depth/compact-time convergence to joint depth or infinite-time limits.

### Reading and integrity attestation

I personally read the required `/etc/codex/skills/solve-math-rigorously/SKILL.md` and every line of all six scientific inputs:

| Scientific input | Lines |
|---|---:|
| FINAL_LAST_ADDITION.md | 2,229 |
| FINAL_SCOPE_ADDITION.md | 1,256 |
| FINAL_SHALLOW_RATE_ADDITION.md | 161 |
| FINAL_LAST_DEPENDENCIES.md | 4,861 |
| FINAL_SHALLOW_RATE_DEPENDENCIES.md | 215 |
| NOTATION.md | 98 |
| **Total** | **8,820** |

Two output truncations occurred in the long dependency reads. The omitted passages were recovered by bounded rereads of lines **2174–2188** and **3411–3462**.

Permitted content reads were limited to those six scientific files, `INPUTS.json`, and the expressly requested skill. I listed the current directory but did not read its logs, prompt copy or other files. I accessed no excluded scientific directory, history, source audit, prior verdict, Git, network or external source. I used no delegation or memory and ran no experiments, simulations or implementation tests. The only calculation tool use beyond reading and hashing was bounded rational arithmetic.

No files were written or modified. Final SHA-256 hashes exactly match `INPUTS.json`:

```text
FINAL_LAST_ADDITION.md
bb28eb597f0e6a230e90674eb379e2a901e2038f8aa1138b1e236e137c212976

FINAL_SCOPE_ADDITION.md
b85476dd8886365c292de1cd19463f988971d6f472800d838a8a1cd6d7b166d6

FINAL_SHALLOW_RATE_ADDITION.md
0adf42cdd2cfa3328a7e21f6e72eb4a9a2f5311a4da05edff3b71130564d11b8

FINAL_LAST_DEPENDENCIES.md
e27b438b599b7e8554f05cca455e9b20913ac06bea5342cb845151e0b8e8aecf

FINAL_SHALLOW_RATE_DEPENDENCIES.md
ec3f238c98356de7ee4311e7cdaff5da6cfca359cf0612b1472cb98165830f35

NOTATION.md
199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b
```