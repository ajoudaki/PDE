# Isolated adversarial mathematical audit

**Verdict: CLEAN.** No required mathematical fixes identified. The L2 global compact-time theorem and the L3 local joint population / finite gradient-flow / exact raw-GD theorem are supported by the supplied proofs, with the stated initializations, topologies, clocks, and restart domains. This verdict does not extend L3 beyond its explicit local interval.

## Scope, read coverage, and input identity

The audit used only these two input files. Both were read completely, including every displayed equation and the notation contract. No repository, history, other review, external source, agent, or experiment was consulted. Calculations below are direct mathematical checks of the supplied arguments.

| Input | Complete coverage | Bytes | SHA-256 |
|---|---:|---:|---|
| `arctan_limits.md` | Lines 1–3117; all of §§1–8 | 143086 | `19f01b6112949f4d186ef17ff94415804830ed51a519b155ed45343c26cbbead` |
| `NOTATION.md` | Lines 1–98 | 5110 | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |

Line references below are to `arctan_limits.md` unless stated otherwise. A truncated display during the initial read was repaired by explicitly rereading lines 500–545. The input files were not edited.

Severity convention: **critical** invalidates a central conclusion; **major** leaves a necessary proof obligation unresolved; **minor** is a localized mathematical or contract defect; **clarity** is an optional presentation improvement with no missing proof obligation.

## Required fixes

None. No critical, major, or minor mathematical defect was identified.

## L2: full contract and proof checks

**Finite model and clocks — verified (lines 58–167, 546–564).** The residual excludes backpropagation, and the loss and mobilities give exactly (L2.1): vector updates carry `−2ηr`, while the hidden-matrix update carries `−2ηr/n`. The stored readout variance is `n⁻²`, not an order-one readout regime. Since `F′φ′=1`, (L2.2) is the continuous transformed flow. The matrix-memory expansions (L2.4)–(L2.6) preserve the transpose of the same trained matrix.

**Adaptive Gaussian reuse and singular queries — verified (lines 175–302).** The first reverse call retains the forward overlap in (L2.8). Conditioning on both orientations yields the two response terms in (L2.9), with the correct projections and denominators. The general conditional mean in (L2.12) obeys both recorded linear constraints; its residual lies in the orthogonal complement of both constraint spaces. Adaptivity is legitimate because each next query is measurable before its answer is exposed.

The net estimate (L2.11) has the correct scaling: the net comparison loses a factor two, and each fixed bilinear form has variance `1/n`. Fresh input perturbations make the limiting Schur complements positive. Conditional averaging supplies joint weak convergence and second moments, while removing finitely many output directions costs `rank(U)/n` in expected squared RMS. The operator-bound coupling (L2.16) removes the perturbations without requiring convergence of inverses at a rank drop. This proves joint empirical convergence, not merely convergence of marginal Gaussian variances.

**Common spaces and actual adjoints — verified (lines 304–339).** Finite unions give consistent same-layer laws. The generated coordinate sigma-fields and bounded coordinate-function family supply the stated `L²` density. The finite operator inequality makes the matrix-answer assignment well-defined on equivalence classes and bounded on their completion. Exact finite adjunction passes to the two population expectations. There is no independent reverse map, coordinate pairing between layers, or asserted operator-norm convergence across widths.

**Global existence, uniqueness, gradient structure, and restart — verified (lines 341–466, 542).** The pointwise readout bound is precisely what makes the top backward product Lipschitz in the state metric. Picard iteration is performed in a complete closed path set carrying that bound. The output chain rule and adjunction give the three nonnegative blocks in (L2.24), hence residual control. The readout grows at most linearly on a fixed physical interval; the operator and transformed-vector bounds then prevent finite-time escape. These are bounds for each fixed `T`, not one uniform-in-time bound.

The rank-one gradient has the correct Hilbert–Schmidt geometry, even though the initial action need not be Hilbert–Schmidt. Chain rules along the constructed `L²` curves are valid and do not assume unrestricted `L²→L²` Fréchet differentiability of the activation map. The measured-law restart argument uses the spaces generated by the current measurements and intertwines their actions, adjoints, and coordinate operations; it does not assume fresh Gaussian initialization at a restart.

**Proof mesh and empirical feedback — verified (lines 450–540).** The Euler estimate is uniform in width on the bounded initialization event. Its stopping argument separately controls the readout supremum, which the state metric alone would not control. The oracle is defined from causally available deterministic population contractions. Its finite matrix memory is not silently identified with its oracle preactivation: (L2.30) explicitly accounts for that discrepancy and its reverse counterpart. The recurrence (L2.31) transfers the finite-program laws to empirical-feedback Euler. The mesh is fixed during the width limit and removed afterwards; no growing-program Gaussian theorem is imported.

**Exact raw GD — verified (lines 546–564).** Expanding the cubic exactly gives the displayed quadratic and cubic defects. From a bounded RMS of the transformed velocity, its coordinate maximum is at most `C√n`; hence one-step RMS defect is `O(η²√n+η³n)`. Accumulation gives `O(η+η√n+η²n)`, which vanishes under precisely the stated `η_n√n→0` condition. The fractional-step cubic identity also treats the transform of the raw linear interpolant. This is a comparison of the requested algorithm, not a substitution of transformed Euler for raw GD.

**Unbounded fields, kernels, velocities, and path laws — verified (lines 566–628).** Uniform joint `W₂` convergence to the compact time-image of `(Z¹,P)` supplies the uniform squared-tail bound (L2.35). This justifies the additional unbounded backward products by truncation, including their subsequent matrix actions. The product rule gives both terms of the second-layer velocity in (L2.36). The GD derivative comparison uses the actual recomputed preactivation and controls the changed gate by a bounded part plus the established squared tails.

Thus all three kernel blocks, predictions, losses, and integrated squared preactivation and feature speeds have the claimed limits. The path estimate (L2.38), together with joint grid laws and the energy bounds, proves `W₂` convergence for the supremum path metric; it does not confuse timewise convergence with path-law convergence. Integrating jointly measurable velocities supplies absolutely continuous population coordinate paths.

**Feature motion, nonaffinity, and kernel change — verified (lines 630–686).** The initial reverse innovation in (L2.40) has variance `ν`, without subtracting its response component. Adjunction gives `E[US₀]=μ₁ν+γ₁>0`. The integral equations give the stated orders of motion using bounded multipliers and strong mean-square limits. In particular, the readout-kernel change adds to the two hidden-block changes:

`K(t)=k₃+8(μ₁ν+γ₁)t²+o(t²)`.

Consequently the total kernel cannot be constant near initialization. Positive preactivation motion also gives positive feature motion because `φ′` is everywhere strictly positive. The affine-fit calculation uses positive initial variances and full-support Gaussian laws, followed by continuity of the necessary first and second moments.

## L3: full contract and proof checks

**Theorem scope — verified (lines 690–900).** The finite initialization and all four mobility-scaled updates match the notation contract. The actual step is `η_n=n⁻²`. The theorem specifies joint same-layer measurements, second moments, uniform convergence in probability, finite collections of times, named backward fields and velocities, four kernel blocks, path laws, integrated speeds, and a same-width state comparison. The population state consists of four fields/operators, not finitely many scalars. Uniqueness and restart apply on the fixed constructed spaces and within the constructed interval.

**Two-matrix source identification — verified (lines 905–1209).** Conditional residual independence survives the interleaving: with the transcript fixed, a new answer imposes a linear constraint only on its queried matrix. Formula (S.1) therefore applies at every call in either orientation.

The response rule (S.3) follows from the orthogonality of the new input remainder to all previous forward inputs. Pairing the old transpose answers with this remainder eliminates their response parts. Gaussian integration by parts then gives the expected source derivatives; substituting the old forward responses cancels the old projection coefficients. The covariance of the resulting forward source is exactly the input second-moment covariance. Fresh innovations and deterministic coefficients also justify the independent source-group construction.

The singular-case argument does not assume pseudoinverse continuity. Input perturbations establish the nonsingular finite-program result; the explicit source recursion, continuous Gaussian covariance square roots, and bounded first derivatives pass its coefficients to the unperturbed recursion. The formal derivatives hold deterministic coefficients and covariance parameters fixed. This convention is consistent even for perfectly correlated source coordinates. The two current-source terms in `b²_kk` at lines 1197–1199 include the return through the other matrix; that term has not been omitted.

The oracle-to-empirical-feedback comparison is finite and uses bounded clipped factors. Its coefficients are constructed causally, and the small prescribed readout is restored by a fixed-program comparison. None of this section is improperly used as a mesh-uniform estimate.

**Common actions and fixed-clipping dynamics — verified (lines 1214–1482).** The countable construction and adjunction argument work for both matrices on the three generated `L²` spaces. The bound 10 follows from the displayed net estimate. The clipping maps have the required identity region, amplitude bound, and Lipschitz constant. The readout bound `|W⁴(s)|≤as` yields the polynomial operator bounds (A.9), independently of clipping. The clipped vector field has Lipschitz constant `C_S(1+R)`; its fixed-clipping existence and width limit follow from the complete path-set argument and fixed-mesh comparison. No monotonicity of the clipped predictor is assumed.

**Response bootstrap and explicit interval — verified (lines 1487–1794).** The bootstrap is causally ordered and closes without using a current row to establish its own hypothesis:

1. Previous bottom response rows give the source-time factor `Δ` in (B.1).
2. Previous top response rows and (B.1) give the middle derivative envelope (B.2) and the individual-source bound (B.3).
3. The Jensen step in (B.4) uses marginal Gaussian variances only; time correlations and dependence of the bounded shift on the Gaussian part cause no problem. Taking the `p`-th root gives exactly the displayed `M_p` bounds.
4. This yields (B.5), then the pointwise top derivative estimate and `V_k≤C₃S` before estimating the current middle backward field.
5. Cauchy–Schwarz gives `U_k≤C₂S`. Earlier-index bounds needed here are obtained by the simultaneous induction stated in the text.

For `S≤S₀`, both current rows are at most one half, contradicting a first exit above one. The constants in (L3.4) match this calculation and are finite positive real constants. The resulting middle backward field is a Gaussian marginal plus a deterministically bounded shift, which proves (B.10) and the uniform exponential moment subsequently used. No independence between the shift and its Gaussian part is required.

**Clipping removal, uncut uniqueness, and restart — verified (lines 1812–2243).** The important estimate is asymmetric. In (C.8), the activation difference multiplies the clipped reference factor, and the discarded tail is measured on that same reference. The full top backward action is Lipschitz by (C.5) using only the reference readout's pointwise bound. Thus (C.9) requires no tail assumption on a competing uncut state.

Fixed-clipping Euler convergence and Fatou pass the proved exponential moment to the clipped flow. Its squared-tail estimate yields

`ε_R=2K exp(−R²/(16K²))`.

This dominates the comparison amplification `exp(C(1+R)S)`, including an additional factor `1+R` when passing velocities to the limit. Consequently the clipped paths are Cauchy, their vector fields converge uniformly, and their limit solves the uncut integral equations. Comparing any bounded-primal competing solution to the same clipped references proves uniqueness. The restart proof also includes the already accumulated discrepancy at the restart time, whose Gaussian decay still dominates the two comparison exponentials. It asserts no continuation beyond `S₀`.

For finite widths the continuous tail surrogate `b_R` is covered by joint `W₂` convergence. Its time-Lipschitz bound upgrades this to the uniform estimate (C.13). The finite uncut flow is compared with the zero-readout clipped reference before removing `R`; a finite-width exponential-moment hypothesis is not needed.

**Exact GD, random computational mesh, and physical clocks — verified (lines 2259–2450).** The readout estimate first establishes `|f_{n,k}|≤3/8`, positive feature increments, and `α_k<3η`. The induction includes the last interpolation endpoint and keeps its clock at most `7S̄/8`. This establishes the clock before using the positive-step bounds.

The exact cubic defect (C.15) has accumulated size `O(η√n+η²n)`, vanishing for the prescribed step. The reference-flow estimate (C.17) uses the asymmetric comparison, not an unproved uncut Lipschitz constant. The pathwise Riemann-sum estimate (C.18) is valid for the random positive partition because the reference tail surrogate is time-Lipschitz. Fractional steps treat the requested raw interpolation.

The population and reference predictors are Lipschitz in feature time. Their scalar physical clocks, and the finite clocks, stay inside the available feature interval on `[0,T₀]`. Scalar Gronwall therefore transfers the feature-time comparisons to physical time and proves (C.21). The raw competing-solution argument also justifies membership of `F(Z¹)` in `L²` from its integral identity; it does not silently assume a sixth moment of every evolved raw coordinate. Restart uniqueness remains within the original local interval.

**Joint observables, velocities, and paths — verified (lines 2245–2257, 2452–2542).** The asymmetric estimate first identifies `δ²` and then the bottom reverse action. Compact `L²` time-images supply the squared-tail control needed for subsequent bounded gates and operator calls. Formula (C.22) includes every matrix-motion and feature-motion term. For GD, the layer-by-layer product rule treats recomputed preactivations and propagates the previous layer's derivative error; the remaining changed-gate term is controlled by truncating the endpoint velocity. The specified endpoint convention makes the uniform velocity statement unambiguous. Integrated speeds and supremum-metric path laws follow with the same finite-grid argument as L2, including feature paths.

**Gradient structure — verified (lines 2547–2711).** The trained rank-one integrals are Hilbert–Schmidt, and their cutoff limits agree with the already constructed operator-norm limits. The scalar remainder estimate (G.3) is sufficient for Fréchet differentiability of the predictor in the stated raw Hilbert parameter geometry: fixed backward coefficients are in `L²`, and terms containing both a matrix/readout change and an activation change are quadratic in the parameter increment. It does not assert false Fréchet differentiability of the activation as an unrestricted `L²→L²` map. The resulting gradient is continuous; local Lipschitz continuity is not needed or asserted. The residual time change yields exactly the original squared-loss gradient flow and the four kernel blocks (G.8).

**Nonlazy behavior and nonaffinity — verified (lines 2716–3117).** The multiplier lemma (F.8) supplies the required strong limits using only a fixed `L²` reference vector. Strong convergence of the gated operators in (F.9), rather than operator-norm convergence of their gates, suffices for the initial expansions. In both transpose conditionings the innovation variance is the full input second moment. The second conditioning retains the positive `c₃` response from the other matrix. Thus `P₁`, `B₂`, and `β₃` are nonzero. The coercive lower bounds in (F.14) and strict positivity of `φ′` prove all six preactivation/feature motion coefficients are positive.

The clock gives `s(t)=2t+o(t)`. The coefficients `2t²` for displacement, `4t` for velocity, and `16T³/3` for squared-speed integrals follow consistently. Adjunction gives the additional readout-kernel coefficient, yielding

`K(t)=μ₃+8(γ₁+γ₂+γ₃)t²+o(t²)`.

The kernel-change claim therefore concerns the total kernel, not just its hidden blocks. The loss and positive affine-fit-error conclusions follow from the gradient identity and continuity of the relevant moments on nondegenerate initial Gaussian laws.

## Self-containment, canonical notation, and locality

The nonclassical work needed here is contained in the chapter: adaptive reused-Gaussian empirical convergence, the source-derivative response rule, the common action construction, the uniform local response bootstrap, and clipping removal. No external tensor-program theorem, population-limit theorem, or continuation theorem is needed.

The classical facts used have appropriate hypotheses: Gaussian conditioning on fixed linear constraints after exposing the adaptive transcript; integration by parts for differentiable functions with bounded source derivatives; finite-dimensional positive-semidefinite covariance-square-root continuity; consistent countable Borel coordinate laws; density on generated probability spaces; `W₂` convergence with second moments; Banach contraction on the displayed complete path sets; Gronwall; Fatou; and Fubini for integrable `L²` velocities. The arguments avoid rank-drop inverse continuity and unrestricted nonlinear `L²` differentiability.

The chapter respects the notation contract's layer count, matrix dimensions, stored-readout scaling, residual convention, mobilities, finite RMS/pairing factors, same-layer contractions, rank-one normalization, transpose/adjoint distinction, and raw-GD interpolation. Auxiliary transformed coordinates, source variables, and backward fields are explicitly typed. Population backward `δ^(ℓ)` is explicitly declared; `NOTATION.md` permits, but does not require, the alternative capital `Δ^(ℓ)`. Plain `Δ` remains a proof mesh.

L2 is global on each fixed finite physical interval. L3's global fixed-clipping existence and finite-width primal bounds are not promoted to global uncut population existence, uniqueness, or convergence. The explicit locality restrictions at lines 899–900, 1480–1482, 2122–2123, 2540–2542, and 2710–2711 agree with the actual proof.

## Optional clarities

1. **Clarity only — scope of the Lipschitz sentence at line 522.** Replace “every forward, backward, and residual difference” with an explicit reference to the fields used in the transformed oracle updates, in particular `δ²` and `(W²)^Tδ²`. The ungated bottom action is controlled there; the product defining `δ¹` is treated by the later truncation argument. That product is not generally Lipschitz on arbitrary bounded sets in the displayed state metric. The proof already observes this distinction at lines 529 and 566–588, so no estimate needed by (L2.31) is missing.

2. **Clarity only — feature path laws in the L3 theorem statement.** Lines 839–842 explicitly name entire preactivation paths. Adding “and feature paths, in `W₂(C([0,T₀]))` for the supremum metric” would expose the additional conclusion already proved at lines 2533–2536. No new proof is required.

Neither optional clarification changes the CLEAN verdict.
