# Resumed assembly review protocol

The coordinator and source assemblers were the only actors given source history. Independent proof/code reviewers ran as fresh ephemeral processes on read-only scientific inputs with no inherited task conversation, prior reports, studies or external source access. Mathematical dependencies were complete files. The three existing assembly agents did not independently review their own additions. The coordinator remained the sole Git writer for this work.

Each reviewer read the two required mathematical skills and relevant references itself. Model/effort and process identifiers are preserved in ASSEMBLY2_REVIEW_SESSIONS.json. The complete supplied prompts follow. These are provenance records only; neither maintained code nor proofs require them.

## Shallow proof

```text
Conduct an independent complete mathematical review of the two supplied files shallow.md and NOTATION.md in this working directory, plus INPUTS.json. Read every line and all mathematical arguments. Only these candidate/dependency inputs are allowed; do not inspect any checkout, studies, other /tmp packets, task history, prior verdict, or external sources. No delegation. Read /etc/codex/skills/solve-math-rigorously/SKILL.md and /etc/codex/skills/investigate-conjectures/SKILL.md yourself, plus the latter research-contract, evidence-ledger and adversarial-audit references. This is a proof audit, not research or training. No computation is required; bounded deterministic algebra checks are allowed outside the input directory. Leave all input files unchanged; verify their hashes against INPUTS.json before and after. The candidate must be wholly self-contained except basic calculus/Gaussian integration, with exact initialization, activation, metric, clock, limit and convergence scopes. The mention of Section8 is only a comparison and cannot be used as a proof dependency. Check every constant recursion, exact transported-defect identity, derivative envelope, expectation interchange, parity/cubic coefficient, sharpness, and dyadic consequence. Return a full audit report, with CLEAN or CORRECTIONS REQUIRED, exact read/hash ledger, independent mathematical checks, numbered required corrections if any, and precise limitations. Do not rewrite candidate files. Your final response itself must contain the entire report, not a link or summary.
```

## Frozen/ReLU proof and code

```text
Conduct a fresh independent complete mathematical and implementation review of every input listed in INPUTS.json in this working directory. Only these files are permitted as mathematical/code inputs. Read every line including all of docs/finite_dynamics.md, notation, supplied Python, tests, guide and requirements. Do not read any checkout, studies, other /tmp packets, previous verdict, task history or external source; no delegation. Read /etc/codex/skills/solve-math-rigorously/SKILL.md and /etc/codex/skills/investigate-conjectures/SKILL.md and its research-contract/evidence-ledger/adversarial-audit references yourself. Leave all packet inputs unchanged; verify hashes before and after. Full review focus: finite-dynamics Sections8–9 (frozen quadratic joint initial layer, exact reduction, no pathwise deletion order, reached ReLU classical obstruction, positive local Euler output compactness and gate occupation identities), and new FrozenQuadraticEvaluation, _frozen_state, frozen_quadratic and frozen_quadratic_step APIs plus their tests and guide. Earlier finite identities/core code are complete explicit dependencies and must be checked at the interfaces actually used. Preserve all init/activation/metric/half-loss clock/interpolation/order and convergence scopes. Check for missing proof steps or unstated external imports, and for invalid leaps from frozen/classical/compactness claims. Run PYTHONPATH=code:code/tests PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 python -B -m unittest test_finite_reductions and the guide example. Small independent deterministic algebra/derivative checks are authorized; no training, trajectory simulations or historical campaigns. Keep diagnostics outside the input directory or run inline. Return a complete report with CLEAN or CORRECTIONS REQUIRED, exact read/hashes before and after, all independent checks, numbered required corrections if any, and precise scope. Final response must contain the entire report, not a link or summary.
```

## Coherent kernel and finite tangent geometry

```text
Conduct a fresh independent complete mathematical review of coherent.md, hessian.md and NOTATION.md plus INPUTS.json, all in this working directory. Read every line. These are the only permitted mathematical inputs: do not inspect any checkout, studies, other temporary packet, prior verdict, history, other task or external source. No delegation. Read /etc/codex/skills/solve-math-rigorously/SKILL.md and /etc/codex/skills/investigate-conjectures/SKILL.md plus its research-contract/evidence-ledger/adversarial-audit references yourself. Verify all input hashes before and after; leave inputs unchanged. Audit every finite metric/gradient factor, exact strong/Bochner carrier, measurability/completeness, local depth existence, Lipschitz training field, Frechet derivative and gradient representation, energy, ordered column-to-row global continuation, uniqueness and restart. Check that the actual full proof is contained, with no unproved external heavy dependency or unjustified bare-L2/depth-maximum/width-limit inference. The benchmark model must retain its explicit coherent W/n scaling, bounded fixed endpoints, bounded C2 activation, finite arbitrary data, lower-bounded C2 loss and exact physical clock. No training or research experiments. Small bounded deterministic finite-algebra checks are allowed inline or outside this input directory. Return the entire full audit report in your final response, with CLEAN or CORRECTIONS REQUIRED, detailed mathematical substantiation for each proof subsection, exact read/hash ledger, any required corrections, and exact scope. Do not stop after the ledger; instruction-file hashes are unnecessary.
The second complete candidate hessian.md is independent of coherent.md and must also receive a full audit: derive the scaled-coordinate metric, full Hessian and material derivatives through all trained blocks, terminal construction and positive Rayleigh quotient, explicit backward continuation to zero readout, and all physical-loss clock terms. Preserve deterministic correlated initialization versus Gaussian-typical scope; primal segment bounds versus terminal-only Hessian bound; pointwise versus integrated estimates. Check every constant and inference. Both candidates must be CLEAN for an overall CLEAN verdict; give separate findings for each. There are no mathematical dependencies between the two candidates or on omitted chapter sections. All original theorem assumptions and any actual proof gap must be stated precisely.
Also fully audit hessian Sections14.6–14.7: two-sided global feature flow, explicit polynomial primal and nuclear bounds, every-plane tangent-volume/Jacobi/inverse argument, exact Gaussian initialization and all required moment estimates, forward physical-clock bounds with every rank-one term, exact projection determinant including singular cases, feature and physical slope equations on invertible branches. Distinguish intrinsic volume from projected lower-volume/entropy/covariance/control of Gaussian queries. These are part of the full candidate and require complete mathematical substantiation in the report.
```

## Scoped assembled-library integration

```text
Conduct a fresh independent scoped scientific-library integration review in this working directory. All permitted mathematical/code inputs are the 27 complete files listed in INPUTS.json plus that manifest. Do not inspect any checkout, study, data, other temporary packet, previous verdict, task history or external source. No delegation. Read /etc/codex/skills/solve-math-rigorously/SKILL.md and /etc/codex/skills/investigate-conjectures/SKILL.md and all three required research-contract/evidence-ledger/adversarial-audit references yourself. Read and verify every input hash/byte/line count before and after; leave every input unchanged. No training, trajectory simulation or historical campaign. Small deterministic algebra/derivative checks and the stated tests are authorized.

Read IN FULL all code (production, tests, guide, checker), Makefile, requirements, docs/README.md and docs/NOTATION.md. Read all of docs/finite_dynamics.md. Also read every newly added proof at these precise ranges and its full introduction:
docs/gaussian_calculus.md: NEW proof from line 4169 through 4719; read every one of those lines and its full introductory scope paragraphs.
docs/finite_dynamics.md: NEW proof from line 663 through 1275; read every one of those lines and its full introductory scope paragraphs.
docs/finite_optimization_and_controls.md: NEW proof from line 2410 through 3394; read every one of those lines and its full introductory scope paragraphs.
docs/continuous_depth.md: NEW proof from line 1886 through 2474; read every one of those lines and its full introductory scope paragraphs.
Read any additional complete proof dependencies needed from the supplied chapter files. The new Gaussian Section11, controls Section14, and continuous Section15 are self-contained with the notation contract; no study dependencies or omitted chapter references may supply arguments. You must audit all new proofs and interfaces, but do not claim to re-audit every older chapter theorem. Give exact read/unread line ranges for all 27 inputs and aggregate counts, distinguishing complete reads, scope/navigation reads, and unreviewed older bodies. Never imply whole-book proof acceptance from boundary tests.

Audit the assembled normalization, initialization, activation, metric, full-versus-half loss, feature-versus-physical clock, data/depth scope, interpolants and convergence mode. In particular check shallow uniform step-doubling/derivative envelopes and dyadic expectations; frozen quadratic arbitrary vanishing-mesh initial layer and exact finite API; ReLU prescribed classical contact failure versus positive local scalar Euler compactness and occupation products; coherent W/n kernel global gradient flow in its actual row/column Bochner carrier with fixed endpoints; complete finite nonlinear Hessian/material/physical-clock identities, reachable deterministic obstruction, positive nuclear/every-plane volume bounds and Gaussian moments, and the additional hidden-projection determinant factor. Check the actual book text, guide and new code, not prior acceptance claims.

Run make check in this standalone directory, then execute the new frozen-bottom Python guide example from code/README.md. This directory intentionally excludes the PDF exporter and its tests. Confirm that neither runtime nor proof needs studies, stored data, archived arrays, chats or historical verdicts. Check local navigation and chapter introductions against actual conclusions; no compactness-to-uniqueness, deterministic-to-Gaussian, formal-to-trajectory, coherent-to-noisy-dense or finite-to-population inference is permitted.

Return the entire complete review report as the final response: CLEAN or CORRECTIONS REQUIRED, exact read/hash ledger, mathematical substantiation for each new subsection and implementation interface, all required corrections if any, test/guide results and exact acceptance limits. A short verdict, a link, or a report without detailed mathematical substantiation is insufficient. Do not edit files, produce artifacts inside the packet, or rely on an unreviewed heavy external dependency.
```

The shallow B2 prompt additionally required a complete substantive mathematical report, because the initial B1 final artifact was incomplete. B2 received no B1 text or verdict. Frozen A2 and B2 received only the corrected complete R2 packet and the unchanged review prompt, without the failed reports or a correction checklist. The kernel/Hessian packet was extended with positive-volume proofs before either independent review began; its R2 filename denotes an assembly freeze, not a failed mathematical review.

## Integration scratch correction

The first full integration process could not create five boundary-check test fixtures in its strictly read-only sandbox. This was an execution-environment limitation; the input bytes were unchanged. A fresh full process received a byte-identical 27-file packet, with all files mode 0444 and containing directories mode 0555. A separate scratch directory was supplied through TMPDIR and --add-dir using workspace-write sandbox mode. No input permissions or source writes were authorized. This permits the required temporary fixtures while preserving the mathematical/code inputs. Both reports are retained; the first is not counted as unconditional integration acceptance.

The complete additional R2 prompt was:

```text
Conduct a fresh independent scoped scientific-library integration review in this working directory. All permitted mathematical/code inputs are the 27 complete files listed in INPUTS.json plus that manifest. Do not inspect any checkout, study, data, other temporary packet, previous verdict, task history or external source. No delegation. Read /etc/codex/skills/solve-math-rigorously/SKILL.md and /etc/codex/skills/investigate-conjectures/SKILL.md and all three required research-contract/evidence-ledger/adversarial-audit references yourself. Read and verify every input hash/byte/line count before and after; leave every input unchanged. No training, trajectory simulation or historical campaign. Small deterministic algebra/derivative checks and the stated tests are authorized.

Read IN FULL all code (production, tests, guide, checker), Makefile, requirements, docs/README.md and docs/NOTATION.md. Read all of docs/finite_dynamics.md. Also read every newly added proof at these precise ranges and its full introduction:
docs/gaussian_calculus.md: NEW proof from line 4169 through 4719; read every one of those lines and its full introductory scope paragraphs.
docs/finite_dynamics.md: NEW proof from line 663 through 1275; read every one of those lines and its full introductory scope paragraphs.
docs/finite_optimization_and_controls.md: NEW proof from line 2410 through 3394; read every one of those lines and its full introductory scope paragraphs.
docs/continuous_depth.md: NEW proof from line 1886 through 2474; read every one of those lines and its full introductory scope paragraphs.
Read any additional complete proof dependencies needed from the supplied chapter files. The new Gaussian Section11, controls Section14, and continuous Section15 are self-contained with the notation contract; no study dependencies or omitted chapter references may supply arguments. You must audit all new proofs and interfaces, but do not claim to re-audit every older chapter theorem. Give exact read/unread line ranges for all 27 inputs and aggregate counts, distinguishing complete reads, scope/navigation reads, and unreviewed older bodies. Never imply whole-book proof acceptance from boundary tests.

Audit the assembled normalization, initialization, activation, metric, full-versus-half loss, feature-versus-physical clock, data/depth scope, interpolants and convergence mode. In particular check shallow uniform step-doubling/derivative envelopes and dyadic expectations; frozen quadratic arbitrary vanishing-mesh initial layer and exact finite API; ReLU prescribed classical contact failure versus positive local scalar Euler compactness and occupation products; coherent W/n kernel global gradient flow in its actual row/column Bochner carrier with fixed endpoints; complete finite nonlinear Hessian/material/physical-clock identities, reachable deterministic obstruction, positive nuclear/every-plane volume bounds and Gaussian moments, and the additional hidden-projection determinant factor. Check the actual book text, guide and new code, not prior acceptance claims.

Run make check in this standalone directory, then execute the new frozen-bottom Python guide example from code/README.md. This directory intentionally excludes the PDF exporter and its tests. Confirm that neither runtime nor proof needs studies, stored data, archived arrays, chats or historical verdicts. Check local navigation and chapter introductions against actual conclusions; no compactness-to-uniqueness, deterministic-to-Gaussian, formal-to-trajectory, coherent-to-noisy-dense or finite-to-population inference is permitted.

Return the entire complete review report as the final response: CLEAN or CORRECTIONS REQUIRED, exact read/hash ledger, mathematical substantiation for each new subsection and implementation interface, all required corrections if any, test/guide results and exact acceptance limits. A short verdict, a link, or a report without detailed mathematical substantiation is insufficient. Do not edit files, produce artifacts inside the packet, or rely on an unreviewed heavy external dependency.

Execution environment: all manifest inputs and their containing directories are read-only. A separate writable scratch directory is supplied through TMPDIR=/tmp/pde_assembly2_integration_scratch and sandbox --add-dir. The five boundary-check tests create their fixtures there. Do not change input permissions or files; write only the temporary fixtures required by tests into that scratch directory, or use inline diagnostics. No other packet, report or mathematical source is permitted.
```
