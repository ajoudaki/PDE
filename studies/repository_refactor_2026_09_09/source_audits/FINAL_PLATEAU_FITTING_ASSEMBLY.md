# Plateau geometry and necessary fitting scales: assembly

This is a proof-only incorporation at the resumed shared-checkout baseline
`8bda541`. The coordinator read the complete model, geometry, confinement,
path-compactness and hostile/mobility records and the complete fitting-scale
proof and both correction/review records. Exact source hashes and actual read
coverage are in `FINAL_ROOT_SOURCE_READS.json`; only its plateau and fitting
rows are dependencies of this package. The Gaussian and operator rows belong
to separate candidates, not this acceptance.

The 947-line plateau candidate contains the actual raw storage/half-sum metric,
a contained smooth activation construction, six Gaussian balls and protected
Gram (including rank-two data), continuous-time freezing/nonentry under
integrable realized controls, finite initial-Gram concentration, fixed Gaussian
nonaffinity and activity, complete planar and last-activation confinement,
conditional strong-path estimates and a contained W2 compactness proof,
protected backward-force and primitive identities, exact bounded learned
returns, and both finite saturation and feature-only restart obstructions.

The 151-line fitting candidate contains the exact equilateral feature-sum
identity, necessary raw-distance estimates, the leading AM–GM constant, and
the stronger fixed-radius first-exit time bound. The source's initialized
norm-two bound is a displayed special case of a given-space hypothesis with
any fixed bound M. The proof is supplied for this general M, so no uninspected
sharp Gaussian norm theorem carries an argument. Zero population readout is
not a reset of a finite random readout. All existence, energy, and successful
fitting premises remain explicit.

The original confinement objection concerned a.e. differential identities
without absolute continuity. The candidate uses Bochner integral equations
and proves the pointwise AC representative by Fubini; the control lemma itself
requires AC. It does not import any canonical population construction. The
selected compactness proof includes only fields whose squared spatial tails
are actually controlled. It does not identify initial-action returns, the
first kernel, true limiting velocities, a common population limit, or cap
removal. A positive initial protected Gram does not force fitting. The
saturation event is positive probability at each fixed width, without a
width-uniform lower probability. The restart witness uses arbitrary finite
raw states, not an asserted Gaussian-reached state.

The first isolated review found no mathematical error and suggested three
wording improvements. The final candidate specifies population activity and
the uncapped physical first kernel, and contains the finite-coupling proof of
relative compactness. The original input and full report are preserved as
`reviews/FINAL_PLATEAU_R1_CANDIDATE.md` and
`reviews/FINAL_PLATEAU_FIT_A1.md`. Two newly isolated agents read every line
of the corrected candidate, fitting candidate and notation, with no project
history, source studies, prior reports or verdicts. Their complete reports are
`FINAL_PLATEAU_FIT_A2.md` and `FINAL_PLATEAU_FIT_B2.md`; both found no required
correction at identical hashes. A later optional wording suggestion in B2
was not a mathematical objection and caused no change to its frozen inputs.

No external specialized theorem, implementation, empirical evidence, retained
array, training experiment or coefficient campaign is part of this package.
The mathematical fragments are inserted byte-for-byte in the existing
special-data chapter. Earlier proof bodies are preserved. Root alone writes
Git, with explicit scoped staging; concurrent exporter and inherited artifacts
remain outside scientific acceptance.
