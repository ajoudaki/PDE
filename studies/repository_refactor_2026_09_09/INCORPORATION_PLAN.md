# Incorporation of the three agreed priorities

Starting checkpoint: `a257e59`. This continues the accepted refactor without
changing the four-directory architecture. The prior library edition is frozen
in `reviews/INCORPORATION_BASE_LIBRARY_INPUTS.json`. New drafts are not accepted
by earlier review verdicts.

## Work and acceptance order

1. Correlated-data first-layer compactness, mixed finite-GF fitting/gates, and
   a cohesive initialization-geometry comparison. Extend the existing special-
   data and finite-controls chapters, preserving partial-result scope.
2. A compact moving finite-network jet oracle, then the smallest justified
   forest/certificate primitives. Extend the existing calculus/code interfaces;
   no automatic promotion of historical high-order tables.
3. Exact-compiler fixed-program discretization bounds and the distinct
   shallow/spectral/all-fixed-depth linear GF constructions, with dependencies
   closed at their actual initialization/clock/observable scope.

Work proceeds in reviewable assemblies. The coordinator handles correlated
data and integration; isolated workers prepare the finite-jet implementation,
quantitative calculus, and initialization comparison in disjoint write scopes.
Fresh adversarial reviewers receive only the resulting candidate and explicit
established dependencies, not the source histories or prior PASS reports.
Any required correction receives a fresh review of the corrected version.

Preserve shared notation and all earlier proof bytes wherever possible. Every
addition must be checkable without studies, temporary files, old conversations,
unprovided specialized theorems, generated tables or external code. Record exact
donor/candidate/review hashes and scope. A source gap remains a gap, not a
conditional replacement advertised as a completed incorporation.

Small deterministic implementation tests and algebraic verification are part of
this work. No historical coefficient campaign, training experiment, native
build, installation, source-seal reset, budget reuse or old research-task
resumption is authorized. Generated empirical claims require a separate full
reproduction chain before inclusion.

## Incorporation state

The additions below are not accepted by the prior library's reviews. The
correlated-data partials, finite calculus/code, quantitative discretization and
exact-capture additions have their own clean isolated reviews and are
incorporated. Initialization has also passed its final corrected-version pair
and is incorporated. The standalone assembled edition has a further fresh
CLEAN integration review; that verdict does not replace the paired full-proof
reviews. All three agreed priorities are completed at their stated scopes.

- First-layer compactness: source read completely and translated to the
  shared first-weight normalization. Two isolated complete reviews of hash
  `d45999fc681e4bf66f95392f04f5940be6d3245d88a9eeea33f066560d665adb`
  required the same two notation repairs (eight undefined Gram entries and
  one wrongly typed continuity codomain), with no other substantive gap
  reported. Both repairs are explicit in the current draft, SHA-256
  `02500ea3eee80f9dadd36790a5ccaea6258c06d0c6901564b54417624452ef93`.
  Two fresh complete reviews of this corrected version are CLEAN. Its complete
  body is incorporated byte-for-byte as Part IV of `docs/special_data_limits.md`.
- Mixed finite-GF fitting/permanent gate mass: a new self-contained assembly
  of the two fully read donor proofs has two CLEAN isolated complete reviews,
  at hash `36a50c2aa599302db2b2942d0ddb21d5561488ac0eff5e4b34dd7e43e3717a78`.
  Its complete body is incorporated byte-for-byte as Sections 10–11 of
  `docs/finite_optimization_and_controls.md`.
  The extra strip-count event, nonzero finite readout, and absence of a
  population/GD/feature-velocity conclusion are retained.
- Initialization geometry: a contained Part V compares odd mixtures, convex
  offsets and sequential calibrated depth limits. The coordinator read the
  entire original proof and every repair. Three retained review rounds led to
  explicit signed endpoint sums, centered-Gaussian and off-diagonal hypotheses,
  derivative index ranges, a self-contained gain comparison, and its identity-
  scaling exception. No theorem constant or conclusion changed. Two fresh full
  reviewers returned CLEAN at hash
  `411fbe15d0701451a0e4fad0b87bfc5a11d283e200b278d3b75872fb05fa8a27`.
  The complete Part V is incorporated at `35f3066`. Initialization-only and
  sequential-limit scope remain explicit.
- Moving physical-GF jets: the bounded L2/one-sample/order-three implementation,
  tests and complete recurrence proof have been read by the coordinator.
  Two fresh isolated full proof/code reviews are CLEAN. The complete recurrence
  is now Section 7.1 of the calculus chapter, with matching maintained code
  and 14 tests. No historical generator was executed.
- Forest/certificate primitives: the coordinator has implemented a small
  colored-forest key, exact finite reversion/determinant, and direct
  regeneration of the fixed quadratic-axis certificate. Seven small
  deterministic tests pass. The candidate includes the separate finite
  forest expectation-factorization and annealed initialization-jet proofs;
  two independent full proof/code reviews are CLEAN. Section 7.2 and the
  maintained implementation contain the actual material. This does not promote the positive-
  first-mobility or canonical all-order Stieltjes claims.
- Quantitative discretization: the complete 863-line exact-compiler addition
  has been read by the coordinator and assembled with its entire existing
  internal fixed-program dependency for isolated review. Two full reviews are
  CLEAN; the proof is incorporated unchanged as calculus Section 8 at
  `ae587f2`. Fixed step count, order-one readout and feature-ascent clock are
  not relaxed. The separate code/API integration review is also CLEAN.
- Shallow/spectral/all-fixed-depth comparison: the coordinator has read the
  entire 1,293-line worker assembly. After removing editorial framing and one
  source-history phrase, one full review was CLEAN and another required more
  explicit trace-class foundations for strict self-containment. A contained
  compact-operator/singular-value/trace-ideal proof is now added as Section 2.A;
  two fresh reviewers read the complete amended chapter and notation only;
  both returned CLEAN. The full addition and foundations are incorporated at
  `a8f820a` without mathematical alteration.
  No raw-GD extension is attached to a GF source.

Implementation runs are bounded deterministic unit tests, small guide examples
and direct algebraic certificate checks. They create no empirical dataset and
do not restart a training or high-order coefficient campaign. Accepted promotions
and exact review reports are recorded in `INCORPORATION_ACCEPTANCE.md`.
