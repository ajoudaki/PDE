# Trained data response: result and concrete promotion proposal

Status: the complete research theorem R1 has two isolated scientific ACCEPT
reports. Both fresh scientific reviews of canonical P1 accept without required
corrections. The first integration review requires packaging corrections only;
the corrected P1I2 packaging is frozen and under fresh complete integration
review. This document does not authorize established-file edits.

## Result and scientific value

All three primary claims have complete reviewed proofs in the research
packet: a forced population tangent equation, actual finite-GF derivative
capture, and a uniformly bounded homogeneous trained population propagator.
No primary claim was refuted or left conditional on an unproved source
estimate. The new work is the actual finite-flow weighted passive-query
estimate, its use in derivative identification, and the singular-endpoint
argument controlling the nonautonomous trained propagator.

The reference's construction, substantial learning, fitting and endpoint
are maintained C.4.5 results and are reused, not claimed as new findings.
The independent selector accepts the new response result for C.4.6 beside
that reference. The result retains changing hidden representations and
both uses of the initialized middle action.

## Exact model, norms and limit order

The model has two equal-width tanh hidden layers, no biases, input dimension
two and normalized direction u=x/sqrt(2) in S1. The output is
`(W3)^T tanh(W2 tanh(W1 u))/n`; stored independent centered Gaussian
variances are `(1,1/n,1/n²)`, mobilities `(n,1,n)`, loss is the unhalved
mean square and time is physical GF time. The actual finite Gaussian
readout is retained. The reference law is the equal mixture of
`(sqrt(2)e1,+1)` and `(sqrt(2)e2,-1)`.

For any fixed deterministic Borel probability nu on the complete input
circle with labels in `[-Y,Y]`, `Y>=1`, use the same initialized arrays
along `(1-epsilon)nu*+epsilon nu`. Differentiate from the right at zero
at each finite width, then take width to infinity. Nonatomic finite losses
are exactly integrated. No atom-count, positive-weight or Gram-rank
restriction is imposed on nu.

The state is
`V=L²(Omega1;R²) ⊕ HS(H1,H2) ⊕ L²(Omega2)`, in the square-sum norm,
for the full row clock tangent, learned middle tangent and readout tangent.
Here `X_a=F(w_a)-F(g_a)`, `F(z)=z/2+sinh(2z)/4`, and
`delta w_a=tanh'(w_a) xi_a`. Raw conversion is bounded and injective;
its inverse is not assumed bounded. Only the learned middle increment
and tangent are Hilbert–Schmidt; the initialized action has its actual
Hilbert adjoint and need not be Hilbert–Schmidt.

Finite comparison uses squared norm
`||xi_n||F²/n+||B_n||F²+||d_n||²/n`. Fixed finite-program approximants
converge on their canonical carriers and approximate actual finite
tangents uniformly in time on their same-width carriers. This identifies
joint named finite-list fields and second moments, HS pairings and both
action directions. It makes no direct cross-carrier operator comparison.

For every fixed nu and every separately fixed finite T, including T=40,
the actual prediction derivatives converge in probability, uniformly on
`[0,T] × sqrt(2)S1`. Constants are independent of nu's support size and
atom weights. No failure probability uniform over all nu, width rate,
growing-horizon theorem or uniform-in-time width capture is claimed.

## Machinery supplied to nonlinear continuation

The exact equation is `v'=L(t)v+b_sigma(t)`, `v(0)=0`, with
`L=-2SE+C`, where `C` is residual curvature and every coefficient comes
from the already constructed autonomous reference. The forcing is the
signed data integral in P1 equation C.4.6.T5; it retains the off-support
inverse-gate product and the actual forward/adjoint action.

With total variation measured as mass, the proved estimates are

\[
\sup_{s\le t}\|U(t,s)\|\le C_U<\infty,\qquad
\sup_{t\ge0}\|b_\sigma(t)\|\le C_{b,Y}\|\sigma\|_{TV},
\]
\[
\sup_{t\le T}\|v_\sigma(t)\|\le C_U C_{b,Y}T\|\sigma\|_{TV},
\qquad
\sup_{t\le T,x}|\mathscr D_\sigma f(t,x)|
\le L_0 C_U C_{b,Y}T\|\sigma\|_{TV},\quad L_0<17.
\]

General forcing is admissible in `L¹([0,T];V)`. Signed zero-mass forcing
extends through the derived linear equation; no two-sided probability
neighborhood is presumed. Weighted moments of passive Q, clock envelopes,
`cosh²(w_a)Q(u)` and `|w|Q(u)` justify the source and observation bounds.

Uniform-in-time **homogeneous population** control is established.
Persistent forcing has the displayed bound growing linearly with T.
Conditioning is explicit:
`C_U=B_infty exp(B_infty J0)`,
`B_infty=1+||S_infty|| ||Gamma_infty^+|| ||E_infty||`.
The singular endpoint kernel relation is proved. No full-rank or spectral
gap premise is imposed; the finite pseudoinverse norm has not been
numerically evaluated. The constants prove finiteness and are not a
practically calibrated numerical certificate.

The next milestone receives this forced evolution, compatible state and
forcing norms, weighted source moments, and whole-circle observations.
It still must control products and nonlinear remainders away from the
reference, including varying inverse gates times passive reverse queries.
The present result does not construct a nonlinear changed-law population
flow, a finite-contamination remainder, a sampling CLT, an expected-risk
expansion, perturbed endpoint continuity, transport forcing control, or a
raw-GD derivative theorem. Training-preserving endpoint tangent directions
are characterized in the canonical raw metric; no risk benefit or sign
of unseen influence follows.

## Exact proposed destinations

1. Append [P1_SECTION.md](P1_SECTION.md), unchanged, as C.4.6 immediately
   after C.4.5.3 in `docs/global_nonlinear.md`. It contains the consolidated
   theorem and complete novel propagator, weighted-source and capture proofs.
2. Replace only the C.4 navigation paragraph specified in
   [P1_ANCILLARY.md](P1_ANCILLARY.md).
3. Update only the Global nonlinear learning table row and C.4 scope
   paragraph in `docs/README.md`, exactly as the complete proposed guide
   in that ancillary file specifies.

No existing mathematical assertion, notation contract, other chapter,
maintained API, code, or PDF-exporter file is part of this addition.
The edition works without study/history dependencies. The study's checking
and assembly scripts are reproducibility artifacts, not proposed maintained
training software.

Frozen P1 manifest SHA256:
`f9f3ac7dd834429f2afd1b2d819e20cf04da37446311405943332300ca4fa53d`.
Section SHA256:
`33c819282d83f7f4704cbd3a90e87b445d17ce161cc922c1ad786b5eb0d9de38`.
Dependency packet SHA256:
`ca696bc4ed14ea337028eb1e6ef9c7f729ed2a0153bc210de8e16dea18f5ee79`.

The proposed complete edition hashes are:

| Destination | Base SHA256 | Proposed SHA256 |
|---|---|---|
| docs/global_nonlinear.md | `d473d95ee27bc39d484185cea2689b5d3ceed448567a1527488f1003e24f1fa1` | `3f32122dea7915e25e4abc7abe9d74017d535badfa5abbe1939e84fe2b100bdf` |
| docs/README.md | `7824df11fe7fa2d89cf2c75dc32716438b20c815a18d1c3809a84a0785d9d34e` | `88757537ae600ebf79cf265288d6ca3034254caba236b4dcb055a21dd568c721` |

## Reviews and reproducible validation

Research R1: [review A](R1_REVIEW_A.md) and [review B](R1_REVIEW_B.md)
both ACCEPT with no required corrections; complete reports, input hashes,
reading coverage and actual checks are retained. The coordinator read both
completely and verified their provenance. [Independent relevance](RELEVANCE.md)
accepts the stated destination and boundaries.

Canonical P1: [scientific review A](P1_REVIEW_A.md) and
[scientific review B](P1_REVIEW_B.md) both accept without required corrections.
The coordinator read every line of both reports (322 and 838 lines),
verified their input and report hashes, inspected the independent check
outputs and confirmed the distinct process identities and successful exits.
The report hashes are respectively
`1dff779a0721ce15b6d660cd7a9b46a555224537771509ccc3fb9e86beda34bd` and
`768b2fc7e20f6d65e530a6bd87e485a07314d8b1960122fe91dab601b8235349`.
The exact neutral assignments and inputs are frozen in
[P1_MANIFEST.json](P1_MANIFEST.json). Their extra independent checking sources
are also preserved as P1_REVIEW_A_CHECK.py and P1_REVIEW_B_CHECK.py; the
original executed copies and complete results remain in reviewer scratch.

The original [integration report](P1_INTEGRATION_REVIEW.md) requires two
packaging corrections: the standalone builder read undeclared structural
link targets, and the integration manifest included an assignment outside
its allowed input list. The reviewer correctly preserved the failed guarded
build and validator runs. It found no scientific integration defect.
The coordinator read all 343 report lines and verified its hash
`47dc9f59e3a3595d34bb7c7e2895aa691dc26246f998dae0a7a4afb52376b834`.

Both packaging corrections are implemented in [P1I2_MANIFEST.json](P1I2_MANIFEST.json),
hash `174bd123c8fd48cdc536925681f7eade0b5f37f3b3b1cebf53d7b383a0a5298f`.
Every structural support file is explicitly listed, hash-checked before
assembly and rechecked afterward; the manifest contains only allowed inputs.
The mathematical section, ancillary text, dependencies and mathematical
check programs retain exactly their P1 hashes. The two destination edition
hashes are unchanged. This packaging-only change requires fresh integration
review under workflow Part 2 step 4; it does not change the scientific inputs.
The fresh instance `01a090d2-454d-7550-8cdd-ef01257c6712` is conducting that
complete review without prior findings. No promotion acceptance is asserted
until the remaining reviews complete.

The coordinator read the entire canonical section and checked its final
notation corrections. Standalone validation passed with exact preservation
of the base edition outside the proposed changes, local guide links and
fragments, mathematical display structure, the nonzero-readout tangent and
metric identities, singular semigroup boundaries, and the exact rational
reference certificate. Evidence is under
`data/generated/trained_data_response/standalone_01/validation_02/`.
An initial validator heading-match bug is retained as a failed run; its
correction changed no scientific input.

Reproduce from the repository root, substituting a fresh run name:

```sh
python studies/trained_data_response/P1I2_BUILD.py --section studies/trained_data_response/P1_SECTION.md --output data/generated/trained_data_response/NEW_RUN/edition
python studies/trained_data_response/P1I2_VALIDATE.py --edition data/generated/trained_data_response/NEW_RUN/edition --output data/generated/trained_data_response/NEW_RUN/validation
```

The checks run from the standalone edition without study/history imports.
No training experiment, parameter sweep, figure-based conclusion, or formal
verification claim is involved. Original source hashes, exact changes and
all assembly/check programs are in the frozen manifest.

Scoped commits currently include `e111b63` (startup), `fed0567` (concurrent
recovery), `2c5cddb` (complete proofs and R1 freeze), and `b922d12` (accepted
R1 reviews and assembly preparation), and `fb2353c` (canonical P1 freeze).
All use owned paths and the common
Git writer lock; unrelated concurrent work is preserved.

## Approval boundary

After the pending reviews complete, the recommendation will concern only
the exact P1 package above. Established-file integration requires the user's
specific approval under RESEARCH_WORKFLOW Part 2, step 5. Any intervening
scientific or dependency change reopens the applicable review and approval
gates. No established file has been edited by this study.
