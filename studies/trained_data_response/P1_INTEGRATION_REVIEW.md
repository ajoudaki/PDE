# P1 independent integration review

**Overall decision: CORRECTION REQUIRED for the frozen integration package.**

The proposed mathematics, its source/propagator/capture interfaces, and its placement are compatible with the supplied established material. I found no scientific defect in this integration review. Exact preservation and all independently executable local checks passed. However, the supplied build cannot complete under the assignment's isolation boundary: it reads undeclared live files, and its validator consequently cannot run to completion. The manifest also requires a hash for an assignment outside this review's allowed reading list. These are concrete packaging and scope corrections, not a mathematical rejection of the response theorem.

This is a fresh integration assessment, not an authorship contribution, a relevance decision, permission to promote, or a new proof audit of the entire library. No input has been repaired or edited.

## 1. Fresh process identity and isolation

The fresh review instance identifier minted for this execution is **39830040-55c1-4d27-8ae6-8f9a0c2fb2f5**, created at **2026-09-11T14:06:47.326536+00:00**. It identifies this review and its newly created scratch directory. It is a locally generated review UUID, not a claimed platform-issued task identifier. The platform has not exposed a unique reviewer task/process UUID in this session. The identity-recording Python command reported PID 10 and parent PID 2; subsequent guarded child PIDs were 10 and 11 in their execution namespace. These PIDs are diagnostic metadata and are not globally unique reviewer identities.

The role label `/root` is reusable. Its occurrence in this session and in the permitted manifest's author list does not identify the same process. The manifest names author task `01a090b5-1a52-7072-a829-cd2aad518558` and selector instance `01a090c0-8a58-7da1-8075-8c0b0f23f959`; neither is this review's identity. Those provenance strings were encountered only in the required manifest, not through author history. Freshness and isolation here mean the new user-launched review context, the documented input boundary, and the actual reads/actions below; the locally minted UUID alone is not independent authentication of launch provenance.

I read only the ten files explicitly required by `P1_INTEGRATION_ASSIGNMENT.md`, the required skills and their two applicable references, and my own generated artifacts. I performed no author startup, Git operation, input edit, training, delegation, contact with another task, or external-source lookup. I did not read the study README, research workflow, other reviews, prior packets, or prior verdicts. General repository instructions were already supplied by the user; their isolated-review branch was followed.

The required skills were read completely:

- [solve-math-rigorously/SKILL.md](/etc/codex/skills/solve-math-rigorously/SKILL.md).
- [investigate-conjectures/SKILL.md](/etc/codex/skills/investigate-conjectures/SKILL.md).
- [research-contract.md](/etc/codex/skills/investigate-conjectures/references/research-contract.md).
- [adversarial-audit.md](/etc/codex/skills/investigate-conjectures/references/adversarial-audit.md).

These skills were applied to the stated review and deterministic checks, without restoring author history or starting an experimental research program.

All working files are in the fresh directory:

`/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5`

The sole report is the user-assigned `P1_INTEGRATION_REVIEW.md`. No other report was opened. The builder was allowed its expressly named five live base files for hashing/assembly. Their scientific bodies were read from the frozen packets, not by opening live chapters for scientific review. A Python audit hook blocked undeclared repository reads before their contents could be returned. In particular, `docs/arctan_limits.md` was not read.

## 2. Complete reading coverage

Every line of the ten required files was read: **7,794 lines in total**, including **7,222 lines** across the section, ancillary file, dependency packet, and integration complement. This includes the complete certificate code embedded in the older proof, not just its recorded output. No scientific lines remain unread within these required files.

The first combined ancillary/dependency display was truncated around the dependency packet's opening prefix. I repaired this by rereading `P1_DEPENDENCIES.md` lines 1–35 completely. The remainder of that display, including lines 33–210, was present. All subsequent chunks were displayed without truncation. `coverage.json` records every inclusive range and checks that their union contains every required line.

| Required input | Completely read lines |
|---|---:|
| `P1_INTEGRATION_ASSIGNMENT.md` | 1–42 |
| `P1_SECTION.md` | 1–2062 |
| `P1_ANCILLARY.md` | 1–300 |
| `P1_DEPENDENCIES.md` | 1–3238 |
| `P1_INTEGRATION_BASE.md` | 1–1622 |
| `P1_MANIFEST.json` | 1–83 |
| `P1_BUILD.py` | 1–98 |
| `P1_VALIDATE.py` | 1–96 |
| `P1_CHECK_IDENTITIES.py` | 1–197 |
| `P1_REFERENCE_CERTIFICATE.py` | 1–56 |

The older scientific reading scope is exact, not a claim to have read the whole book:

| Frozen source | Source lines read | Contents |
|---|---|---|
| `docs/README.md` | 1–273 | Complete old reading guide |
| `docs/NOTATION.md` | 1–98 | Complete notation contract |
| `docs/finite_dynamics.md` | 1–227 | Complete supplied §§1–4, including gradients, normalization, energy, existence, and width bounds |
| `docs/special_data_limits.md` | 3785–4326 | Complete supplied III.F.1–11: fixed programs, singular queries, common actions/adjoints, HS increments, strong differentiation and associated proof units |
| `docs/global_nonlinear.md` | 1840–2453 | Complete A.1–A.4 and B.1 proof units, including their sum-loss convention and GD discussion |
| `docs/global_nonlinear.md` | 3836–6892 | All old C.4, including its statement, C.4.1–C.4.4 and complete C.4.5 statement/proofs |

This totals **4,811 distinct old source lines**. The dependency packet supplies C.4 source lines 3972–4296 and 5465–6585; the integration complement supplies 3836–3971, 4297–5464, and 6586–6892. Their union is exactly the full old C.4 with no gap or overlap. All ten frozen excerpts match the corresponding reconstructed baseline slices byte-for-byte; their individual hashes and packet-line mappings are retained in `integration_checks.json`.

The precise unread complement is:

- `docs/global_nonlinear.md`: 1–1839 and 2454–3835.
- `docs/finite_dynamics.md`: 228–1275.
- `docs/special_data_limits.md`: 1–3784 and 4327–27274.
- No unread complement within the old guide or notation file.
- All other chapters, maintained implementation sources/guides, external papers, and study/history/review material were not scientifically read. In particular C.2, special-data J.1, and the Gaussian-calculus three-query obstruction were not imported as fresh proof inputs. C.2 is a dependency of preserved older local material, not a missing premise of the new response proof. The new section's references to the latter two obstructions are scope comparisons; its positive estimates are proved in the supplied section.

Full bytes of the five declared base files were handled only by the permitted build and generated-edition preservation checks. Byte copying/hashing and range comparison do not enlarge the scientific reading scope above.

## 3. Frozen hashes

All nine manifest-hashed files that are explicitly authorized inputs matched both their SHA-256 and declared line counts. A final pass confirmed all ten allowed inputs, including the manifest itself, were unchanged since the opening check.

| Authorized frozen input | SHA-256 verified |
|---|---|
| `P1_SECTION.md` | `33c819282d83f7f4704cbd3a90e87b445d17ce161cc922c1ad786b5eb0d9de38` |
| `P1_ANCILLARY.md` | `649f0ee17af3c0a2f4b995e971929f8e7a62d9d420614baee418af62d6cdd420` |
| `P1_DEPENDENCIES.md` | `ca696bc4ed14ea337028eb1e6ef9c7f729ed2a0153bc210de8e16dea18f5ee79` |
| `P1_INTEGRATION_BASE.md` | `675898673ac2be8c12540d0994e15c09695dba3d2cf76ebe2279cc5d31d671ba` |
| `P1_CHECK_IDENTITIES.py` | `b0bbbde5f1f024dbb46975b2092d3aa2a79cabd0375f4520c05489f934d3902f` |
| `P1_REFERENCE_CERTIFICATE.py` | `112ce04c6d8e20859b5a778cfdeed42690949af807d421b7db90e82c2576a49e` |
| `P1_BUILD.py` | `b3801f5181ffbbbb7725a64620aaa1a254f796c8a253f6de4937cfbc833ebc11` |
| `P1_VALIDATE.py` | `758b17ad4a84309a5a1814c5f3d11d36dc39846c0ec1b31b069857c598a32b40` |
| `P1_INTEGRATION_ASSIGNMENT.md` | `90e77cf2ca6fd807ba5792fd5a45680bcd96a5f7e6250e2fd18bf4bc58b22c41` |

The manifest itself has observed SHA-256 `f9f3ac7dd834429f2afd1b2d819e20cf04da37446311405943332300ca4fa53d`; no self-hash was declared.

**Unverified, outside the explicit allowed list:** `P1_ASSIGNMENT.md`. The manifest declares SHA-256 `a9f61d5f27d84e8a9de85d7ba5faaf39c4adb19e7829c27c285a455916488199` and 47 lines for it. I did not open it, even for hashing, because the launch says the neutral integration assignment names the complete allowed inputs and that assignment does not include this file. Thus I do not claim that every entry in the manifest was verified. This mismatch is required correction R2 below.

All five declared live base hashes passed the builder's assertions before it reached the undeclared read. Independently reversing the three selected navigation edits and removing exactly the inserted section and its separator recovers these same hashes:

| Base file | SHA-256 verified/recovered |
|---|---|
| `docs/global_nonlinear.md` | `d473d95ee27bc39d484185cea2689b5d3ceed448567a1527488f1003e24f1fa1` |
| `docs/README.md` | `7824df11fe7fa2d89cf2c75dc32716438b20c815a18d1c3809a84a0785d9d34e` |
| `docs/NOTATION.md` | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| `docs/finite_dynamics.md` | `a57d832a0ce0ad2bf40fa1ec574af787d05bade3264b619a8faca5545bc0012a` |
| `docs/special_data_limits.md` | `5b7b48aa5deab320042217a6f284002366a683bf0f7f0b63c05d8167c526a489` |

## 4. Commands, actual results and retained checks

The exact command arrays, working directories and environment overrides are retained in `required_runs.json` and `standalone_runs.json`. The requested build and validation were attempted unchanged, using the provided `--section` argument rather than the builder's out-of-scope default filename.

The read guard is my own check harness at `guard/sitecustomize.py`; it does not alter any supplied program. It admits the ten explicit inputs, the five declared live bases, and this scratch directory, blocks other repository opens, and forbids repository writes outside scratch. `PYTHONDONTWRITEBYTECODE=1` prevents incidental input-directory bytecode files. `access_audit.jsonl` retains the permitted and denied repository opens. This was a reviewer-imposed isolation guard, **not** an automatic approval-review rejection.

Exact runner invocation from `/home/amir/Codes/PDE`:

```sh
python data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/run_required.py
```

Its two subprocess invocations and environment were:

```json
[
  {
    "command": [
      "python",
      "/home/amir/Codes/PDE/studies/trained_data_response/P1_BUILD.py",
      "--section",
      "/home/amir/Codes/PDE/studies/trained_data_response/P1_SECTION.md",
      "--output",
      "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/edition"
    ],
    "cwd": "/home/amir/Codes/PDE",
    "environment_overrides": {
      "PYTHONPATH": "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/guard",
      "PYTHONDONTWRITEBYTECODE": "1",
      "P1_REVIEW_SCRATCH": "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5"
    },
    "exit_code": 1
  },
  {
    "command": [
      "python",
      "/home/amir/Codes/PDE/studies/trained_data_response/P1_VALIDATE.py",
      "--edition",
      "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/edition",
      "--output",
      "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/validation"
    ],
    "cwd": "/home/amir/Codes/PDE",
    "environment_overrides": {
      "PYTHONPATH": "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/guard",
      "PYTHONDONTWRITEBYTECODE": "1",
      "P1_REVIEW_SCRATCH": "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5"
    },
    "exit_code": 1
  }
]
```

**Build result: exit 1.** It verified all five declared hashes and assembled all five destination files. It then reached `P1_BUILD.py:63–67` and attempted to read `/home/amir/Codes/PDE/docs/arctan_limits.md`. The audit hook raised:

```text
PermissionError: P1 isolated review blocked undeclared repository access: /home/amir/Codes/PDE/docs/arctan_limits.md
```

`required_0.stdout` is empty; the complete traceback is in `required_0.stderr`. The partial `edition/` is retained exactly as the failed builder left it. It was not silently completed or relabeled as a successful build.

**Validation result: exit 1.** `P1_VALIDATE.py:32` failed opening the absent `edition/edition_manifest.json`, which the builder would create only after its undeclared copies. `required_1.stdout` is empty; `required_1.stderr` retains the complete `FileNotFoundError` traceback. The newly created empty `validation/` directory is retained. No supplied `validation.json`, link-check PASS, or end-to-end validation PASS exists.

The two scientific check programs were then run separately, with the same guard environment, from the fresh scratch directory. This preserves useful authorized checks without misrepresenting the failed validation command. Their exact argument arrays and results are:

```json
[
  {
    "command": [
      "python",
      "/home/amir/Codes/PDE/studies/trained_data_response/P1_CHECK_IDENTITIES.py",
      "--output",
      "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/independent_identities"
    ],
    "cwd": "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5",
    "exit_code": 0
  },
  {
    "command": [
      "python",
      "/home/amir/Codes/PDE/studies/trained_data_response/P1_REFERENCE_CERTIFICATE.py"
    ],
    "cwd": "/home/amir/Codes/PDE/data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5",
    "exit_code": 0
  }
]
```

The executed small runner is retained in equivalent file form as `run_standalone.py`; its original execution used an inline Python command. Both scientific subprocesses returned zero and both stderr files are empty.

`P1_CHECK_IDENTITIES.py` reports:

```text
tangent central-difference errors:
1.0292819331017236e-06
2.573205074969171e-07
6.433012185025226e-08
1.608263607613758e-08
loss/metric pairing error: 7.900680110140001e-12
singular semigroup errors:
0.0, 2.5723099259051144e-16, 6.175621784463053e-16,
3.9466663458119664e-15, 1.5265444420160054e-14
checks: PASS
Python 3.10.12; NumPy 1.26.4; SciPy 1.13.0
```

The last central-difference error is below `1e-7`, and successive errors decrease by the required factor greater than 3.8. The program also checks the complete weighted raw Gram, the singular compatible factorization, and a nilpotent counterexample showing why positivity of `ES` alone is insufficient. These deterministic checks exercise nonzero stored readout and both matrix directions; they are not training or a proof of a width limit. Full output and results remain in `standalone_0.stdout` and `independent_identities/results.json`.

The rational reference certificate reports:

```text
[0.392108947877, 0.396376711612, 0.233120735618,
 0.339792209687, 0.631761866359]
```

Its integer/rational assertions pass, including the density/tail inequalities and the auxiliary `e^18` and `e^8` margins. These certify the conservative initialization bounds used for the reference; they do not evaluate endpoint conditioning or the new response constant. The standalone certificate is byte-identical to the complete displayed program in the supplied dependency proof. Full stdout is `standalone_1.stdout`.

My independent structural/preservation check was run from the repository directory:

```sh
python data/generated/trained_data_response/integration_p1/39830040-55c1-4d27-8ae6-8f9a0c2fb2f5/check_integration.py
```

**Result: exit 0; all 36 local checks passed.** They verify literal builder/manifest agreement, exact section insertion, unique reversible edits, all five recovered base hashes, all ten excerpt bodies, complete C.4 coverage and insertion position, the complete revised guide and navigation against the ancillary file, equation references/collisions, delimiter balance, absence of study-artifact dependencies, certificate correspondence, and the available local anchor. The full script, complete `integration_checks.json`, command record, stdout summary and empty stderr are retained. This independent check reads no live established source and does not replace the failed supplied validator.

## 5. Scientific contract and interface findings

### Model, clock and metric

The addition keeps two tanh hidden layers, full first row in dimension two, normalized input `u=x/sqrt(2)`, scalar output pairing divided by `n`, independent stored variances `(1,1/n,1/n^2)`, mobilities `(n,1,n)`, and unhalved mean loss. These match the complete notation contract and old C.4. The section explicitly assumes `Y>=1` so the fixed labels `+1,-1` belong to its data space. It does not silently strengthen the old theorem's `Y>0` convention.

B.1 uses the unhalved sum loss. Setting its three `kappa` values to `1/2` yields the two-atom mean-loss equations used here, as already explained in C.4.5.2 §5 and C.4.5.3 §2. Thus the reference, its GF width identification and the new derivative use the same physical clock. The old relation `ds/dt=2(1-b)` and first `b=1` feature endpoint at `s_dagger<=10` supply the actual autonomous reference, not a fitted coefficient table.

The exact transform satisfies `F'=cosh^2` and `j_X=sech^2`. Consequently `delta w_a=phi'(w_a) xi_a`, while the active feature differential is `delta H_a^1=phi'(w_a)^2 xi_a`. The distinction between these two gates is maintained in T0, P8, T14 and the finite implementation. Differentiating the raw gate in physical time cancels the same term produced by differentiating the conversion; it is therefore correct that P10 contains no additional own-first-gate curvature term.

The tangent uses row L2, middle HS and readout L2 in a square sum. Its finite version uses `||xi||F^2/n + ||B||F^2 + ||d||2^2/n`; a rank is `q h^T/n`. This matches III.F.8 and C.4.5.1's raw metric. Only the learned increment and its variation are HS. The initialized Gaussian action remains bounded, with its actual adjoint. The raw conversion is bounded and injective but has no assumed bounded inverse. No operator distance across different carriers is used.

### Exact forcing and admissibility

Differentiating the training measure directly gives `-2 integral r q d(nu-nu_*)`, including the signed reference subtraction. On the active atoms `u=e_a`, the inverse first gate cancels before state differentiation. On passive inputs it remains as `phi'(w.u)/phi'(w_a)`; T5 retains it. No label appears inside a residual-free backpropagated derivative.

The new source proof supplies the additional weighted integrability that the older RMS result alone could not provide. Its actual finite-GF column-deletion argument retains the learned cavity flow and its own residuals, uses the full matrix on the first reverse error term in S21, and conditions Gaussian estimates on the column-independent event `E_n^i`, not on `E_n`. The resulting finite value-envelope moments are proved before width passage. The exact bound `cosh^2 j(X,g)<=cosh^2 g+2|X|` then controls the inverse-gate weights without an exponential in an uncontrolled clock.

Uniform population source control is obtained separately from the bounded feature segment. Only that auxiliary feature system starts with zero finite readout; the actual finite GF and actual derivative keep the prescribed random readout. The dense-parameter envelope is transferred using bounded tests, moment bounds and L2 continuity, without claiming pointwise continuous Gaussian sample paths for arbitrary population queries.

With `M_w` as in T8, the row block is bounded using `sum_a u_a^2=1`; the middle and last squared norms contribute at most 10 and 1. This gives exactly `2(sqrt(10)+Y)sqrt(M_w^2+11)` in T9. Source §7 proves continuity into the stated Hilbert space, separable compact range and Bochner integrability. Total variation is its mass convention, so `||nu-nu_*||TV<=2`. The extension to all finite signed zero-mass measures is a bounded linear forcing extension, not an assertion that every such measure is a two-sided probability tangent.

### Propagator and conditioning

The interfaces satisfy `E=S*D`, `Gamma=ES=S*DS`, with `D=R*R`. Since the gate is positive almost surely, `D` is injective despite its lack of a uniform positive lower bound. Therefore `ker Gamma=ker S=ker E*`; this is the specific compatibility needed to exclude a nilpotent zero mode in `SE`.

The finite-dimensional pseudoinverse formula P16 is valid also at singular rank and yields the frozen endpoint bound P17. No continuity of pseudoinverses along the path or uniform spectral-gap assumption is used. The passage from the frozen endpoint to the trained evolution is supplied by P18–P22: raw exponential endpoint convergence, the established active endpoint L4 moment, and bounded multipliers give an integrable operator-norm difference of the finite-rank factors, while residual curvature is integrable. P23–P27 construct the strongly continuous evolution and bound it by `C_U=B_infty exp(B_infty J_0)`.

This establishes uniform homogeneous population propagation. A uniformly bounded nondecaying forcing then gives an upper bound linear in `T`, not a uniform-in-time bound on the forced response. The theorem and revised guide respect this distinction. The constants are finite reference quantities, not useful numerically evaluated conditioning certificates.

The endpoint projection is onto `ker E_infty`. After raw conversion it is the orthogonal projection off the weighted raw training-gradient span on the admissible clock domain. This does not require equivalence of the raw and clock norms, or convergence of the full nonautonomous propagator to that endpoint projection. Unseen response signs and risk benefits are correctly left undetermined.

### Actual finite-GF capture and passive observations

At every fixed width, compact data support and smooth activations justify differentiating the exactly integrated finite loss. Energy supplies a common finite parameter ball for contamination parameters in `[0,1]`; the finite ODE difference-quotient argument constructs the right derivative there before any width limit. Its constants may depend on width and are not used as width-uniform remainder bounds.

The capture proof then uses a separate hierarchy: fixed source cutoff and finite data quadrature, fixed transformed reference/tangent mesh, fixed-program width identification, followed by removal of these approximations. The clipping and weighted tails are supplied by the source theorem. A.1 and III.F apply to the fixed continuous at-most-linear instructions; readout clips are outside already proved bounds. Learned matrices are finite sums of ranks, so contractions identify their HS geometry and both orientations.

F16 and the compact-family cutoff argument justify multiplier consistency only on the L2 vectors actually tested. F20–F22 compare actual finite tangents with same-array mesh proxies using an integrated defect and a width-independent compact-time generator bound. This avoids both an unjustified operator-norm convergence assertion for multiplier fields and application of a fixed-program theorem to a growing transcript.

The output representing field F23 is uniformly bounded. Weighted `|w|Q` moments give its circle H1 estimate; the resulting one-half Hölder modulus, finite tangent bounds, and time equicontinuity justify the final finite time/input net argument. Hence the entire-circle prediction claim is supported by more than convergence at a finite panel. The claimed joint hidden response topology is finite lists with second moments and the specified proxy state comparison, not hidden-coordinate path-law convergence in a supremum norm.

Convergence is for each fixed deterministic Borel law and each fixed physical horizon, with no claimed rate or supremum of failure probabilities over all laws. The proof does not assume a nonlinear changed-law population flow at arbitrary horizons. It claims no raw-GD derivative theorem, finite-contamination remainder uniform in width, sampling CLT, empirical-law TV convergence, generalization advantage, or training-time Taylor-series convergence.

### Adversarial checks of the bridges

| Possible obstruction | Finding within this integration scope |
|---|---|
| Stability without a controlled source | Addressed by the separate actual finite cavity and weighted source proof; not inferred from the propagator |
| Singular Gram or nonnormal zero mode | Kernel compatibility is proved; the general nilpotent counterexample is explicitly distinguished |
| An inverse gate invalid on L2 | The weighted query envelope supplies its domain; the raw inverse conversion is never assumed bounded |
| Unbounded multiplier continuity in operator norm | The proof uses strong continuity, fixed-vector cutoffs and compact testing families |
| Future-trajectory or history playback | Coefficients come from the existing autonomous state and causal generated operations; no retained empirical trajectory is needed |
| Fresh reverse matrix or zeroed actual readout | Both orientations and the actual finite readout are retained, with auxiliary comparisons explicitly separated |
| Finite-panel or law-quantifier substitution | Circle moduli and fixed-law quantifiers are explicit; no uniform failure probability over all laws is promoted |
| Homogeneous all-time bound promoted to an all-time width theorem | These claims remain separate in the theorem, proof and guide |

These findings describe checked mathematical interfaces and the absence of an identified defect, not a computational proof of the infinite-width theorem or certification of all unread established results.

## 6. Placement, duplication and preservation

The section is inserted as C.4.6 immediately after the complete old C.4.5.3 at baseline line 6892. Its `####` heading is a sibling of C.4.5; its subordinate headings are consistent. It follows the reference construction it uses, and does not interrupt an older proof unit.

There are 106 new equation tags: 15 statement tags, 30 propagator tags, 45 source tags and 16 finite-capture tags. Every explicit C.4.6 equation citation resolves, each new tag is unique, and none collides with an older tag. Gaps in local numbering are not dangling references. T5/T6/T14 refer to the actual definitions consumed in P8–P11 and the source/capture proofs; P22 is the stated finite `J_0`.

The new mathematical contribution is distinct from the preserved local law-stability theorem and the fixed-accuracy nonlinear transfer theorem. Restating the reference norms, fields and constants supplies the interface for the new derivative result. No second conflicting version of an established theorem is introduced. Some definitional repetition across the statement and proof units is substantial but mathematically consistent.

The exact generated section is byte-identical to `P1_SECTION.md`. Reversing the single C.4 navigation replacement restores the complete old global chapter hash; reversing the two guide replacements restores its old hash. The complete revised guide equals the ancillary text exactly. The other three declared base destinations are unchanged. All ten supplied old excerpt bodies also match recovered baseline slices. Thus established statements, their quantitative margins, physical-step qualifications and limitations are preserved in the assembled five-file content.

The revised guide accurately describes finite-GF response capture on each fixed horizon, whole-circle observations and bounded homogeneous population propagation. Its scope paragraph explicitly says the TV bound gives response control linear in the horizon and constructs neither nonlinear changed-law flows nor a finite-contamination remainder. The older local expected-gap ordering, exceptionally small C.4.5 neighborhood, risk guarantee and early activity qualification remain intact.

The available local guide targets and the existing C.4 anchor checked correctly. Six guide targets are unavailable in the permitted partial edition: `gaussian_calculus.md`, `arctan_limits.md`, `linear_dynamics.md`, `continuous_depth.md`, `finite_optimization_and_controls.md`, and `../code/README.md`. These are preserved guide links, not newly broken mathematical references; the inability to finish their packaging/checking is R1. External orientation links were not opened and are expressly not theorem dependencies.

The section contains no required study path, generated-data path, historical report filename or empirical campaign artifact. The word “oracle” in the fixed-program argument denotes a causal deterministic-coefficient proof proxy already defined in the supplied theory; it does not supply future trained trajectory values. The numerical helper checks supplement self-contained arguments and do not become scientific premises.

## 7. Required corrections

### R1 — Make the build's read set match the frozen authorized input set

**Location:** `P1_BUILD.py:63–70`, with the dependency in `P1_VALIDATE.py:56–69`.

The builder globs all live `docs/*.md` and reads/copies every target not among its five already assembled files. It then reads `code/README.md`. These additional files have no declared expected base hashes, and their bodies are outside the complete allowed input list. The generated `supporting_files` hashes would merely record whatever live bytes happened to be copied; they do not verify a prior freeze. The final concurrency check also covers only the five declared bases.

This is not hypothetical: the guarded required command failed at the first additional read, `docs/arctan_limits.md`. The validator then failed because the build never produced its manifest. Running the builder unguarded would violate this launch's explicit isolation instruction, so I did not do so. I did not create stubs, edit the script, or weaken its checks to produce an artificial PASS.

**Required change:** remove unlisted live reads from this isolated review package, or explicitly supply and freeze the complete intended support-file set with matching authorization. Align the standalone-link validation scope with that decision. Then rerun the unchanged intended build and validation on fresh output paths and retain their actual complete results. This request concerns a concrete failing packaging path; it does not require rewriting the scientific theorem.

### R2 — Resolve the manifest's extra assignment entry

**Location:** `P1_MANIFEST.json:44–47`, compared with `P1_INTEGRATION_ASSIGNMENT.md:8–17` and the launch's complete allowed-input boundary.

The manifest includes `P1_ASSIGNMENT.md`, which is not among the ten inputs this isolated integration reviewer is told to read. The instruction to verify every frozen hash therefore cannot be completed literally without opening an out-of-scope file. Its hash remains unverified; there is no allegation that its bytes are wrong.

**Required change:** make the integration manifest contain only this review's authorized inputs, or explicitly authorize and supply the additional file for the required verification. The integration reviewer must not infer permission to open a different assignment from the manifest entry alone.

There are **no required scientific corrections identified** in this integration scope. R1 and R2 prevent unconditional acceptance of the submitted frozen integration package.

## 8. Optional suggestions, separate from the decision

Once the read-set issue is fixed, an explicit immutable support-file list would make the edition reproducible and auditable without globbing. An integration-specific manifest would also make accidental inclusion of another assignment less likely. These implementation choices are suggestions; the required outcome is the scope/freeze consistency stated in R1 and R2.

No further scientific claim, new numerical campaign, sharper conditioning constant, raw-GD response theorem, or nonlinear-contamination extension is required for the present integration scope.

## 9. Retained evidence and final disposition

The report retains the complete independent findings rather than replacing them with a pass/fail summary. The assigned scratch contains:

- Identity, initial/final input hashes, and complete line coverage: `identity_and_input_hashes.json`, `final_input_hashes.json`, `coverage.json`.
- The isolation guard and read record: `guard/sitecustomize.py`, `access_audit.jsonl`.
- Both exact requested command records and complete failure logs: `run_required.py`, `required_runs.json`, `required_0.*`, `required_1.*`, partial `edition/`, and the empty `validation/` left by the attempted validator.
- Separately executed scientific checks: `run_standalone.py`, `standalone_runs.json`, `standalone_0.*`, `standalone_1.*`, `independent_identities/results.json`.
- Independent preservation/reference checks: `check_integration.py`, `integration_checks.json`, `integration_check_run.json`, `integration_check.stdout`, `integration_check.stderr`.
- The report generation script, report-completeness checks (including a retained initial checker failure caused by bold Markdown in a literal substring test), and an artifact inventory with hashes. The inventory records the report's hash separately and excludes itself from its own hash list.

**CORRECTION REQUIRED:** resolve R1/R2 and obtain a successful permitted standalone build/validation. The scientific interfaces, scope and preservation checks reported above passed within the complete allowed reading scope. This report grants no promotion or established-file editing approval.
