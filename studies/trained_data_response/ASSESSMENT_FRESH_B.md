# Fresh independent scientific assessment B — frozen P1

**Verdict: ACCEPT for the exact proposed C.4.6 scope and its proposed guide/navigation descriptions.**

The package proves the actual finite-GF right-data derivative limit on each separately fixed physical horizon and the whole input circle, on the stated clock/HS/readout topology. It also proves a bounded population homogeneous propagator uniformly over all physical times and an admissible total-variation forcing map. The resulting forced estimate grows linearly with the horizon. These are different conclusions, and the package keeps them different. I found no surviving mathematical gap requiring a correction to those conclusions.

This is not acceptance of nonlinear changed-law population continuation, a finite-contamination remainder, a uniform-time width limit, an endpoint law-continuity theorem, or a GD derivative theorem. None is supplied or claimed by this addition.

## 1. Reviewer identity, isolation, and authority

Reviewer process: `/root/milestone1_fresh_b`, launched on 2026-09-11 as a fresh isolated scientific auditor. This process is distinct from the author/assembler identities and selector recorded in `P1_MANIFEST.json`. I used only the neutral launch assignment, `P1_ASSIGNMENT.md`, and the six other allowed frozen files listed below. I did not read the study README, drafts, other assessments, research history, other study material, build/integration packets, Git history, or external linked papers. I did not delegate, contact another task, or perform any Git operation.

The required skills were read completely:

- `/etc/codex/skills/solve-math-rigorously/SKILL.md`;
- `/etc/codex/skills/investigate-conjectures/SKILL.md`;
- its complete `research-contract.md`, `adversarial-audit.md`, and, because deterministic computation was authorized, `decisive-experiments.md` references.

The scientific target was to verify the exact frozen statement, reconstruct its dependencies and proof bridges, and try to defeat it with the strongest relevant operator, product, conditioning, and limit-order objections. Computation was restricted to the two required programs and fixed independent algebra/boundary checks. There was no optimization trajectory, training experiment, stochastic simulation, or parameter search. My only writes were this report and scratch under `data/generated/trained_data_response/assessment_fresh_b_20260911/`.

## 2. Frozen input identity and complete coverage

The following SHA-256 values were computed from the files. Every input having a manifest entry matched it. The manifest itself has no self-hash; its computed digest was retained to detect changes. Baseline and final hash results are retained in scratch as `input_hashes_before.json` and `input_hashes_after.json`.

| Allowed file | Lines | SHA-256 |
|---|---:|---|
| `P1_SECTION.md` | 2062 | `33c819282d83f7f4704cbd3a90e87b445d17ce161cc922c1ad786b5eb0d9de38` |
| `P1_DEPENDENCIES.md` | 3238 | `ca696bc4ed14ea337028eb1e6ef9c7f729ed2a0153bc210de8e16dea18f5ee79` |
| `P1_ANCILLARY.md` | 300 | `649f0ee17af3c0a2f4b995e971929f8e7a62d9d420614baee418af62d6cdd420` |
| `P1_CHECK_IDENTITIES.py` | 197 | `b0bbbde5f1f024dbb46975b2092d3aa2a79cabd0375f4520c05489f934d3902f` |
| `P1_REFERENCE_CERTIFICATE.py` | 56 | `112ce04c6d8e20859b5a778cfdeed42690949af807d421b7db90e82c2576a49e` |
| `P1_ASSIGNMENT.md` | 47 | `a9f61d5f27d84e8a9de85d7ba5faaf39c4adb19e7829c27c285a455916488199` |
| `P1_MANIFEST.json` | 83 | `f9f3ac7dd834429f2afd1b2d819e20cf04da37446311405943332300ca4fa53d` |

Coverage was complete, including both full reading guides, all dependency proofs, and both complete check programs. Candidate reads covered lines 1–520, 521–1040, 1041–1560, and 1561–2062. Dependency reads covered 1–550, 551–1100, 1101–1380, 1381–1660, 1661–1940, 1941–2220, 2221–2500, 2501–2780, 2781–3020, and 3021–3238. The displayed dependency read 551–1100 was truncated in its middle; lines 760–940 were read again in full, recovering the Gaussian source-rule and singular-query proofs. A later combined output was truncated across the dependency/ancillary boundary; dependency lines 3160–3238 and ancillary lines 1–150 were read separately, recovering the complete finite-reference tail passage and guide opening. The remaining ancillary lines through 300 were present in the original output. No truncated proof was treated as read.

The complete scientific dependencies inspected were the shared notation and finite metric/energy proof; III.F.1–11, including adaptive conditioning, singular regularization, common action completion, HS algebra and strong differentiation; global-nonlinear A.1–4; the complete B.1 activation-transform theorem and proof; the supplied C.4.1/C.4.2 state/action construction; and the complete C.4.5.1–2 fitted reference, endpoint, rational certificate, and source-response proofs. Existing unrelated guide entries were read for context and consistency. External links in those guides are expressly context, not imported theorem dependencies, and were not used to fill any proof step.

## 3. Contract and component conclusions

I use A, B, C below as review labels for the three central conclusions; these are not new equation labels in the candidate.

| Component | Exact conclusion assessed | Verdict |
|---|---|---|
| A | Bounded strongly continuous trained linear generator on the clock/HS/readout Hilbert space; compatible singular endpoint; uniform population homogeneous propagation | ACCEPT |
| B | Actual finite-reference query moment bounds, weighted uniform integrability, canonical uniform population source envelope, and continuous TV-to-forcing map | ACCEPT |
| C | Finite right differentiation first, actual finite-tangent identification by same-array proxies, and uniform fixed-horizon/whole-circle output convergence | ACCEPT |
| Reference dependencies | Same canonical action and adjoint, finite GF identification, fitted feature endpoint and required active-query moment | ACCEPT for the uses made here |
| Ancillary additions | Accurate separation of fixed-horizon finite-GF capture, all-time population homogeneous control, and nonlinear-continuation exclusions | ACCEPT |

The object is the two-hidden-layer, equal-width, bias-free tanh network with normalized input `u=x/sqrt(2)`, output `c^T H2/n`, independent stored Gaussian variances `(1,1/n,1/n²)`, mobilities `(n,1,n)`, and unhalved mean-square loss. The trained reference law has exactly the two orthogonal inputs and opposite labels. The perturbing law is any fixed deterministic Borel probability on the stated compact circle/label space. It may be nonatomic, singular, or have arbitrarily small atom masses.

The limit is taken after the finite-width right derivative in the contamination parameter. There is no derivative of an assumed nonlinear population law-to-flow map. Population states are actual generated function/action fields; they are not claimed to be finitely many scalars. Coefficients are generated by the autonomous reference equation and current reference fields. Neither source nor initialization contains a future prediction trajectory.

## 4. Independent reconstruction of the model and tangent equation

For a finite passive input, differentiating `f=c^T phi(A phi(wu))/n` gives the raw metric gradient

\[
 (u\,\phi'(wu)Q,\ \delta H_1^T/n,\ H_2),\qquad
 \delta=c\phi'(Z_2),\quad Q=A^T\delta.
\]

Pair this with a raw increment using `1/n` in the row/readout blocks and the ordinary Frobenius pairing in the middle. This recovers the scalar derivative and explains every normalization. Multiplying by `-2r` and integrating gives the physical vector field. At the reference the two weights `1/2` cancel the loss factor two, giving exactly S11. This also gives

\[
 R'(t)=-\{\|w'(t)\|_F^2/n+\|A'(t)\|_F^2+\|c'(t)\|_2^2/n\}.
\]

The finite same-width middle metric is therefore Frobenius, not Frobenius divided by `sqrt(n)`. Its population counterpart is HS: the normalized finite-layer orthonormal basis is `sqrt(n)e_i`, and the HS norm computed in that basis is precisely the matrix Frobenius norm. A rank has finite representative `q h^T/n`, HS norm `||q||_2||h||_2`, and pairing with `B` equal to `<q,Bh>`.

The scalar primitive satisfies `F'=cosh²`, is strictly increasing, and diverges at both ends. With `F(w_a)=F(g_a)+X_a`, differentiation gives `j_X=sech²(j)=D_a`; consequently `delta w_a=D_a xi_a` and the active first hidden variation is `D_a² xi_a`. The map `R:v -> ((D_a xi_a)_a,B,d)` is bounded, injective and contractive on the tangent Hilbert space. Its inverse is not a bounded raw-Hilbert map. The statement uses the stronger clock norm throughout.

The active clock equation is `X'_a=-2p_a r_a Q_a`. Its derivative is

\[
 \xi'_a=-2p_a\{\ell_a[v]Q_a+r_a q_a[v]\}+b_{X,a}.
\]

For an independent check of the cancellation, differentiate the raw velocity `-2p_a r_a D_a Q_a`. The extra own-gate term is `-2p_a r_a phi''(w_a)D_a xi_a Q_a`. Differentiating `delta w_a=D_a xi_a` in physical time produces the same term on the left. They cancel identically. Thus a term multiplying an arbitrary row tangent by the unbounded active `Q_a` is not missing from P10; the clock change removes it exactly.

Forward variation gives `h_a[v]=D_a² xi_a`, `z_a[v]=BH_a^1+A h_a[v]`; the upper backward variation is

\[
 d_a[v]=d\phi'(Z_a^2)+c\phi''(Z_a^2)z_a[v],\qquad
 q_a[v]=B^*\delta_a+A^*d_a[v].
\]

The middle rank derivative is `d_a[v] tensor H_a^1 + delta_a tensor h_a[v]`; the readout feature derivative is `phi'(Z_a²)z_a[v]`. Differentiating residuals gives the separate finite-rank `-2SE` part. These are exactly P8–P11. Both the forward action and the actual adjoint occur; replacing the transpose by an independent matrix would change these identities.

For a general law, dividing the raw row field by `D_a` gives the off-support ratio `u_a phi'(w.u)/phi'(w_a)`. The direct signed-law derivative is therefore exactly T5, with residual `f-y`, factor `-2`, and `sigma=nu-nu_*`. This does not require differentiating that ratio with respect to a changed population state.

At each finite width, the integrated loss is smooth in its finitely many parameters: on a compact parameter ball all integrand derivatives are uniformly bounded over the compact data space. Finite energy bounds the paths for every contamination in `[0,1]`. The mean-value formula for the difference of the two integral equations, followed by the finite-dimensional integral inequality, proves the uniform-time right derivative. Its constants may depend on width; no width passage uses them. The coordinate change is smooth at every finite coordinate, so the actual finite derivative is the unique finite linear solution just derived. Identical initialization makes its initial value zero, including the derivative of the actual nonzero random readout.

## 5. Component A: topology, strong evolution, and singular endpoint

### Boundedness and strong continuity

The state types in P8 are correct for every vector of the full tangent Hilbert space. Gates are bounded. `B` and `B*` are bounded by `||B||HS`. The only upper backward product with a possibly unbounded tangent is multiplied by the fixed reference `c`, whose supremum is at most ten. Thus `c phi''(Z2) z_a[v]` is an L2 field. No product of two arbitrary L2 tangents is required.

For a unit tangent, the bounds are respectively `1`, `1+M`, `a1`, and `a_q` for `h,z,d_a,q_a`, using `M=2+sqrt(10)` and `C=sqrt(10)`. The two rank pieces give `a1+C`, so the square-sum constant `a_*` bounds each `T_a`. The absolute residual average is `e(t)`, yielding `||C(t)|| <= 2 a_* e(t)`. Synthesis columns have squared norm at most `M²C²+C²+1`; weighted column Cauchy–Schwarz gives `||S||<=L0`. Evaluation has the same bound, also from `E=S*D` and `||D||<=1`. The numerical inequality `L0<17` follows directly from its displayed expression.

Strong continuity is sufficient and is actually proved. If `b(z_j)` is a uniformly bounded multiplier converging in probability, then its product with a fixed L2 vector converges in L2: cut the fixed vector at `M`, use bounded convergence in probability on the bounded part, and let its squared tail vanish. Apply this to each gate and a fixed tested vector in P8. Reference actions vary in HS/operator norm and rank factors vary in L2; `c` is Lipschitz in supremum norm on the bounded feature interval. These facts pass through every sum, action and rank in the generator. They do not imply operator-norm continuity for all multiplication operators, and the proof does not need that assertion.

The generated spaces are separable, as follows from III.F.7's countable generated sigma-fields and the explicit cylinder-function density argument. Their HS space and the finite direct sum are separable as well. Thus the norm-measurability argument for the bounded perturbation is valid. The vector-valued Picard series has the majorant `L^j(t-s)^j/j!`; its uniform convergence gives a bounded evolution on each compact interval, strong differentiability on fixed vectors, uniqueness and the evolution identity. The corresponding construction for L1 forcing gives strong absolute continuity and the equation almost everywhere; continuous data forcing gives a strongly C1 solution.

### Kernel compatibility and projection

Adjunction with the precise HS metric gives

\[
 E=S^*D,\quad D=R^*R,\quad\Gamma=S^*DS=(RS)^*(RS).
\]

Since `w_a` is finite almost surely, `D_a>0` almost surely. Hence `R` and `D` are injective even though they have no uniform coercivity constant. For `alpha` in the two-dimensional training space,

\[
 \alpha^T\Gamma\alpha=\|RS\alpha\|^2=0
 \quad\Longleftrightarrow\quad S\alpha=0.
\]

Also `E*=DS`, so `ker E*=ker S=ker Gamma`. Taking orthogonal complements in the finite-dimensional training space gives `ran E=ran Gamma` (the candidate only needs inclusion). This is the required compatibility at a singular endpoint. A positive semidefinite `ES` by itself is insufficient for a general rectangular pair; the independent nilpotent check below explicitly attacks that alternative.

For `j>=1`, `(SE)^j=S Gamma^(j-1) E`. On `ran Gamma`, its pseudoinverse cancels positive powers. Since `E` has range there, summing the absolutely convergent exponential series yields P16, including zero time and the zero-Gram case. Nonnegative eigenvalues give

\[
 \sup_{\tau\ge0}\|e^{-2\tau S_\infty E_\infty}\|
 \le 1+\|S_\infty\|\|\Gamma_\infty^+\|\|E_\infty\|=B_\infty.
\]

For this one fixed finite-dimensional reference Gram the pseudoinverse norm is finite even if its rank is less than two. The theorem needs no full-rank assumption and takes no limit of pseudoinverses along the path. It gives no useful numerical conditioning bound and makes no such claim.

The limiting frozen projection satisfies `P²=P`, `EP=0`, `PS=0`. If `Pv=0`, then `v=S Gamma+ Ev` is in `ran S`; if `Ev=0`, then `Pv=v`. Hence its range and kernel are exactly the stated ones. With `G=R S`,

\[
 RP=(I-G\Gamma^+G^*)R.
\]

Here `G Gamma+ G*` is the raw orthogonal projection onto `ran G`, verified by self-adjointness, the pseudoinverse square identity and its action on that finite-dimensional range. This does not say that `P` is orthogonal in the stronger clock norm, or that every raw direction has a bounded inverse clock image. Nor does it identify a limit of the nonautonomous evolution with `P`.

### Integrability of the actual coefficient perturbation

The supplied feature reference has `b_s=||theta_s||raw² >= m >= 1/10`, the first `b=1` at `s_dagger<=10`, and physical clock `s_t=2(1-b)`. Therefore `e_t=-2b_s e`, `e<=exp(-t/5)`. Cauchy–Schwarz along the feature path gives `d_ref <= sqrt(10)e`. Its endpoint is on the same common action spaces; it is not an abstract limiting law without a carrier.

Factor subtraction bounds the active `H1,Z2,delta,Q` differences by `d_ref`, `(1+M)d_ref`, `a1 d_ref`, `a_q d_ref`. The same rank inequalities give `||S-Sinf||<=a_* d_ref`. The additional evaluation row factor is `D_a² Q_a`. Split at the fixed endpoint Q and use

\[
 |D_a^2-D_{a,\infty}^2|\le\min(1,4|w_a-w_{a,\infty}|),\qquad
 \|Q_{a,\infty}\|_4\le M_4.
\]

Hölder gives the extra bound `2M4 ||w_a-w_a,inf||2^(1/2)`. The dependency C.4.5.2 supplies this endpoint L4 moment by the actual active source representation, with no independence assumption between its Gaussian and bounded remainder. Thus P20 is a legitimate operator-norm bound for the finite-rank evaluation map, not an invalid operator-norm assertion about a general multiplier.

Expanding `-2SE+2Sinf Einf` now gives exactly

\[
 \|\mathcal B(t)\|
 \le(4L_0a_*\sqrt{10}+2a_*)e(t)
       +4L_0M_4\,10^{1/4}e(t)^{1/2}.
\]

The integrals of the two scalar majorants are respectively at most 5 and 10. This reproduces every factor in the displayed `J0`. Variation of constants around the bounded frozen semigroup and the ordered-integral exponential bound yield

\[
 \|U(t,s)\|\le B_\infty\exp\left(B_\infty\int_s^t\|\mathcal B(q)\|dq\right)
 \le B_\infty e^{B_\infty J_0}.
\]

This controls the actual trained homogeneous coefficients, including residual curvature. An endpoint-only stability argument would not suffice; the integrable perturbation estimate provides the missing bridge explicitly.

## 6. Component B: actual weighted sources and admissible measures

### Finite cavity estimate

On `E_n`, initialization gives `R(0)<=4`; energy gives raw displacement at most `2sqrt(T)`, and the readout equation gives `||c||inf<=1+4T`. Consequently S12–S16 follow with deterministic constants independent of width. In particular passive delta is Lipschitz in normalized L2 over time and the circle; the input estimate uses only the full first-row RMS. These estimates remain true for the column-deleted flow.

Delete one initial column `a_i`, retaining all subsequent learned ranks, residual feedback and both orientations. The cavity is measurable with respect to the remaining initialized variables. Its good event `E_n^i` is measurable there, while `E_n` is a subset of it. The conditional Gaussian step must be performed on `E_n^i`; doing it on `E_n` would bias the column. The candidate uses the correct event.

The forward discrepancy `a_i tilde H_i` has RMS at most `||a_i||/sqrt(n)`. The reverse discrepancy is the scalar cavity probe `Z_i=a_i^T tilde delta`, placed at coordinate i and divided by `sqrt(n)` in RMS. The identities S21 use the full action on the remaining reverse error; therefore they introduce no uncontrolled column-dependent term. Substituting the four S22 estimates into the three velocity differences gives the claimed S24 with slack in `100D0^4`. In particular changed residuals are included; the cavity is not driven by the full trajectory's prescribed residuals.

Integrating gives `sqrt(n) sup d <= J_T (m_i+2Z_i#)`. The actual learned transpose contribution has the coordinate bound `4TC²`, because its integrand contains a bounded first hidden coordinate and a normalized pairing of two bounded-RMS backward fields. Combining it with the cavity delta difference and `m_i<=10` gives exactly `N_i<=A_T Z_i#+B_T`. This is the causal sensitivity estimate that makes the adapted query controllable.

Conditional on the remaining variables, `Z_i` is a centered continuous Gaussian process with covariance equal to the normalized cavity delta pairing. The deterministic L2 time/input modulus bounds its canonical increment metric on a two-dimensional compact parameter set. The supplied scalar Gaussian tail, union bound on each dyadic grid, and summable parent increments imply `||Z_i#||p <= C_Z sqrt(p)`. No independence between grid points is needed. Averaging the conditional bound using `E_n subset E_n^i` gives S34 for every coordinate and width. Thus the proof establishes actual finite empirical higher moments; it does not infer them from RMS convergence or exchangeability.

### Weights, canonical passage, and continuity

The exact identity

\[
 \partial_X\cosh^2 j(X,g)=2\tanh j(X,g)
\]

bounds the clock weight by `cosh²g+2|X|` for either sign of X. Integrating the active clock velocity bounds `|X_a|<=3T N_i` and `|w_i|<=|g_i|+6T N_i`. Gaussian linear-exponential moments and Hölder combine with S34 to give every fixed moment of the source envelopes. No independence of roots and evolved queries is used. For `p>2`, the empirical square tails are bounded in expectation by `C R^{-(p-2)}`; Markov and `P(E_n^c)->0` yield the ordered uniform-integrability assertion S39.

Uniform population time control uses a separate, legitimate auxiliary feature flow on `[0,10]`. Its finite initialization has zero readout solely to match this auxiliary equation. Deterministic feature bounds provide the same cavity argument, with fixed controls and no residual-difference terms. It gives S42 on the entire bounded feature segment. It does not assert finite-GF symmetry or finite-GF fitting.

At every separately fixed transformed Euler program, A.1, the actual action/adjoint construction and HS rank contractions identify the canonical law. Same-root Lipschitz estimates remove the mesh with width-independent errors. For each finite rational parameter list, bounded tests `min(R,max |Q_j|^p)` transfer the finite envelope bound to the canonical law. Boundedness permits expectation passage; the good-event complement disappears. Monotone convergence in R and the finite lists gives `N#` on a countable dense set. L2 continuity extends the domination to every deterministic parameter in its equivalence class. Integrating the active clocks by Fubini then yields

\[
 |\cosh^2 w_a(s)Q(s,u)|
 \le (\cosh^2 g_a+10N^\#)N^\#.
\]

This is sufficient for every deterministic observation measure and for Bochner integration. It does not silently assume jointly continuous population sample paths of all queries. The Gaussian moment calculation and `||N#||4<=2C_*` reproduce S45. The physical path lies in this bounded feature segment, so the estimate is uniform over all physical times.

For source continuity, interpolate the L2 query difference against its uniformly bounded L8 norm to obtain its L4 difference with exponent `1/3`. Pair it with an L4 clock weight. Weight differences are controlled by `2|X-X'|`, and gate/input differences by products of `N#`, the Gaussian root and the query envelope. Those products are in L2 by the proved moments. Changing one factor at a time proves joint V-valued continuity on the compact feature/data parameter set. Its image is compact and separable, hence integrable against every finite signed Borel measure. The norm of that integral is bounded by total variation mass times the uniform integrand norm.

For the sharper T9 constant, the row block has squared norm at most

\[
 \sum_a u_a^2\|\cosh^2w_a Q(u)\|_2^2\le M_w^2.
\]

The middle and readout squared norms are at most ten and one. Also `|r|<=sqrt(10)+Y`. This proves the displayed `2(sqrt(10)+Y)sqrt(M_w²+11)` exactly. Total variation is its mass convention, so a contamination direction has norm at most two. The extension to all signed mass-zero measures is a bounded linear forcing construction; it does not claim a two-sided probability neighborhood.

For arbitrary Borel laws, moving cell masses in a finite small-diameter partition controls the clipped source by its data Lipschitz constant, or the uncut source by the proved random Hölder modulus. These are norm estimates on integrated forcing, not false TV approximations to a nonatomic law. No Gram inverse, smallest atom mass or support-count factor appears. Fixed-law convergence is what is stated.

## 7. Component C: actual finite-tangent capture and observations

### Fixed programs do not substitute for mesh removal

The uncut source is first approximated uniformly in time/input by fixed source clips, using actual finite higher moments. A fixed finite quadrature then approximates the forcing, with exact cell masses. At fixed source clip, quadrature and mesh, every tangent/reference rank update expands into finitely many typed node operations, initial action calls in their actual orientations, and same-layer scalar contractions.

Every coordinate map used in that fixed program has at most linear growth: the tangent enters multiplied by bounded gates, and the reference readout is clipped outside its proved range. Scalar feedback is handled causally by convergent pairings, rather than being an unbounded extra coordinate product. A.1 applies to such value instructions; the ordinary fixed-program result is applied only before refining the mesh. The initial readout discrepancy from the zero limiting root has both vanishing RMS and vanishing supremum. Fixed-program comparison propagates it without replacing the actual finite GF or derivative initialization.

The central consistency device F16 is valid: on a common finite carrier, split the tested fixed-program vector at M. The bounded part costs `Lip(b) M` times the RMS coefficient error; the complement costs its L2 tail times `2||b||inf`. Continuous positive-part cutoffs transfer those tails through W2 convergence. A compact population L2 family has uniformly vanishing square tails by a finite L2 net. This is stronger than bounded norms alone and is what the proof uses.

Reference transformed Euler has a deterministic `C_T h` same-root clock/HS/L2 error on `E_n`; replacing operator distance by HS distance is valid because all learned velocities are ranks and the rank difference inequality is identical in HS. The readout supremum is controlled separately. Strong multiplier continuity then makes the population generator approximation uniformly consistent on each fixed tested vector, and the common operator bound extends this to compact vector sets. The continuous population solution has compact image. Integrating its consistency defect and iterating the Euler recurrence gives F19.

For the separate actual finite comparison, the defect F20 contains: the generator coefficient error tested on a mesh tangent; the mesh tangent increment; and the forcing error. The last two have integrated `O(h)` bounds. The first includes all gate differences on `xi`, `d` and `dot z2`, all action/HS differences, the reverse `B*` and `A*` differences, the rank products, and the evaluation field `D_a² Q_a`. In particular the changed readout multiplier is treated in L2 by a bounded clip and F16; no unjustified supremum convergence of readout differences is used.

The population tested node family along a countable refining mesh sequence is relatively compact because of F19, reference convergence and strong multiplier continuity. Its tails therefore vanish uniformly. At each fixed mesh, finite empirical cutoff moments converge. Summing the mesh-node tail errors with cell lengths gives an upper bound by T times that common population tail. Take width to infinity at fixed mesh, then h to zero at fixed M, then M to infinity. This proves the integrated finite defect convergence F21. The actual finite generator bound propagates that defect with `exp(C_T T)`. Removing source clipping and quadrature using the same linear estimate yields F22.

This establishes actual finite tangents, not merely differentiated population programs. It also explains the topology: finite paths are compared only to proxies on their own arrays; canonical proxies converge in V; fixed proxy same-layer laws and second moments converge; ranks identify HS norms, pairings and both tangent action orientations. The proof does not compare matrices and operators on different carriers in operator norm. Products with hidden tangents pass by compact-family L2 tails, without assuming new tangent higher moments or hidden tangent path laws in coordinate supremum norm.

### Uniform physical time and the entire circle

The passive evaluation field F23 is the correct Riesz vector for the clock metric. Its norm is bounded by `L0` in the population and by a compact-time deterministic bound on `E_n` at finite width. Pairings with the captured tangent and rank contractions give pointwise prediction-derivative convergence.

For time uniformity, reference `Q(t,u)` is L2 Lipschitz uniformly in passive u, from the direct differentiated forward/backward products and the compact-time bounds. Gate changes multiplied by Q are handled with the actual reference envelopes. Thus the evaluation field has a tight uniform time modulus. The tangent equation and bounded forcing make its norm and time-Lipschitz constant tight. Their pairing has the required time equicontinuity.

For circle uniformity, `u(alpha)` is a smooth unit-circle parametrization. Strong curve differentiation gives F24. The sole extra unbounded row product in the derivative of the Riesz field is the product `|w|Q`, whose uniform empirical moments were proved by the cavity estimate, not assumed. Population Q is strongly C1 in alpha; Fubini yields absolutely continuous coordinate representatives. Its agreement with the countable dense-parameter representatives permits the same envelope to control the gate difference quotient. Dominated convergence gives the stated strong derivative and the H1 estimate. The other terms require only bounded actions, readout supremum and rank product rules.

The fundamental theorem for Hilbert-valued absolutely continuous curves and Cauchy–Schwarz give the `|alpha-beta|^(1/2)` modulus from the H1 bound. Pairing with the tight tangent norm yields a uniform-time scalar circle modulus. A finite product of time and circle nets, a finite union bound, and refinement after the width limit prove T10 on the whole compact cylinder. No uniform input-derivative Gaussian tail, law-uniform failure probability, or growing-time width theorem is needed.

## 8. Dependency audit and reference constants

The Gaussian foundation is supplied rather than borrowed from a named external result. Sequential conditioning on an adaptive transcript leaves independent residual matrix factors with the imposed linear constraints. The minimum-Frobenius-norm conditional mean satisfies both orientations, and its residual is projected on both unexplored complements. The removed fresh-noise projection has expected normalized squared norm equal to rank divided by width. Conditional variance then proves the finite-program weak/second-moment limit.

The named-source response follows by Gaussian integration by parts after projecting the new input off previous forward inputs. The response term cancels the old regression coefficients and retains the Gram source covariance. Singular Grams are handled by fresh input noise at each fixed call, width first, followed by zero-noise passage via covariance square roots and controlled derivatives. The proof does not assert inverse continuity at rank loss. The common generated action is well-defined by finite linear identities, the initialized norm bound and density; the reverse is the genuine Hilbert adjoint by limiting normalized finite adjunction.

A.1 extends value instructions by continuous linear-growth approximation and W2 tails, selecting the smooth approximation before the earlier-prefix tolerance. A.2's unbounded product source derivatives have finite polynomial envelopes at each fixed graph. For the tanh clock, C.4.5.2 additionally clips roots before applying bounded-derivative programs and removes that clip using the uniformly bounded X derivative. Root derivatives are not used. These are adequate hypotheses for their applications here.

B.1's same-root clock stability supplies global reference GF construction and width identification, with bounded readout supremum. Orthogonality is used in the reference's two clock equations and has not been silently extended to arbitrary training laws. The new result linearizes at that orthogonal reference and treats other inputs in the source, which is a different claim.

For the fitted reference, the symmetry is a generated population-law symmetry, not a pointwise finite-network symmetry. With zero limiting readout it implies opposite predictions. The `g=||c||` convexity argument gives `b_s>=m`; the strong energy identity gives both a finite feature endpoint and exponential physical residual decay. The passive gradient bound retains the full first row. The supplied initial Gaussian reuse calculation in the hidden-activity subsection retains both response terms and its conditional variances; its rational certificate and strict margins are consistent. Those activity/risk facts are not used as substitutes for a response proof.

The targeted source-response dependency estimates fresh-root pulse effects on fixed meshes, takes the width limit at a fixed nonzero pulse, uses Gaussian integration by parts, and only then removes the pulse using the supplied source-derivative continuity. This is enough to extract the named response coefficients even at singular support. Summing those coefficients and learned-rank contributions gives the stated bounded active-query remainder. Cross-program source covariance isometries and common-space mesh convergence pass that decomposition to the actual reference endpoint, providing the L4 moment used in component A. The new cavity argument independently supplies the more demanding actual finite and passive weighted bounds in component B.

## 9. Hostile checks and why they do not defeat the statement

| Attack | Decisive mathematical check | Consequence for the candidate |
|---|---|---|
| Raw L2 and clock L2 might be silently interchangeable | For Gaussian g, normalized indicators of `{g>k}` have unit raw norm but inverse multiplication by `sech²g` has norm at least `cosh²k` | Such a bounded inverse is false; the candidate explicitly uses only its bounded forward conversion |
| An unweighted L2 query might suffice for the clock source | `Q(g)=1_{g>1} exp(g²/4-g)` is in Gaussian L2, but `cosh²g Q(g)` is not: the latter square integrand grows at least as `exp(2g)/16` | This defeats an RMS-only proof, which the candidate does not use; actual finite higher moments and the Gaussian-root envelope supply the needed bridge |
| Nonlinear product estimates might be hidden in the linear generator | On `(0,1)`, `x^(-1/3)` is L2 but its square is not L2 | General products of two L2 directions are unavailable; every homogeneous tangent product here has a bounded reference factor |
| Strong multiplier continuity might be mistaken for norm continuity | `z_m=1_(0,1/m)` tends to zero in L2, but multiplication by `tanh(z_m)` has norm `tanh(1)` | The generator proof and finite consistency use strong convergence on fixed/compact tested vectors, which survives this example |
| Ambient activation differentiability might be assumed | For the same shrinking indicators, the ratio `||tanh(z_m)-z_m||2/||z_m||2` is the nonzero constant `1-tanh(1)` | The L2-valued Frechet assertion would fail; the candidate relies on curve derivatives and finite derivatives instead |
| PSD `ES` might hide nilpotent growth of `SE` | `S=(1,0)^T`, `E=(0,1)` gives `ES=0`, `(SE)^2=0`, and `exp(-2tSE)=I-2tSE` | The explicit metric/kernel compatibility excludes this pair; the independent code confirms the growth |
| A singular endpoint might be treated as if uniformly coercive | Exact rational rank-one Gram and nonconstant positive D satisfy all projection identities; the clock projection norm exceeds one | The pseudoinverse formula handles singularity and nonnormality; it makes no contraction claim in clock norm |
| Conditioning on a column-dependent good event | `E_n` involves the deleted column, whereas `E_n^i` does not | The Gaussian estimate is correctly conditioned on the latter and restricted to the former only afterward |
| Bounded operator norm might be substituted for coordinate moments | Column deletion has order-one operator change, but its bounded-query forward error is `O(n^(-1/2))` and its reverse defect is one Gaussian probe | The source proof uses the latter mechanism and retains the full learned cavity |
| Finite-program convergence might be applied to a growing transcript | F19 establishes population compactness independently; F16 plus integrated defects then compare actual finite tangents to each fixed proxy | The nested width/mesh/cutoff order is explicit and noncircular |
| A finite input list might masquerade as whole-circle capture | The extra `|w|Q` estimate controls the H1 norm of the Riesz field | The circle modulus and net refinement prove the stated observation topology |
| Homogeneous boundedness might imply bounded forced response | A constant forcing in a neutral subspace of a zero generator produces `v(t)=tF` | The candidate states linear-in-T forcing control and does not assert convergence or boundedness of the forced endpoint response |

These attacks target the actual needed bridges. Their counterexamples demonstrate why the stronger excluded statements would need new hypotheses; they are not counterexamples to the exact theorem.

## 10. Commands, actual outputs, and numerical limits

Working directory for all commands was `/home/amir/Codes/PDE`. Both supplied programs were read before execution. Commands and result summaries are retained in scratch `commands_and_results.json`; the rational certificate stdout is retained as `reference_certificate_stdout.txt`.

Required algebra command:

```text
python studies/trained_data_response/P1_CHECK_IDENTITIES.py --output data/generated/trained_data_response/assessment_fresh_b_20260911/identities
```

Exit status 0, Python 3.10.12, NumPy 1.26.4, SciPy 1.13.0. It reported PASS. Central derivative errors were

```text
[1.0292819331017236e-06, 2.573205074969171e-07,
 6.433012185025226e-08, 1.608263607613758e-08]
```

The successive ratios are approximately four, as expected for the central difference. The loss/metric discrepancy was `7.900680110140001e-12`. The largest singular-semigroup identity discrepancy was `1.5265444420160054e-14`. Its output includes the correct script hash.

Required reference certificate command:

```text
python studies/trained_data_response/P1_REFERENCE_CERTIFICATE.py
```

Exit status 0. Exact rational assertions all passed. Printed output:

```text
[0.392108947877, 0.396376711612, 0.233120735618,
 0.339792209687, 0.631761866359]
```

The proof correctly bounds the exponential remainder by a geometric majorant, derives rational density bounds from the alternating arctangent formula, uses monotone endpoint quadrature and outward rounding, and adds a certified two-sided tail for the upper q bound. The assertions certify the weaker constants actually consumed in the reference proof; decimal output is not their justification.

Independent check command:

```text
python data/generated/trained_data_response/assessment_fresh_b_20260911/independent_checks.py
```

The script was created only in assigned scratch. Before running it, its fixed gates were set to exact rational projection identities, passive directional discrepancies below `1e-8`, and clock-envelope slack at least `-1e-8`. It checks one exact singular rational factorization, one nonsymmetric two-neuron passive query with a nonzero readout, five fixed scalar clock boundary cases including zero and both signs of shift, and the incompatible nilpotent factorization. These are finite algebra checks, not a training experiment or a search over configurations.

Exit status 0. Results:

- Exact rational singular projection, kernel and D-symmetry identities: PASS.
- Clock projection operator norm: `1.1043133990719032`, explicitly greater than one.
- Passive reverse directional error with actual transpose: `1.6606894252005403e-11`.
- Passive prediction directional error: `3.6212699505711043e-12`.
- Passive Riesz pairing discrepancy: exactly `0.0` in the calculation.
- Clock-envelope slacks: `[399.99753627023165, 0.726582186274844, 0.0, 32.59095384322592, 399.99753627023165]`.
- Incompatible nilpotent semigroup norm at time 10: `20.04987562112089`.
- Independent script SHA-256: `9b7570a632fd416d04fdc0913076962e3b5a730272db9b7db0797d96b335ccd3`.

The exact rational test supports algebraic identities; the floating-point tests detect sign, orientation, normalization and boundary mistakes on their fixed inputs. They do not establish the population limit, uniformity in time, or infinite-dimensional well-posedness. Those conclusions are supported by the derivations above.

## 11. Surviving gaps, required corrections, and optional suggestions

**Required corrections for the exact submitted scope: none.** No required mathematical input was missing. All invoked substantive probability/operator facts have a supplied proof or were reconstructed with their hypotheses above. No numerical conditioning certificate is supplied for `Gamma_infty+`; this is an explicitly excluded quantitative improvement, not a missing assumption for its finiteness.

Optional presentation improvement: the endpoint projection discussion could add the phrase “orthogonal after the raw conversion, on the image of that conversion.” Equations P30–P31 and the existing prose already make this precise, so this is not a condition of acceptance. A short A/B/C dependency diagram could also make the independent roles of homogeneous propagation, weighted source control and finite capture easier to navigate.

## 12. Exact response scope and nonlinear continuation boundary

The theorem proves a linear forced response at one trained reference, identifies it from the actual finite right derivatives on every fixed horizon, and observes it on the full circle. It supplies a complete Hilbert state for that linear equation, an autonomous reference from which coefficients are computed, a continuous measure-to-forcing map in TV norm, and an all-time bound on the homogeneous population propagator. The endpoint kernel has an exact raw-metric interpretation. It supplies no sign, size, or risk benefit for an unseen prediction change.

To obtain nonlinear changed-law continuation one would additionally need a specified admissible neighborhood of population states, existence and uniqueness there, and estimates controlling changes of inverse first gates times off-support reverse queries. Products of readout increments with hidden increments and activation Taylor remainders require stronger integrability or a suitable admissible state topology; they are not bounded bilinear operations on arbitrary L2 directions. One must then prove that the nonlinear forcing/remainder is small in the L1-in-time norm transported by the present propagator and justify its identification from finite changed-law flows. A finite-contamination approximation needs a uniform differentiability/remainder bridge beyond the first derivative at zero. Endpoint continuity needs separate control of neutral modes and long-time forcing. A GD derivative theorem needs its own discrete derivative consistency estimate.

Empirical laws of a nonatomic distribution do not converge in total variation to that distribution, so this particular TV response bound alone is not a sampling-limit theorem. Nor does fixed-T convergence permit taking T to infinity together with width. The candidate and its ancillary changes preserve all of these limits of scope.

**Final scientific decision: ACCEPT.**
