# Broader and quantitatively useful two-input activations

Date: 2026-09-08. Authorized scope: theoretical investigation, proof
extension and independent adversarial checking. No experiments are used.

The target is the exact two-input three-hidden-layer Gaussian raw-training
model of ../two_sample_separated_angle_theorem/PROOF.md: normalized fixed
inputs, binary labels, original raw metric and initialization, all four
blocks trained, actual finite GF and simultaneous raw GD with step n^-2.
The user permits the stronger geometry |rho| <= 1-delta, 0<delta<=1.

The essential conclusion is a single autonomous global canonical strong
population flow, uniqueness and reached-state continuation, identified
jointly with the actual finite-width algorithms on each fixed finite
physical interval. Preserve the earlier kernel, path and velocity
observables wherever their bridges apply. State nonaffinity and initial
feature motion separately; they cannot replace the population limit.

The activation is selected before the actual dataset, dimension, width,
and observation horizon. It may depend on delta and declared shape-class
bounds. No supremum over datasets is inserted into a probability limit.
No growing-depth or infinite-horizon uniform width limit is requested.

Two improvement axes are investigated separately:

1. Replace arctangent by explicit analytic obligations on a shape class.
2. Avoid severe dependence on delta AND a microscopic numerical
   prefactor. Relative nonlinearity must be examined: multiplying the
   affine gain does not by itself establish practical nonlinearity.

Terminal outcomes: a completed proof surviving fresh checking, or exact
new lemmas with the necessary remaining implication explicitly open.
Finite-dimensional loss dissipation alone is not a population theorem.
No new architecture, changed initialization/metric, frozen layer, or
trajectory-dependent coefficient is permitted.

Existing mathematical sources are read-only. Later workspace results
must be reconciled with the earlier conversation; their historical review
verdicts are not premises. New claims and artifacts stay in this folder.
