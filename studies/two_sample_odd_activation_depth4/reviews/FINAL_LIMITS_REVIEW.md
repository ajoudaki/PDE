# Fresh complete-proof review: PASS

2026-09-07. Independent reviewer: `depth4_final_limits`. The rigorous-math
skill was read and applied. All five new mathematical files and CONTRACT.md
were read; the actual dependency passages listed below were checked. Earlier
review outcomes were neither read nor used as evidence. No experiments,
mathematical-file changes, old-file changes, or commits were made.

**Verdict: PASS for the complete stated L4 theorem with the identical numerical
pair `(c_poly,10)`.** No blocking mathematical gap was found. This verdict
does not assert a common admissible positive coefficient at every finite
depth; that question remains open in this candidate.

## Critical checks

- **Model and quantifiers.** Direct differentiation of the specified raw metric
  gives exactly the first-layer factor `1/d`, each of the three square-matrix
  factors `1/n`, the readout update, and the five displayed kernels. The finite
  readout remains `N(0,n^-2)`. The theorem fixes the dataset, depth, and physical
  horizon before the full-width-sequence probability limit. Its conclusions
  do not require a cross-layer neuron pairing or operator-norm convergence
  between unrelated widths.
- **Affine certificate and the numerical coefficient.** Differentiating the
  adjacent balances verifies the bounds `16+c^2,32+c^2,48+c^2`. The middle
  gradient gives `F'>=c^6(1+c^2)`; the five-gradient large-radius argument gives
  `F'>=5(c^2-32)_+^4`. These justify both radial inequalities, strong
  continuation to `g=3/2`, and `S< M^5`. The radius-one Hessian integral is
  bounded by `4000+4 log M`, including the region below the large-radius
  threshold. The explicit forcing integration gives raw error `H e M^10`;
  forward and endpoint errors have the stated powers 13 and 9. The enlarged
  beta paths fit inside the proved extension interval. The independent-answer
  probes include current identities, the first-preactivation observation,
  and the top-readout injection. Their field and time transformations give
  the claimed original-time densities and rows. The conservative source
  interface and its positive lower density therefore follow from the new
  affine calculation. The constants `C_0,C_z,C_g,eta_*,c_*,H,c_poly` agree
  exactly with the inspected L3 sources; `M^100<=76^20 delta^-10` provides
  the asserted unchanged amplitude bound.
- **Actual nonlinear source closure.** All six coefficient families use full
  input second moments and actual learned moments. The three backward current
  returns retain the next layer's current response. Named sources remain
  distinct at singular covariance. The same-array derivative subtraction
  retains `N J` and both noncommuting gate terms. Strict integration controls
  the derivative envelope using deterministic weighted marginal tails, with
  no random supremum over source times. Substitution of the local powers
  gives forward defects `(35,35,37)` and backward defects `(52,50,41)`.
  The two additional resolvent pairs give backward excess power 89; the
  complete-row radius adds 9, for power 98. The strict-factor sandwiches
  retain the actual source step and give the forward restriction 86.
  The inactive sector has its separate zero-affine-backward comparison.
  Thus the chronological homotopy closes without assigning a density to
  an arbitrary backward-row error or using an unproved coupled inverse.
- **Strong population construction and uniqueness.** The actual conditioning
  dependency states its theorem for finitely many independent Gaussian
  matrices, despite its section title. Its conditional projection argument,
  independent-query regularization, and common generated-space construction
  apply to all three matrices in both orientations. Finite transpose
  identities give genuine adjoints. At fixed cap the raw field is locally
  Lipschitz on the stopped primal ball, and the Euler bounds and strict
  affine margin continue its strong solution. In the asymmetric comparison,
  the cap factor multiplies only forward state discrepancies; reverse
  substitution therefore gives `K(1+eR)`, not a product of cap factors.
  Reference subGaussian tails defeat that factor after Gronwall, for both
  states and directions. This gives a strong C1 uncut solution. Comparing
  a general competitor to the constructed capped reference requires no
  competitor tail hypothesis. Retaining its two residuals proves physical
  uniqueness against nonsymmetric bounded-primal competitors; the same
  estimate, including the convergent capped initial discrepancy, proves
  restart from a reached state.
- **Global time and all-time nonaffinity.** Odd label folding is an exact raw
  loss identity. Deterministic limiting contractions and exchange symmetry
  establish the scalar population path before uncut uniqueness is invoked.
  The endpoint above one and bounded `g'` make the physical-time integral
  diverge at its first hit of one. The trajectory chain rule only needs
  bounded continuous activation derivatives and truncation of fixed L2
  factors; no unsupported Frechet smoothness on the entire L2 space is
  required. Convexity of the readout norm gives `kappa>=kappa(0)` and hence
  the loss rate `2a^8 delta`. The rectangular-gain rate `delta/128` and
  convex-mixture rate `delta/32` are correctly distinguished. Positive Wick
  expansion, fixed-program uniform integrability, and inactive freezing give
  affine variance at least `1/64`; the third-Hermite calculation therefore
  retains the old `eta_*`. The 1-Lipschitz square-root regression residual
  transfers this margin through the cap limit at every finite physical time.
- **GF/GD, velocities, kernels, and path laws.** Width is first taken at a
  fixed cap and finite auxiliary transcript. Rank-one unrolling and stopped
  Euler comparison remove the mesh. For uncut GD, its direction at the
  preceding node can be compared asymmetrically to the capped reference;
  bounded raw node displacement supplies the additional `K_{R,T} n^-2`
  term. No conditioning theorem is applied to that growing GD transcript.
  The three appended velocity queries have full forward cross moments and
  the actual reverse-input responses. Their product instructions are first
  clipped, then unclipped successively with earlier clips removed at fixed
  outer clip. The derivative-row induction extends through the third query
  using only initialized L2 action bounds. The final deterministic velocity
  comparison has a single reference truncation factor. The compact L2 time
  image of the uncut reference supplies uniformly vanishing tails in the
  stated order of limits. L2 products give all five kernels, second moments,
  and integrated speeds. Fixed-grid joint W2 convergence and the stated
  interpolation inequality give the same-layer uniform-norm path laws.
- **Initial motion and depth claims.** Nonconstant positive `phi'` makes the
  top beta second-moment matrix positive definite. Each of the three
  downward reused transposes has Gaussian covariance equal to the *full*
  preceding beta second moment and retains its response derivative.
  Conditional covariance thus proves positivity through both middle
  populations. Positive trace contractions prove every hidden block moves;
  adjunction and exchange prove both sample accelerations at every layer.
  The initial kernel coefficients 2 in feature time and 8 in physical time
  follow with `s'(0)=2`. Independently, the convex initialization recursion
  gives `q_L~1/(2 theta L)`, preserves `|c_L/q_L|<=|rho|`, and gives regression
  residual asymptotic `1/(12 theta L^3)`. These exclude positive depth-uniform
  numerical rates and margins, while leaving the distinct common-activation
  qualitative question unresolved. The energy-normalized family is not
  incorrectly included in the convex contraction obstruction.

## Frozen candidate identities

All five hashes were recomputed and match CANDIDATE_HASHES.json exactly.

| File in the depth4 directory | SHA256 |
|---|---|
| PROOF.md | `f91ce73868922220fd7f6621e08b167472639ebe10f8775d6bb632c10e31c585` |
| AFFINE_CERTIFICATE.md | `541d18c292f63fbd1d408953af9ade9c3911cb623fd68baee3dede4c72d0eb82` |
| SOURCE_RESPONSE.md | `25b0c23ad475f1d0565e15441de01cf9df8336aeff101c1dbf6f83224ced2e79` |
| POPULATION_AND_MOTION.md | `fd1dc71289992880ad4560afa787ddb29bb234f4883addc16cc62e49d519c837` |
| DEPTH_UNIFORMITY.md | `5782203423cba6e5695d7aa36ce1d271f75a2984801b859d426f870d7d10e9fb` |
| CONTRACT.md | `d46ad315beb3f92cd96aa6c608ecc23068106cdc8399e30a9987f4605b909eed` |

## Actual older dependency reads

Paths below are relative to `studies/mean_field_peeling/`. Hashes identify
the entire file; scopes state the passages actually inspected. References
mentioned by these sources were not automatically treated as premises.

| Path | Actual scope | SHA256 |
|---|---|---|
| two_sample_odd_activation_theorem/PROOF.md | Lines 1–245: model, metrics, observable scope, symmetry and affine interfaces | `a4d6ed0ee99a1e111b5eba068f2219cf561a2281b1828b26227aca7a0a54a050` |
| two_sample_odd_activation_theorem/SOURCE_AND_LIMIT_BRIDGE.md | Full file: capped source hypotheses, asymmetric physical comparison and limit ordering | `2a140f2a5a39f911b5c2ca51bc79102d20e9209f1450d38005360c88103f6e77` |
| two_sample_odd_activation_theorem/INITIAL_MOTION_AND_NORMALIZATION.md | Full file | `c96316fdc481ef3f9e6fc402514ca9b45bcff12e199023505fbbb86d4e15bf24` |
| two_sample_odd_activation_theorem/AFFINE_CORE.md | Lines 1–155 and 230–335: folding, exact active metric, inactive freezing and Gaussianity | `634dd3bbc35436e9d1760bee5c11cbf2124b3c42e8920a1bf3a4fcbb15039711` |
| two_sample_odd_activation_theorem/sources/L3_LOCAL_COMPLETE_PROOF.md | Lines 297–405 and 456–549: generic conditioning, full source covariance, singular regularization, common actions and adjunction; heading search for scope | `f97148f84979ab5b8489927d477b6abb3d919e27fd457455f2e3b98416725de4` |
| two_sample_odd_activation_theorem/sources/FIXED_CAP_VELOCITY_BRIDGE.md | Full file: nonlinear probes, derivative rows, product-query justification, raw Euler and velocity topology | `a2322a8dbacf28244b9fef63e757ba3a915628dbdf31020062f8c5772a1c0aa0` |
| two_sample_odd_activation_theorem/sources/TWO_SAMPLE_SOURCE_BASELINE.md | Lines 629–790: finite-difference Gaussian probe identity and actual density/row normalization; source/probe search for scope | `a84187ecd3639d0c4b7b209255056597326c97f11548faaf9b918659ae07477f` |
| two_sample_odd_activation_power10/PROOF.md | Lines 1–120: unchanged coefficient, theorem scope, L3 scale and source interface | `37b9e8a27135b95b8dffe05fe2b734229fadedc98bc1609259e365dbb2aeba37` |
| two_sample_odd_activation_power10/AFFINE_SOURCE_CERTIFICATE.md | Lines 1–480: normalization, inactive sector, probe costs/direct terms, beta positivity | `bc55c0f8e25de1f3a4316fe36e86afa25c9486e014192f2eb818358909b048eb` |
| two_sample_odd_activation_quantitative/PROOF.md | Lines 1–225: original numerical constants, Hermite margin and exact `c_*` | `0a7dbd32cb9cc291e706813b59c0142394e6f9532244e52c21a5ab5d89ed4d02` |
