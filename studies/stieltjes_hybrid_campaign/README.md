# stieltjes hybrid campaign

This is a preserved research study, not an established-library entry. Historical
claims and audit labels retain their original scope; consult the
[reconciled research map](../project_wide_audit_2026_09_08/MASTER_RESEARCH_REPORT.md)
for corrections and the [established reading guide](../../docs/README.md) for
currently maintained self-contained proofs.

## Source documents

- [PROTOCOL.md](PROTOCOL.md)

Generated data were separated during the repository cleanup. Old absolute paths,
commands and frozen hashes may describe the historical layout; the
[migration record](../repository_refactor_2026_09_09/PLAN.md) explains how to locate
preserved files. Do not run an old campaign assuming its former output layout.

Fresh routed products use repository `data/generated/stieltjes_hybrid_campaign/`;
retained arrays use `data/historical/studies/stieltjes_hybrid_campaign/`, with
small provenance manifests remaining in source where explicitly required.
Successive-width analysis accepts `--input-dir`, `--manifest-dir`, and
`--output-dir`; `--historical` selects retained arrays and source manifests but
writes a separate generated `historical_review/`. Outputs must be in the owning
generated tree or external scratch, without existing symlink/hardlink aliases.

The FP64 successor/successive launchers are archive-only and refuse before work.
The n8192 analyzer and width comparison still refuse a changed frozen base
analyzer. Stage V retains its source, unlock, predecessor and one-attempt gates;
the old unlock does not bind the migrated generated root. Its timeout finalizer
can only mark a matching, reserved generated attempt failed, not authorize a
launch or rewrite a historical attempt. Do not refresh hashes or reset budgets
to bypass these restrictions; routing repair is not campaign reauthorization.
