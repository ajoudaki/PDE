# Preserved source snapshots

These are unmodified preservation copies, not newly proved or newly accepted results. In particular, a proof filename or inclusion here does not confer mathematical acceptance. Consult the master claim table and exact-version review records.

`arctan_destination/` preserves the available Markdown source bundle at `/tmp/l3-supervisor-recovery-59x8oL/`, including its post-handoff continuation. Its original relative layout is retained for dependencies. Existing historical archives elsewhere in the repository remain unchanged.

Copying is non-overwriting. The separately generated SHA-256 manifest records snapshot bytes. It verifies preservation, not proof correctness or completeness of the research inventory.
