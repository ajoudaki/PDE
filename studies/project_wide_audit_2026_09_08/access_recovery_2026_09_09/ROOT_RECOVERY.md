# Scaling and Stieltjes history recovery

9 September 2026. Coordinator's direct source audit; no experiments, historical-command execution, old-task resumption or theorem campaign. Supersedes the access/attribution statements for PDE-2 tasks 10, 11 and 19 in the 8 September coverage map, not the independent mathematical qualifications in that report.

## Read coverage and provenance

The coordinator read the complete deduplicated user/assistant dialogue of all three task histories, not only their terminal replies. The reader omitted repeated app metadata; empty compaction markers and encrypted/internal administrative records are not mathematical source documents. The complete raw files were separately opened and parsed without errors. Tool records were searched for primary artifact locations and source amendments; this is not a claim to have human-read every administrative or tool-output byte.

- Task 10: `01a03e53-2e5f-75e2-8dcf-a02ee6f8d10c`, 1,303,340 raw bytes; mathematical/dialogue view 41,782 characters, read through EOF. The 26 August terminal argument at raw line 506 is preserved verbatim in `sources/TASK10_FINAL.md`, SHA256 `8ebfd4d6cf16437561765903e8ea3cac61e8ea28198e7566866e09f67a35aff0`.
- Task 11: `01a03d8a-3d77-7441-a157-7bc33559ecd2`, 16,861,986 raw bytes; dialogue view 93,900 characters, read through EOF. The final proof at raw line 4272 is preserved verbatim in `sources/TASK11_FINAL.md`, SHA256 `3acb441cc0459c6417ea06d787adce5a4ac2ca198907d1ce07d85fedc7de2124`. Earlier proof and full theorem-invocation audit were also read, including their corrections.
- Task 19: `01a01598-b22b-7663-a7c2-2fdcb57971c9`, 20,846,113 raw bytes; dialogue view 160,825 characters, read through EOF. The side-chat attachment `/home/codex-b/.codex/attachments/27e9ef0b-b0dd-4c05-8037-3a88ff4115c4/pasted-text.txt` is now readable and was read fully (44,242 bytes; SHA256 `8794c21cd3cdfa9d0d422567243c038fce7705c5a901c8fb6fd505a85f26e6f2`). It includes the original overstatement and explicit correction of the conditional DMFT step-loss claim.

Full rollout paths and raw-file SHA256 hashes are in `SESSION_INVENTORY.json`. Exact extraction provenance for the two recovered proof responses is in `sources/ROOT_SOURCE_MANIFEST.json`. These extracts are historical primary arguments, not fresh CLEAN-reviewed manuscripts.

## Task 10: theoretical scaling work, not an experiment

The title was misleading. This task developed necessary scale balances, a bounded activation candidate `2+sin(z)`, initial nonlinear activity identities, and an obstruction to ordinary neuron/one-edge closure. Its final answer explicitly left compact-time Gaussian-operator convergence, uniqueness and restartability open. No numerical experiment is inferred from the title, and the old quadratic/ReLU mesh reports must not be attributed to this task merely by thematic similarity.

The exact first-layer identity, for the unaveraged output convention and layer mobility lambda_1, is

    (1/n) sum_i |dot z_i^(1)|^2 = 4(f_n-1)^2 (lambda_1/n) K_n^(1).

For raw linearly interpolated GD, the residual and kernel on the right are evaluated at the left grid endpoint. This explains why forcing all layer mobilities to be bounded on that clock would force the first-layer RMS speed to vanish whenever residual and kernel remain bounded. Allowing lambda_1 of order n removes that artificial obstruction.

The final source calls its entire scaling regime uniquely forced. The present consolidation retains the explicit identity and the balanced candidate; it does not promote every necessity claim to a separately audited theorem. In particular, the zero-readout argument identifying a unique middle-layer mobility from a second-order coefficient needs more than merely bounded mobilities: passage of that coefficient through the stated trajectory limit must be justified.

The one-edge obstruction is exact on an appropriate bounded current-state family. If w is orthogonal to h^(1), replacing W^(2) by W^(2)+kappa delta^(2) w^T/n leaves z^(2) unchanged, while the reverse action changes by kappa (||delta^(2)||_2^2/n)w. On bounded-coordinate examples its change in each rescaled initial-size edge vanishes, but its operator action survives. This rules out that insufficient state description, not an enriched current operator limit and not a Gaussian-trained-trajectory theorem. The master already adopts the richer description.

## Task 11: the actual arctangent construction

The full chronology corrects a substantive attribution error. It does not end with a coherent 1/n-edge or nested finite-type positive theorem. It ends with independent Gaussian middle entries of variance 1/n, a genuinely nonlinear arctangent activation, and an action-operator construction.

Three contracts occurred in sequence:

1. A divergent-background, two-scale tangent construction was proposed. The user explicitly excluded it because raw hidden states were not tight and its limiting kernel was frozen. It is not an admissible positive result for this project.
2. A bounded-mobility clock briefly produced the elementary vanishing-speed obstruction above. The user removed that artificial normalization. The obstruction does not survive the amended clock.
3. The final raw-weight witness uses variances (1,n^-1,n^-4), mobilities (n,1,n^-1), and master step n^-2. With stored readout n times the raw output weight, its readout variance is n^-2 and its limiting readout is zero. This is the later audited small-readout L2 arctangent line, not a different coherent-edge model.

The full recovered proof uses F(z)=z+z^3/3 to remove phi'(z^(1)) from the continuous first-layer state equation. Bounded arctangent controls the stored readout in L-infinity; the transformed state equations are locally Lipschitz in field L2 plus learned-operator trace norm on the controlled tube. Fixed finite auxiliary meshes are encoded using the initial Gaussian matrix, its actual transpose, and the complete learned rank memories. Width-independent mesh removal is a separate argument.

The raw-GD change of coordinates is not exact Euler: its exact scalar defect is a s^2+s^3/3 for a raw increment s. The recovered proof explicitly controls its accumulated error at step n^-2. It also transfers squared-tail uniform integrability to the gated reverse observables and obtains path-W2 convergence using integrated velocity differences rather than interchanging supremum and empirical average.

At zero limiting readout the two hidden movements begin at order t^2, their speeds at order t, and the hidden kernel blocks at order t^2. The total-kernel increment has a strictly positive quadratic coefficient. Gaussian full support gives positive initial affine-fit residual for arctangent, which persists locally by W2 continuity. These are the source's local activity conclusions, not an independent proof of every-positive-time activity.

The source itself read TP IV and TP III and corrected attribution: the imported theorem is only the fixed-program probability backend. Its terminal proof is not fully self-contained in the user's strongest sense, since it imports the rank-free pseudo-Lipschitz result. The existing Appendix-N/all-moment dependency qualification in master section 11 is not closed by recovering this earlier favorable invocation audit. The later assembled L2 proof and local/global variants remain the proper audited authorities. No new all-depth/global result is promoted from this historical sketch.

## Task 19 and attachment: confirmations and worthwhile omissions

The recovered chronology links the existing persisted certificates to their actual producer and explicitly stops the output computation at order 17. It confirms that the first-hidden order-18 quantity is obtained from Q_1'=8F, not a hidden output-order-19 computation. It confirms the positive-alpha extension and the later sharper finite-prefix threshold; the old 1/100 theorem was strengthened, not retracted.

The full attachment also establishes the provenance of two corrected errors: alpha=0, not alpha=1, freezes the first layer; and the conditional two-hidden-layer DMFT step loss was initially overclaimed as an actual network limit, then expressly withdrawn. Neither error should be reintroduced through the side-chat text.

Two pieces deserve greater visibility in the master:

- On beta=1 the degree-36 determinant polynomial has exactly one positive root alpha_*=0.017519225541486..., bracketed by 17519225541486/10^15 and 17519225541487/10^15. The six-moment Hankel tests fail below it, are singular at it, and pass above it. This is not an all-order phase boundary. The persisted exact coefficient-sign/bracket certificate and the preceding full primary-proof audit support this refinement.
- The validated FP64 experiments are empirical evidence, not missing proofs: A/M/V passed the fixed n=4096 qualification without relaxing gates; the subsequent C/A/M/V matched 4096/8192 panels did not show strict central adjacent-order improvement. C/A/M centrally favored M2, while V moved from M2 to M4 at the larger width. Every stringent paired 99% width-change interval crossed zero. The histories do not turn this into either hierarchy convergence or divergence. All three persisted terminal experimental reports were reread fully in this recovery; the raw arrays and bootstrap were not rerun.

The normalized-sine negative was called fully audited in the old conversation. That label does not remove the newer source auditor's explicit lack of an independently checked rigorous transcendental enclosure. Preserve the qualification. Similarly, the fixed-order Gaussian recurrence and retained large integers do not identify a positive-time quadratic population curve.

The pertinent persisted theorem/certificate families were already read and checked in the 8 September Stieltjes source audit. This recovery supplies the missing chronology and attachment, not a rerun of their high-order generators or a blanket new proof certificate. The recovered historical recommendation to truncate Gaussian initialization is not adopted: it conflicts with the present project's stronger scientific contract.

## Effect on strategy

The recovered material strengthens the need to distinguish clock artifacts, insufficient state descriptions, fixed-order algebra and true compact-time limits. It does not justify a new restrictive data assumption or establish the unresolved global canonical Gaussian nonlinear theorem. The main reached-response continuation recommendation is unchanged at this source group's scope.
