# PDE-2 task 9: recovered Operator MFP history and operative-source audit

9 September 2026. Assigned source auditor: task **9 only**, `Operator MFP L=2, general t`, identity `01a03544-ddce-77a1-b5af-44df07c54f4b`. This is an isolated source audit and supersession record, not a new research campaign or a review of the entire project. Only this report was written. No experiments, historical commands, task communications/resumptions, agents, commits, permission changes, or other file edits were performed.

## 1. Conclusions for the coordinator

1. **Material omission to repair:** task 9 contains an assembled, repaired **identity-activation theorem at every fixed hidden depth**: the width-first coarse/fine discrepancy has an explicit uniform `C_L t^4 |h|^5` remainder on `|h| <= c_L/t`. Its accompanying polynomial operator state also has a unique local continuous-time limit after width. This is not the older conditional arbitrary-depth rooted-path proposal. It deserves a separate positive row in §§4 and 6 of the master.
2. **Do not overcorrect the omission:** this all-depth result is a local **width-first** feature-ascent result. Its sources do not assemble an arbitrary-depth, arbitrary-data, global physical-time joint finite-GD theorem. The later fully assembled **L3, one-sample deep-linear joint theorem** remains the stronger joint/global result at its own depth. These statements coexist.
3. **Exact joint GD survives the source check:** the final L3 linear proof, SHA256 `ca3407d83f19faa857307c976d680b4c5b97f90901fd5faf48920d2f9e20d799`, proves convergence of recomputed output, total kernel and loss on each compact physical interval for every deterministic normalized mesh tending to zero. It also identifies fixed rooted-word/finite-rank observables. The residual in exact GD is recomputed from the updated parameter state, not advanced by a separate Euler equation.
4. **PDE negatives survive only with their encoder restriction.** The recovered final proof includes the corrected stable graph argument, total spatial derivatives, analytic jet-space basepoints, both physical-label cases, and a uniform-per-jet-cutoff qualification. It excludes fixed local finite-order polynomial/analytic PDEs whose encoded fields have a common bounded-degree current-contraction alphabet. It does not exclude arbitrary scalar fields. A one-field transport output realization is local in feature time and global forward in physical loss time.
5. **The last PDE audit actually finished before interruption.** The main task has no closing final answer, but the raw agent-message records contain all three final frozen-version `CLEAN` replies at rollout lines **35203, 35210, 35217**, after the exact hash was printed at **35178**. The turn was interrupted at 35220. Thus “final round still pending” would be an incorrect inference from the last main-assistant commentary.
6. **Nonlinear withdrawals remain withdrawals.** The all-history L3 `sin+cos` theorem was explicitly withdrawn, not repaired by merely making the GD step extraordinarily small. The operative corrected report retains finite programs, finite algebra and initialization/activity coefficients. The tiny-readout one-sample L2 arctangent theorem is a separate, successfully repaired source with its own fixed-parameterless-program import; it does not revive the withdrawn multi-data/multi-layer tail claim.
7. **Do not restore the old quadratic/ladder negatives from historical PASS labels.** Full dialogue recovery confirms those historical claims and their intended quantifiers, but supplies no later repair of the deterministic activation-stability or covariant-Schur defects already identified in the current master. The fixed-step positive-polynomial obstruction, the frozen-bottom arbitrary-mesh result, and ReLU scalar compactness remain distinct claims.

## 2. What was read, and what was merely parsed

### 2.1 Operational instructions and old claim ledger

The auditor personally read the complete operational `/etc/codex/skills/solve-math-rigorously/SKILL.md`, `/etc/codex/skills/investigate-conjectures/SKILL.md`, and its `references/evidence-ledger.md`, `references/adversarial-audit.md`, and `references/research-contract.md`. Their use enforced source/version separation and proof-obligation checks; it did not authorize solving the remaining conjectures.

The initial **587-line** master and all **149 lines** of `source_audits/ROOT_CONTINUATION_AND_OPERATOR.md` were read completely, with truncated initial reads repaired. The old source audit was used as a claim ledger, not as unquestioned mathematical truth. The master was being amended concurrently by the coordinator: a later observation had 595 lines and SHA256 `718f561c56da24e8dc5192aafc091530b158d8907a364a1056a9e3b55578bf7a`; its revised access-recovery introduction was also inspected. That observation is not represented as a frozen exact-version full review of the coordinator's evolving edition. Old-audit SHA256: `ae2ca46825a5f6e54dda5b540d33b29e746ebfd3b2ac8fea32d4f505bc9629ae`.

### 2.2 Exhaustive main mathematical dialogue coverage

The supplied inventory and the helper's recursive inventory locate **one** matching task-9 rollout, hence no additional task-9 branch file was silently omitted:

`/home/codex-b/.codex/sessions/2026/08/24/rollout-2026-08-24T21-35-00-01a03544-ddce-77a1-b5af-44df07c54f4b.jsonl`

- Bytes parsed: **112,211,063**; JSONL lines: **35,221**; parse errors: **0**.
- SHA256: `f94dae2f2b9897acf36a5c226eb813c258a418cb92436af708f0699ef4168d6d`.
- `chat 9` view: **568,239 characters**, **391 unique records**, **66 exact duplicates omitted**, **28,514 other/administrative records excluded by the helper**.
- All character ranges were read, not just search hits or finals: `[0,40000)`, `[40000,80000)`, `[80000,120000)`, `[120000,160000)`, `[160000,200000)`, `[200000,240000)`, `[240000,280000)`, `[280000,320000)`, `[320000,360000)`, `[360000,400000)`, `[400000,440000)`, `[440000,480000)`, `[480000,520000)`, `[520000,560000)`, `[560000,568239)`; final EOF verified. The first range was reread after a display truncation. The later zero-length EOF check was bookkeeping, not counted as reading.
- Coverage includes older turns, user-supplied mathematical arguments, amendments, explicit withdrawals, and the late linear/PDE continuation. No claim is made to human reading of encrypted compaction content or every administrative JSON byte.

The helper's “administrative” count is a **view classification**, not a claim that every excluded record lacks mathematical evidence. In particular, raw `agent_message` records contain important audits and were separately inspected as described below.

### 2.3 Tool and audit evidence

The tools view was inventoried: **6,250 records**, **16,898,944 rendered characters**, no exact duplicates in that view. It was not all read character-by-character, and no such claim is made. Read-only JSON inspection inventoried tool names, document references, and **465 candidate document-writing calls mentioning 88 distinct Markdown paths**. This is a lexical mutation inventory, not a certificate that every attempted write succeeded. The initial repository-wide listings and external-literature searches were not treated as newly authored task-9 proofs.

The operative late calls, outputs and patches were inspected against the actual final text and hashes. Particularly important are:

| Rollout lines | Evidence and disposition |
|---|---|
| 3013–4058; 4607–5016 | Numerous finite-step proof edits, followed by rung-three width identification. Inventoried as predecessors, not independent final theorems |
| 5492–6818; 7315–8218 | General-time repairs, concentration/cubic supplements, then arbitrary-depth finite-step assembly. Their mathematical chronology was fully read in main dialogue |
| 10275, 10350, 10355, 10375, 10377 | Initial all-depth identity assembly rejected for missing bridges; explicit fixed-operator dependency and signed-coefficient bridge restored; re-audit passes after the stated coefficient sentence. The current full text contains that sentence as (4.5a) |
| 32131–32625; main withdrawal 32644 | Attempted L3 self-contained manuscript replaced by the corrected open-status report. Current `/tmp/L3_AUDITED_REPORT.md` and expanded copy have identical hashes |
| 32820, 32848, 32943, 33092, 33113, 33120, 33296, 33333, 33383, 33621, 33628, 33786, 33798, 33867, 33874 | All available substantive audit replies in the selected 32701–33890 interval were read: L2 oracle, common-space, cyclic-restart, path, kernel and source-proof objections; subsequent repairs; final CLEAN replies |
| 33804–33816, 33879–33880 | The first source-appendix patch **failed**. Later patches supplied the six correction categories; final hash is `e9f963…`. A proposed patch is not evidence of a successful edit |
| 34268, 34334, 34541, 34639, 34709, 34714, 34719–34720 | Linear no-go scope audit, source/continuum checks, rejection of an earlier snapshot's Fock wording, frozen proof and audit-record hashes |
| 34609–34629 | Two attempted Fock repairs failed to match their contexts; the narrower final patch corrected the annihilator sentence. Current frozen text has all four relevant annihilators and the explicit real form |
| 34833, 34841, 34948, 34983, 35036, 35049 | Contrary PDE/encoding audits: field-count loophole, heat/Bessel alternative with well-posedness qualifications, incomplete feature transport, zero-mobility loophole, quotient-graph class, total derivatives, analytic basepoints, and per-cutoff width uniformity |
| 35065–35119 | Inspected repair patches defining the cyclic operator, enlarging the auxiliary incidence class, using total prolongation and separate readout-point jets, recentering analytic jets, restricting the quantitative necessity statement, and displaying the physical trace identities |
| 35161, 35168, 35173; 35178; 35203, 35210, 35217 | Second-round CLEAN replies; final frozen PDE hash; all three final CLEAN replies before interruption |

Additional raw agent evidence was read from the beginning of its deduplicated view through character 40,000 (chiefly inherited MFP source/scaling audits). The whole 1,036,003-character deduplicated agent-message corpus was **not** claimed as a second exhaustive read. Every main user/assistant dialogue range, however, was exhausted. The selected late audit intervals above include contrary verdicts, not only acceptance replies. The final operative documents were checked directly, so conclusions do not rest on audit adjectives.

No task-9 user-message attachment/PDF/image reference was found by a read-only attachment-marker scan, consistent with the full dialogue read. User-supplied mathematical text was inline. Tool-downloaded papers and temporary audit reports are separate source artifacts, not an unexamined user attachment.

### 2.4 Full-read operative proof/dependency corpus

The following **12 distinct contents, 7,996 lines**, were freshly read in full. Here `M` means `/home/amir/Codes/PDE/studies/mfp_program_history/`; it is only a path abbreviation, not a cross-host identity assumption.

| Full local path relative to M | Lines | SHA256 |
|---|---:|---|
| `temporary_omfp_continuous_time/route_d.md` | 590 | `174a226d8e5b1763591338b10d30e4efaefa11e45f0cfcfbe92386133146c27d` |
| `temporary_omfp_continuous_time/THEOREM_AND_AUDIT.md` | 648 | `1b4bb58b3484655b08d08ea1fd624cfb95eed470a5d7018b1bfdf6588fa5c12c` |
| `temporary_omfp_continuous_time/EVIDENCE_LEDGER.md` | 28 | `784e9c924f6a5c7a75d247597910ac53fb158d7b08014deb2c4677f2e3e1ce3e` |
| `temporary_depth_time_doubling/CUBIC_DEPTH_TIME.md` | 1700 | `e61b02a8c8a06db7baefbaa9e02ec7b9bb73f09f18cc21cc17a49e8862958edc` |
| `temporary_depth_time_doubling/WIDTH_DEPTH_TIME.md` | 1321 | `57e123a02c28f034f86222330882cf997176b5721679233953330b44bd9bd149` |
| `temporary_depth_time_doubling/UNIFORM_BSERIES.md` | 567 | `cc125b6a393845b0b18f5cdbc5e76cc61ff8afbb2a69dc0c718909c96b7b88a5` |
| `temporary_depth_time_doubling/AUDIT_UNIFORM_BSERIES.md` | 179 | `ce1535ec56904d1ab362befb0864329fbe9b7199a783dc669267f6176f2f817a` |
| `temporary_deep_linear_joint_audit/JOINT_DEEP_LINEAR_THEOREM.md` | 1065 | `ca3407d83f19faa857307c976d680b4c5b97f90901fd5faf48920d2f9e20d799` |
| `temporary_deep_linear_joint_audit/AUDIT_RECORD.md` | 32 | `53bfd8e718a169671d80d584728d843dbd8bebbf8081050fef031e6d1f714a6b` |
| `temporary_deep_linear_pde_no_go/PDE_CLOSURE_BOUNDARY.md` | 654 | `cc8b6fa5c8084ed69d56c794214edb5c14d8208801e47aab2fb4a5d5715bcd2e` |
| `four_thread_consolidation/expanded_sources/OPERATOR_L2_FULL_AUDITED_PROOF.md` | 1077 | `e9f963cde5061f4f5b4d8d3913a62ccd013af451334e46356134f624759d42fb` |
| `four_thread_consolidation/expanded_sources/OPERATOR1_L3_AUDITED_REPORT.md` | 135 | `bf6c25c58122400794da9226380263546be3165f5cd9245e5296e34eecf7f82e` |

The two expanded nonlinear files were additionally compared by SHA256 with `/tmp/L2_FULL_AUDITED_PROOF.md` and `/tmp/L3_AUDITED_REPORT.md`: **exact matches**, respectively. The L2 hash also matches the historical output at 33880; the joint-linear and PDE hashes match historical outputs at 34720 and 35178. This is byte-identity evidence, unlike simply observing the same pathname in two environments.

The two identity-activation notes have different hashes and presentations. They were both read, not treated as byte-identical copies. The stronger statement that the whole width-first state converges locally is explicit in `THEOREM_AND_AUDIT.md` (4.11); `route_d.md` §7 explicitly limits its displayed dyadic corollary to scalar output and leaves the finite-GF bridge separate.

## 3. New all-depth identity conclusion: proof-skeleton check

### 3.1 Exact contract and constants

This is the one-input, no-bias, fully trained identity network with standard Gaussian raw endpoint weights and raw connectors, connector factor `1/sqrt(n)`, output average `1/n`, and simultaneous recomputed feature-ascent step `h n grad f`. First take width at fixed finite step count and fixed nonzero step. Define

\[
D_{t,L}(h)=F_{t,L}(2h)-F_{2t,L}(h).
\]

For every fixed integer `L >= 1`, every integer `t >= 1`,

\[
\left|D_{t,L}(h)+\frac{t(2t-1)L(L+1)^2(L+2)}3h^3\right|
\le C_Lt^4|h|^5,\qquad |h|\le\frac1{16K_Lt},
\]

where

\[
K_L=\max\!\left\{1,\max_{0\le r\le4}(L+1)(L)_r3^{L-r},
\max_{1\le r\le4}(L+1)_r3^{L+1-r}\right\}
\]

and falling factorials past their degree are zero. `C_L` is the terminating recursion printed in the source, not a supremum defined through an unknown output. In particular `K_2=27`, `c_2=1/432`, `K_3=108`, `c_3=1/1728`. Reversing coarse-minus-fine reverses the cubic sign.

### 3.2 Load-bearing chain checked

1. **Width first.** `WIDTH_DEPTH_TIME.md` proves exact Gaussian conditioning across the `(2N+1)(L-1)` alternating matrix calls. The query depends on the whole preceding transcript; it is not independent of the reused matrix. Both regression cancellations are retained. For nonconstant activation, the finite-history rank proof treats the affine top-cotangent case separately. The stopped coupling uses a finite moment tower, inverse estimates only on its good branch, a superpolynomially small stopping probability at fixed `(L,N,h)`, and a raw polynomial norm majorant to remove stopping and prove terminal UI. The identity activation satisfies every printed hypothesis. This proof supplies fixed-step convergence without the disputed general scalar-feedback TP extension.
2. **One operator, both orientations.** `CUBIC_DEPTH_TIME.md` §4 constructs fixed Gaussian spaces and bounded connectors `W_0=I+J*`. Gaussian integration by parts identifies the adjoint response on each finite cylindrical query, including redundant/singular histories. Learned matrices are finite sums of rank-one operators. §4.3 proves the entire finite Euler/DAG equality, not merely equality of initial jets. The nonlinear generated-core regularity is finite-history only; no ambient higher-moment bound is smuggled into the linear argument.
3. **Why identity closes.** On the sum of the two endpoint Hilbert spaces and `(L-1)` trace-class spaces, every gradient component is an `L`-factor multilinear word. On the unit ball about initialization each untouched factor has norm at most three. Ordered slot differentiation gives exactly the two displayed bounds entering `K_L`. There is no Nemytskii multiplication loss because higher scalar derivatives vanish.
4. **Exact paired defect, then derivatives.** `UNIFORM_BSERIES.md` proves `E_h^2 x=E_{2h}x+h^2 a_h(x)`, then telescopes the pullback operators in their correct noncommutative order. The defect sum has `t` terms. Total Euler weight is at most `4t`; the first-exit bound keeps all interpolation and transported paths in the derivative ball. Three step derivatives cost `t^3`; summing defects costs the fourth power. Only `C^4` regularity of the genuine Banach field/observable is needed here.
5. **Parity and coefficient.** The readout-reflection/step-sign involution makes the limiting discrepancy odd. Writing it as `h^2 Q_t(h)` gives `Q_t(0)=Q_t''(0)=0`; the integral Taylor remainder contributes `|h|^5 sup|Q_t'''|/6`. The cubic recursion evaluates `S=0`, `H=L(L+1)^2(L+2)/6`, hence `J=4H`. This gives the coefficient above with no interchange of a finite-width Taylor derivative and the width limit.
6. **Time scope.** Substituting `h=T/(2t)` gives a summable `O(1/t)` dyadic discrepancy for `|T|<=1/(8K_L)`. The same width-first state is Euler for a locally Lipschitz polynomial field, so its whole state converges to a unique local operator flow. No depth-uniform constants, nonlinear interval estimate, finite-width GF identification at all depths, or arbitrary-data global theorem follows merely from that statement.

No decisive gap was found in this assembled identity-only conclusion after reading its operative dependencies. A minor scope caution: `route_d.md` Lemma 2.1 phrases the width identification for *every finite real schedule*, whereas the cited width supplement's theorem is stated for a common fixed nonzero step. The main uniform bound only needs the latter. The report does **not** promote the stronger arbitrary-schedule probabilistic assertion on that citation alone.

The earlier assembly FAIL at rollout 10275 is not the final status. The missing telescoping, first-exit, coefficient and width/operator bridges are present in the final full texts; the later reply 10375 specifies the last coefficient sentence, now printed as (4.5a). Conversely, the abstract theorem's old “nonlinear application open” wording is not a retraction of the subsequently verified identity subclass.

## 4. Exact L3 joint GD and PDE boundary: source checks

### 4.1 Joint linear theorem

The model is `f=a^T R B x`, with all entries of the four normalized factors independent `N(0,1/n)` initially, one scalar label, and mobility `eta>0`. It is not the newer tiny-stored-readout arctangent model. With the four-cycle block operator `C`, exact simultaneous gradient ascent is `C'=(C*)^3`, and physical loss flow is `dot C=2 eta e (C*)^3`, `e=y-f`.

The following checks support the final theorem:

- Every length-three cycle path meets a rank-one endpoint block: `rank C^3 <= 4`. The four traces in `C^4` give `f=Tr(C^4)/4`; all four squared gradient blocks, once each, give `K=||C*^3||HS^2`.
- The explicit Fock source has two independent connector colors and two orthogonal root-tail sectors. The final text includes the annihilators for both starred and unstarred letters and the conjugation-invariant real form. Fixed-degree Wick pairings, Gaussian-root concentration, and a finite net prove the needed source convergence directly. The later theorem no longer needs Xiang–Zhang or its dependency chain.
- Writing `C=C0+Q` makes the vector field trace class. Local trace-norm Lipschitzness follows by telescoping products; loss dissipation gives `||Q(T)||1 <= 2 |y| sqrt(eta T)` in the limit, with the analogous initialized-residual bound at finite width. This prevents finite-time escape.
- A fixed Picard depth has a finite rooted-word expansion. Its finite Gram matrices determine both trace norms and scalar coefficients continuously, even at singular Grams. The factorial Picard tail and common source/energy ball give compact-time width identification without identifying different ambient Hilbert spaces by fiat.
- Exact GD is precisely Euler for the **single reduced Q equation**, with `e_k=y-Tr(C_k^4)/4` recomputed. A cutoff with a positive buffer, and the bounded/Lipschitz reduced field, give dimension-free error `O_T(delta_n)`. An independently Euler-updated residual would be a different discrete scheme. The source's reference to the earlier product-state cutoff is read through this explicitly displayed reduced equation; the master already makes the necessary distinction.
- Recomputed interpolated predictions/kernel/loss follow from trace-norm readout bounds. Arbitrary deterministic `delta_n -> 0` suffices in normalized coordinates; an additional demand that the raw multiplier `n eta delta_n` vanish requires `delta_n=o(1/n)`.

The finite-moment ODE negative is state-universal and width-uniform, not orbit-specific. Products of finitely many contraction coordinates generate only disjoint unions of a fixed connected alphabet. Endpoint differentiation produces

\[
4^{m-1}x^T(B^TR^TRB)^m x
\]

inside the `(2m-1)`st feature derivative with positive coefficient. Stable graph independence blocks a fixed polynomial closure; tensor-degree expansion proves the ambient analytic-germ variant. The later PDE proof provides an especially explicit auxiliary incidence-class formulation for the quotient step. A monotone orbit-fitted scalar equation and a fixed-width scalar closure are outside the negative's quantifiers.

### 4.2 PDE proof, including its repaired loopholes

The final 654-line proof was fully read. Its encoder hypothesis is a smooth spatially varying linear combination of complete contractions of tensor degree at most a single `D`, uniform in width and space. Polynomial PDE/readout identities hold on a nonempty state-open set; analytic variants require a full neighborhood of the zero network state and analytic domains containing its realized zero-state **jet tuples**. Empty-graph constants are allowed and explicitly subtracted before Taylor expansion.

The essential checks are:

1. Quotients of pairwise-contraction graphs need not themselves have pairwise-valent index vertices. The final proof therefore first enlarges a finite auxiliary incidence-network class, then proves injective-monomial independence. This repairs the original invalid within-class triangular argument.
2. Spatial derivatives act on encoder coefficients, never create a new tensor graph, and commute with the state derivation. The final prolongation uses **total** spatial derivatives and independent jet copies for each readout point. Repeated time differentiation can raise spatial order, but not enlarge the connected contraction alphabet.
3. For nonzero labels, the lowest tensor-degree part of the physical iterated derivative is `(2 eta y)^k D^k f`. At label zero, positivity retains a term `f^k D^k f` whose graph still has the long connected component. The theorem explicitly fixes `eta>0`; at zero mobility a static encoder would be a counterexample.
4. The quantitative necessity statement assumes a width-uniform finite graph support for **each fixed jet cutoff**. Under this hypothesis a jet of order at most `rho+(2m-1)r` must already contain the degree-`4m+2` connected ladder graph. A merely finite but width-dependent support would not justify stable-width coefficient comparison.
5. Feature transport uses the local germ `E(theta)(s)=f(Phi^s theta)`, not an all-time feature half-line. Feature flow can blow up even on the scalar ray. Physical loss flow is global by the displayed rank/energy trace bound, so `Ehat(theta)(s)=f(Psi^s theta)` is defined on `[0,infinity)` and right translation gives the global forward transport semigroup.
6. This positive transport construction is an **output semiconjugacy**, not an injective representation of every network observable. It stores the future output profile (equivalently its unbounded Lie jet locally), so it defeats unrestricted field-count impossibility but does not meet the project's non-vacuous compression objective.

The alternative heat/Bessel entire-profile encoding appears in the recovered contrary-audit dialogue, but was not adopted as an operative global well-posed PDE theorem. Its growth-class, hierarchy-tail and state-decoding qualifications must remain. Likewise, the elementary failure of commuting monomial word encodings is a representation-specific observation, not a universal PDE impossibility.

## 5. Complete task-9 strand disposition

“History” below means the full main dialogue was read; it does not mean every superseded ancestor manuscript or numerical certificate was freshly re-audited. Per the recovery coordination, already-audited ancestors and unrelated runs were not recursively reopened. `M` remains the path root defined in §2.4.

| Strand / operative evidence family under M | Recovered contribution and present disposition |
|---|---|
| Inherited MFP inventory and initialization jets | Exact local Wick–Stein specializations, high-order certificates, general compiler/concentration gaps. History confirms scope; inherited repository-wide audit claims are not all newly certified here |
| `temporary_finite_step_single_hidden_mlp/{README,TWO_STEPS,K_STEPS}.md` | Exact iid same-Gaussian-seed Euler recursion, including input-Gram factor in bottom update. One-step identity `2 h q`; square `7 h q^2+12 h^3 q^4`. Euler and continuous-flow jets are different. History retained |
| `temporary_finite_step_two_hidden_mlp/FINITE_STEPS.md` | Two-sided reused-matrix forward/reverse histories, learned Gram terms and responses; not fresh independent matrices. Linear one/two-step controls `3h` and `6h+24h^3+20h^5`. History retained |
| `HALF_STEP_BOUND.md`; `temporary_width_first_operator_peeling/` | Initial finite-width cubic comparison was incorrectly advertised as the width-first theorem, then explicitly retracted. Fixed-eta cavity/rank/response and regularity repairs are later, distinct obligations |
| `temporary_quantitative_width_first_bound/PROOF.md`; rung-three/rung-four/general-time successors | Fixed finite horizons and explicit derivative-envelope remainders. Repaired concentration and regularity do not automatically make constants uniform in time. Full chronology recovered; existing master's finite-horizon classification retained |
| `temporary_general_time_doubling/` | Compact coefficient `-t(2t-1)J/2`, explicit fifth majorant, full concentration/cubic-ledger repairs. General time means arbitrary **fixed** t unless a separate uniform estimate is supplied |
| `temporary_depth_time_doubling/` | Arbitrary fixed depth/time finite-step package, singular Price calculus, compact cubic recursion. Core width/cubic/operator dependencies freshly read; no generic nonlinear interval-uniform upgrade |
| `UNIFORM_BSERIES.md`, `UNIFORM_DIRECT.md`, `UNIFORM_L1.md`, `UNIFORM_L2.md`, `UNIFORM_L3.md` | Exact paired-defect mechanism; one-hidden-layer uniform positive; nonlinear L2/L3 generated-response condition unclosed. Whole-space Nemytskii/product or adjoint-moment bounds rejected. The abstract B-series proof was freshly read |
| `temporary_omfp_continuous_time/` | **Newly restored all-fixed-depth identity theorem**, §3 above. Also exact scalar dyadic summability criterion and conditional state/Osgood sewing. `O(t^(5-delta))` suffices; bare `o(t^5)` does not. Scalar convergence is not state/GF identification |
| `temporary_near_identity_omfp/` | Normalized identity perturbations, finite cubic continuity and exact fifth-jet time polynomial of degree at most four. Local jets do not establish the requested `h~1/t` interval theorem; repeated user objections make that boundary explicit |
| `temporary_uniform_near_identity_l2/` | Same-space Gaussian Banach-algebra/weight incompatibility and analytic-scale loss; conditional paired telescope; attempted causal-composite charging rejected. These disprove proposed proof norms, not the scalar uniform-remainder conjecture |
| `temporary_quadratic_l2_order5/` | Nonzero quadratic perturbation gives positive-polynomial/frozen comparison and enormous fixed-step moment growth. The special polynomial width bridge is separate from bounded-derivative theorems. History retained; no fresh experiment or broad GF-negative certification |
| `temporary_polynomial_uniform_no_go/` | All signed polynomial **frozen scalar** Chebyshev obstruction versus full-network sign-coherent subclass. Signed deletion comparisons fail through cancellations; generic signed full-network theorem remains open |
| `temporary_linear_growth_uniform_counterexample/` | Narrow remote bump/ladder, six-term paired-transition and two-rate diagonal claims. Historical all-six certificates/PASS replies recovered, but no later repair of the master's false deterministic activation-stability premise. Do not reinstate unconditional bounded-slope super-`t^5` no-go |
| ReLU/leaky-ReLU uniform-remainder strand | Boundary-crossing candidate `h|h|` behavior and smoothing/bridge claims are not interchangeable with the smooth `C^12` theorem. Hard-kink marked-boundary/fixed-program obligations remain scoped; no global nonlinear negative follows |
| `temporary_loss_gradient_flow_audit/`, `temporary_loss_time_doubling/` | Residual feedback destroys the pure-ascent odd parity. Loss discrepancy has a quadratic leading term; finite jets and time change do not themselves remove the mesh. Classical ReLU gate failure differs from Euler compactness |
| `temporary_loss_mesh_resolution/`, `temporary_joint_width_mesh_quadratic/` | Frozen-bottom arbitrary-vanishing-mesh initial layer; full-network sufficient ultra-fine mesh shadowing **conditional on** the contested finite-GF initial-layer theorem; intervening full-model scales remain open. ReLU exact-Euler predictors/losses tight on a stated short interval for every vanishing mesh; uniqueness/full-state limits not supplied |
| `temporary_scaling_comparison/INDEPENDENT_COMPARISON.md` and inline user-supplied finite-type discussion | Equivalent-coordinate mobility does not equate iid `1/sqrt(n)` connectors with coherent neuronal-embedding `1/n` connectors. Finite-type nonlinear examples concern another initialization dependence structure. Root owns task-10/11 primary attribution; no transfer of their conclusions is made here |
| `temporary_arctan_mfp_reconstruction/MFP_RECONSTRUCTION.md`; later L2 complete proof | Natural coordinate removes the single bottom gate in continuous time; exact GD has explicit quadratic/cubic coordinate defects. Final tiny-readout L2 one-sample theorem and repairs freshly read. It is not the earlier order-one stored-readout theorem |
| Duplicate/contradictory-data and multi-input attempts | Duplicate opposite labels obstruct the requested strict loss/activity package, not all population flows. A random-output scaling loophole was separately bounded using kernel/hidden-speed scaling. Distinct-input negative route failed: mixed-parity `sin+cos` gives positive initial feature Grams even for singular raw Grams; no uniform separation-free conditioning constant is inferred |
| Multi-data L2 and L3 `sin+cos` global claims | Several advertised compact/global joint theorems and very small meshes rested on unproved all-history tails. AMP/full-history/source theorem invocations were narrowed, then L3 claim explicitly withdrawn. Corrected task-9 report is 135 lines, not task-8's different 885-line report. Newer other-task local theorems must be attributed independently |
| `temporary_deep_linear_joint_audit/` | Fresh full proof check supports global physical L3 one-sample operator/GF/joint-GD theorem, fixed-word observable identification and precise finite-contraction ODE no-go; §4.1 |
| `temporary_deep_linear_pde_no_go/` | Fresh full proof check supports restricted polynomial/analytic PDE negatives, physical zero/nonzero-label cases and scoped transport counterexamples; final exact-version audit replies recovered; §4.2 |

### 5.1 L2 arctangent: what the recovered repairs actually establish

The frozen source takes raw variances `(1,n^-1,n^-4)`, multipliers `(n,1,n^-1)`, step `n^-2`, one input/label equal to one, and unhalved square loss. With stored readout `C=nc`, its initial variance is `n^-2` and its limit is zero. This normalization distinction is substantive.

The final proof constructs one saturated initial probability/action space, not unrelated one-time GNS spaces. The deterministic finite population oracle contains only fixed coefficients; all empirical scalar feedback is restored by explicit finite-rank Gram residuals. Same-width auxiliary Euler comparison removes the mesh. Current cyclic spaces, rather than an unjustified unitary on the entire initial realization, give restart equivalence. Joint `W2` convergence and square-tail truncation handle `q(A)P`, `q(A)^2P` and subsequent matrix actions. A fixed coarse-grid projection plus integrated slope bounds proves path-`W2`, avoiding the false inference from `sup_t average error^2` to `average sup_t error^2`.

The cubic coordinate identity gives accumulated exact-GD error `O_T(eta+eta sqrt(n)+eta^2 n)`, so the stated step works. Strict hidden motion, nonaffinity, changing kernel and loss decrease are proved on a common sufficiently short interval, not for every positive time. For example, with the source's `d>0`, `m>0`, `nu>0`, the total kernel is `k3+8(d+m nu)t^2+o(t^2)`; the initial hidden displacements are order `t^2`, not order `t`.

The only nonclassical import is the **fixed finite parameterless** TP III theorem. The final source explicitly excludes the problematic arbitrary conditional-a.s. and mean-convergence appendices, uses its own Gaussian norm bound, and records six source-algebra correction categories. The recovered late audits corroborate this specialization and distinguish it from the broader scalar-feedback/rank-changing import discussed in master §11. This audit freshly checked the final manuscript's internal bridges and correction algebra, but did **not** freshly reread the inaccessible original `proofs.tex` entire external induction; it therefore does not advertise a new independent complete primary-literature certification or erase the master's separately stated import audit boundaries.

The corrected L3 report still mentions TP Appendix A.2 as a possible oracle mean-convergence input. The later L2 source audit rejects its printed proof as missing UI and does not use it. The L3 report's retained actual-network claim is explicitly **in probability**, not an actual-network L1 theorem; no additional mean claim should be imported from that sentence.

## 6. Provenance limits and unresolved artifacts

There is **no missing operative final file** for the new all-depth identity, L3 joint-GD, PDE boundary, tiny-readout L2 theorem, or corrected task-9 L3 report used above. No file text needed to be reconstructed from an incomplete patch stream. Current `/tmp` copies were hash-compared instead of equated by path.

The following historical ancestors are currently readable but were not newly fully re-audited because their operative replacements were read in full and the coordinator specifically asked not to reopen every ancestor. Their hashes are recorded to prevent mistaken identity claims:

| Available ancestor | SHA256 | Disposition |
|---|---|---|
| `/tmp/L3_SELF_CONTAINED_PROOF.md` | `ea9762d9b67901e0e168c206149cb10db44c53d2df020b0302629489a58d3d23` | Withdrawn draft, not the final open-status report |
| `/tmp/l2_algebra_activity_audit.md` | `6b147ac3fd5214cd74e2300f481629cc38f3ad4604cff255784b771919ca131b` | Earlier audit; substantive reply and final merged algebra read |
| `/tmp/l2_tp_primary_audit_report.md` | `76c7c5c0078beddcb5f1448220eaa544c8a80054a5d4fa5945ffc28c34084d48` | Earlier source audit; later six-category repair supersedes narrower early version |
| `/tmp/l2_continuum_audit.md` | `da6c179f11d73d7ce9afd66df1ab59952e7e9feac332f0195dd78ceccf71ef5f` | Earlier continuum audit; final assembled replacement read |
| `/tmp/L2_CONTINUUM_REPAIRS.md` | `9969c5013bd74f374bc6a0818764339c2e12d94d5e5697357777ebda3e4628b6` | Insertion-ready ancestor, not an independently current theorem |
| `/tmp/L2_SECTIONS_4_5_REPAIR.md` | `66bdcd24ab62d794c4d9b4648678d2a5a39109ce60dc936422dc2fa11a6aebf1` | Insertion-ready ancestor; final current-cyclic/oracle treatment read |

Two explicit residual source limits:

- `/tmp/tp3-audit.Trwr6N/proofs.tex` still returns **EACCES** to this auditor. No permissions were changed. Its historical audit replies and the operative manuscript's explicit corrections were read; a complete fresh external source-proof read was not performed or claimed.
- `M/temporary_depth_time_doubling/AUDIT_UNIFORM_T4.md` is **ENOENT** although referenced by historical writing call 8783. The supported abstract proof, its actual `AUDIT_UNIFORM_BSERIES.md`, later identity assembly and its audit replies are present. The missing filename was not silently replaced by a presumed identical file or reconstructed from a partial update.

The mutation inventory also found `temporary_quantitative_width_first_bound_rung4/CONCRETE_F4_THEOREM.md` and `temporary_quantitative_width_first_bound_general_t/ALGEBRA_AND_OBSTRUCTION.md` still present. These belong to the older finite-rung lineage, not missing support for the new all-depth identity conclusion. Large polynomial/paired-transition numerical certificates and unrelated external-paper search/download outputs were inventoried through the history but not recomputed or represented as hand-verified coefficient tables.

## 7. Concrete proposed master changes

The coordinator, not this auditor, owns master edits.

### 7.1 Add an identity-only exception to §4's uniform-time boundary

Suggested insertion:

> A separate repaired task-9 theorem proves more for the identity activation. For every fixed hidden depth `L>=1`, the actual width-first one-input network satisfies
> \[
> \left|F_{t,L}(2h)-F_{2t,L}(h)+\frac{t(2t-1)L(L+1)^2(L+2)}3h^3\right|
> \le C_Lt^4|h|^5,\quad |h|\le(16K_Lt)^{-1},
> \]
> with the source's explicit finite constants (`K_2=27`, `K_3=108`). The fixed Gaussian operator plus trace-class learned state is polynomial, so its width-first Euler states converge to a unique local operator flow. This is not a nonlinear uniform-remainder theorem or an all-depth joint finite-GD/global physical-time theorem.

Link the [assembled theorem](../../mfp_omfp_continuous_time/THEOREM_AND_AUDIT.md), [independent route](../../mfp_omfp_continuous_time/route_d.md), and this audit. Keep the generic nonlinear generated-response condition open.

### 7.2 Refine, not delete, §6's depth qualification

Replace any blanket implication that *all* arbitrary-depth linear identification is conditional by:

> The older arbitrary-depth rooted-path global physical-width proposal remains conditional beyond its completed cases. Separately, task 9 proves the identity-only all-fixed-depth finite-step/uniform-remainder and local width-first state theorem. The fully assembled global physical-time **joint finite-GD** theorem recovered here is still the L3 one-sample theorem.

Retain the exact-GD residual-recomputation warning and the distinction between normalized vanishing step and vanishing raw multiplier. Do not add general data or a full nonlinear observable bundle to the linear theorem.

### 7.3 Make the PDE encoder/transport boundary explicit

The existing negative paragraph is substantially correct. Add, if space permits, that analytic functions are expanded at the realized zero-state jets; the physical proof covers zero labels at positive mobility; and the transport positive is a local feature-time germ versus a global forward physical-time output realization. A compact citation to the [final proof](../../deep_linear_closure_boundary/PDE_CLOSURE_BOUNDARY.md) can carry the detailed quantifiers. Do not describe it as an injective all-observable encoder or useful finite-information compression.

### 7.4 Replace PDE-2 task-9 row in §13

Suggested row text:

> Full recovered main mathematical dialogue read through EOF, all matching branch files covered. Contributions include exact finite-step/general-time MFP, a repaired identity-only all-fixed-depth uniform `t^4 h^5` theorem and local width-first operator flow, scoped quadratic/ReLU/loss-mesh analysis, the repaired tiny-readout L2 arctangent theorem, explicit withdrawal of the L3 `sin+cos` growing-mesh claim, and the final L3 linear joint-GD/PDE-encoder boundary proofs. Operative versions and exact hashes freshly checked; external-source and ancestor coverage limits are recorded in `access_recovery_2026_09_09/OPERATOR1.md`.

This is substantially more informative than “earlier related fixed-horizon/operator work.” Do not assign this entire long mathematical sequence to task 8 or to the scaling-test tasks merely because their titles overlap.

### 7.5 Leave the contested negatives and newer other-task positives at their current grades

The restored historical PASS replies do not repair the current master's deterministic activation-stability/covariant-Schur objections. Likewise, the later broad local and original-L3-local theorems in other tasks are independent sources, not retroactive validation of task 9's withdrawn all-source lemma. No experiment or new proof campaign is proposed or authorized by this report.

## 8. Final audit disposition

**Task-9 main dialogue recovery is complete.** The bounded new-claim source check is complete for the all-depth identity theorem, L3 exact joint-GD theorem and PDE encoder negatives, with their operative dependencies and contrary late audits inspected. The final L2 proof and corrected L3 report were fully read and byte-matched to recovered originals. The report makes no claim to a human read of all raw tool/agent/administrative bytes, no blanket re-certification of every predecessor proof, and no new external primary-literature certification where access remains unavailable.

The principal substantive master change is a **restored, precisely scoped positive all-depth linear result**, not a general nonlinear theorem and not a reversal of the master's recently qualified negatives.
