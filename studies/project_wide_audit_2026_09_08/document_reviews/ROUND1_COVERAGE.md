# Round 1 — isolated document-only consolidation review

**Verdict: NEEDS_CORRECTIONS**

Reviewed document: `/home/amir/Codes/PDE/studies/project_wide_audit_2026_09_08/MASTER_RESEARCH_REPORT.md`.

Source SHA256: `dee7a2fb5c4350d1883d2939f06bca67581274d9b94b44dce9b23225d7a8e62a`.

Source extent: 541 lines, 79,991 bytes. Review date: 8 September 2026.

**Full-read confirmation:** I read the complete document, including every inventory row, all displayed mathematics, the source-coverage qualifications, and the final remaining-obligations paragraph. The contiguous reads covered lines 1–115, 116–235, 236–345, 346–435, 436–495, and 496–541 without truncation. The source hash and extent were checked again after the full read and matched the initial values. All line references below refer to that version.

**Isolation and verification limit:** This document was the only research evidence. I did not open links, source manuscripts, project instructions/context, histories, prior audit files, or other agents' outputs, and did not browse. I read only the operational review skill and its severity rubric in addition to the designated document. No experiments, proof campaigns, or source edits were performed. The skill's usual external verification and additional artifacts were excluded by the user's document-only, single-output instructions. This file is the sole written artifact.

Consequently, “reported,” “checked,” “Internal,” and similar source-status descriptions below refer to what the master says its source auditors established. They are not independent certifications by this reviewer. External/source verification is unavailable under this isolation, not attempted and failed. The verdict concerns whether this is an accurate, intelligible consolidation; it does not judge the entire research theorem solved or unsolved on independent evidence.

## Assessment

The inventory contains exactly 36 rows: 17 under PDE / black-chatgpt and 19 under PDE-2 / black-chatgpt-2 (lines 483–523). There is no missing numbered entry or extra “Work” entry. Actual task identities, version matches, and history completeness cannot be verified here. The master itself correctly separates identity resolution from exhaustive historical recovery (lines 23–35, 477, 525).

The principal scientific synthesis is substantially coherent. It distinguishes finite-order calculations, fixed-program identification, local and global compact-time limits, finite optimization, and generalization (lines 55–66). It preserves the difference between canonical and modified Stieltjes models, the three clipping constructions, and finite fitting versus population identification. Its pending covariant-Schur, imported-proof, and special-variant checks are explicitly marked. Those honest limitations are not defects requiring the underlying theorem to be proved before the master can become accurate.

Required corrections remain. Some substantive positive or corrective contributions appear only in the inventory; several theorem summaries have unstated domains or inherited restrictions; a mixed-evidence table overstates its numerical row; and damaged Markdown/math changes or obscures mathematical statements. The findings below separate consolidation coverage from localized repairs. No finding establishes a fatal flaw in an unseen source proof.

## Required corrections

### M1 — Restore substantive inventory contributions to the synthesis, with their existing qualifications

**Locations:** lines 387 and 492; 120–141 and 494–495; 208–238, 292 and 511; 68–114, 312 and 512; 389–417 and 520.

**Issue:** All tasks are listed, but the prose does not actually consolidate several results that its own inventory records. In particular:

| Inventory contribution | What the synthesis currently conveys | Required addition |
|---|---|---|
| PDE 10, line 492: “global finite action” in the RMS-quadratic program | Line 387 mentions finite identities, initial kernel/activity, and open population gates | State what is global at finite width, retaining the distinction between an action bound and any stronger existence/identification assertion. |
| PDE 12, line 494: QI/IQ exact reductions and invariants | Line 136 gives one architecture's negative Hankel test; line 202 gives a general spectra-only limitation | Restore the constructive reductions/invariants as reported contributions. Define the architecture abbreviations and say whether each reduction concerns finite dynamics, formal coefficients, or a population object. |
| PDE 13, line 495: H3 finite positive prefix | The architecture table contains several negatives, but no H3 row | Define H3 and record its positive finite prefix, with the available order/certificate qualification. Do not silently identify H3 with the raw-cubic model in line 135. |
| PDE-2 7, line 511: a “complete L3 local result” | Line 292 highlights global finite fitting and open global population identification; §7.1 gives the broader, dependency-qualified local theorem | Identify the original L3 local result separately, or explicitly explain its relation to §7.1. Its own observable scope and audit/dependency status are not supplied by the inventory phrase “complete.” |
| PDE-2 8, line 512: corrected sin+cos fixed-program/kernel/initial-activity result and withdrawn growing-mesh proof | §4 discusses general compiler successes and limits; line 312 concerns a different calibrated sine construction | Add the corrected sin+cos result and the precise withdrawal. Do not substitute the modern calibrated-sine local theorem for this fixed-program result. |
| PDE-2 16, line 520: zero-label stationary result and initialization equicontinuity | §10 restores the scalar-particle residual theorem and dense-ResNet gap, but these two tanh contributions are absent | State the stationary regime's initialization/residual conditions and the equicontinuity object's time scope. Zero labels alone must not be presented as sufficient for stationarity of every finite network. |

These are synthesis omissions, not absent task identities or independently verified missing theorems. The QI/IQ, H3, RMS, and tanh details cannot be reconstructed reliably from this document alone. An accurate interim master can explicitly attribute and delimit them rather than supply invented formulas.

**Proposed minimum wording, distributed to the relevant sections:**

> The task inventory also reports constructive QI/IQ reductions and invariants, a finite positive moment prefix for the architecture denoted H3, and a global finite action result for RMS-normalized quadratic dynamics. These survive separately from the canonical quadratic population questions. Their exact state variables, architecture definitions, or bound statements have not yet been reproduced in this synthesis; the present summaries do not certify an additional population-limit theorem.

> The original L3 arctangent line also contains a reported complete local result (PDE-2 task 7), whose relationship to the broader dependency-qualified theorem should be stated explicitly. The corrected sin+cos report retains fixed-program identification, kernel, and initialization-activity results; its growing-mesh proof was withdrawn. The tanh audit additionally retains a zero-label stationary regime and initialization equicontinuity, without thereby proving a general global nonlinear population limit.

For a fully informative master, replace the explicitly missing details in this interim wording with the already established source-specific statements during authorized consolidation. This review does not require a new proof campaign or upgrading their audit grades.

### L1 — Keep certification language consistent with each entry's evidence level

**Locations:** lines 27–29, 129–137, 242–248, 429.

**Issue A:** “There are nevertheless decisive scoped negatives” introduces a table whose sine row contains only retained high-precision signs and expressly says that no rigorous transcendental enclosure was independently audited. The row's caveat is responsible, but the categorical introduction and “Negative result” column overstate what this master establishes for that row. This does not show that a sine claim is false, or that its source has no proof.

**Repair:** Replace line 129 with:

> There are scoped negative results, dependency-qualified negative certificates, and numerical negative evidence; their evidentiary status differs as indicated below.

Rename the table's middle column “Result or evidence.” Describe the sine row as “Retained high-precision negative signs; no independent rigorous enclosure certified in this audit.” Retain the separate coefficient-generation qualification on the QI row. Do not downgrade all retained exact certificates to numerical evidence.

**Issue B:** The affine-first extension row (line 248) states a global full bundle but omits an audit grade, whereas its neighboring broad-L2 row states the fixed-program dependency qualification and the table defines grades in line 242. It is unclear whether that qualification is inherited or bypassed.

**Repair:** Attach the appropriate existing grade directly to line 248. On the evidence supplied here, safe wording is:

> Global full bundle; no separate bypass of the shared fixed-program audit qualification is specified here. The first layer is affine, so the theorem does not meet the all-hidden-nonlinearity target.

If there is a separately audited bypass, name it and its scope during consolidation. This is a missing status label, not a demonstrated contradiction between the theorems.

### L2 — Make separation and activation quantifiers explicit in the global table

**Locations:** lines 39, 250–255, 286–287.

**Issue A:** The three-sample assumptions are printed as $G_{ab}\le1-\delta$ and $\lvert G_{ab}\rvert\le1-\delta$, without $a\ne b$. Line 39 sets $G_{aa}=1$. Applied to diagonal entries, the printed inequalities give an empty class for positive separation. The intended off-diagonal reading is apparent, but must be stated. The pair correlation $\rho$ is also never explicitly identified with $G_{12}$.

**Repair:** Introduce the table with:

> Inputs in these separated-data rows are normalized: $G_{ii}=1$, and for two samples $\rho=G_{12}$. Every pairwise separation condition is imposed only for distinct samples. In the three-sample rows the indices are $1\le i<j\le3$. “All binary labels” means every vector in $\{-1,+1\}^m$.

Write the three-sample conditions as $G_{ij}\le1-\delta$ or $\lvert G_{ij}\rvert\le1-\delta$, for $i<j$. Supply the admissible positive range of $\delta$ for each family; do not automatically transfer the initialization theorem's $\delta\le1/4$ to all trained results.

**Issue B:** Line 252 states only $e\le c_L\delta^{p_L}$, although its conclusion includes absolute nonaffinity. The inequality literally includes $e=0$, making $az+e\arctan z$ affine. Line 253 also omits positivity of $e$. “Same geometry/labels” in line 252 does not explicitly carry over the previous row's restriction on $a$.

**Repair:** Write $0<e\le c_L\delta^{p_L}$ in the nonaffinity row and $0<e\le c_{\rm dyn}\delta^{31/8}$ when asserting nonaffine activity in the shape row. State the allowed range of $a$ directly. If the preceding row's $1/2\le a\le1$ is intended to govern both extensions, say so; otherwise reproduce each actual range. A fitting-only assertion at $e=0$, if retained, must be separated from nonaffinity. These are local scope repairs, not evidence against the intended positive-$e$ theorems.

### L3 — Define the quantities and domains in the sharp initialization and scale claims

**Locations:** lines 261–266, 296–310.

**Issue A:** The sharp formulas introduce $q_L$ and $Q_L(0)$ without definitions, and both infima lack a domain. Line 306 refers to “the stated ... class,” but it only lists ranges of $L,d,\theta,\delta$; it does not state the triple's Gram/separation conditions. Thus the reader cannot distinguish a bound for every admissible triple from a sharp worst-case bound over triples.

**Repair:** Define the initialized sample feature Gram and its common diagonal before the display, for example $Q_{L,ij}(0)=\mathbb E[H_i^{(L)}(0)H_j^{(L)}(0)]$ and $q_L=Q_{L,ii}(0)$, if these are the intended objects. Explicitly define the admissible input class $\mathcal X_{\delta,d}$ and write

\[
\inf_{X\in\mathcal X_{\delta,d}}\lambda_{\min}Q_L(0;X),
\qquad
\inf_{X\in\mathcal X_{\delta,d}}
\lambda_{\min}\!\left(Q_L(0;X)/q_L\right).
\]

If the intended class is normalized, pairwise two-sided-separated triples, give it as $\{(x_1,x_2,x_3):\|x_i\|^2/d=1,\ \lvert x_i^Tx_j\rvert/d\le1-\delta\text{ for }i<j\}$; otherwise specify the actual class. This proposed definition is not externally verified. Distinguish uniform lower bounds for all triples from the existence of triples matching their order. Also state that the line-308 asymptotics take $L\to\infty$ with $\theta>0$ fixed.

**Issue B:** The explicit gain in line 265 depends on an undefined $c_\psi$. Its meaning, positivity condition, and dependence cannot be recovered from the displayed formula. Line 310 likewise invokes “the stated strict loss target,” although no threshold is stated, and “a diverging excursion” without naming a variable or norm.

**Repair:** Define $c_\psi>0$ and its dependence before giving the explicit gain; otherwise state only that a sufficiently large finite gain depends on separation and the shape. For the fitting-scale claim, specify the loss normalization, fixed target, excursion variable/norm, and which constants may depend on depth. If those details are not supplied, use the accurately limited wording:

> The source reports a necessary time and parameter-motion cost as the nonlinear coefficient tends to zero on this singular-Gram example. This summary does not reproduce the target loss or excursion norm; it does not assert failure of fitting at fixed positive coefficient.

These repairs preserve the reported initialization and necessary-cost results without pretending that their currently incomplete statements are self-contained quantitative theorems.

### L4 — Specify the update-count and population-law interpretations of the MFP formulas

**Locations:** lines 72, 89–103, 375–380.

**Issue A:** In $F_{t,L}(2h)-F_{2t,L}(h)$, $t$ denotes an update count, but elsewhere it denotes continuous physical time. $F_{t,L}$, $J_{\phi,L}$, and the admissible range of $h$ for the general remainder estimate are not defined. The line-99 range is explicitly for the sharper one-layer result and does not provide the general range. The estimate as displayed is therefore not a usable quantified statement, and could be read as holding for all real $h$.

**Repair:** Use an integer $k\ge1$ for the paired update count, define $F_{k,L}(h)$ as the relevant fixed-program limiting output in its stated model, and define the observable/coefficient $J_{\phi,L}$. State the activation/initialization restrictions and small-step range associated with the bound. Then print

\[
F_{k,L}(2h)-F_{2k,L}(h)
=-\frac{k(2k-1)}2J_{\phi,L}h^3+R_{k,L}(h).
\]

Say explicitly that the remainder constant/exponent depends on fixed $L,k$. If the precise small-step range is not available in this consolidation, call it a local small-step estimate and mark that range as not reproduced; do not invent a numerical radius.

**Issue B:** Line 72 starts with a finite Gaussian matrix and a vector $Y=W\mathbf1$, but gives a damaged, scalar-looking expectation as its response coefficient. Lines 375–380 similarly describe Gaussian matrix calls and then write an equality in law between a matrix-action expression and scalar random variables. The preceding convention about population adjoints helps, but neither passage explicitly states whether it is a limiting coordinate law or a finite-width identity. The order of the width limit and $\varepsilon\downarrow0$ matters for the latter obstruction.

**Repair:** At line 72, use a scalar $Z\sim N(0,1)$ and identify $\mathbb E[g'(Z)]$ as the response coefficient in the relevant large-width coordinate law; distinguish this from the finite conditional-projection coefficient. For the generated-action example, write:

> For each fixed $\varepsilon>0$, the population coordinate law obtained from this fixed Gaussian program is $X_\varepsilon=\sigma G+c\,\psi(Y/\varepsilon)/\sqrt{v_\varepsilon}$, jointly with a standard Gaussian $Y$, where $G$ is an independent standard Gaussian. The $L^p$ lower bound concerns this family of population laws as $\varepsilon\downarrow0$. No width-dependent choice $\varepsilon=\varepsilon_n$ or exact finite-matrix scalar law is asserted here.

Alternatively, if $W_0^{(3)}$ is already the population operator in this passage, say so explicitly before the equality. Preserve the important statement that these are not established training queries.

### L5 — Expose the horizon dependence behind the all-physical-time projection claim

**Locations:** lines 292, 322, 357.

**Issue:** The table says $R\ge C\sqrt n$ makes projection inactive, including all physical time on the fitting event. The supporting paragraph supplies only $\|\delta^{(2)}\|_\infty\le C_S\sqrt n$, with a feature-horizon-dependent constant. The conclusion is compatible with line 292's bounded total feature time, but this bridge and the uniformity in width are not stated at the point of use. This is a quantifier omission, not a demonstrated inconsistency or objection to the projection argument.

**Repair:** Connect the two existing claims explicitly:

> On each fixed feature interval $[0,S]$, a cap $R_n\ge C_S\sqrt n$ is inactive. On the canonical fitting event, the total feature time is bounded by a width-independent $S_{\rm fit}$; choosing $R_n\ge C_{S_{\rm fit}}\sqrt n$ therefore gives inactivity throughout physical time. Constants and the event are those of the finite-network bounds.

If a width-independent total-feature-time bound is not intended by line 292, restrict the projection statement to each fixed feature horizon instead. Preserve the current warning that a coefficient-one $\sqrt n$ cap is not justified.

### L6 — Repair the global comparison table's Markdown delimiters

**Locations:** lines 244–255, especially 251, 253, 255.

**Issue:** The three-column table uses unescaped vertical bars within its math. Ordinary rows have four pipe characters including the edges. Lines 251 and 255 have six, and line 253 has ten. In standard Markdown table parsing, the absolute-value bars create extra cells; this can shift or drop the audit/conclusion column. This is particularly consequential in the central theorem comparison. Some norm bars are escaped, but the absolute-value bars are not.

**Repair:** Use `\lvert ... \rvert` and `\lVert ... \rVert` inside table cells, or valid escaped table pipes. For example:

```text
$\lvert\rho\rvert\le1-\delta$
$\lvert G_{ij}\rvert\le1-\delta$ for $i<j$
$\max\{\lvert\psi(0)\rvert,\lVert\psi'\rVert_\infty,
\lVert\psi''\rVert_\infty\}\le1$
```

Keep each actual table row on one source line if required by the renderer. This review checked the raw Markdown structure, not a live rendered preview.

### L7 — Repair corrupted mathematical expressions and missing math delimiters

**Locations and concrete repairs:**

| Lines | Current problem | Proposed repair |
|---|---|---|
| 41, 51 | `W^{$\ell$}_{ij}` splits an exponent across math delimiters; first-weight equations are plain parenthesized TeX | Use $W^{(\ell)}_{ij}\sim N(0,1/n)$, $z_a^{(1)}=W^{(1)}x_a/\sqrt d$, and $z_a^{(1)}=V^{(1)}x_a$ in complete math spans. |
| 72 | `(mathbf1)` lacks its command; the expectation `(\mathbb E g'$Y$)` is damaged and has vector/scalar ambiguity | Use $\mathbf1$ and $\mathbb E[g'(Z)]$ with the scalar/law qualification in L4. |
| 108 | $W^{(3)}_0=\sqrt n,e_1$ has a comma in place of a scalar-vector product | Write $W^{(3)}_0=\sqrt n\,e_1$. State that this is the stored readout vector of the two-hidden-layer witness if that is the intended layer convention. |
| 153 | `(r(u)=$1+u^2$^{-1})` does not correctly express the function in line 150 | Write $r(u)=(1+u^2)^{-1}$; write its coefficient as $(-1)^k(2k-1)!!$ in one math span. The zero-radius/smooth-ODE distinction itself is coherent. |
| 159 | `((t,g(t)))` is unrendered notation | Write $(s(t),q(t))=(t,g(t))$. |
| 169 | `(T_0=1/$384\cdot6^4$)` leaves the denominator grouping ambiguous | If the intended denominator is the full product, write $T_0=(384\cdot6^4)^{-1}$, and $C([0,T_0])$ in math. This repairs the apparent intended expression; its numerical theorem constant is not source-verified here. |
| 249, 252–255 | Activations, exponent list, and shape constraints are partly outside math | Put each entire expression in a math span; use the table-safe delimiters in L6. |
| 278 | `(operatorname{Var}\psi(Z)...)` is missing the command slash and math delimiters; the brief variance proof does not mention the centering intercept | Write $\operatorname{Var}(\psi(Z))\le\operatorname{Var}(Z)$. For the displayed ratio bound, select slope $a$ and intercept $e\mathbb E\psi(Z)$, plus any fixed activation offset. This yields the stated numerator bound directly. |
| 296 | `(\phi_\theta=$1-\theta$z+\theta\arctan z)` damages the defining activation | Write $\phi_\theta(z)=(1-\theta)z+\theta\arctan z$. |
| 308 | `(1/$2\theta L$)`, `(1/$12\theta L^3$)`, `(1/$6L^2$)` split denominators across text/math | Write $(2\theta L)^{-1}$, $(12\theta L^3)^{-1}$, and $(6L^2)^{-1}$, with the fixed-$\theta$, large-$L$ qualification in L3. |
| 349, 357, 375, 385, 387 | Projection variables, the generated-query definition, a tail envelope, and the RMS kernel are literal TeX in text parentheses | Put each complete expression in math; in particular $e_R=M(\delta^{(2)}-u_R)$, $\exp[-cR/\log(e+R)]$, and $111/[27+\varepsilon(3+\varepsilon)^2]$. |
| 437, 439, 448, 463 | Symmetry, feature interval, contrast bound, and loss hypotheses are partly unrendered; line 448 obscures division of the entire numerator | Use $f(-x)=-f(x)$, $[c-a,c+a]$, and $\lVert W^{(L+1)}\rVert_{L^2}\ge(1-\varepsilon)/a$. Put the loss hypotheses in complete math spans. |

Also repair the remaining same-pattern raw TeX spans at lines 78, 105, 112, 165, 175, 183, 191, 259, 270, 287, 310, 321, 331, 333, 382, 399, and 406. Ordinary prose parentheses are harmless; the problem is mathematical commands and grouping outside their math spans. These are mostly localized presentation defects, but several affect the actual activation, time horizon, or scaling as printed.

## Coverage of all 36 inventory entries

The following checks coverage against this document's inventory only. “Covered” means that its central reported contribution/status has a recognizable synthesis location; it does not certify a source. A qualified or recovery-only task does not need to contribute a new theorem to count as covered.

| Host group / task | Inventory line | Synthesis coverage and disposition |
|---|---:|---|
| PDE 1 — Resume L3 (4) | 483 | Covered: all-depth bounded shifted-atan theorem, lines 249, 259. |
| PDE 2 — Resume L3 (3) | 484 | Conditional Borel/basis routes retained in inventory; no unconditional result claimed. More method detail is optional. |
| PDE 3 — Resume L3 (2) | 485 | Covered: two-sample offset/separation extension, line 250, with extension qualification. |
| PDE 4 — Resume L3 | 486 | Covered substantively: clipping/adaptation/projection and obstructions, §§8–9. |
| PDE 5 — Generalize non-local GF | 487 | Covered: local theorem and finite-GF corollary, §7.1; time/dependency limits explicit. |
| PDE 6 — L3 nonlinear arctan extension | 488 | Original readout regime and open/retracted status retained; related synthesis at lines 49, 282, 292 and §9. No tiny-readout substitution inferred. |
| PDE 7 — L2 arctan IDE | 489 | Covered: original-readout global result and import qualification, line 282. |
| PDE 8 — L3 linear nonclosure | 490 | Covered: restricted representation barriers, lines 195–202. |
| PDE 9 — Quadratic boundary layer | 491 | Covered: disputed canonical claim explicitly withheld, lines 165–167. |
| PDE 10 — RMS quadratic | 492 | Partial: finite identities/open gates in line 387; global finite action contribution needs M1. |
| PDE 11 — Linear depth program | 493 | Covered: L2, completed L3, and conditional arbitrary-depth bridge, §6. |
| PDE 12 — Quadratic operator IDE | 494 | Partial: QI negative/spectral warning covered; exact reductions/invariants need M1. |
| PDE 13 — Architecture survey | 495 | Partial: negatives covered; H3 positive prefix/definition need M1. |
| PDE 14 — Quadratic nonclosure / DMFT | 496 | Covered: formal no-gos, smooth-ODE correction, conditional DMFT, §5. |
| PDE 15 — Generalized Stieltjes | 497 | Core open transform/reconstruction disposition covered, line 141. Exact observable mobility identities are only named in inventory; more detail optional. |
| PDE 16 — Output-kernel Stieltjes | 498 | Covered: output order 17, hidden Ward extension and open all-order claim, lines 120–127. |
| PDE 17 — Backward-kernel audit | 499 | Covered: calculus and unexecuted concentration step, line 76. |
| PDE-2 1 — Find Resume (4) | 505 | Covered: shape theorem and calibrated-sine local result, lines 253, 312. |
| PDE-2 2 — Find Resume (3) | 506 | Covered: all-depth two-sample and odd-gain results, lines 252, 255, with scientific scale limitation. |
| PDE-2 3 — Find Resume (2) | 507 | Covered: exponent-two result, line 251; private-branch limitation retained in inventory. |
| PDE-2 4 — Find Resume / Work | 508 | Covered: separation, gain/shape, sharp initialization, §§7.2, 7.4; private-history limit explicit. |
| PDE-2 5 — Recovery/reconstruction | 509 | Adequately retained as recovery work; no new theorem inferred. |
| PDE-2 6 — L2 two inputs | 510 | Covered: broad/affine exceptions and separately qualified special variants, lines 247–248, 286–290. |
| PDE-2 7 — Explain audited proof | 511 | Partial: baseline and global finite fitting covered; distinct original L3 local result needs M1. |
| PDE-2 8 — Operator MFP general t (2) | 512 | Partial: linear results covered in §6; corrected sin+cos scope/withdrawal need M1. |
| PDE-2 9 — Operator MFP general t | 513 | Covered: finite-program hierarchy and quadratic/ReLU mesh distinctions, §§4–5. |
| PDE-2 10 — Test scaling | 514 | Mathematical mesh content covered; attribution/history limitation honestly retained. |
| PDE-2 11 — Derive scaling | 515 | Mobility/scaling distinction covered in §3; unavailable nested finite-type theorem not certified. |
| PDE-2 12 — Operator MFP t=2 | 516 | Covered: finite-step/Price/cubic package, §4.1; version-link limitation explicit. |
| PDE-2 13 — Original L3 attempt 2 | 517 | Open original-model continuation preserved with PDE 6/§9; history limitation retained. |
| PDE-2 14 — Original L3 program | 518 | Exact-algebra/open-bridge disposition preserved; changed activation/readout not substituted. |
| PDE-2 15 — Teach L2 theorem | 519 | Appropriately treated as exposition; mathematical result in line 282, no extra review/theorem counted. |
| PDE-2 16 — Tanh and ResNet audit | 520 | Partial: residual/dense distinction covered in §10; stationary/equicontinuity contributions need M1. |
| PDE-2 17 — Continuous-time mirror | 521 | Covered: scalar-particle theorem versus dense gap, §10. |
| PDE-2 18 — Order-five mirror | 522 | Covered: order-five recurrence and finite-step limits, lines 87–103. |
| PDE-2 19 — High-order Stieltjes | 523 | Relevant mathematics covered in §5; no claim that unavailable private amendments were exhausted. |

## Optional suggestions, separate from required corrections

1. Expand the inventory-only Borel/basis route (line 484) and exact observable mobility identities (line 497) by one sentence each, if method-level completeness is desired. The document supplies too little detail to classify these as omitted completed major theorems. Preserve the Borel conditions.
2. Use host-qualified task references throughout, as in the coverage table above. Numbering restarts between hosts, so “task 7” alone is ambiguous.
3. Replace “SG's finite-GF corollary” (line 536) with “the broad local theorem's finite-GF corollary.” SG is not defined in the document. Expand GF, GD, TP, and DMFT at first substantive use for a broader readership.
4. Add a compact per-family column distinguishing the mathematical conclusion from the current source-audit grade. The grade vocabulary in §2 and §7.2 is mostly compatible, but a separate column would make qualified theorems easier to scan.
5. Refresh the workflow references to the pending final document review (lines 3 and 541) when incorporating this review. Their current pending status is not a scientific inconsistency.

## What does not require correction on this evidence

- The canonical unit-metric Stieltjes question may remain open despite negatives for other metrics/activations and a finite positive prefix (lines 120–141).
- A failure of a deterministic bounded-slope estimate does not refute the final annealed negative theorem; the text explicitly retains that distinction (lines 103–112).
- The covariant-Schur claim and the fine-mesh fully trained claim may remain under focused recheck. The master does not currently certify them (lines 165–167, 491, 537).
- The rank-free fixed-program dependency can remain audit-qualified without declaring all importing theorems false or mathematically conditional (lines 28, 221, 238, 429).
- The special-angle, same-label, and mixed finite-GF variants explicitly retain historical status pending representative rechecks (lines 286–290). This is not a hidden claim of fresh certification.
- A complete three-hidden-layer deep-linear theorem can coexist with conditional arbitrary-depth identification and restricted scalar/PDE nonclosure (lines 177–202).
- Canonical finite arctangent fitting can coexist with an open global strong population limit (line 292).
- The three clipping/projection conclusions do not combine into an uncut population theorem; the master explains the missing bridges (lines 314–359).
- Scalar-particle joint width/depth convergence does not fill the dense Gaussian-matrix ResNet gap or prove generalization (lines 389–431, 473).

After the required coverage, scope, status, and mathematical-presentation repairs, an accurately qualified master could receive a CLEAN document-only verdict while the named external proof checks, history gaps, and main global/generalization objectives remain open. This review supplies no external proof certification.
