# Literal convex-mixture follow-up

The user's latest request replaces the large gain by the literal
activation \(\phi_\varepsilon(z)=(1-\varepsilon)(1+z)+
\varepsilon\psi(z)\), with one positive coefficient independent
of hidden depth. Preserve the preceding normalized bounded C2
shape class, original initialization and raw training rule.
Do not hide a large affine gain inside the non-affine component.

The intended target remains the full global trained-limit result
for three separated inputs at every separately fixed finite depth.
Depth-dependent rates and quantitative constants must be
distinguished from the requirement of one activation coefficient.
Failure of a uniform initialized kernel floor alone does not refute
the qualitative trained target.

Current completed analytic candidate: a self-contained proof that
every normalized bounded shape with \(0<\varepsilon<1/2\) has
exponentially collapsing pairwise feature separation and initialized
kernel conditioning as depth grows. It gives an explicit example.
This result explains an obstruction to transferring the preceding
proof, but is not a claimed solution or counterexample to the full
global trained theorem.

Continue independent proof attempts and adversarial validation.
Keep the separately developed gain-based result and its reviews
distinct from this stricter activation constraint.
