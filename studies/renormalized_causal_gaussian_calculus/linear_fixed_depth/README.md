# Linear fixed-depth validation

This stage attempts to prove identity activation for every separately fixed
hidden depth by one common pointed-action theorem. It deliberately does not
invoke the depth-two spectral oscillator or the depth-three cyclic lift.

The promoted physical-time proof is in
`LINEAR_FIXED_DEPTH_THEOREM.md`. Two hostile reconstructions corrected the
source, probability mode, varying-space transfer, and nuclear tails. The
second also proved that global unit feature time is false for every
\(H\ge2\); the theorem now explicitly uses the autonomous physical residual
flow, for which the path-length energy estimate gives global continuation.
The final isolated reconstruction accepted the autonomous physical-time
theorem. This validation gate is promoted; the feature-time negative result
remains part of the evidence rather than being hidden.
