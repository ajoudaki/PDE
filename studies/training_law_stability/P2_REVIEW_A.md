# Independent complete scientific review A of P2

**Verdict: scientific acceptance for the stated local theorem. No required corrections were found.** This verdict concerns the frozen P2 mathematical addition and its proposed scope descriptions. It does not authorize promotion, certify novelty, or extend any conclusion beyond its stated model, interval, topology, probability mode, or observables.

## 1. Identity, isolation, and exact read coverage

Reviewer identity: `/root/p2_scientific_a`. I am distinct from the named authors `/root`, `/root/transport`, `/root/population`, `/root/nonlazy`, assembler `/root`, and selector `/root/selector`.

The only assignment read was `P2_SCIENTIFIC_ASSIGNMENT.md`, all 40 lines. I did not read the study README or history, author startup material, prior research packets, prior verdicts, another review, or another reviewer's findings. I received no desired verdict. I did not contact another reviewer. I performed no candidate edits, Git operations, or training experiments. The only output written is this assigned report. Hash verification read bytes of the manifest-listed files; it did not expose or use the scientific content of the unchanged chapter complement, baseline files, live workflow, or live AGENTS file.

I read every line of the following frozen scientific inputs, including all displayed proof bodies:

| Input | Complete coverage |
|---|---|
| `P2_ADDITION.md` | Lines 1–1423: theorem, C.4.1 transport, C.4.2 common strong flow, C.4.3 actual GD/statistics/observables, C.4.4 activity |
| `P2_DEPENDENCIES.md` | Lines 1–1310: notation; III.F.1–9; A.1–A.2; complete weighted response proof C.2; finite dynamics §§1–4 |
| `P2_DOCS_README.md` | Lines 1–269, including all chapter descriptions, contextual literature descriptions, and final scope statements |
| `P2_GLOBAL_EDITS.json` | Lines 1–22, every old and new replacement string |
| `P2_MANIFEST.json` | Lines 1–31, all metadata and hash entries |

One combined output initially truncated part of dependency lines 1–300. I reread that entire interval in a separate untruncated output. The remaining dependency intervals were 301–540, 541–850, 851–1120, and 1121–1310. Addition intervals were 1–300, 301–610, 611–920, 921–1200, and 1201–1423. No scientific interval remains unread.

Required skill and reference coverage was complete: `/etc/codex/skills/solve-math-rigorously/SKILL.md` (115 lines), `/etc/codex/skills/investigate-conjectures/SKILL.md` (185 lines), and the latter's `references/research-contract.md` (99 lines) and `references/adversarial-audit.md` (121 lines). I used their hypothesis, limit-order, mechanism-preservation, and hostile-audit requirements. No experiment or proof-search reference was needed for this isolated theoretical review.

All paths below are relative to the checkout unless prefixed by `/`. Frozen P2 names in the hash table are relative to `studies/training_law_stability/`.

## 2. Hash verification

I independently computed SHA-256 from file bytes and compared each result with the manifest. Every listed entry matched:

| File | Verified SHA-256 |
|---|---|
| `P2_ADDITION.md` | `b1d34b78bd50354ce2d036046a180a2beba415530472cbd32264684b0b378774` |
| `P2_DOCS_README.md` | `95b14c5a0430a783023d412d0103d8598a476963bad19180e2d4d0e2291bce3e` |
| `P2_GLOBAL_EDITS.json` | `bd802de5b3a69d1c903eb1454f7a5d353195e340ceb16a63ba85de9c820e369a` |
| `P2_DEPENDENCIES.md` | `606fe87a97b91a8cad31a26d9e545c4b51ebefd582e55a7ba1b2311bfa2f9469` |
| `P2_GLOBAL_BASELINE.md` | `8c575acb99ed713ef688cafb39fe9d8d8430e80815d2a69ac6686bac2cc19101` |
| `P2_README_BASELINE.md` | `4d3cf63cf09e2effb3342f96272a754e8f8f127aada44179b893f6e1a36df453` |
| `P2_GLOBAL_EDITION.md` | `1945ef5d407eafd534b32185e952fa3ed479605f3ffec09afd468b6266fd18d9` |
| `docs/NOTATION.md` | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| `docs/special_data_limits.md` | `5b7b48aa5deab320042217a6f284002366a683bf0f7f0b63c05d8167c526a489` |
| `docs/finite_dynamics.md` | `a57d832a0ce0ad2bf40fa1ec574af787d05bade3264b619a8faca5545bc0012a` |
| `AGENTS.md` | `a5e5b3749d9e9ee088c659bc2cf4ddb2d88adf371d98bf34140ded532020a517` |
| `RESEARCH_WORKFLOW.md` | `4323e5ada1a4875af2c8121c742c50f07d8ff5b569c3aa71e11ef9a14605b442` |

This verifies integrity, not semantic equivalence of the assembled edition to its edit instructions. I did not conduct the separate integration audit or read the unchanged assembled complement.

## 3. Reconstructed theorem and research contract

The actual model has two hidden tanh layers of equal width, no biases, input dimension two, inputs `x=sqrt(2)u` with `u` on the unit circle, and labels in the fixed compact interval `[-Y,Y]`, with `Y>0`. Its stored weights have independent initial Gaussian entries of variances `1`, `1/n`, and `1/n^2`. The prediction is `c^T tanh(A tanh(wu))/n`. The loss is the unhalved probability-weighted squared loss. The raw block mobilities are `(n,1,n)`, and every GD update uses the same preceding state. All observation randomness is independent of initialization.

The population state is an explicitly infinite-dimensional state `(w,A,c)`, with the full two-component first row in `L^2`, a bounded middle action between generated probability Hilbert spaces, and an `L^2` readout. The metric is the sum of the full-row `L^2` norm, middle operator norm, and readout `L^2` norm. The initialized action and its reverse are the limiting actions of the same Gaussian matrix and its transpose; the reverse is the actual Hilbert adjoint. Learned increments are Hilbert–Schmidt. No finite-dimensional scalar closure is claimed.

The common local interval and all law-stability constants may depend on `Y` and this fixed architecture/initialization/support, but not on atom counts, weights, Gram rank, width, sample count, or a sufficiently small actual step. The theorem establishes a unique strong autonomous flow for every observation law, a near-linear Wasserstein law modulus for the full state and forward observations, replacement stability of the deterministic limiting learner, and a bound on `sup_t |E_S[population risk - empirical risk]|`. It establishes simultaneous finite-width/raw-GD/data-law consistency in probability without a relative growth condition, with uniform predictions and the two risks on that local interval. It separately proves a positive same-neuron, initial/current activation displacement in both layers on a specified relative open family of laws.

The fixed reference oracle is a proof approximation whose coefficients come causally from earlier population Euler nodes. It is removed from the final dynamics and from the finite algorithm. The activity time is defined from the existing reference flow; the theorem does not claim a numeric time bound or an executable finite-dimensional surrogate. There is no trajectory playback in the autonomous equation, hidden finite-dimensional compression claim, empirical coefficient fitting, or substitution of an easier optimizer.

## 4. Dependency audit

### 4.1 Finite Gaussian programs, degeneracy, and source responses

The supplied III.F. proof is sufficient for the fixed programs used here. Its admissible root tuples have fixed finite second moment and are independent of all initialized matrices. Coordinate maps first have bounded derivatives; both orientations of each reused matrix are retained. The operator norm estimate follows from a `1/4` sphere net, the bilinear Gaussian tail, and a finite union bound. The numerical threshold 10 is more than sufficient: the exponent `2 log(9)-100/8` is negative.

The adaptive-conditioning argument explicitly conditions on the existing transcript before exposing the next linear answer. The conditional mean and projected Gaussian residual solve both forward and transpose constraints. Normalized removal of a projection of fixed rank has squared mean `rank/n`, so it vanishes. The conditional empirical-law calculation controls bounded tests and second moments and thus yields the claimed `W_2` mode. It never assumes that trained coordinates are independent.

The source-response identities retain the response correction from the opposite orientation. In the nonsingular case, the Gram factor produced by Gaussian integration by parts cancels the inverse query Gram. I checked the forward calculation: orthogonality removes the old forward-input correction from `E[q_s h_perp]`; substitution cancels the old response part in `Y alpha`, leaving the stated source plus reverse-input correction. The transpose calculation has identical types with the roles interchanged. Independence of oriented source groups is not confused with independence of the resulting matrix answers.

For singular queries, a fresh independent Gaussian perturbation for each call gives a positive Schur complement at each fixed positive perturbation. The same-array comparison has a finite program-dependent `O(epsilon)` RMS error on a high-probability operator event. The zero-noise limit uses continuity of positive-semidefinite square roots and convergence of coefficients/derivative expectations, rather than continuity of a pseudoinverse. The statement concerning formal derivatives on singular Gaussian support is correctly contracted with reverse inputs: ambiguity in a null direction has zero `L^2` contribution. These arguments cover duplicate, zero, and linearly dependent queries. No rank-stability assumption is reintroduced downstream.

### 4.2 Common action spaces and extensions to neural products

III.F.7 constructs a countable language closed under both action orientations and dense smooth coordinate functions. Finite unions have compatible laws because they use the same arrays and unused instructions change no finite vector. Passing normalized second moments through the finite norm bound makes the limiting assignment bounded, linear, and well-defined on `L^2` equivalence classes. The supplied cylinder/simple-function density proof makes its domain dense in each generated `L^2` space. Passing the exact finite transpose pairing on this dense set establishes the adjoint identity on the completed spaces.

The full Gaussian pair, rather than one training projection, is included in P2's language. Rational approximation and the continuous-value extension therefore represent arbitrary circle directions and any separately fixed list of passive inputs. The language can be fixed independently of the law. No arbitrary action extension depending on a law is used.

A.1 handles continuous maps of at most linear growth by `L^2`/`W_2` continuity and a carefully ordered smooth approximation. In particular `phi'(z)q` is continuous with a linear envelope. The predecessor tolerance is selected after fixing the smooth map, so an increasing clipping Lipschitz constant is not silently treated as uniform. A.2 supplies the needed response formulas for these neural products: with deterministic coefficients frozen, values have linear envelopes and first source derivatives have fixed-degree polynomial envelopes. SubGaussian roots and finite Gaussian source lists give the required moments and uniform integrability at each fixed program. This is enough for C.2; no growing-program or empirical higher-moment theorem is used.

The strong multiplier and curve-chain-rule proofs III.F.9 correctly avoid claiming Frechet differentiability of a nonlinear coordinate map on an entire `L^2` space. Hilbert–Schmidt rank-one identities in III.F.8 have the proper finite representative `uv^T/n`; their norm is `||u|| ||v||` on probability Hilbert spaces.

### 4.3 Weighted response and tails

I reconstructed C.2's proof rather than accepting its conclusion as a black box. Its required preliminary RMS and residual bounds are supplied in P2 by bounded tanh and the state ball. All mobilities equal one, `|G_ab|<=1`, each first preactivation has variance one, and the population readout is zero. These satisfy the stated assumptions, including the more permissive root assumptions of C.2.

The exact memory and response formulas carry `Delta omega_b` in learned terms. The norm `N(U)=sup_{p>=2} ||U||_p/sqrt(p)` permits sums without independence. Jensen uses probability weights proportional to `Delta omega_b`, and only individual marginal bounds are needed. This is not a Gaussian maximum estimate.

For a forward derivative row, the direct source contributes one; earlier memory contributes a weighted sum of backward magnitudes times the previous derivative maximum. The discrete Gronwall exponent consequently contains a weighted time/input sum. Cauchy–Schwarz bounds the expectation of the product with the single current backward field. The maximum over derivative rows is used pointwise, while no norm of a maximum of Gaussian fields is invoked.

For a single reverse-source pulse, differentiating the direct term gives exactly the indicator for its own input/time. Its first effect on a first-row update or forward memory has size proportional to `Delta omega_b`. Gronwall preserves this factor. Thus the bound written as a quotient by `Delta omega_b` does not produce a constant proportional to `1/omega_b`.

The cap choices are not circular: forward-response caps are selected upward using their zero-time prefactors, then backward-response caps downward, then time is reduced so all finitely many exponents are small. At each mesh time the forward construction uses past backward fields, and the reverse construction uses already computed higher-layer responses. The explicit causal induction closes. Its constants do not depend on a number of observations or steps. Variable positive step lengths and a final partial step preserve exactly the weighted sums, so the interpolation extension is also valid. The proof supports the integrated individual Gaussian tails used by P2, not a tail bound for a path or observation supremum.

### 4.4 Exact finite equations

Differentiating the normalized prediction gives `grad_w f=delta^1 u^T/n`, `grad_A f=delta^2(h^1)^T/n`, and `grad_c f=h^2/n`. Multiplying the unhalved-loss derivatives by `(n,1,n)` gives precisely (T2), with the finite rank-one action divided by `n`. In particular the first row does not carry a missing input or width factor. The supplied finite energy and continuation proofs are internally consistent, but P2's GD argument needs only its direct ball bound and does not incorrectly import continuous-flow energy dissipation into GD.

## 5. Audit of the new theorem

### 5.1 Full-row transport and local bounds

Locations: addition lines 139–361.

The state ball gives `||H^ell||_2<=1`, `||delta^2||_2<=B`, `||P^1||_2,||delta^1||_2<=B^2`, and `|r|<=B+Y`. These give the stated sum velocity bound. With initial individual norms at most 11, `B=24` is valid; the Gaussian full-row norm tends to `sqrt(2)`, the initialized action is at most 10 with high probability, and the squared readout RMS has expectation `n^-2`. The time chosen in (T5) leaves adequate room for Euler interpolation and first-exit control.

I checked the key transport decomposition. Changing an input costs `||bar w||_2 |u-u'|` in the full first preactivation. Bounded actions and Lipschitz tanh propagate this through all forward fields and predictions. For a backward gate, cutoff of the reference multiplier gives `2R ||Z-bar Z||_2+2 tau_R(bar P)`. At the next layer, the previous backward error is multiplied only by a bounded operator and bounded gate, while the new gate contributes an additional term linear in `R`. Thus the coefficient is proportional to `1+R`, not `R^2`.

The first-weight gradient has the separate changed-input term `bar r bar delta^1(u-u')`; this is present and bounded. The matrix gradient uses the exact rank-one difference formula. All tails depend only on the second marginal of the coupling, so their integrated form is the reference-law weighted sum. Taking the infimum of coupling costs is justified without existence of an optimizer. The same argument uses precisely the finite normalized norms. No control of a Gaussian first-row/backward product or training-data maximum is hidden here.

### 5.2 Strong integration, finite-law construction, and arbitrary-law completion

Locations: addition lines 363–713.

Joint state/input continuity of backward fields follows from bounded-multiplier continuity and operator/adjoint continuity. Compactness of the input circle upgrades convergence to uniformity in input for a convergent state sequence. Each observation integrand is continuous into the appropriate Banach space with compact, separable range, resolving strong measurability even though the ambient bounded-operator space need not be separable. The middle integrand is continuous and bounded also in Hilbert–Schmidt norm, so learned time increments belong to that class.

Finite-law Euler calculations fit the common fixed-program construction. Projecting the full-row update reproduces the active Gram recurrence without requiring that active directions span the full input space. C.2 provides the necessary individual reference tails uniformly in mesh and finite law.

Comparing two Euler meshes with the same initialization gives (P13a): the preceding-grid discrepancy adds only their mesh sizes times the speed bound. The Gaussian remainder survives scalar Gronwall as `exp(aR-cR^2)`. Fixing `R`, removing meshes, and then removing `R` makes the family Cauchy in the complete strong path space. Continuity of the field passes the integral equation to its limit. This constructs a strongly `C^1` finite-law solution; it is not merely prediction compactness.

Every compactly supported observation law has the stated finite-net approximation. The finite-law comparison gives Cauchy convergence of full states for such approximations. Joint continuity of the vector field in state and law, together with compactness of the time interval, passes the strong equations uniformly in time. No boundary-zero partition or Gram inverse is required.

The tail passage for arbitrary laws is carefully weaker than a pointwise uniform subGaussian claim. Bounded Lipschitz truncations of the exponential first pass through uniform-in-input `L^2` convergence; then weak law convergence passes their integrals; monotone convergence removes the truncation. Cauchy–Schwarz over the law yields the integrated RMS tail. The resulting constants are uniform in time because each time has the same bound; this does not interchange expectation with a time supremum.

### 5.3 Uniqueness, restart, and quantitative modulus

Locations: addition lines 714–783.

A competing strong solution needs no tail assumption: use the constructed solution as the sole reference in the one-sided transport estimate. The first-exit bound keeps the competitor in the common ball, and the upper bound `C exp(aR-cR^2)` tends to zero. This proves the advertised uniqueness. It also proves independence of finite-law approximation choices.

For `0<q<=1`, taking `R=K sqrt(log(e/q))` with `cK^2>=2` makes the remainder at most `q^2`. The factor `1+R` and the Gronwall exponential are absorbed into the displayed near-linear modulus. It tends to zero. The proof treats `q=0` by equality and `q>1` by the bounded state/prediction ball, without evaluating an invalid logarithm.

Restart means continuation of a reached state on the remaining original local interval, among strong solutions in the stated ball. That assertion follows from the same one-reference uniqueness argument. Restarted Euler paths can be compared directly with the existing reference path without assuming new Gaussian tail bounds for them. Closing current coordinates under coordinate operations and both actions retains the law integrals by finite-sum approximation. The state therefore has sufficient information to restart; response histories are proof devices rather than omitted dynamical state. No global or arbitrary-state restart theorem is inferred.

### 5.4 Actual simultaneous raw GD and prediction interpolation

Locations: addition lines 789–969.

The fixed proxy is a finite deterministic-coefficient program at a fixed finite reference law and proof mesh. Its parameters retain the actual initial first and middle arrays, plus the actual random initial readout as an additive term. The oracle's limiting readout root is zero, but the finite algorithm's readout is not replaced by zero. Its normalized discrepancy vanishes by Markov's inequality.

Recomputation from proxy parameters differs from oracle actions by finite sums of contraction errors, exemplified by (A4). Each contraction converges and each multiplying node has bounded RMS in probability. The transpose expansion has the appropriate analogous contraction. Backward consistency uses cutoff of a fixed oracle multiplier; width is taken first for each cutoff, followed by removal of the cutoff. The assigned velocity error consequently tends to zero in the full same-width state metric.

The weighted finite tails (A6) follow from same-array closeness and continuous cutoff second moments. The proof does not need empirical convergence of a discontinuous tail indicator. It takes a maximum only over the fixed coarse grid and weights the individual reference-input tails. The proxy ball and speed bounds follow from initial norm bounds and a finite sum of normalized rank-one increments; their limiting upper constants depend on total time, not on the chosen mesh or reference cardinality.

The comparison with actual GD uses its preceding fine state and the proxy's preceding coarse state. Their distance from their own interpolants costs at most `C(eta+Delta)`. The transport estimate imposes no tail hypothesis on the actual state or actual dataset. Therefore (A7) legitimately permits arbitrary actual atom counts, weights, and a number of GD steps increasing without restriction relative to width.

At fixed reference/mesh, appended passive-input and intermediate-time forward evaluations are still fixed finite programs. Input and time Lipschitz bounds allow finite nets to prove uniform prediction convergence. Intermediate-time parameters are affine, and forward quantities are recomputed there; the proof does not substitute affine interpolation of predictions. It never compares operators on different-width or finite/population spaces in operator norm.

The limit order is explicit and sufficient. The probabilistic limit is first taken at a fixed finite reference, fixed mesh, and fixed cutoff. The proof mesh and reference-law errors are then removed, and finally the cutoff remainder. Equivalently, for an error tolerance one first selects the cutoff, then a fine fixed reference/mesh, and only then a large actual index. All constants in the deterministic comparison are uniform in the actual law. This validates the simultaneous claim without promoting a fixed-program theorem to a growing-program theorem.

### 5.5 Risks, random observations, and ghost exchange

Locations: addition lines 971–1070.

On the high-probability state ball, bounded predictions and uniform input Lipschitz constants imply a common joint-observation Lipschitz constant for squared loss. Predictor error controls either risk, and Wasserstein error controls changing the law for the fixed limiting predictor. This proves both uniform-in-time risk limits in probability; no expectation convergence requiring control of the exceptional initialization event is asserted.

The elementary finite-partition proof gives empirical `W_1` convergence for every law on the compact observation space, including atomic or singular laws. The reference-program random errors in (A7) do not depend on the actual sample, so a union bound with empirical-law convergence proves the iid result. There is no unproved supremum of fixed-program error over all datasets.

For one replacement, coupling the unchanged observations gives `W_1<=diam(Z)/m`. The proof applies the unoptimized cutoff bound directly, so it does not assume that the displayed formula for the modulus is monotone on the whole interval. The finite set of small sample sizes is covered by the bounded prediction difference. Identical replacements and coincident observations are included.

I checked both equalities of (A12). Independence first replaces the population risk by evaluation on an independent `Z_i'`; exchange of the iid pair `(Z_i,Z_i')` then turns `ell(f_S,Z_i')` into `ell(f_{S^(i)},Z_i)`. The second term remains `ell(f_S,Z_i)`. The same deterministic measurable empirical-law map is used on both samples. Uniform replacement stability bounds every resulting loss difference. Taking absolute value of its expectation and then a supremum over deterministic times gives exactly (A13). It does not give `E|gap|`, `E sup_t |gap|`, or a data-selected stopping-time guarantee. The theorem and guide preserve this order.

### 5.6 Paired activations and positive displacement

Locations: addition lines 1072–1423.

The observable is the squared normalized difference on the same neuron indices at initial and current times. Its finite value is bounded by four, and it has the claimed state/input/time continuity on the ball. Retaining both times in a fixed oracle's joint second moments proves its limit. Predictor convergence alone would not have sufficed; the supplied proof does not make that substitution.

For the reference law, `p=Y/4` and `-2 omega_b=-1` give the correct physical factors. Independent lower Gaussian coordinates and odd tanh yield the diagonal initial covariance `q_0 I`, with `q_0>0`, hence independent upper Gaussian coordinates of positive variance.

I rederived the expansion: `c(t)/t -> 2S`; multiplying by the upper gate gives `delta_a^2(t)/t -> 2U_a`; adjunction and the lower gate give the lower backward limit. Since the residual tends to `-y_0`, integration of these linear-in-time velocities produces `2t^2 T_a` for the lower preactivation and `2t^2 sum_b p U_b tensor h_b` for the middle increment. Bounded-multiplier continuity yields `2t^2 C_a` for the lower activation. Expanding the actual product `A(t)H_a^1(t)` retains both the moving-matrix term `M_a=pq_0U_a` and the moving-lower-feature term `A_0 C_a`; their cross increment is of order `t^4`. This gives the displayed upper activation coefficient `E_a` with the correct factors. No formal-series existence assertion is needed.

Actual adjunction gives `<h_a,P_a>=<xi_a,U_a>`. The cross upper-input term vanishes by independence and oddness; the remaining integrand `xi_a tanh(xi_a) phi'(xi_a)` is positive almost surely and integrable. Thus every `P_a` and `C_a` is nonzero. For the upper layer, identity (9) is a sum of nonnegative terms with a strictly positive term; it precludes cancellation of both upper coefficients. This proves the required averaged upper activity, without unnecessarily asserting activity of each upper input separately.

The expansion implies positive `c_1,c_2` and a positive interval on which the leading term dominates its remainder. The definition using `s_0` is legitimate because the defining set is nonempty and downward closed; choosing `t_0=s_0/4` stays strictly within it. The lower activity margin and continuity estimate for `J_ell` then yield a positive Wasserstein radius. Changing the averaging law costs at most the input Lipschitz bound; changing the evolved state costs the law-to-state modulus. A radius satisfying the bound for every smaller positive `q` exists merely from its limit at zero, without a monotonicity assumption.

The relative open ball contains nonatomic laws obtained by small arc/label spreading and correlated finite laws obtained by small angular perturbation. The paired-activation limit transfers the fixed positive margin to both finite layers by a two-event union bound. The conclusion is positive absolute hidden activation displacement at a fixed local physical time, independent of width/sample/step in the limit. It is not a fitting, risk-improvement, relative-nonlinearity, or universal-activity theorem.

## 6. Explicit hostile attacks and outcomes

| Attack | Check actually performed | Outcome |
|---|---|---|
| Point mass or coincident inputs with conflicting labels | Reconstructed the vector field as an integral; inspected every transport and completion denominator | Covered. No Gram inverse, interpolation requirement, or uniqueness-of-label assumption occurs. |
| Singular query/data Grams | Read the full perturbation/source-response proof and nullspace contraction argument; checked use in finite Euler programs | Covered. Only fixed positive perturbations use invertible Grams; perturbations are removed after fixed-program width limits. |
| Arbitrarily tiny positive atom weights | Followed a single backward-source pulse through (31)–(32), cap choice, and weighted tail integration | Covered. Its factor `Delta omega_b` is preserved; there is no weight-dependent constant. Zero weights may be omitted. |
| Many observations or fine steps | Traced the Jensen and derivative-row bounds; identified every maximum | Covered for the claimed result. Maxima of derivative rows and a fixed coarse grid do not become Gaussian-data or fine-history maxima. |
| Passive input outside the span of training directions | Checked full Gaussian root pair, full-row metric/update, and passive net evaluations | Covered. A one-direction law does not erase the unused root coordinate. |
| Zero signal or zero conditional label mean | Substituted `c=0`, unchanged `w,A`, and `E[y|x]=0` into (T2) | Stationary population solution exists and is unique. This is compatible with the restricted activity family. |
| `q=0`, `q>1`, and all small sample sizes | Checked the separate bounded-ball cases and direct replacement cutoff optimization | Covered without evaluating the logarithm outside its domain or assuming global monotonicity of the formula. |
| Actual random readout | Computed its expected squared RMS and tracked its additive presence in proxy parameters | Covered. It is retained in actual GD and both-time observables and removed only as a vanishing initialization error. |
| Noncommuting block updates | Recomputed the finite gradients and checked the proxy/actual assigned velocities | Covered. All blocks use the same old state; recomputation is only for observation of interpolated parameters. |
| Arbitrary relative sample/width/step growth | Reconstructed the fixed-reference triangle and order of approximation choices | Covered in probability on the local interval. No quantitative width rate is produced. |
| Tail escape in the limiting law or competing solution | Checked exponential truncation under changing laws and the one-reference uniqueness argument | Covered in the integrated reference topology. No unsupported uniform continuum/path tail is asserted. |
| Upper-layer cancellation or lazy readout-only motion | Recomputed both upper second-order terms and the positive adjunction identity | Ruled out on the specified reference and its relative open neighborhood, with the stated averaged observable. |
| Confusing signed expected gap with absolute generalization error | Performed the iid exchange explicitly and checked the placement of all operations | The stated weaker expected-gap bound is proved; stronger orders are neither proved nor claimed. |
| Hidden state or oracle playback | Checked the fixed-program coefficient provenance and reached-state restart argument | No loophole for the stated infinite-dimensional autonomous state. The theorem does not promise finite-dimensional compression. |

These were theoretical checks, not simulation outcomes. No numeric experiment is needed for the signs, normalization, or limiting arguments above.

## 7. Guide and scope edits

The new C.4 description in guide line 155 matches the candidate: fixed two-hidden-tanh model, all bounded-label laws on the normalized input circle, local quantitative law/replacement stability, correctly qualified expected gap, simultaneous sampling/width/GD consistency, and an open activity family. Guide lines 254–261 explicitly distinguish the input-population theorem from a global theorem and distinguish absolute value of expectation from expectation of absolute value. The guide's broader scientific aspirations earlier in the document are presented as questions, not as conclusions of C.4.

All five old/new replacements in `P2_GLOBAL_EDITS.json` make the necessary exception for C.4 while preserving the narrower claims of the other units. They identify C.1 and C.3 separately and restrict C.4 activity to its specified open family. Removing a hard-coded proof-unit count adds no scientific claim. I found no new claim of quantitative finite-width replacement, useful risk reduction, fitting, global-time control, or universal activity.

I read the entire guide, but did not independently re-certify its unchanged descriptions of unrelated chapter theorems or its nondependency literature context; their full source chapters/papers are outside this isolated assignment. This does not leave a missing dependency for C.4, whose necessary arguments are all supplied in the frozen packet.

## 8. Component verdicts, corrections, and limits

| Component | Verdict |
|---|---|
| Fixed Gaussian programs, actual source responses, singular queries | Pass |
| Common generated spaces, full Gaussian row, bounded actions and actual adjoints | Pass |
| Value/response extensions and complete weighted marginal-tail proof | Pass |
| Exact finite normalization, full-row transport and local state ball | Pass |
| Strong law integrals, arbitrary-law completion, uniqueness and local restart | Pass |
| Optimized law modulus and replacement stability, including boundary cases | Pass |
| Actual raw-GD proxy comparison and unrestricted relative joint growth | Pass |
| Uniform prediction and both risk limits in probability | Pass |
| Precise ghost exchange and expectation/time-supremum order | Pass |
| Paired-activation convergence and positive two-layer displacement on the relative open set | Pass |
| New guide and chapter scope claims | Pass within the assigned scientific scope |

**Required corrections:** None.

**Optional suggestions:** None needed for scientific acceptance. In any later exposition, retain the existing explicit statements that the limiting state is infinite-dimensional, that the expected-gap order is weaker than an expected absolute gap, and that the activity time/margin are existential and local. Those statements already appear in P2 and are not requested changes.

**Missing necessary inputs:** None found. The supplied frozen dependencies contain the mathematical arguments needed for every new theorem component. Baseline/assembled semantic integration, authorization for promotion, unrelated historical chapter claims, novelty, quantitative rates, and empirical utility were not assessed here.

**Completion:** The complete isolated scientific review is finished. All assigned new scientific lines and all necessary dependency proof bodies were read and reconstructed, hashes were verified, the requested boundary cases were attacked, and this report records the original findings. There is no unresolved required correction or unreported proof gap in the stated P2 result.
