# Route S post-freeze evidence ledger

This ledger supersedes the open statuses in the pre-freeze
`EVIDENCE_LEDGER.md` without modifying any byte covered by the primary freeze.

### S-1: chronological fixed-depth compiler

- Statement: the typed arbitrary-depth compiler specializes at `H=2` to the
  accepted order-five compiler.
- Status: **algebraically audited**.
- Evidence: zero exact-rational differences in both the arbitrary-`Q0`
  layer-tagged and unit quotients for all `A,B,C` atoms.

### S-2: explicit H=3 and H=4 normal forms

- Statement: finite terminal tagged and unit-Gram formulas exist for `A,B,C`.
- Status: **algebraically audited**.
- Evidence: four frozen CSE files, four fully distributed maps, terminal
  derivative ceilings `(1,3,5)`, and byte hashes in
  `PRIMARY_FREEZE_MANIFEST.json`.
- Independent falsification: zero discrepancies against the separately
  frozen route over every one of the 492,000-plus tagged/unit coefficients.

### S-3: arbitrary Q0

- Statement: the layer-tagged formulas retain the correct symbolic first-layer
  metric dependence.
- Status: **algebraically audited**.
- Evidence: exact independent comparisons at six rational interpolation
  points, explicit degree bounds `(1,3,5)`, and an unused `7/2` holdout, all
  with zero discrepancies at both depths.

### S-4: controls

- Statement: constant, affine, linear, quadratic, and smooth nonpolynomial
  evaluations are consistent across routes.
- Status: **algebraically audited** for polynomial/analytic moment evaluation;
  **empirically supported** for the finite-width nonpolynomial discriminator.
- Evidence: independent `CONTROL_AUDIT.json`; leading-width path/Wick linear
  enumerations; 80-digit normalized-sine Fourier atoms; two-oracle seedwise
  gate; preregistered 7,700-network regression pass.

### S-5: annealed limit

- Statement: for each fixed `H=3,4`, the terminal coefficient equals
  `lim_n E[D_n^k f_n]` for `k=1,3,5`.
- Status: **theorem-level under stated assumptions**.
- Scope: polynomially smooth `C-infinity` activation with polynomial growth
  of every derivative, using Golikov--Yang Theorem 3.7; alternatively
  conditional on a weaker probability limit plus the displayed uniform
  `(1+epsilon)` moment bound.
- Nonclaim: no simultaneous `H=H(n)` result or depth-uniform constants.

### S-6: arbitrary-fixed-H recurrence

- Statement: the response/Wick--Stein compiler has 66 deterministic registry
  states per hidden matrix at order five and emits a finite terminal formula
  for every separately fixed depth.
- Status: **exact finite construction**.
- Limitation: fully distributed map size grows rapidly; the claim is not a
  constant-size scalar closure.
- Deep-linear extension: `A_H=H+1` is proved; the displayed closed forms for
  `B_H,C_H` are exact finite-difference discoveries through `H=10` and remain
  a conjecture until a symbolic depth-degree/transfer proof is supplied.

