# Root read of assembled special-angle document

2026-09-06. Entire 1935-line completed candidate read by root at SHA256
5cd54a6125dfbf4b7d1bc86149760b842d7d2c1f9a44fe73fcf57f5d3d15f12e.
This is a root integration check, NOT an independent isolated audit.
Three fresh complete-document reviewers are running and their verdicts
have not yet returned. Source kept unchanged for that review round.

The assembled argument includes the previously missing exchange-law
calculation: reflection swaps equal-norm inputs, negation of readout
preserves the opposite-label loss/metric, finite laws are invariant,
and deterministic limiting scalar norms/predictions inherit symmetry.
There is no false finite pathwise symmetry at rho=0. The antiparallel
raw factor four, one-field energy normalization, and actual nonzero
finite readout are retained.

The Gaussian-source forcing argument explicitly takes fixed mesh,
nonzero independent query-answer root, width limit, Gaussian integration
by parts in that local root, then perturbation to zero. It includes
empirical-feedback changes in the finite comparison and does not infer
an off-support derivative from an unforced singular source law.

The all-time nonaffinity argument uses unbounded tails with bounded
Gaussian remainders, not merely the nonlinearity of the scalar formula.
The full-kernel expansion includes the positive readout contribution
as well as the hidden contributions. Scope remains rho=0,-1 only.

Required exposition correction before final handoff: the assembled
document reintroduced normalized-inner-product and normalized-norm
aliases in (8) and later. The user explicitly requested ordinary norms
with explicit 1/n and 1/sqrt(n). Replace these consistently in a later
version, then repeat complete-document reviews; do not mutate the
snapshot while its current independent reviewers are working.

Clarification to consider in that same revision: in theorem 1.3,
"Every original hidden-field velocity" should name the preactivation
and activation velocities explicitly, as actually enumerated/proved
in (73), rather than risk an unintended claim about derivatives of
every backward query and delta. No broader backward-velocity claim
is needed for the stated task contract. Do not silently weaken any
explicit target observable; check exact contract at integration.

No mathematical theorem PASS is issued by this root record. Await
the three independent complete-document audits and repair all required
issues before a clean revised round.
