# Independent mathematical audit

**Verdict: PASS.** I found no remaining substantive correctness or completeness objection to the manuscript's actual stated results, and require no repair. This verdict covers the two-input theorem, the three-input initialization and conditional trajectory results, and the foundational appendix at its own stated activation. It does not assert the open global theorem for the odd three-input model.

## Reading coverage, identity, and isolation

The sole mathematical reading input was `/tmp/report-cf533a099202/REPORT.md`, 5,743 lines. Its SHA256, checked before and after reading, was

`daa5a57856db4edc58e58f5f9efff12fbe6e0b442b503e1ef731eecfd444ae7b`.

I read the entire document, including every appendix and the final Part N, in the following nonoverlapping bounded chunks: 1–300, 301–600, 601–900, 901–1200, 1201–1500, 1501–1800, 1801–2100, 2101–2400, 2401–2700, 2701–3000, 3001–3300, 3301–3600, 3601–3900, 3901–4200, 4201–4500, 4501–4800, 4801–5100, 5101–5400, and 5401–5743. Every read completed without output truncation. No section was substituted by a search result or another review.

I did not read skills, AGENTS files, directory listings, other workspace files, other agents' findings, task history, prior drafts or reviews, or web material. I did not edit the manuscript or run numerical experiments. Tool use consisted of reading this file, counting its lines, hashing it, and writing this review. The audit is based on the proofs actually reproduced in this file.

## Scope and quantifiers

1. Theorem T.1 fixes one strictly positive coefficient before dimension, geometry, labels, width, mesh, and physical horizon. The displayed recipe supplies that order: all primitive bounds depend only on separation, and the source restriction is evaluated at the uniform numerical bounds, although each affine path ends at its own hitting time.
2. The two-input claim includes both label sectors and both signs of the correlation, with only realizable normalized geometries quantified. The closed orthogonal endpoint at delta equal to one creates no inconsistency. In dimension one the separated two-input class is empty, as allowed explicitly.
3. Compact-time convergence is not silently strengthened to uniform convergence over datasets or over the infinite half-line. The canonical population is specified by the generated Gaussian action construction, not by arbitrary operators satisfying an operator bound.
4. The three-input chapter explicitly separates proved initialization geometry and algebraic directions from conditional conclusions along a canonical strong solution. Its global existence, uniqueness, all-time regression gap, and algorithmic bridges remain open for the odd activation under pairwise separation alone. Treating that declared open question as a failure of T.1 would misread the statement.
5. Appendix C's main theorem has the shifted high-gain activation. The report does not apply that main theorem to the odd mixture. The imported intermediate proofs admit the changes checked in the main text: removal of the offset, two sample slots, gain in [1/2,1], and the new reference argument.

## Two-input algebra, affine reference, and global clock

**Normalization and symmetry.** The raw gradients in T.6 have the stated metric factors: the first block gets the inverse factor n/d, the two middle blocks keep the 1/n rank-one normalization, and the readout inverse metric cancels its prediction normalization. The four kernel blocks are the corresponding blockwise gradient Grams. Oddness and even gates give the exact finite label-folding identity. The population sample-exchange argument is made at deterministic limiting contractions before uncut uniqueness is used. It therefore does not presuppose the uniqueness it is meant to support. The finite random realization is correctly excluded from the scalar prediction identity.

**Affine active/inactive decomposition.** Equations A.3–A.4 have the correct gain powers and input variance factors. The active dynamics do not depend on the inactive first-layer Gaussian root. The conditional identity A.10 requires that independence; it is supplied by orthogonality of the input projections. Ordinary Frobenius norms of the learned increments stay bounded in a fixed transcript because each update is uv^T/n, whose Frobenius norm is the product of normalized vector norms. The product increment BA−B0A0 has the same property. Conditional mean-square estimates thus establish exact inactive freezing in the population limit. A mere Hilbert–Schmidt/operator bound would not establish this, but the report supplies the missing finite Gaussian argument.

The cross contractions in A.13–A.14 remove covariance with the active field. Affine scalar programs remain linear in Gaussian sources, since contractions are deterministic coefficients; their strong Euler limits consequently remain Gaussian. The frozen inactive variance yields the uniform lower variance delta/32 in every layer. This argument works in either label sector, including near either excluded angular endpoint.

**Existence of the affine endpoint.** The affine polynomial field is locally Lipschitz on the raw Hilbert increment space. C''=JJ* C implies convexity of the readout norm and therefore g' at least its initial projected kernel. The initial kernel is at least delta/128. Gradient energy up to the first hit of 3/2, followed by Cauchy–Schwarz, gives both the displacement estimate and a strong endpoint. This excludes a finite maximal endpoint below the target and proves the target is reached by time 192/delta. The bound is used only through the reference's own hitting time, avoiding an unsupported extension of a polynomial flow to the larger uniform time bound.

**Perturbation and nonaffinity.** The same-state nonlinear-minus-affine estimates require only bounded arctangent, bounded gates, and bounded action norms. They are cap independent. Comparing with the affine field on the larger ball, rather than assuming an uncut nonlinear Lipschitz constant, gives the stated stopped comparison. The cutoff gives strict interior slack and an endpoint prediction above one.

The Hermite calculation Q.1–Q.3 is correct: integration by parts gives the negative integral with factor 2 nu^3, Jensen against t exp(−t) gives the denominator (1+4nu^2)^3, and the resulting function is increasing in nu. The regression stability estimate Q.5 is also valid for arbitrary square-integrable coupled variables, including a constant one: monotonicity and 1-Lipschitzness place an optimal slope in [0,1], so transferring the optimal affine predictor costs at most twice the L2 perturbation. Absorbing the linear part gives exactly the factor e^2. These facts produce the displayed all-time margin with its stated constant; they do not merely prove a positive variance.

**Physical continuation.** After constructing the uncut feature path, the strong chain rule gives C''=JJ* C for the nonlinear path as well. The same convexity argument ensures a strictly increasing g. The first hit of one occurs strictly before the constructed endpoint. A bounded feature derivative implies 1−g(s) is at most a constant times the remaining feature time, making the physical clock diverge. The resulting physical path exists for every time and stays within the controlled feature interval. The loss exponent follows from g'_s at least delta/128. For capped paths, the report only needs continuity, a first hit, and bounded derivative; it correctly does not assume that a capped field is a gradient or that its prediction is monotone.

## Foundational Gaussian and functional-analytic proofs

**F.1–F.4.** I checked the adaptive conditioning induction, the minimum-norm conditional mean in F.6, its compatibility relation, and the two-sided Gaussian residual. The discarded output projection has normalized expected square rank/n. The source-response formulas retain the complete opposite-orientation query list and follow by Gaussian integration by parts after cancellation of the old forward regression terms. The Gaussian source covariance is the full second moment of the actual input, not a centered or regression-residual substitute. Independence applies to oriented source groups, not to the answers themselves.

The singular-query proof adds fresh input noise at each call, proves a positive Schur complement at fixed noise, and compares finite programs using bounded action norms. Continuity of the scalar recursion is established with covariance square roots, linear growth, bounded first derivatives, and finite chronological induction; it does not assume continuity of a pseudoinverse. This also preserves the separate formal-source convention when sources coincide or have zero variance. Causal empirical scalar feedback is handled by a finite comparison with deterministic limiting contractions and does not require differentiating feedback in formal source derivatives.

**Common action spaces.** The countable generated construction includes both action orientations and a dense coordinate language. Deterministic limiting norm inequalities give well-defined bounded actions on the dense span; zero-norm equivalent inputs have zero-norm equivalent answers. Density extends the actions to the L2 spaces. Passing the exact finite adjunction identity through contractions establishes genuine Hilbert adjoints. The construction neither pairs unrelated layer neurons nor assumes cross-width operator-norm convergence. Learned rank-one increments have precisely the Frobenius/Hilbert–Schmidt normalization needed by the raw metric.

**Regularity.** F.5 and F.6 prove bounded-multiplier continuity and a strong chain rule along L2 curves. F.7 separately proves scalar Fréchet differentiability by the weighted Taylor remainder F.41, repeatedly moving actual adjoints through scalar pairings. This avoids the false general assertion that a nonlinear Nemytskii map is Fréchet differentiable from all of L2 to L2. Gradient continuity follows from the bounded-multiplier lemma and rank-one continuity. Fixed-cap local existence is obtained from an actual locally Lipschitz raw field. No general uncut local well-posedness theorem is silently used.

## Source closure, constants, and lack of circularity

I checked the response construction through the proofs in Part R, including its current-time terms, rather than accepting its concluding statement.

- R.11–R.17 follow the forward/reverse instruction order. Forward responses are strictly past; reverse responses include current forwards. Learned moments have the displayed control and step factors.
- Affine Gaussian-probe perturbations are made at finite answer slots. Taking width at a fixed nonzero amplitude and only then the amplitude to zero controls the formal derivative row, including singular source directions. The sharper two-input multipliers in T.16 give the advertised T.17 constants after accounting for the two-column/time-row factors.
- The affine finite-array premise is supplied separately by bounded affine Euler approximations and exact finite update lengths. It does not follow from an assumed cross-width convergence of trained operator norms.
- Nonlinear primal comparison bounds actual query norms and source variances before any response-tail estimate is invoked. This prevents a source-variance/response bootstrap loop.
- On a bounded coefficient prefix, the coordinate moment bounds use deterministic maxima of Lp norms and finite Gronwall. The exponential envelope uses a weighted convexity estimate, not independence over time or an unproved random time supremum.
- R.46–R.49 retain L_k J_k terms and the middle current reverse-return path. R.65–R.66 retain their terminal incoming multiplier. R.93–R.94 explicitly verify the current blocks.
- Same-array affine derivative comparison is distinct from comparison to the actual affine coefficient arrays. R.69–R.88 then control the latter by chronological deterministic systems.
- Closure constructs A2, A3, B3, B2 in that order. At each stage the coordinate and derivative bounds use only past rows and the already bounded current rows. The product identity bounds epsilon+I_k from past completed rows. I found no circular invocation of the new row's bound.

The gain-one numerical specialization dominates the zero-offset family because every actual gain is at most one, while its lower gain is used separately in the coercivity and variance arguments. Retaining the small coefficient in Q.11 legitimately reduces the exponential envelope under Q.12; the two expectations required by the remainder proof are explicitly bounded. The improved initial constants feed the otherwise unchanged chronological chain.

The final recipe is positive and elementary at each fixed separation. Its monotonicity follows after simplifying the two apparently problematic numerator ratios. The claimed triple-exponential sufficient scale follows from the displayed hierarchy: primitive exponential constants, then double-exponential response constants, then the final exponential of K*. The statement is correctly a sufficient lower bound on the allowed recipe and is not an upper bound on the true permissible coefficient. The shrinking sufficient recipe and endpoint incompatibilities do not prove that the optimal admissible coefficient must vanish. The report states that distinction correctly.

## Cap removal, algorithms, true kernels, and velocities

The asymmetric gate comparison T.24/V.10 is algebraically correct. Sequential backward substitution multiplies incoming discrepancies only by bounded gates and actions; a cap factor occurs only on an already controlled forward discrepancy. Thus the growth is linear in the cap in the Gronwall exponent, not a product of cap factors across layers. Uniform sub-Gaussian reference tails defeat that exponent. The resulting uniform Cauchy bounds apply to raw states and directions, establish the strong uncut field, and compare any bounded-primal strong competitor using tails only from the constructed cap reference. The same vanishing initial discrepancy at a reached state proves the stated continuation uniqueness.

The finite-algorithm argument takes width at a fixed cap and fixed auxiliary mesh. Exact update lengths give a finite primal ball with slack. Width-independent capped field bounds and Euler defects then remove the mesh. The actual small random readout remains in finite training; only a fixed-program comparator has zero readout. The uncut GF/GD comparison uses the same-width cap flow. For GD, its direction is the uncut field at its preceding node, and the extra defect comes from the smooth cap reference, so no uncut Lipschitz estimate or Gaussian theorem for growing transcripts is required.

I checked the additional observation arguments because raw state convergence by itself would be insufficient:

- V.3–V.4 derive fixed-cap expected source rows by nonlinear Gaussian probes and then pointwise derivative rows and primary moments. The finite primal premise comes from fixed-program node laws and update lengths, not from those derivative bounds.
- V.5 identifies bottom and then middle velocity action queries using nested smooth product truncations. The bottom derivative envelope depends on already established primary moments; the next derivative envelope depends on the established first action. This avoids assuming the velocity moments that are being proved. New same-family source correlations are retained.
- V.8 supplies the full true backward chain as observations at capped states. It uses bounded multiplication, action continuity, and nested L2 truncation; it does not equate capped update kernels with true gradient kernels.
- V.40 has a single reference tail cutoff multiplying the raw state discrepancy. Strong population velocity convergence is first obtained using the compact L2 time image of the uncut reference. Finite cap removal then takes width, cap at fixed velocity cutoff, and finally that cutoff to infinity. This does not multiply a potentially growing fixed-cap fourth-moment constant by an uncontrolled cap error.
- Uniform velocity laws control squared speeds. The path interpolation estimate V.57 supplies the missing passage from joint fixed-grid laws to Wasserstein convergence on continuous path space with the supremum norm.
- Fixed finite generated probes pass both orientations through bounded action comparison and the common-space construction. This is not a claim about arbitrary width-dependent probes.

These arguments support the full observation scope of T.1, including off-diagonal kernel entries, individual samples' velocities, finite joint time tuples, integrated speeds, and path laws.

## Initial learning and three-input geometry

**Two inputs.** Appendix B proves full forward Gram positivity and top beta Gram positivity by the everywhere identity and the nonconstant gate derivative. V.I justifies the needed unbounded-product transpose observations through explicit nested truncation and derivative domination. Conditional reverse-source variances give the lower beta Grams and bottom sample directions. Actual adjunction gives positive sums of pairings for upper sample directions; the stated reflection preserves the objective in either label sector and equalizes the two squared sample norms. This legitimately upgrades 'at least one sample' to 'every sample'. Hidden velocities divided by feature time converge to V, giving hidden acceleration V in feature time and 4V in physical time. The projected kernel coefficient is 8 times the hidden squared norm. The positive all-time regression margin plus nonzero hidden accelerations and actual kernel change establishes the claimed nonlinear, non-frozen trajectory criteria, without claiming perpetual motion.

**Three inputs.** The cubic tensor separating functionals have norm one, annihilate the other two tensors, and have diagonal pairing at least delta(2−delta). Summing the three scalar coefficient bounds gives the stated positive lower eigenvalue of the cubic Gram even when the original Gram has rank two. The Hermite projection uses the nonzero cubic arctangent coefficient; the first-chaos propagation then preserves positivity through the next layers. Thus a linear three-point input relation does not cause exact cancellation of the odd nonlinear features.

The matching upper construction uses an actual Gram-kernel vector, so its linear activation contribution cancels exactly. The symmetric second-difference estimate and the remaining gain discrepancy are both of order 1−c in L2. The closed choice c=1−delta and the strict choice c=1−2delta satisfy their claimed pairwise inequalities in the displayed range. Their Rayleigh quotients prove the uniform worst-case Theta(e^2 delta^2) statement in every fixed dimension at least two. The direction need not be a binary-label vector, as the report explicitly notes; this is sharpness of the least eigenvalue, not of every label projection or of a training cutoff.

For every upper sample, equations (8)–(11) add a genuinely new forward Gaussian innovation. Its residual variance is bounded below by a conditional variance from the reverse Gaussian group, independently of the old feature span. The remaining terms in the sample direction are measurable in the old source groups, so they cannot cancel that innovation. The extra source calls are covered by finite-transcript truncation with integrable derivative envelopes, and the alternate finite Gaussian-conditioning argument confirms the positive residual survives matrix reuse. No permutation symmetry or nonzero affine target direction is required here.

Along an assumed canonical strong solution, the hidden initial acceleration factor is 9 and the projected kernel coefficient is 18. These follow directly from the physical residual equations and the scalar feature-energy differential, without a scalar residual clock. Readout acceleration is nonzero because the initialized feature Gram is positive definite.

The rank-two affine obstruction and the equilateral equal-label stationary affine example are correct. They invalidate the proposed affine-clock proof route, not the positive-nonlinearity theorem. The chapter's remaining analytic obligations are accurately declared open.

## Appendix C's own shifted theorem

The augmented-Gram proof G.1 handles the possible two-positive/one-negative sign pattern and gives the stated delta-squared lower bound without an inverse. G.3 controls all positive effective control meshes directly using hidden displacement and readout growth. The numerical large-gain choice makes the stopping estimates strict, so the affine premise for Part R is separately available. G.4 keeps the capped hidden contribution as a possibly nonsymmetric operator and bounds its absolute norm; it does not assume positivity of that contribution. Readout coercivity then supplies the residual-clock bound used to globalize the cap estimates.

Part N's specialized affine upper-sample lower bounds are also supported internally. The conditional covariance of B*H in N.26 and the resulting identity N.27 are obtained from the Gaussian second/fourth moments. Normalized trace limits are justified by fixed Gaussian probes plus uniform operator moments, rather than expectation calculations alone. The trace identities for AA*, BB*, and BAA*B* give the displayed positive completed squares. The direct perturbation bounds N.41–N.53 preserve both upper sample norms at the stated cutoff. The scalar feature-energy differential and the strong backward multiplier limits then give the physical acceleration and kernel coefficients. I found no dependence here on a hypothesis belonging only to the odd two-input theorem.

## Objections and required repairs

No substantive objections remain after checking the above candidate failure mechanisms. No repair is required for a PASS of the results the document actually states. In particular, the report does not claim an optimal two-input amplitude scaling, a global odd three-input theorem, affine cancellation of the positive odd mixture, an ambient L2-to-L2 Fréchet derivative, or cross-width operator convergence; none of those unsupported stronger statements should be attributed to it.

**Final verdict: PASS.**
