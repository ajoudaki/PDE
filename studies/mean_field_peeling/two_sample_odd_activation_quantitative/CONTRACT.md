# Quantitative sharpening of the odd two-input theorem

2026-09-07. The user's target is a substantially larger guaranteed
nonlinear mixing coefficient as the angular margin decreases.

Preserve the entire theorem in ../two_sample_odd_activation_theorem/PROOF.md:
two deterministic RMS-unit inputs, |rho|<=1-delta, 0<delta<=1, all binary
labels, the original independent Gaussian initialization, three common
hidden activations, raw gradient metric, and simultaneous raw GD n^-2.
The coefficient must be selected using delta alone, independently of
angle, labels, dimension, width, caps, meshes and physical horizon.
Keep the strong autonomous global population flow, uniqueness/restart,
full-sequence GF/GD limits on each fixed finite physical interval,
canonical actions/adjoints, and stated kernel/path/velocity observables.
Retain nonaffinity and initial feature-learning certificates.

Quantitative target: absolute c>0 and a fixed finite exponent p>0 such
that the same full theorem holds with theta_delta=c delta^p in
phi_delta(z)=(1-theta_delta)z+theta_delta arctan(z). Interpret the user's
example 1/delta^2 as polynomial smallness such as delta^2, since theta
is a convex mixing coefficient. A stronger interval 0<theta<=c delta^p
is desirable if the argument proves it. The broader compact gain family
may be retained to support normalization, but it must not weaken the
requested convex-mixture conclusion.

First quantify the existing sufficient selection honestly. An upper
bound on that deliberately conservative selection is not an upper
bound on the largest coefficient for which the theorem is true.
Polynomial control of one intermediate lemma is not a polynomial
coefficient theorem unless every response/construction/limit premise
also closes at that coefficient.

Authorized work: new theoretical analysis, local proof artifacts,
independent delegated proof routes and adversarial checks. No numerical
experiments, algorithm/architecture changes, original-proof edits,
external posting, Git commits or user-created goal are part of this task.
Available concurrency: root plus three agents. No user-imposed time or
token budget. Pursue useful independent routes, consolidate and audit
any complete theorem; if a decisive estimate remains unproved, state
it explicitly and preserve rigorous quantitative improvements without
promoting them to the full polynomial theorem.
