# Evidence ledger: nonlinear depth-three operator IDE

Status: Phase I in progress, 22 August 2026.

| ID | claim | rung | status | decisive evidence or missing bridge |
|---|---|---|---|---|
| E1 | The architecture and mixed-metric feature equations in `PROTOCOL.md` are exact | C0 | proved algebraically and regression checked | block differentiation plus `test_finite_identities.py` at widths 3, 7, and 19 |
| E2 | The raw kernel is the sum of the four squared block gradients | C0 | proved algebraically and central-difference checked | exact chain rule and the same finite regression |
| E3 | Two immutable joint Ginibre actions with their true adjoints plus two current trace-class perturbations retain all weight memory | C1 | accepted from the pointed two-sided program construction | requires no fresh transpose and no time-indexed source |
| E4 | Bare normalized \(L^2\) makes the nonlinear middle multiplier stable | C2--C4 | falsified for every nonaffine \(C^1\) activation | shrinking-set concentration example in `PHASE_I_ACTIVATION_AUDIT.md` |
| E5 | Scalar loss dissipation and operator norms imply square-tail compactness or raw-kernel convergence | C5 | falsified as an inference | adaptive Ginibre spike and temporal kernel-spike examples |
| E6 | Fixed Euler meshes are finite two-matrix transpose-reusing source programs | C3 | proved | exact elimination of both learned matrices |
| E7 | Fixed-mesh all-moment convergence proves the continuous trajectory | C4--C5 | falsified as an inference | no mesh-uniform moment or stability constant follows |
| E8 | A bounded activation makes learned-adjoint pieces tail-safe | C4--C5 | proved | present-rank operator identity (5.1) |
| E9 | A half-density/natural moving frame eliminates the nonlinear analytic obstruction | C2--C4 | falsified for pointwise bounded gauges | logarithmic connection is an unbounded \(L^2\) multiplier unless activation is affine |
| E10 | Piecewise-linear or absolute-value activations supply a theorem-ready deep-linear conjugacy | C0--C2 | rejected | open attracting switching configurations destroy canonical uniqueness/stability |
| E11 | Arctangent has an exact autonomous one-time algebraic contract | C1 | proved | prior depth-three contract with two current trace-class operators |
| E12 | Arctangent has unconditional continuous-time canonical width convergence at depth three | C4--C6 | open | mesh-stable reachable \(\psi_1\) bound for the middle static-adjoint query |
| E13 | \(\phi_m'(x)=(1+x^2)^{-m}\), \(1/2<m<1\), improves the bounded-candidate inverse-tail tradeoff | Phase I | proved algebraically | bounded output, positive derivative, bounded log curvature, inverse polynomial of degree \(2m\) |
| E14 | \(\phi=\operatorname {arsinh}\) is the Pareto endpoint of the power-damping family | Phase I | proved algebraically | deterministic \(L^2\)-to-exponential forward regularization plus exact cotangent skew rotation |
| E15 | The asinh skew lift proves mesh-stable carrier delocalization | C4--C5 | under focused attack | full averaged leave-one-column response lemma, including an Osgood-strength uniqueness bridge |
| E16 | A sine phase lift turns the middle multiplier into a tail-improving evolution | C2--C5 | falsified | it preserves carrier amplitude and only rotates a concentration defect |
| E17 | Finitely many current joint laws or diagonal/finite-word trace energies determine true-adjoint reuse and raw squares | C2--C5 | falsified | sign/singular-vector orientation examples and generic full matrix algebra |
| E18 | One full current Malliavin operator or first column tangent closes concentration without a derivative hierarchy | C2--C5 | falsified for the tested norm classes | the exact asinh phase response leaves the positive mixed density `n^{-1} sum_i d_i^2 p_i^2 S_i`; scalar base and averaged tangent energies do not control it, while a second integration by parts regenerates higher responses |
| E19 | Failure of the tested activations is an impossibility theorem for the admissible class | terminal | not established | no reachable canonical counterexample and no class-wide no-go beyond bare-\(L^2\) stability |
| E20 | A fixed nonlinear perturbation of the linear activation can be handled by Duhamel resummation of the linear core | C4--C5 | falsified for normalized-\(L^2\)/operator/path estimates | the first nonlinear insertion already contains `Gamma_1^* M_{h'(Z_2)} Gamma_2^* B_3`; a sign-column stress state gives an order-\(epsilon\) discontinuity for every fixed \(\epsilon>0\) |
| E21 | Canonical asinh trajectories exhibit the algebraic concentration stress state on a fixed clock horizon | C4--C5 | not found | exact residual-clock and current Gram identities bound every local same-row feedback rate by `O_{T,y}(1)`; only collective adaptive alignment remains, so this is evidence against the proposed spike mechanisms rather than a proof of delocalization |
| E22 | The finite-width asinh tail stress test shows width-growing backward moments through the tested horizon | empirical | not observed | at feature time 1.5 and widths 128, 256, 512, the empirical \(L^2,L^4,L^8\) norms of both `R2` and `Q1` remain stable; this is discriminating evidence only and supplies no continuous-time theorem |
| E23 | Smooth homogeneous nonlinear activations gain coercive balance laws sufficient for the target | Phase I/C2 | rejected | the exact balances are indefinite, degree \(p\ge2\) feature ascent has positive-cone finite-time blow-up, and the initial weighted Gram norm grows at least polylogarithmically |
| E24 | A finite-order generalized Young/DiPerna--Majda state repairs concentration while remaining closed | C1--C5 | falsified | equal quadratic concentration data can have different current kernel flux; adjoining one higher defect only moves the failure to the next derivative |
| E25 | PSD current Gram identities and a scalar entropy/stress Lyapunov estimate replace cavity | C2--C5 | falsified | the normalization-correct leverage bound loses \(\sqrt n\), and exact algebraic states with bounded macroscopic norms violate the proposed Osgood stress estimate |
| E26 | The bounded phase activation \(\phi=\operatorname{gd}=\arctan(\sinh x)\) has an exact five-species one-time contract | C1 | proved algebraically | \(r=\sinh u\), \(X_1=\arctan r\), \(d=\operatorname{sech}\), and \(d+i\tanh=e^{i\phi}\) give a bounded forward channel and an exact phase lift |
| E27 | Gudermannian boundedness makes its learned-adjoint pieces pointwise tail-safe | C4--C5 | proved | \(A(t)=A_0+O_T(1)\), all three features are bounded, and `P_l^* B` is a bounded feature times a current scalar overlap integral |
| E28 | A scalar same-column Onsager correction leaves an \(n^{-1/2}\) cavity remainder for Gudermannian | C4--C5 | falsified | the column perturbation changes the trainable readout and returns through the same column at order one; a proof must bound the full phase/carrier finite difference, not refresh the adjoint |
| E29 | The tested Gudermannian trajectories show width-growing backward moments | empirical | not observed | at feature time 1.5 and widths 128, 256, 512, `R2` and `Q1` have stable \(L^2,L^4,L^8\) diagnostics and top-one-percent square fractions; this is not a tail theorem |
| E30 | A sine phase lift plus fixed-order nonlinear Picard identification proves the continuous trajectory | C3--C5 | falsified as a proof mechanism | learned forward motion leaves an exact `A(t) B3(s)` two-amplitude block; the phase does not cancel it in relative energy, and fixed-order bounds do not control the triangular tail at Picard order comparable to width |
| E31 | The available sine analysis supplies a width-uniform exact Picard remainder | C4 | not established | a valid proof needs an all-order finite-width summable envelope, a growing-depth transpose-program theorem plus a remainder, or uniqueness in a completed asymptotic norm; none was proved |
| E32 | One full first-response field closes a finite ordinary Hilbert--Schmidt Lyapunov hierarchy | C2--C5 | falsified for the tested norm class | `E_m=<|B|^(2m) S>` generates the next multiplier power; its unique cancelling weight is inverse-derivative and has lognormal or singular tails |
| E33 | A sufficiently small fixed nonlinear residual of identity inherits the proved deep-linear theorem | Phase I/C4--C5 | rejected as theorem-ready; actual convergence remains open | exact `V_epsilon=V_0+epsilon W` gives width-uniform `O(epsilon)` closeness, a finite feature clock, raw-`K` UI, and nonlazy witnesses, but exact fixed-`epsilon` identification needs an all-order reachable source-word bound uniform in width and reset order; ordinary epsilon Taylor has zero `L2` radius already at width one |
| E34 | A smooth activation with compactly supported flat derivative makes large cotangents exit the active region and then quenches them | Phase I/C4--C5 | falsified as a proof mechanism | flatness implies `int ds/phi'(s)=infinity` at the support edge, so finite self-forcing never reaches it; cross-layer Gram forcing can reactivate inactive coordinates, and an interior `A phi''(Z)` multiplier is still `Omega_P(sqrt(log n))` |
| E35 | A finite smooth pointwise lift can cancel every arbitrarily large local backpropagation signal in a uniformly coercive metric | Phase I/C2--C5 | possible only for a bi-Lipschitz derivative; route still fails nonlocally | pullback rigidity gives `w phi'^2=constant`; hidden-layer natural coordinates then generate the unbounded commutator `[D^{-1}HD,M_h]a`, which vanishes identically only for affine activation |
| E36 | Physical MSE flow is disqualified when the associated feature-ascent flow blows up | C0 | falsified | loss dissipation gives finite compact-time parameter path length; a finite target is encountered before a feature-ascent escape |
| E37 | The full sine cyclic-traffic module plus fixed-mesh Gaussian regression proves a unique continuous semigroup and raw-`K` convergence | C4--C6 | retracted | no mesh-uniform empirical sub-Gaussian estimate was proved; coalescing query spans destroy uniform inverse-Gram bounds, and a full diagonal operational module is microscopic/tautological rather than demonstrated compression |
| E38 | A positive-output, small fixed nonlinear activation may create a finite-clock high-temperature regime without becoming lazy | Phase I | under clean-room attack | shifted arctangent and `c+exp(epsilon asinh z)` have nonzero width-limit gradients at every layer; the unresolved test is a mesh-free moderate-moment theorem for both reused adjoints |
| E39 | Shifted arctangent supplies all-time actual-flow control and uniform integrability of every scalar raw-square observable | C0--C4 | proved | the positive feature floor gives a finite absolute residual clock; recursive operator/RMS bounds have moderate moments through order `c n`, and the initial gradients of all trainable layers and the feature velocity have strictly positive width limits for every sufficiently small fixed `epsilon` |
| E40 | The shifted-arctangent natural-coordinate gauge closes the reused-adjoint convergence proof in normalized `L2` | C4--C6 | falsified as an inference | exact curvature resummation leaves `2 epsilon^2 d_2^{-1} G_1(h_1 d_1^3 Q_1 w_1)`; separate RMS bounds do not control `Q_1 w_1`, and a localized tangent incurs the sharp `sqrt(n)` loss |
| E41 | Full global Orlicz bounds are the necessary tail target for a depth-three nonlinear theorem | C4--C6 | falsified | even bounded shifted trigonometric activations have a finite-width three-Gaussian backward carrier and need not be `psi_1`; the sufficient target is a moderate-moment and centered empirical estimate only through `p=O(log n)` |
| E42 | A typical single immutable row/column has width-growing influence in the two high-temperature candidates | empirical | not observed | after resampling one row or column and evolving the exact feature flow to clock `0.5`, `sqrt(n)` times every tested normalized field/predictor/kernel difference remains stable at widths 64, 128, 256 for shifted arctangent and `c+exp(epsilon asinh)`; this supports but does not prove a dynamic cavity estimate |
| E43 | Super-Gaussian derivative damping self-quenches every extreme output cotangent | Phase I/C4 | falsified | at initialization `B3_i=A_i phi'(Z3_i)` and the largest independent readout among the positive-density active coordinates is `esssup|phi'| sqrt(2 log n)`; erf and stronger damping cannot remove it without making `phi'` identically zero |
| E44 | A classical layerwise Gaussian `L2` forward operator together with its Hilbert adjoint can represent the backward width limit | C1--C5 | falsified | for `B=A d(GX)`, every fixed upstream pairing with `G^*B` vanishes while its empirical square norm has a positive limit; the missing energy is a weakly-null transpose fluctuation, so the limit must retain two-sided open/traffic information |
| E45 | The positive hyperbolic activation `c+exp(epsilon asinh z)` closes the convergence proof by phase energy and small transport charge alone | C4--C6 | falsified as a proof mechanism | its phase lift preserves carrier `L2` but not co-localization; the Nemytskii product fails normalized-`L2` continuity, fixed moments give a non-Osgood modulus, and its nonlocal charges `epsilon^3,epsilon^4` are no smaller than shifted arctangent's |
| E46 | A bounded shifted-sine activation is theorem-ready after an absolute Wick/Picard expansion of its current operator equations | C4--C6 | falsified as a proof route | a connected width-one local ladder has `L2` size at least `m^{-O(1)}(C epsilon^3 s sqrt(m))^m`, so its terms do not tend to zero; resumming those ladders leaves a plausible width-first expansion, but its required exact-trajectory reachable-channel hypercontractivity and connected-contraction bounds are precisely an unproved adaptive reused-adjoint theorem |
| E47 | The shifted-tanh polynomial lift closes the positive-time width theorem in one weighted graph Banach algebra | C2--C6 | falsified under the same-space Banach-algebra reading | Gaussian evaluation is incompatible with continuous multiplication of the unbounded readout in one Banach algebra, and a nonzero graph derivation cannot be bounded there; a Banach scale or affiliated-operator traffic law remains formally possible but still needs weighted tightness, positivity, uniqueness, concentration, consistency, and raw-`K` continuity |
| E48 | A typical single immutable row/column has width-growing influence for shifted tanh | empirical | not observed | at widths 64, 128, 256 and feature clock `0.5`, `sqrt(n)`-scaled field, predictor, and kernel differences stayed stable under single-row/column resampling; this is consistent with, but does not prove, a dynamic cavity theorem |
| E49 | Local covariant resummation proves the shifted-arctan adaptive-query theorem | C4--C6 | retracted after normalization audit | the exact tangent retains `-(phi''/phi') H theta`; this is still one local carrier, but nested learned rank-one edges create empirical scalars such as `<delta X2,D2 Z2dot>` whose uniform self-averaging at all insertion orders is unproved; the earlier collision moment also missed the required `C^(m p)` factor before taking the `p`th root |
| E50 | A bounded positive monotone saturator can make its naked inverse derivative Osgood on both tails | Phase I/C2 | falsified | `int dx/(1/phi'(x))=int phi'(x)dx` equals the finite output range; barely integrable log-corrected gates approach the threshold and improve Gaussian inverse-gate moments over arctan, but every bounded choice stays on the non-Osgood side unless paired variables cancel the inverse gate |
| E51 | The chronological learned rank-one edges themselves force a second unsuppressed cavity carrier | C4 | not found; narrowed | a product of rank-one operators has one open carrier and normalized scalar contractions; differentiated side branches are trapped in a centered empirical scalar and gain `n^{-1/2}` under the collision grammar, but the full theorem still needs a uniform exact-local-propagator/source bound |
| E52 | Shifted Gudermannian removes the direct Gaussian-readout tangent amplifier | C0--C2 | proved exactly | in the coordinate `r=tan(gd Z)=sinh Z`, the direct characteristic is additive and its phase propagator is bounded by `2(1+I^2)`; the coupled top `(A,r)` tangent has coefficients independent of the magnitude of `A` |
| E53 | The shifted-Gudermannian local characteristic alone yields a collision-free moderate-moment theorem | C4--C6 | falsified as an inference | differentiating the learned `P2` action leaves an unsuppressed through term `-epsilon^2 d3(t) int beta(s,t) A(s) sin(theta3(s)) delta theta3(s) ds`; its deterministic overlap is generically nonzero and its `m`-fold chronological iterate has size `(C epsilon^5 S)^m (mp)^(m/2)/m!` |
| E54 | The surviving Gudermannian through ladder necessarily destroys width convergence | C4--C6 | not established | the ladder is summable, with total `L^p` growth of order `exp(C epsilon^10 S^2 p)`; a sufficiently sharp cavity-innovation/Onsager theorem could still beat this growth at `p=c log n`, but no such theorem has yet survived reconstruction |
| E55 | A scalar high-temperature innovation--Onsager recursion follows from an `exp(a p)` phase-tangent bound | C4--C6 | falsified as an inference | the exact identity is `Gamma_j^*B=delta_g(B)+n^-1 div_g B`; a common lognormal mode `B=U g` satisfies the proposed tangent and centered-error estimates but leaves an order-one random Onsager term `U`, so replacing it by its mean is invalid; the actual depth-three tangent also contains all differentiated learned `P/P*` branches and requires a coupled current Stein-closure theorem |
| E56 | The learned-through mechanism produces visible width amplification on a longer finite-clock stress test | empirical | not observed | through feature clock `1.5`, all single-row/column resampling differences retained the predicted `n^-1/2` scale at widths 64, 128, 256 for shifted arctan, Gudermannian, tanh, sine, and asinh; shifted arctan had the smallest constants in this normalization, but the experiment does not establish the coupled cavity theorem |
| E57 | At high temperature, the immutable transpose query is fresh noise plus a response in only the current feature direction | C3--C6 | falsified on the actual network | for shifted arctangent or Gudermannian, the determinant `Delta_n(t)=<Gamma2 X2(0),B3(t)><X2(t),X2(t)>-<Gamma2 X2(t),B3(t)><X2(0),X2(t)>` has `epsilon^-4 Delta_n'(0) -> -c^4 m_c^2<0`; an older feature direction survives at every sufficiently small fixed `epsilon` |
| E58 | A finite list of polynomial readout-weighted energies closes the full shifted-Gudermannian cavity tangent | C4--C6 | falsified as a proof route | the first transpose creates `G2^*(A^2 d3^2 tanh(Z3))`; differentiating successively creates `G2^*(A^k F_k(Z3))` for unbounded `k`, with unresummed Gaussian size `(C epsilon sqrt(k))^k`; time-ordered resummation may still succeed, but finite weighted Gronwall does not |
| E59 | A smooth genuinely nonlinear scalar activation can cancel both direct and learned-through top tangent sources pathwise | Phase I | falsified | `B3dot=phi phi'+q2 A^2 phi''phi'+A phi''H`; varying the full-support forcing `H` forces `phi''=0`, then `phi phi'=0`, hence `phi` is constant; eliminating only learned-through curvature forces affine `phi` |
| E60 | A fixed finite multiplier/gate algebra can close all nonlinear forward-adjoint response words | Phase I/C1 | falsified for `C1` scalar activations | every round trip inserts `g=phi'^2`; finite span of `1,g,g^2,...` implies a polynomial identity for continuous `g`, hence `g` is constant and `phi` is affine |
| E61 | Small nonlinearity plus a generic factorial Hida/Vage algebra proves the current traffic ODE | C2--C6 | falsified as a proof route | normalized Hermite products force the multiplication constant across a regularity gap `delta` to grow at least double-exponentially in `1/delta`; with exponential chaos weights each product consumes a fixed gap, so arbitrarily many Picard steps leave the domain regardless of a fixed small coefficient |
| E62 | A cell-resummed graded-traffic proof for shifted arctangent is complete | C2--C6 | conditional only | its exact state `(A,L1,L2,h)` and finite clock/raw-`K` moment bounds are unconditional, but the needed reachable-traffic estimate `||DV(Y)H||_{alpha-delta,beta-delta} <= C epsilon delta^-1 ||H||` is unproved; naive Wick grading gives `delta^-2` for `G1^* M_{ell2 B2} G1 M_{d_h X1}` |
| E63 | Compact-curvature residuals are a better depth-three convergence target than bounded positive saturators | empirical/Phase I | disfavored | `x+0.2 tanh x` and `x+0.2 gd(x)` entered the deep-linear feature-time growth/overflow regime by `s=1.5`, while `1+0.2 exp(-x^2/2)` and `1+0.2 x/sqrt(1+x^2)` retained stable `sqrt(n)` row/column influence through widths 64,128,256; the bounded candidates still have the reused-adjoint theorem gap, and shifted arctangent has the cleaner monotone gate and lower-degree inverse metric |
| E64 | A classical isonormal/Malliavin current field plus finitely many response coordinates closes ordinary reused matrix multiplication | C1--C6 | falsified for every fixed derivative order | `F_n=n^-1 sum_j W(e_j)e_j` vanishes in every fixed Gaussian-Sobolev seminorm while `Tr(DF_n)=1`; Hermite versions put an independent diagonal defect at every derivative order, so exact ordinary multiplication requires the full contraction/traffic hierarchy |
| E65 | The exact weak rooted-open Liouville hierarchy is already the requested compact convergence theorem | C1--C6 | falsified in the natural weak/current class | WOT x weak loses adaptive actions and raw squares; every finite query-depth enrichment is nonclosed under the generator; the full separating traffic character is the complete all-orders hierarchy and needs an additional reachable quotient, continuity, UI, and uniqueness theorem |
| E66 | Compactly supported or decaying curvature makes a fixed nonlinear residual a width-uniform perturbation of the deep-linear theorem | C3--C6 | falsified as an ambient perturbation route | on a positive-density curvature interval `max_i |A_i phi''(Z3_i)| >= c epsilon sqrt(log n)` at Gaussian initialization; Gaussian `L2` Taylor has zero radius and adaptive Ginibre action is unbounded on empirical `L^p`, so a reachable delocalization theorem is still indispensable |
| E67 | A bounded rational saturator with derivative `(1+x^2)^(-q/2)`, `1<q<2`, has a genuinely subcritical inverse-gate exponent compared with arctangent | Phase I/C2--C6 | proved algebraically; convergence consequence under focused audit | boundedness forces `q>1`, while Gaussian inverse-gate moments grow as `p^(q/2)` and a one-cost chronological series is summable exactly for `q<2`; it remains unproved that the middle reused-adjoint expansion pays only one such cost per insertion, and unit-range normalization near `q=1` can merely suppress layer motion |
| E68 | The apparent stability of the subcritical rational family is solely an artifact of unit-range derivative suppression | empirical | not supported | with fixed gate amplitude `lambda=1/8`, the `q=4/3,3/2` matrix and input velocities have nonzero width limits comparable to or larger than shifted arctangent, while `sqrt(n)`-scaled single-row/column influence remains stable through clock `1.5` at widths 64,128,256; this is discriminating evidence only |
| E69 | A nonlinear activation can make the direct top cotangent orbit finite-dimensional and thereby replace the immutable Gaussian source by finitely many innovation modes | C1 | falsified for `C1` activations with a common local flow/germ | finite orbit rank forces the natural-coordinate gate to have finite translate rank, then a Koopman-generator weight argument forces it constant; the theorem is deliberately scoped and does not rule out retaining the allowed full fixed two-sided source operator |
| E70 | The depth-two fixed-cutoff/tail-transfer proof extends at depth three from loss dissipation and square-tail UI alone | C4--C5 | falsified as a proof route | fixed componentwise clipping is Lipschitz, but its constant grows with the cutoff and explicit shrinking-coordinate examples give a non-Osgood cutoff-removal modulus; for rational exponent `q`, a new uniform `L^(2+eta)` bound would suffice only when `q<=1+eta/2`, and `f'=K` supplies no such coordinate moment |
| E71 | The composed activation `1+(1/8)F(asinh x)`, `F'(v)=(1+v^2)^(-3/4)`, improves the bounded/gate-tail balance without vanishing layer motion | Phase I/C2--C6 | exact algebra and tail exponent proved; convergence consequence under hostile audit | it has positive floor `0.672...`, gate maximum `1/8`, bounded log derivative, and inverse-gate Gaussian moments `sqrt(p)(log p)^(3/2)`; its one-cost chronological series would be supergeometrically summable, but the one-cost reachable-traffic induction is not yet proved |
| E72 | Near-critical logarithmic damping lets the depth-three cutoff be removed from square information alone | C4--C5 | falsified; exact threshold sharpened | desaturation for the composed candidate is `|x|(log|x|)^(3/2)`, so every genuine uniform `2+eta` estimate would suffice, but explicit one-coordinate examples still defeat square UI; the activation lowers the missing source theorem to arbitrarily small excess integrability rather than proving it |
| E73 | Existing quantitative tensor-program convergence supplies the required logarithmic-depth/continuous-time source-word estimate | C3--C6 | not supported by the primary theorems audited | arXiv:2607.06290 proves an `O(n^(-1/2))` bound for each fixed Netsor program, with constants depending on the program length and structural data; arXiv:2308.01814 likewise treats fixed finite programs/steps. Neither theorem gives constants uniform for `m=O(log n)`, an exact-flow remainder, or the adaptive feature-learning continuous-time bridge used here |
| E74 | The single-root inverse-gate invariant by itself implies the summable chronological source-word bound | C3--C5 | falsified as an inference; the gate invariant itself survived | the exact off-diagonal term `d3_i^(-1)d3_k A_k sum_j d2_j^2 G2_ij G2_kj` contains a readout carrier and a two-sided Wishart fluctuation. Gate ratios telescope along chains, but fixed-order collision suppression is not uniform at large chronological order; a grouped Gram/collision theorem is still required |
| E75 | The final IDE needs to evolve all three natural-coordinate fields | C1 | falsified; contract compressed further | only `r=Theta(u)` is dynamic. Both hidden preactivations are current algebraic observables `Z2=(Gamma1+P1)Psi(r)` and `Z3=(Gamma2+P2)phi(Z2)`, giving the exact five-species physical state `(A,r,P1,P2,e)`; `h2,h3` are optional proof variables |
| E76 | A stopped first-Malliavin/LSI bootstrap validates the logarithmic-depth source expansion | C3--C5 | falsified as a proof route | the exact sensitivity energy contains `n^-1 sum_i A_i^2 d'(Z3_i)^2 ||D Z3_i||^2`, and true-adjoint reuse creates the corresponding weighted column energy. On a `sqrt(log n)` stop, ordinary Gronwall makes the Gaussian concentration exponent degenerate; the weighted hierarchy must be resummed jointly |
| E77 | Sequentially clipping the top and middle carriers extends the depth-two cutoff proof | C3--C5 | reduced but not closed | the top cutoff is removable from `A=A0+bounded`, and `Q1` is additive in the minimal state, but removing the middle `B2` cutoff is exactly the uniform square-equiintegrability statement (6.2). Fixed-cutoff convergence and `L2` time continuity do not imply it |
| E78 | A shifted-sine phase lift avoids the depth-three adaptive-source obstruction | Phase I/C3--C6 | falsified as an inference | its exact unit-circle lift removes local Taylor expansion and is nondegenerate for every fixed `epsilon>0` when `y!=0`, but the first true-adjoint Malliavin response is nonzero and unorganized collision enumeration loses a factorial. No complete logarithmic-depth collision theorem was obtained |
| E79 | A genuinely nonlinear activation can close through a finite dynamically invariant algebra of ordinary scalar gates | Phase I/C1--C6 | impossible in this scoped mechanism | finite-dimensionality forces a polynomial identity for the continuous gate `(phi')^2`, hence it is constant and `phi` is affine; finite-valued nonsmooth gates generate an infinite boundary-flux hierarchy, while constant-magnitude folds have a positive-probability width-two cusp with nonunique and regularization-unstable continuation |
| E80 | A whole-middle-neuron row-plus-column cavity closes the excess-moment estimate at finite response order | C3--C5 | falsified as a proof route | after the first source column and receiving row are removed, `R_(2,i)` and the response `Delta_j Z_(2,i)` still share a distinct source column `C_i`; decoupling generates a third index and then an all-order alternating row/column hierarchy, with order-one aggregate energy at every generation |
| E81 | Nonzero Gaussian innovation beyond the finitely queried span disproves the five-species Markov contract | C1--C2 | falsified | the contract retains the complete immutable jointly realized source actions `Gamma_1,Gamma_2` and their true adjoints; matrices that differ on a new direction are different fixed model data, so conditional variance after revealing finitely many queries rules out only finite-innovation closures, not restartability with the allowed source operators |
| E82 | A finite current list of local (L^p), Orlicz, phase, or Gram-word energies can remove the depth-three middle-carrier obstruction | Phase I/C2--C5 | impossible in this scoped mechanism | the exact entropy `E_(p,c)=<|R|^p>/p-c<R,X2>` cancels local PSD Gram transport but true-adjoint forcing needs `R` in `L^(2p-2)`; natural-coordinate linearization leaves a Gram--diagonal commutator whose word length grows under differentiation, so finite local lifts close only for affine gates or diagonal Gram operators |
| E83 | Two gate factors and one Gaussian carrier pair per connected feedback generation give a collision-safe geometric traffic contraction | C3--C6 | falsified as stated | exact differentiation contains `G1* M_(C2 B2) G1 D1^2` with `B2=D2 G2* D3 A`, and a deeper term with four explicit matrix actions; treating `B2` as bounded is circular, fixed `L^p` is not stable under its diagonal multiplication, and palindromic true-adjoint pairings survive at order one rather than being `O(r^2/n)` collision errors |
| E84 | Residual-erf becomes theorem-ready after exact local-cell and deep-linear Gram-block resummation | C3--C6 | falsified as stated | the interrupted sandwich is a weighted-Wishart block; its same-row `2k`th trace contains the leading factor `(2k-1)!!`, so it is not uniformly bounded on ordinary Gram/operator stops and the proposed geometric skeleton remainder omits a surviving carrier Wick multiplicity |
| E85 | A shifted-tanh high-temperature proof closes in an absolute exponential operator/Dyson norm | C3--C6 | falsified as a proof route | the trained part `D2 P2* B3` is pointwise controlled, but the exact static block `Gamma1* M_((d2'/d2)B2) Gamma1 D1^2` has no positive exponential operator moment at any nonzero fixed activation scale, even after covariance/Onsager centering; this does not rule out a width-first typical-coordinate theorem |

No entry below C6 licenses a compact-time predictor/kernel theorem.
