# Recovered global single-sample theorem at every fixed depth

Recovery and scope audit, 8 September 2026. This is not a new proof search or a fresh independent mathematical certification.

The accepted manuscript is `GENERAL_DEPTH_SELF_CONTAINED_PROOF.md`, 1,727 lines, SHA-256 `741331782e571a38ed11896fc342f2ce6291d63b057d456419d799757fe4327f`.

The original file was `/tmp/general-depth-proof-QpSvt6/GENERAL_DEPTH_SELF_CONTAINED_PROOF.md`, inside an inaccessible private directory. The complete manuscript was recovered from the untruncated, numbered primary-file reads recorded in original isolated reviewer A's task `01a0774c-59ff-7c11-a068-669d1ada7609`. Removing the displayed line-number prefixes and concatenating the consecutive ranges recovers every line and exactly the historically accepted hash. This is an exact primary-artifact recovery, not reconstruction of the mathematics from a summary. `RECOVERY_PROVENANCE.json` records the ranges and identity check. The full reviewer transcript is preserved in `../DEPTH_REVIEW_A.json`.

The coordinating consolidation agent read the entire recovered manuscript. The main task, **Resume L3 proof research (4)** (`01a07740-6670-7863-bf1d-f588a0417d95`), records three fresh isolated complete-manuscript PASS reviews, at the same unchanged hash. `ORIGINAL_ACCEPTANCE_EVIDENCE.md` preserves the original acceptance and hash evidence. Reviewer A's complete transcript is recovered; the complete B/C transcripts were not separately recovered in this pass. The three historical PASSs are supported by the original parent acceptance record, not by claiming three new reviews here.

## Exact accepted result

- Every separately fixed integer hidden depth `L >= 3`; one sample, input and target exactly `1`.
- The same bounded activation `phi(z) = 1 + atan(z)/10` at every layer and depth. The coefficient is independent of width, depth and time.
- Independent Gaussian first preactivations `N(0,1)`, hidden matrix entries `N(0,1/n)`, and stored readout entries `N(0,n^-2)`.
- Fully trained prescribed raw GF and simultaneous exact raw GD with physical step `n^-2`; loss `(f-1)^2`.
- A unique autonomous global population flow, including raw-coordinate uniqueness and restart from reached states in the stated bounded-primal integral-solution class.
- Joint full-sequence population/GF/GD convergence on every fixed finite physical interval. Includes prediction/loss, every kernel block, both current action directions and actual adjoints, the named backward and velocity fields, same-layer path laws in `W2(C[0,T])`, and integrated squared speeds. Same-width GF/GD distance also vanishes in the stated transformed-field/operator norm.
- Persistent positive best-affine activation error on every compact physical interval, and nonzero hidden feature velocities at every positive finite time. These are stronger than a merely initial-motion certificate.

The proof supplies its own Gaussian source and response arguments; no nonclassical external theorem is a premise. The positive activation floor allows the finite auxiliary feature-time construction to cover every finite physical time. It does not prove a simultaneous growing-depth/width limit or depth-uniform bounds for every observable.

This supersedes the earlier separate `L=3` single-sample theorem for the same shifted arctangent. It does not settle pure arctangent at `L=3`, arbitrary labels, or multiple samples. The earlier consolidation's statement that this result was only historically reported is now obsolete: the complete accepted primary manuscript has been recovered with exact hash agreement.
