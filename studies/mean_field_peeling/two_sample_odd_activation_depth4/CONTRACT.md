# Four hidden layers and the depth-uniform question

2026-09-07. The user authorizes theoretical extension of the latest
two-input power-ten theorem from three to four hidden layers, retaining
the numerical pair (c_poly,10) if possible, and investigation of a common
pair at arbitrary finite depth. Independent adversarial agents are
authorized. No trajectory experiments, external publication or commits.

The canonical object is the original fully trained raw network with
Gaussian first weights N(0,1/d), square weights N(0,1/n), and finite
readout N(0,n^-2), two deterministic RMS-unit inputs, arbitrary binary
labels, and absolute correlation at most 1-delta. L counts hidden
layers. The raw metric, loss normalization, physical time, simultaneous
GD step n^-2, and interpolation conventions are fixed. All hidden
layers use the same activation az+e atan(z). The requested mixture is
a=1-theta, e=theta. Its coefficients may depend on delta alone.

The target includes one autonomous global strong C1 population flow on
canonical adjacent action spaces, uniqueness against bounded-primal
nonsymmetric competitors and restart from reached states. Required
width limits are the full sequence in probability at each fixed dataset,
depth and finite physical horizon: predictions, all L+1 raw kernels,
both orientations on generated probes, genuine adjunction, same-layer
path/velocity laws, second moments and integrated squared speeds.
Strict activation regression nonaffinity at every finite time and all
initial hidden-block/sample-layer motion certificates are retained.

Width is taken at fixed cap and fixed finite auxiliary transcript; the
auxiliary mesh and caps are removed by deterministic estimates. Velocity
product truncations are removed in the specified reference-tail order.
No growing-transcript probability theorem, cross-layer neuron pairing,
operator-norm identification across widths, uniform-in-dataset width
limit, or simultaneous width-depth limit may substitute for the target.
No activation, coefficient, or initial state may encode the trained
trajectory. Proof-only beta families and feature times may depend on
the dataset; the actual activation may not.

Two depth questions remain distinct: one common admissible activation
with other estimates allowed to depend on each finite L, and positive
loss/coercivity/regression bounds uniform in L. A vanishing initial
kernel as L tends to infinity falsifies the latter, not the former.
The contraction obstruction applies to the literal convex mixture;
it is not a no-go theorem for the energy-normalized positive mixture.

The prior theorem directories are immutable dependencies. New results
extend them and do not supersede the valid L3 claims. A new completed
candidate requires fresh independent complete-proof reviews; previous
review outcomes are not mathematical premises.
