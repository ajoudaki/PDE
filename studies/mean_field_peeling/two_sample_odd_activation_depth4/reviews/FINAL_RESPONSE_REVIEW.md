# Independent complete-proof review

2026-09-07. Reviewer: independent agent `depth4_final_response`.

**PASS for the five exact mathematical files identified below.** I found
no unresolved mathematical gap in the assembled four-hidden-layer theorem
at the unchanged numerical coefficient
`c_poly = min(1/4,c_*,10^-70 H^-400)` and exponent ten. This is an
entire-proof review, including the affine input, population construction,
all requested limits, nonaffinity, motion, and the separate depth question.
It is not a signoff confined to the source-response component.

The arbitrary-finite-depth common-coefficient question remains open in
this candidate. The proved large-depth obstruction concerns positive
numerical bounds uniform in depth for the convex mixture. It is not a
counterexample to the different common-coefficient qualitative theorem.

I read `solve-math-rigorously/SKILL.md`, the contract, and all five new
mathematical files in full. The actual prior mathematical dependencies
inspected are listed below. Historical review outcomes were not evidence
or premises. No trajectory experiment, old-file edit, mathematical-file
edit, or commit was performed.

## 1. Model and affine certificate

The raw metric gives exactly the displayed first-block factor `1/d`,
matrix factors `1/n`, and readout update. Thus all five kernel formulas
and the loss normalization agree. The original random finite readout is
retained; it vanishes only in the population limit. Raw GD and its hidden
derivative conventions are unchanged. Odd label folding preserves the
finite raw loss as a function of parameters. Reflection and deterministic
fixed-program contractions prove population symmetry before invoking
uncut uniqueness; finite paths and competitors are not assumed symmetric.

I differentiated every adjacent balance. The endpoint identities give
`||p||^2=1+c^2` and `(c^2)'=2F`. The A3 gradient supplies
`F' >= c^6(1+c^2)`, and differentiation of
`F^2-c^8/4-c^10/5` proves the radial inequality without division at zero.
Forward and backward ratio induction loses at most sixteen per interior
step, proving `F' >= 5(c^2-32)_+^4`. Convexity of `||D||` gives the
small-c duration bound; the radial power gives the remaining duration.
Bounded primary norms and HS derivatives justify strong continuation
through the first target hit.

The Gaussian net estimate truly supplies action norm four: its exponent
`4-(3/2)log 9` is positive. The Hessian integral has leading large-c
term `4/c`; its displayed early part and integrable remainder fit within
`4000+4 log M`. Consequently the stronger propagator
`exp(4000) M^4` is valid, as is the conservative M9 interface used by
the source proof. The primitive numerical allowance
`10^30 exp(4200)` lies below the unchanged `H >= 10^30 exp(5640)`.
Later nonlinear products correctly pay further powers of H.

The same-state nonlinear forcing is cap independent because
`|tau_R(q)| <= |q|`. Integrated affine comparison gives raw error
`H e M^10`, forward error `H e M^13`, and endpoint error
`H e M^9`; the latter retains the exact lambda factor. These are
stronger than the conservative response-interface inputs.

## 2. Normalization, probes and beta room

The field transformations make every normalized matrix update carry
the factor `lambda=a^4 r`. A strict density conversion introduces
`Delta t_j/h_j=lambda`, whereas a complete row conversion does not.
This distinction gives the stated original-time powers.

Independently checking complementary products gives the following probe
costs: injecting x_l affects later updates with degree 4-l, injecting
d_l affects earlier updates with degree l-1, observing x_l costs degree
l-1, and observing d_l costs degree 4-l. With the M4 propagator these
give normalized A powers (4,6,8), B powers (8,6,4), local resolvent row
power seven, and top strict-transfer power ten. Their conversions imply
every conservative interface entry. The first-preactivation and
top-backward probes retain the integrated state and current direct terms.

The inspected independent-root proof takes width at a fixed finite mesh
and nonzero probe amplitude, uses Gaussian integration by parts with
arrays frozen, and then removes the amplitude by finite-program
continuity. It neither differentiates a covariance square root nor
interchanges an uncontrolled derivative and width limit. Named singular
source slots remain distinct. The inactive coefficients
`2 beta^2 I_t, 3 beta^4 I_t, 4 beta^6 I_t` prevent a hidden inverse
inactive variance. Positive local resolvents give the required active
lower bound `V_i,kj >= H^-2 M^-10 h_j`.

Homogeneity gives `beta Theta_1(beta^3 t)`. The continuation room
contains the enlarged family, and its beta gaps do not depend on mesh
size. Wick positivity concerns expected polynomial coefficients, not
signs of realized Gaussian entries.

## 3. Exact source system and nonlinear defects

Unrolling all three matrices gives all six coefficient families and
their actual learned moments. The generic conditioning theorem inspected
in L3_LOCAL_COMPLETE_PROOF is formulated for finitely many independent
Gaussian matrices, each reusable in both orientations. Its projection
and response-derivative proof therefore covers this third matrix.
Source covariances are full input second moments, not residual
covariances after regression.

The order `A2,A3,A4,B4,B3,B2` is chronological. B4 has its current
N4 return; B3 and B2 each retain the higher current return times the
expected gate product. C uses strictly earlier times, so no current C
derivative is inserted. Exchange diagonalizes deterministic coefficients
only: individual sample gates remain full matrices in the estimates.

Solving the local affine part at the actual arrays gives the displayed
value identities. The largest absorption power is 23. The four incoming
scales are `M^30,M^28,M^26,M^17`; the bottom direct `U1 zeta1`
term costs only M8, below the common forward M12 allowance.

Expanding `Vgate B G` proves exactly
`P=N+a DeltaV B+a B DeltaG+DeltaV B DeltaG`.
Subtraction gives `J-Jaff=U[DeltaV Izeta+P J]`; applying the backward
resolvent gives the exact L identity. This cancellation avoids paying an
extra B-row power and retains the current N term.

Strict U gives the actual h_j for a single transpose slot. A complete
forward-source derivative row starts from the local resolvent row.
Nonlocal B terms are bounded against the preceding derivative envelope;
weighted Jensen uses marginal subGaussian moments, with no random time
supremum. The largest envelope condition is `e H^30 M^35 <= 1`.
The forward defect powers `2u+5+q` are 35,35,37. The relevant backward
powers `12+12+q` are 52,50,41. The independently bounded learned-moment
errors are smaller and included. The H40 allowance covers the displayed
finite products and fixed moment orders.

## 4. Positive closure and unchanged amplitude

The backward supersolution correctly uses unit actual variance versus
beta-squared affine variance: `beta^2 >= 1` and positive affine
learned-moment enlargement provide the necessary slack. Its excesses are
J4,
`J3+a^2 L3 J4 R3*`, and
`J2+a^2 L2 J3 R2*+a^4 L2 L3 J4 R3* R2*`.
Their largest powers are 41,65,89. The local persistence identities
control both resolvent orientations without assigning a strict density
to an arbitrary current or concentrated row error.

The positive inequality
`V_i L_(i+1) <= V_(i+1)/(beta^2 a^2)`
compresses each left strict chain. The bottom sandwich density powers
are 57,68,73. The active lower bound adds ten and the beta gap adds three,
giving the strongest forward restriction 86. The backward outer radius
adds nine to 89, yielding the sufficient exponent 98. Both sides of
every J sandwich remain strict, preserving h_j. No complete row norm is
silently substituted for a strict density.

The inactive closure is triangular because its affine backwards vanish.
The proposed increasing H powers dominate its forward increments below
their fixed margins. Finite chronological comparison and amplitude
homotopy exclude first exit, including arbitrarily small positive steps.

Since `M^100 <= 76^20 delta^-10` and `76^20 < H`, the old coefficient
gives `e M^100 <= 10^-70 H^-399`, hence
`e H^72 M^98 <= 10^-70 H^-327`.
This enforces the source, backward, forward, tube, endpoint and
nonaffinity conditions with strict room. No coefficient is reselected
for the extra layer.

## 5. Nonaffinity, strong construction and global physical time

At every fixed affine Euler mesh, the forward polynomial contains its
initialized path with nonnegative coefficients; every surviving Gaussian
monomial expectation is nonnegative. Width-uniform integrability at
that fixed mesh and then strong affine Euler convergence give
`||x_l||^2 >= 1`. Independence of the inactive root and the bounded
ordinary Frobenius chain correction proves inactive freezing and
orthogonality. Every affine sample variance is therefore at least 1/64.

The third-Hermite integral becomes monotone in sigma after the stated
change of variables, giving the old eta_* without an upper variance
bound. Arctangent regression slopes belong to [0,1], making the square
root regression residual 1-Lipschitz under L2 coupling. The forward
comparison proves the asserted `e^2 eta_*/4` margin.

At fixed cap, bounded coordinate derivatives and rank-one estimates
give local raw-Hilbert Lipschitzness. The countable generated-program
construction extends all three initialized actions boundedly; exact
finite transpose identities give genuine adjoints. The affine tube
supplies continuation through S. Backward substitution in the asymmetric
gate difference produces one factor `1+eR`, so Gaussian reference tails
defeat `exp(CR)`. Raw states and directions converge strongly to an
autonomous strong C1 uncut path. Only reference tails enter uniqueness.
Keeping both residuals proves physical uniqueness against nonsymmetric
bounded-primal competitors, and the same argument gives restart.

The endpoint above one and bounded g' make the clock diverge at its
first hit of one. For the uncut path, `C''=J J* C` proves radial
convexity and `kappa >= kappa(0)`. Initialization yields
`kappa(0) >= a^8 delta/2`. Thus the complete gain rectangle has safe
rate delta/128, and the convex specialization retains delta/32; those
claims are correctly distinguished.

## 6. Full limits and initial motion

Width is taken at fixed cap and finite auxiliary transcript. Rank-one
unrolling and stopped deterministic Euler estimates remove the mesh;
the asymmetric reference-tail bound removes the cap. Raw GD adds
`C_(R,T) n^-2` without an uncut width-uniform Lipschitz constant or a
growing-transcript theorem. Finite GF continues by loss dissipation and
the finite-time raw displacement estimate.

All three extra velocity queries are included. The conditioning argument
first clips `phi'(Z)P`; derivative-row bounds and bounded L2 actions
justify removal in layer order. Each new forward source keeps its full
covariance with previous forward queries and is a distinct formal source
argument. No Gaussian operator Lp estimate is assumed. The deterministic
velocity comparison has only one reference-truncation factor. Width
precedes cap removal at fixed truncation; compactness of the uncut L2
time image removes the final truncation. This proves the same-layer
velocity laws, second moments and integrated speeds. Fixed-grid joint
laws and the interpolation inequality give the uniform-path-norm W2
laws. L2 products give all five raw kernels and the required generated
action observables.

At initialization, nonconstant phi' makes the top beta Gram positive
definite. All three downward transpose calls retain their full covariance
and actual response derivatives; conditional covariance propagates
positive definiteness to the bottom. Every raw hidden acceleration is
a positive Gram contraction. Adjunction telescopes sample acceleration
pairings into positive sums of block norms; exchange symmetry then
gives motion for both samples in every layer. The kernel coefficients
are `2||V||^2 s^2` and `8||V||^2 t^2`. These are initial-motion claims,
not assertions of perpetual nonzero velocity.

## 7. Separate depth conclusion

The scalar initialization recursion is studied only after the width
limit at each fixed depth. Strict contraction gives q_l tending to zero.
The cubic expansion yields
`q_(l+1)=q_l-2 theta q_l^2+O_theta(q_l^3)`, hence
`l q_l -> 1/(2 theta)`. Odd Hermite covariance expansion implies
`|c_l/q_l| <= |rho|`, placing the initial projected kernel between
`delta q_l/2` and q_l. Right differentiation of an asserted uniform
exponential bound at zero excludes a common positive rate.

Removing the cubic linear projection gives residual
`(2/3) theta^2 q^3+O_theta(q^4)`, hence `1/(12 theta L^3)`.
This excludes a positive depth-uniform regression margin while preserving
strict positivity at each finite layer. General fixed-L balances and
source algebra do not supply a common-amplitude ledger. The candidate
correctly leaves that question open and does not apply the convex
contraction obstruction to the energy-normalized family.

## 8. Exact reviewed file identities

All five new mathematical files were read in full, then hashed. Their
hashes match CANDIDATE_HASHES.json:

| File | SHA256 |
|---|---|
| PROOF.md | `f91ce73868922220fd7f6621e08b167472639ebe10f8775d6bb632c10e31c585` |
| AFFINE_CERTIFICATE.md | `541d18c292f63fbd1d408953af9ade9c3911cb623fd68baee3dede4c72d0eb82` |
| SOURCE_RESPONSE.md | `25b0c23ad475f1d0565e15441de01cf9df8336aeff101c1dbf6f83224ced2e79` |
| POPULATION_AND_MOTION.md | `fd1dc71289992880ad4560afa787ddb29bb234f4883addc16cc62e49d519c837` |
| DEPTH_UNIFORMITY.md | `5782203423cba6e5695d7aa36ce1d271f75a2984801b859d426f870d7d10e9fb` |

The complete CONTRACT.md read has SHA256
`d46ad315beb3f92cd96aa6c608ecc23068106cdc8399e30a9987f4605b909eed`.

Actual prior mathematical files inspected are below, relative to
`studies/mean_field_peeling`. Hashes identify whole files even where
only the stated dependency passages were required and read.

| Dependency and passages read | SHA256 |
|---|---|
| two_sample_odd_activation_power10/PROOF.md, lines 1-110: statement, constants and affine-interface opening | `37b9e8a27135b95b8dffe05fe2b734229fadedc98bc1609259e365dbb2aeba37` |
| two_sample_odd_activation_power10/AFFINE_SOURCE_CERTIFICATE.md, all sections | `bc55c0f8e25de1f3a4316fe36e86afa25c9486e014192f2eb818358909b048eb` |
| two_sample_odd_activation_power10/REFINED_RESPONSE.md, lines 1-280: transfers, box, values and derivative subtraction | `3622a0f903cd51b712d1d524a140d834471b5eb588611f059535c41f6efd2332` |
| two_sample_odd_activation_power10/POSITIVE_SUPERSOLUTION.md, all sections | `e2be183521919ce69c7ae41b81f5cd78566c5430a78c2408687f52f1b6d86774` |
| two_sample_odd_activation_quantitative/PROOF.md, lines 1-110 and 132-184: model, constants and eta_* | `0a7dbd32cb9cc291e706813b59c0142394e6f9532244e52c21a5ab5d89ed4d02` |
| two_sample_odd_activation_theorem/PROOF.md, lines 1-190: raw model, population objects, observables and symmetry | `a4d6ed0ee99a1e111b5eba068f2219cf561a2281b1828b26227aca7a0a54a050` |
| two_sample_odd_activation_theorem/AFFINE_CORE.md, all sections | `634dd3bbc35436e9d1760bee5c11cbf2124b3c42e8920a1bf3a4fcbb15039711` |
| two_sample_odd_activation_theorem/INITIAL_MOTION_AND_NORMALIZATION.md, all sections | `c96316fdc481ef3f9e6fc402514ca9b45bcff12e199023505fbbb86d4e15bf24` |
| two_sample_odd_activation_theorem/SOURCE_AND_LIMIT_BRIDGE.md, all sections | `2a140f2a5a39f911b5c2ca51bc79102d20e9209f1450d38005360c88103f6e77` |
| two_sample_odd_activation_theorem/sources/L3_LOCAL_COMPLETE_PROOF.md, lines 297-449 and 456-730: finite conditioning, singular queries, feedback, common actions and adjunction | `f97148f84979ab5b8489927d477b6abb3d919e27fd457455f2e3b98416725de4` |
| two_sample_odd_activation_theorem/sources/TWO_SAMPLE_SOURCE_BASELINE.md, lines 478-741: answer stability and independent-Gaussian probe identity | `a84187ecd3639d0c4b7b209255056597326c97f11548faaf9b918659ae07477f` |
| two_sample_odd_activation_theorem/sources/FIXED_CAP_VELOCITY_BRIDGE.md, all sections | `a2322a8dbacf28244b9fef63e757ba3a915628dbdf31020062f8c5772a1c0aa0` |

No mathematical correction is requested for this hash-specific candidate.

