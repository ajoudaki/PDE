# Evidence ledger: frozen depth-three tanh contract

| ID | Claim | Status | Decisive evidence or dependency |
|---|---|---|---|
| T1 | Equations (1)--(4) are the exact finite-width gradient flow and raw kernel | proved | Direct differentiation with the normalized rank-one convention |
| T2 | `(Gamma_1,Gamma_2;Y,A,P_1,P_2)` is a one-time autonomous restartable state | proved algebraically | Every forward/backward field is recomputed from the current state; actual adjoints retained |
| T3 | Compact-time normalized `L2`, operator, and trace bounds hold uniformly in width | proved | MSE energy identity and rank-one trace norm |
| T4 | Forward features and learned-adjoint pieces are coordinatewise bounded; `A,B_3` are empirical `psi_2` | proved | `|tanh|<=1`, (6), Gaussian initialization |
| T5 | Fixed finite Euler meshes converge through one joint Gaussian program with actual adjoints | inherited/proved for fixed programs | Same two-sided conditioning construction as the audited arctan source; tanh is bounded smooth |
| T6 | `(AG_T)` holds at initialization | proved | Exact Gaussian regression/Onsager decomposition for a forward query followed by the actual transpose |
| T7 | `(AG_T)` holds uniformly on every compact interval | open | Requires trajectory-specific low influence/no row-or-column selection |
| T8 | Conditional on `(AG_T)`, the IDE is unique and the exact finite systems converge uniformly with raw `K` | proved conditionally | Exponential-tail truncation, Bihari--Osgood, fixed-mesh source convergence |
| T9 | Bounded inputs, exchangeability, and Gaussian operator norm imply `(AG_T)` | falsified | `b_i=sign(Gamma_{i1})` gives one `sqrt(n)` coordinate in `Gamma^*b` |
| T10 | Fast scalar gate decay alone gives uniform raw-kernel tails | falsified as a proof route | A sign-changing focused cotangent pulse can return the preactivation to an open gate while ending with an order-one kernel spike |
| T11 | A genuine nonlinearity is uniformly continuous on arbitrary normalized-`L2` restart balls | falsified | One-coordinate multiplier localization; only affine activations avoid it |
| T12 | Weak compactness plus the MSE energy equality identifies the limiting raw gradient and removes concentration defects | falsified as a stand-alone route | Equality of dissipation upgrades weak to strong convergence only after the limiting chain rule/vector field has already been identified; using it earlier is circular |
| T13 | The gated cotangents alone form a restartable closed state | falsified | They discard a raw adjoint hidden behind a nearly closed gate; reopening the gate changes future dynamics, while differentiating the hidden part generates longer Gaussian words |
| T14 | The ambient adaptive-selector defect can be invisible in the empirical coordinate law while carrying order-one raw-kernel energy | proved | `b_i=sign(Gamma_iJ)` gives a Gaussian bulk plus one `sqrt(n)` coordinate and an extra `2/pi` in normalized square norm |
| T15 | `K_n` is stochastically equicontinuous at canonical initialization | proved | Gaussian regression gives square-UI of both immutable-adjoint fields at `t=0`; a layerwise truncation argument propagates normalized-`L2` continuity through the tanh multipliers |
| T16 | A canonical concentration boundary layer can occur at a deterministic or random time `t_n -> 0` | falsified | `lim_{delta->0} limsup_n P(sup_{t<=delta}|K_n(t)-K_n(0)|>eps)=0`; the same proof excludes concentration on any `o(n)` coordinate set |
| T17 | The cavity response has uncontrolled diagonal exponential growth from `A`, `Q_2`, or `Q_1` | falsified | Exact identities express these diagonal terms as logarithmic derivatives of `sech^2`; an integrating factor removes them |
| T18 | Energy plus the integrating factor closes the full cavity estimate | falsified as a proof route | The remaining transverse products are anisotropic `L2 x L2 -> L2` terms; normalized `L2`, operator, and trace bounds alone do not control them |
| T19 | An immutable transpose admits a current-time Gaussian creation--response decomposition | proved | Gaussian divergence gives `(Gamma^*b)_j = delta_j(b/sqrt(n)) + n^{-1/2} tr D_j b` exactly |
| T20 | A same-order Malliavin source-regularity estimate implies `(AG_T)` and raw-`K` convergence | proved conditionally | Gaussian divergence inequalities give empirical moments `O(p)` through `p ~ log n`, which is equivalent to the required empirical `psi_1` bound |
| T21 | Ordinary absolute Malliavin/energy bounds prove the source-regularity estimate | falsified as a proof route | Actual-transpose differentiation creates an order-one rank-one operator tangent; Hölder then generates weights of every order and exhausts a fixed exponential-moment radius |
| T22 | MSE dissipation or the tanh logarithmic-gate identity supplies a pathwise signed response contraction | falsified | The residual-Hessian part of the parameter-flow Jacobian is indefinite, can expand tangent volume on an open reachable configuration, and the exact Stein recurrence has two response terms of either sign |
| T23 | A same-`p` annealed Stein coercivity estimate would close `(AG_T)` | proved conditionally | The exact even-moment recurrence reduces the issue to controlling the response trace and shear at order `p`, without a `p -> 2p` loss |
| T24 | A quantitative tail modulus gives a deterministic local restart interval | proved | Layerwise truncation bounds the local kernel modulus by `(1+M) delta` plus the square tails of `A,U_2,U_1` at the restart time |
| T25 | The local Orlicz estimate propagates its own restart hypothesis | falsified as a proof route | A frozen adaptive transpose can convert a bounded-coordinate increment into a `sqrt(n)` coordinate; fine partitions permit coherent accumulation on the same coordinate |
| T26 | The nonzero-label theorem follows from the zero-label case | falsified | For `y=0` the residual clock is `o_p(1)` and initialization equicontinuity suffices; a fixed nonzero target travels order-one feature time and retains the adaptive-transpose problem |
| T27 | Fixed-mesh Gaussian-program convergence extends automatically to a slowly growing mesh | falsified as a proof route | Adaptive conditioning has `O(m)` left/right queries, but quantitative control also requires robust query-span rank and averaged Euler stability |
| T28 | Small singular query directions may be discarded without changing later transpose queries | falsified in the ambient program class | Two individually sub-Gaussian queries separated by `e_J` span `sqrt(n)e_J`; tanh followed by the opposite transpose converts that weak innovation into order-one concentrated square energy |
| T29 | Robust-rank and uniform-stability hypotheses yield an explicit growing-mesh schedule | proved conditionally | If their constants grow at most `exp(C m log m)`, then `m_n=((log n)/(log log n))^(1/3)` makes stochastic program and Euler errors vanish |
| T30 | A fixed small nonlinear perturbation is an `o_n(1)` correction to the identity dynamics | falsified as a proof route | The first clock derivative already contains a canonical adaptive-transpose response whose nonlinear excess is `O(epsilon)` but does not vanish with width |
| T31 | A smooth residual-sine activation is the cleanest near-identity perturbative candidate found | proved selection fact | It is unit-variance, globally bi-Lipschitz, and every derivative of order at least two is uniformly `O(epsilon)` |
| T32 | One current sensitivity operator `J=D_{Theta_0}Theta_t` closes a width-limit Malliavin-response proof | falsified | Finite-width `(Theta,J)` is algebraically Markov, but controlling its response trace by the next Stein/Sobolev step requires `H=D^2Theta`, and the derivative order then rises inductively |
| T33 | The full-`J` operator norm is dimension-uniform for a small residual-sine perturbation | falsified | Already at initialization `||J_n'(0)||_op` is at least order `epsilon sqrt(log n)` because the readout maximum multiplies nonzero curvature |
| T34 | Gaussian divergence is continuous under convergence of its current argument alone | falsified | `u_k=sin(kg)/k -> 0` in `L2`, while `delta(u_k)` retains nonzero `L2` norm; the next Malliavin graph datum is necessary |
| T35 | Retaining the full finite flow Jacobian plus every tensor amplification is a non-vacuous compressed closure | rejected by the contract audit | It is an `O(n^4)` tangent-lift repackaging of the microscopic flow and has no established canonical strong width limit |
