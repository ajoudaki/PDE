// Campaign 4 intentionally reuses the exact Campaign-1 double-graded
// connected recurrence without semantic changes.  This wrapper gives the
// Campaign-4 build an isolated source identity while preserving the audited
// implementation byte-for-byte through preprocessing.
#include "../campaign1/graded_sector.cpp"
