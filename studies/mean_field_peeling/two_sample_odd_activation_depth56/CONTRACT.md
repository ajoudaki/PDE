# Explicit depth-five, depth-six and fixed-depth activation bounds

2026-09-07. The user asks whether c_poly delta^10 is proved for all
hidden depths L>=3, and otherwise requests explicit complete proofs at
L5/L6 and the strongest justified dependence on delta and L. The
starting established range is L3/L4 only. This turn authorizes new theory
and independent adversarial proof agents; no trajectory experiments,
external publication, commits, or changes to the original model.

The canonical model is the one in
../two_sample_odd_activation_depth4/PROOF.md with L hidden layers, all
using phi(z)=az+e atan(z), and the same Gaussian initialization, original
finite random readout N(0,n^-2), normalized predictions, raw metric,
physical loss flow, simultaneous raw GD step n^-2 and interpolation.
Two deterministic RMS-unit inputs satisfy |rho|<=1-delta, with either
binary label pair. The requested convex activation has a=1-theta,e=theta.

Retain the complete theorem: autonomous global strong C1 population
solution on L canonical neuron spaces with all L-1 adjacent initialized
actions and genuine adjoints; uniqueness against bounded-primal
nonsymmetric competitors and reached-state restart; full-width-sequence
GF/GD limits in probability at every fixed dataset, depth and finite
physical horizon; all L+1 raw kernels, both action orientations on
generated probes, same-layer path and velocity W2 laws, second moments,
integrated speeds, positive activation-regression nonaffinity at each
finite time, and all initial hidden-block/sample-layer motion certificates.

Width is taken at fixed cap and finite auxiliary transcript, before
deterministic mesh/cap removal. Velocity product truncations retain their
ordered reference-tail limits. No growing-transcript theorem, uniform
width-depth limit, across-layer neuron pairing, or changed raw metric
is allowed. Activation coefficients may depend only on delta and the
explicitly stated depth; a common coefficient is claimed only if proved.
Proof-only times, beta scales and bounds may depend on the dataset.

Preserve three distinctions: a depth-independent exponent with a
depth-dependent prefactor; one common pair valid at every finite depth;
and depth-uniform numerical convergence/nonaffinity estimates. The
previous convex initialization obstruction rules out the third in its
stated form, not the second. A sufficient exponent is not an optimality
claim, and an improved exponent with an extremely small prefactor is
not a practical-size activation guarantee.

Parallel routes: explicit affine curvature/probe constants at L5/L6
and fixed L; nonlinear source coefficient errors and positive comparison;
independent chain-compression/whole-theorem audit. Root reconciles the
routes and assembles the complete proof. A completed candidate is frozen
for fresh independent complete-proof adversarial reviews before promotion.
All earlier theorem directories are immutable mathematical dependencies.
