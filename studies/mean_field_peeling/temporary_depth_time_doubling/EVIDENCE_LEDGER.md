# Evidence ledger

| ID | Claim | Evidence required | Current status |
|---|---|---|---|
| E1 | Exact depth--time finite-width updates have only past-time learned terms | direct differentiation and summation of the rank-one updates | **established**: `WIDTH_DEPTH_TIME.md`, Section 2; `PROOF.md`, Section 1 |
| E2 | The raw chronology has `(2N+1)(L-1)` predictable reused-matrix actions | complete forward/backward action list | **established**: `WIDTH_DEPTH_TIME.md`, Section 3 |
| E3 | Every nonterminal population history Gram is positive definite for nonconstant `phi` and fixed `h != 0` | chronological Schur-complement induction, with affine top branch separated | **established**: `WIDTH_DEPTH_TIME.md`, Section 7; independently passed in `AUDIT_WIDTH.md` |
| E4 | Every reused row/column response equals the inverse-free derivative expectation | exact adaptive Gaussian regression plus integration-by-parts cancellation | **established**: `WIDTH_DEPTH_TIME.md`, Sections 4 and 6, including the current reused-column dependence |
| E5 | Empirical Grams, cross moments, regression coefficients, and output converge with uniform integrability | explicit finite-action coupling, moment tower, stopping probabilities, and raw energy majorant | **established**: `WIDTH_DEPTH_TIME.md`, Sections 8--10; independently passed in `AUDIT_WIDTH.md` |
| E6 | The width-first DAG is `C^5` through `h=0` | singular Price identity plus finite local envelope recursion | **established**: `COMPILER_DEPTH_TIME.md`, Sections 3--4; independently passed in `AUDIT_COMPILER.md` |
| E7 | The exact cubic coefficient `kappa(phi,L,t)` is a terminating activation-integral formula | chronological exact Price compiler, followed only afterward by derivative identification | **established**: `COMPILER_DEPTH_TIME.md`, Sections 6 and 8; restated non-circularly in `PROOF.md`, Section 3 |
| E8 | The exact coefficient is quadratic in `t` and equals `-t(2t-1)J_{phi,L}/2` for the nine-moment depth recursion | a common singular `C^3` gradient germ, enlargement/cocycle compatibility, and nodewise temporal-operator intertwining | **established**: the fixed bounded operators `W_{a,0}=I_a+J_a^*` give one compatible action on moving singular queries; see `CUBIC_DEPTH_TIME.md`, Sections 4--7, `OPERATOR_BRIDGE_CHECK.md`, and the clean hostile pass in `AUDIT_CUBIC_REAUDIT.md` |
| E9 | A fixed `(L,t)` fifth-order constant uses one activation-only base and an explicit `(L,t)` exponent | counted compiler majorant | **established**: `COMPILER_DEPTH_TIME.md`, Sections 5 and 8; independently passed in `AUDIT_COMPILER.md` |
| E10 | The unconditional depth-three comparison has an explicit coefficient and remainder | specialize the compact coefficient and compiler exponent to `L=3` | **established** with `kappa(phi,3,t)=-t(2t-1)J_{phi,3}/2` and explicit `E_{3,2t}` in `PROOF.md`, (C.12), (T.19)--(T.21) |
| E11 | A uniform all-`t` `t^4` remainder holds for the actual network | uniformly bounded one-step operator or an equivalent stability theorem | **open**; the fixed-time compiler does not imply it, and linear activation rules out powers below four |
| E12 | One shared activation base majorizes the compact coefficient at every depth | termwise absolute-value recursion for the nine activation moments | **established**: `A_phi`, `overline J_L`, and `|kappa(phi,L,t)| <= t(2t-1)overline J_L/2` are proved in `CUBIC_DEPTH_TIME.md`, Section 10, and restated in `PROOF.md`, (M.1)--(M.5) |
