# Lower powers and three-input extension

2026-09-07. The user requests an extension of the complete proved
odd two-input L3 theorem from theta <= c_poly delta^4 to delta^3,
then delta^2, and an analogous complete three-input theorem under
pairwise |rho_ij| <= 1-delta. The same convex mixture and all original
raw-model, initialization, GF/GD, population, uniqueness, action,
path/velocity, nonaffinity and feature-motion conclusions are targets.
The amplitude may depend on delta alone. A practical prefactor is not
an additional requirement; preserve the old explicit prefactor where
valid. No exponent-two or three-input result is assumed to be true.

Three inputs may have a singular input Gram. No spectral-gap or
label-compatibility hypothesis may silently be added. An initialization
Gram bound or failure of the affine reference is not a complete answer.
For every sample count, width limits are for each fixed dataset on each
fixed finite physical interval, whereas the population flow is global.
No uniform-in-dataset width theorem or infinite-halfline convergence
norm is requested.

Authorized: new theoretical proof research, local artifacts, delegated
routes and fresh independent adversarial review of complete candidates.
No trajectory experiment, old-proof edit, external action or commit is
needed. No hard user time/token budget has been specified. Existing
unrelated workspace changes must be preserved.

Initial route portfolio: sharper response/primal estimates; structured
positive coefficient closure; a new three-input global construction.
Root independently analyzes paired-input geometry and weighted affine
comparison. Each route must return proved lemmas, exact dependencies,
and every remaining obstruction. Old proof opinions are not premises.

Final research outcome: the complete two-input theorem is proved for
0 < theta <= c_poly delta^2 with the same explicit prefactor, so the
requested delta^3 extension is included. The generic three-input theorem
remains open. The completed three-input work consists of initialization
bounds, an affine-reference obstruction, necessary displacement/time
bounds, and an explicitly conditional symmetric scalar-clock lemma.
REVIEW_STATUS.md records the fresh complete-proof audits. No numerical
trajectory experiments were run and no earlier proof files were edited.
