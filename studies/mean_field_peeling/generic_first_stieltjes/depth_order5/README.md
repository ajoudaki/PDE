# Depth-order-five extension: current state

This subtree extends the one-sample generic-activation calculation through
`F^(5)(0)` from two hidden layers to the separately fixed depths `H=3,4`.

The canonical presentation is
[`primary/H3_H4_ORDER5_SELF_CONTAINED.md`](primary/H3_H4_ORDER5_SELF_CONTAINED.md).
It contains the exact model, moment grammar, Padé coefficients, arbitrary-
fixed-depth compiler equations, controls, theorem boundary, audit ledger, and
embedded terminal formulas.

Authoritative supporting artifacts:

- primary frozen maps and hashes: [`primary/`](primary/);
- independent frozen derivation: [`independent/`](independent/);
- hostile equality/transpose/probability/numerical audits:
  [`audit/`](audit/);
- neutral exact finite-width jet and preregistered nonpolynomial contract:
  [`common/`](common/).

Current claim discipline:

- finite-width Taylor identities and readout parity are exact;
- the `H=3,4` Gaussian normal forms are algebraically audited by literal
  independent coefficient-map equality;
- the annealed width limit is theorem-level under the stated polynomially
  smooth activation assumptions, or conditional on a separate UI bridge in
  the weaker tier;
- the compiler runs for arbitrary fixed depth, but no simultaneous
  width/depth limit and no depth-uniform small flattened formula is claimed.

