# Approved promotion of C.4.8

Date: 2026-09-12. Coordinator and sole Git writer: `/root`.

## Explicit approval and exact scope

After receiving the complete version-2 proposal, final mathematical result,
review outcomes and destination list, the user stated in this task:

> yes I approve the promotion, please carefully follow the promotion gate to established part in the workflow for this

This approves only `proposal_C4_8_v2.md` (SHA256
`98fa7614b6449b58b07c65df047b68a3484bf0760b36a3a25052f67d72692928`)
and `promotion_edits_v2.json` (SHA256
`50a95f4576c56c1438974a76da25a793ebe188e39c00e2f98f79bd79cb294f2c`):
append C.4.8 to `docs/global_nonlinear.md`, replace its one C.4 coverage
summary, and replace the two specified summaries in `docs/README.md`.
No maintained code change is included. The approval was recorded before
modifying either established file.

## Gate recheck before integration

The coordinator reread `AGENTS.md` and all of `RESEARCH_WORKFLOW.md`, including
Part 2, and the study's current README/proposal. Their process hashes and all
frozen scientific dependencies match the final review packets. The exact
candidate, edit specification and three complete final review reports are
unchanged. The coordinator personally read those complete reports in the
preceding research turn; no interruption invalidates their identical inputs.
The separate [promotion preflight](promotion_preflight.md) verified 65 hash
comparisons and found no unresolved required correction. This administrative
recheck does not substitute for the completed scientific reviews.

The coordinator also read the subsequently added same-study outcome, fresh
proof, scoped statistics and evidence assessments completely, repairing the
one truncated fresh-proof read. They add no required mathematical correction.
Those concurrently owned assessment files are preserved and are not included
in this task's commits. The evidence assessment's historical selector-input
hash is for the selected precursor before assembly; final proof and integration
reviews cover the entire actual version-2 text. It is not a changed reviewed
dependency and requires no change to the approved package.

The assessment's limited access to historical launch/completion records is
addressed by newly retained actual records:

- `promotion_spawn_records_v2.json` extracts only the three original review
  launch calls from this task's own session log. Each explicitly records
  `fork_turns="none"`; their original line numbers and raw-record hashes are
  retained. The at-rest encrypted message payload is omitted.
- `promotion_review_events_v2.json` retains the app's corresponding start and
  completion events, distinct subtask IDs, completed-turn metadata, command
  execution records and final messages with the frozen report hashes.
- Complete neutral assignments and complete scientific reports remain the
  packet's record of permitted inputs, actual reading and attacks. The app
  outputs were requested with truncation; no truncated output is represented
  as a new complete proof audit.

Only this task and its three own review subtasks were retrieved. The launch
call IDs match the app's started events. The original coordinator directly
initiated these fresh isolated reviews and personally checked their completed
reports. These records add attributable launch/isolation-mode/completion
evidence; they do not infer mathematical correctness from an agent name.

The separate [provenance addendum](promotion_provenance_addendum.md) checked
the three exact raw launch lines against their hashes, matched their call IDs
to the app events and distinct completed task IDs, verified final report
hashes, and confirmed the requested-read interval unions. The coordinator read
this complete report and the preflight, and verified their hashes:
`e1afa5d8af248344fb01223509a6573c9e3786f6f9ab00f5a666f53646722e63`
and `80d2fb8cf0433f2e981add2dfe8a9e2e3fc2687cd64dd67ccba61dda46ba5d53`.
All 118 retained reviewer command entries completed with exit zero. The
addendum explicitly distinguishes requested read coverage from semantic proof
reading, and does not claim to inspect encrypted prompts or omitted interaction
bodies. Those limits do not alter the original coordinator's direct isolation
record, full report reads and checked final scientific inputs.

Starting HEAD: `a68fed6c1f3a10abc4bcee683a70f8ac36d87f45`. The index was
empty. Concurrent modifications and untracked files were identified as
metadata and are excluded from writes and staging. No checkout/worktree was
created or copied. Root owns only the two approved destination edits and its
listed study records; the common nonblocking Git lock is used for commits.

## Application and correspondence

Status: the exact approved package is applied and correspondence checks PASS.
The application first verified both original destination hashes, computed all
four approved changes in memory, and checked the resulting hashes and exact
equality with the frozen standalone edition before writing either file. It
rechecked each destination's original bytes immediately before writing.

| Live destination | Exact final SHA256 | Mapping |
|---|---|---|
| docs/global_nonlinear.md | bda93ec446425c05f8c06ac3a67fa1505906dff309b74e13bab4effc1527bf05 | Candidate lines 1–1552 equal live lines 11440–12991; one C.4 summary replacement |
| docs/README.md | 5210ccd284ea79215d81c18f2fba93762538cfb1bb5b6b295c6e33e558225e1f | Exactly the two specified coverage-summary replacements |

The full post-integration verification was executed once from the repository
root with Python 3.10.12:

```
python studies/trained_prediction_sampling/verify_live_promotion_v2.py --output data/generated/trained_prediction_sampling/promotion_live_20260912
```

Exit 0, PASS. Validator source SHA256:
`8dcdba382fa909a4dd1db512bd3f636b22e77dc829ac839e1813203d9a70fd9f`.
Complete generated report SHA256:
`a0e76f819e692d85ccd5fd6a65010bc63bd8545cfc99b90a23fee9508cebba4f`.

The live documents equal both separately assembled reviewed editions byte for
byte. Removing the exact appended suffix and reversing only the three summary
replacements recovers the original full-file hashes. All other scientific and
process dependencies and the frozen candidate, packets and final reports are
unchanged. The 88 new equation labels are unique in the live chapter; all 67
explicit new references and the new guide navigation target resolve. The
addition contains no study/generated-data scientific dependency.

The two complete original deterministic programs were copied to fresh generated
scratch and run with isolated Python, no `PYTHONPATH`, and that scratch as the
working directory. Six exact Gaussian identities and all 748 exact sampling
assertions passed, with empty stderr. The Gaussian output hash is
`74379f1ba103d4dbb1f24ab221c3d38eb247ac3d364339b07a828f1378342e6c`;
the sampling output hash is
`0a68bc0ccfcbfb4341d1364a435b0d18976b76ac9b418f11aa35a2353c233d56`.
The coordinator inspected the full verification report. These are affected
integration/algebra checks; they are not new proof reviews, a whole-book
exporter audit or a training experiment. No maintained API changed.

The scoped integration commit is
`0557717fb873818404f98cce66ab8c7d40faf7b0`, with message
`Promote reviewed trained-prediction sampling theorem to C.4.8`.
It contains exactly the two approved destination files, this execution record,
the live-verification source, the frozen administrative preflight/addendum and
the two review-event metadata records. A nonblocking common Git lock protected
the stage/verify/commit transaction. The index was empty before staging;
the staged path set and frozen file versions were verified explicitly;
no generated product or concurrently owned assessment/unrelated file was staged.
The follow-up study-only closure records this commit and final state in the
README. No scientific difference from the reviewed edition was introduced.

Post-commit verification read both destination blobs directly from that commit
and confirmed exact equality with the live files and reviewed final hashes.
All frozen scientific/review inputs and the new deterministic-result hashes
were rechecked; closure links and whitespace checks passed. The two established
destinations have no remaining working-tree diff. Promotion is complete for
the exact approved scope; the original frozen proposal/review records retain
their historical pre-approval wording and are superseded operationally by this
execution record and the current README.
