# Reducing the polynomial exponent to ten

2026-09-07. Target: prove that an absolute c>0 makes every
0<theta<=c delta^10 admissible for the complete original odd two-input
L3 theorem. Retain all hypotheses, raw dynamics, initialization,
observables, uniqueness/restart and limit quantifiers in
../two_sample_odd_activation_theorem/PROOF.md. In particular the width
limits are on every fixed finite physical interval for each fixed
dataset with |rho|<=1-delta, not uniform in dataset or infinite time.

The existing delta^800 theorem remains an unchanged dependency.
An intermediate bound or conditional response lemma does not establish
the requested improvement. The new coefficient must depend on delta
alone and work for every binary label choice. The compact gain family
1/2<=a<=1 may be retained, with theta=e and a=1-e for the requested
convex mixture.

New normalization, auxiliary proof trajectories and weighted estimates
are permitted as proof devices; changes to the actual network,
optimizer, data class, population equation or limit order are not.
No original proof file is to be modified. No trajectory experiment,
external publication or commit is authorized by this task.

Three parallel theory routes: exact normalized affine source inputs;
refined nonlinear response errors; positive affine supersolutions
instead of a generic inverse/Taylor estimate. Root assembles the proof
and derives normalized gate/primal bounds. A complete candidate must
undergo fresh independent adversarial audits before promotion.

Current status: target achieved in the assembled proof; three fresh
independent complete-proof audits returned PASS. The prefactor remains
the same conservative explicit constant as before.
