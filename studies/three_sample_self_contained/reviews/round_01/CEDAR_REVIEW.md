# Independent adversarial mathematical audit

**Verdict: PASS.** I identify no remaining mathematical correctness or completeness objection after reading the complete manuscript and auditing its stated theorem and proof dependencies. This is a manuscript audit, not a claim of machine-checked formal verification.

## Input, isolation, and actual reading coverage

- Sole mathematical reading input: `/tmp/manuscript-37a446c39d7b/REPORT.md`.
- Input size: 3,547 lines; 210,132 bytes.
- SHA256, checked before and after the reading: `d4ab2401e4659ab4a7aef66f83ad40ab14b2db99984df8185cfa7770965304a5`.
- Complete sequential reading coverage: lines 1–220, 221–440, 441–660, 661–880, 881–1100, 1101–1320, 1321–1540, 1541–1760, 1761–1980, 1981–2200, 2201–2420, 2421–2640, 2641–2860, 2861–3080, 3081–3300, and 3301–3547. Each was read in a bounded tool response without truncation. No section or proof was skipped.
- I did not read project files, skills, source notes, previous reviews, research history, or other mathematical inputs. I did not browse the web, perform numerical experiments, or contact other agents. The only output file written is this review; the manuscript was not edited.
- Particular emphasis: source-response bounds, explicit constants, chronological closure, arbitrary positive meshes and controls, residual feedback, global continuation, removal of caps, uniqueness, and the order of limits. I also audited the Gaussian construction, all stated finite-algorithm observables, and the initial-motion proof.

## Objections and repair requirements

No remaining objections were identified. There is consequently no severity-ranked defect list or required mathematical repair. The detailed findings below record why the potentially delicate invocations are supported by the manuscript, rather than treating a lemma label as sufficient evidence.

## Model, scaling, quantifiers, and theorem statement

The raw metric in (M.6) produces exactly (M.7): the first-block Euclidean gradient carries (1/n), the inverse metric contributes (n/d), and the readout inverse metric cancels its (1/n). Normalized rank-one matrix actions have the stated ordinary Frobenius norm, so the population Hilbert–Schmidt formulation matches the finite metric without a missing width factor.

The finite GF continuation argument is valid in its stated finite-dimensional setting. Energy controls the integral of squared raw speed, hence length on every finite interval, hence a finite strong endpoint before any alleged blowup. Finite GD needs no existence theorem beyond its everywhere-defined finite updates. The finite random readout is retained in both algorithms and is discarded only in an auxiliary fixed-cap comparison whose initial discrepancy vanishes.

The admissible-geometry restriction is consistent, including singular input Grams. The separation-only activation choice does not use a bound on the full first-weight norm independent of (d): all relevant bounds use the three normalized projections and hidden displacement. The theorem asserts convergence on each fixed finite physical horizon, not uniformity in datasets or an interchange with infinite training time. Its generated probes have fixed instruction count. These distinctions agree with what the proof actually establishes.

## Part F: Gaussian foundations and the raw Hilbert calculus

1. **Adaptive conditioning.** The sequential conditional-independence argument before (F.6) is sufficient for adaptive interleaving of both matrices: a new query is measurable from the existing transcript, and conditioning its linear answer affects only the queried residual factor. The compatibility relation and minimum-Frobenius-norm calculation verify both the conditional mean and the residual projection in (F.6). Formula (F.7) has the correct normalized variance and reverse-response coefficient. The discarded output projection has conditional expected squared RMS norm equal to its finite rank divided by (n).

2. **Empirical laws.** The nonsingular induction supplies both weak convergence and second moments; the conditional Gaussian cross term is negligible under the stated bounded-in-probability norms. The root law requires only its given finite second moment. The passage from weak convergence plus second moments to Wasserstein convergence is supplied inside the manuscript. No independence of trained neurons is used.

3. **Source rules and singular Grams.** In (F.11)–(F.12), orthogonality removes the old forward-input combination, Gaussian integration by parts gives the reverse-input Gram times the derivative row, and the old response terms cancel when inserted in (F.7). Thus the full-source rule is derived, including both orientations and interleaved calls. Its formal derivative convention keeps deterministic coefficients fixed and retains distinct named source coordinates at rank loss. The regularization in F.4 adds a fresh independent input noise at every query; the positive Schur-complement argument establishes the provisional nonsingularity hypothesis. The finite-array comparison (F.13) and finite-dimensional covariance-square-root continuity then remove the regularization without continuity of a pseudoinverse. The derivative passage is justified by bounded first derivatives of each fixed finite unrolled expression. This proves the precise nonclassical result later used as internal Lemma F.

4. **Canonical actions.** The countable language includes both action orientations and enough bounded coordinate functions for density. Its finite unions have the correct joint laws. Passing the finite norm inequality to generated inputs makes the action assignment well-defined on (L^2) equivalence classes; density extends it to the complete layer spaces. Passing the finite adjunction identity gives actual Hilbert adjoints, rather than independent reverse actions. Arbitrary fixed real coefficients and admissible coordinate functions are handled by the stated approximation argument. Nothing here asserts operator-norm convergence between unrelated finite-width spaces.

5. **Scalar differentiability.** The multiplier lemma F.5 and curve chain rule F.6 are valid in strong (L^2). The weighted Taylor estimate (F.41), with fixed incoming weight and a tail split, supplies the needed scalar Fréchet remainder. The successive backward expansions in F.7 have (O(eta^2)) bilinear cross terms and (o(eta)) weighted nonlinear remainders. This establishes a continuously Fréchet differentiable scalar predictor and its raw gradient, without an invalid claim of Fréchet differentiability of a nonlinear Nemytskii map from all of (L^2) to itself. The energy and kernel identities follow from that proved gradient formula.

6. **Local capped dynamics.** Fixed-cap gates have bounded coordinate first derivatives. Forward propagation, backward substitution, and the rank-one inequality therefore give a locally Lipschitz raw vector field on primal balls. The contraction and Euler proofs are included. Their constants can be made width-independent on the specified bounded events. The proof explicitly reserves growing-transcript and cap-uniform estimates for later parts.

## Part R: uniform controlled responses and the complete constant chain

I checked the source equations (R.11)–(R.16), their sample-index placement, and their learned terms against the raw update (R.10). Forward returns are strictly past; reverse returns include the current stage. The construction order (R.17) makes all coefficients causal. The controls have ℓ¹ norm at most one and are frozen in formal derivatives; no derivative of residual normalization is used.

The following are the main checks of the explicit chain defining ε*:

- On the ball (b=2B), (R.20)–(R.21) yield affine raw-component Lipschitz bounds (a^3b^2,2a^3b^2,3a^3b^2,3a^3b^2). Their sum is dominated by (Q=100a^3b^3). The answer-perturbation table has the correct affected updates and factors. The probe recurrence carries a factor (h_j) when only time (j) is forced; its finite-product bound uses only total mesh length, with no maximum-step or minimum-step hypothesis.
- The Gaussian probe argument (R.25)–(R.27) takes width at fixed nonzero perturbation and then takes perturbation amplitude to zero. Independence of the new root justifies the pairing, while continuity is established for the finite affine recursion without a covariance inverse. Choosing deterministic signs bounds the absolute derivative row, including separately named singular slots. This supports the affine bounds (A_0,M_0) in (R.28), including the harmless sample-row conversion factor.
- The nonlinear same-state errors (R.31)–(R.32), inserted in the rank-one difference inequality, are dominated by (Qε). Using the affine Lipschitz field gives (R.33) without any cap-dependent nonlinear Lipschitz constant. A first-exit induction works for arbitrary positive coarse steps: the sum of preceding increments bounds the next node strictly inside the ball when ε ≤ (B/(2T_0)).
- The source variance bound comes from these actual coupled query norms, before any response estimate is assumed. The moment-difference estimate (R.35) uses the actual shared-action coupling, not a proposed coupling of growing covariance square roots. The control weights retain exactly the required (h_j) factors in learned moment errors.
- On a bounded coefficient prefix, the recurrences (R.38)–(R.40) have forcings proportional to √p and nonnegative time-sum feedback. Their finite-product solutions give (R.37), uniformly over the number and inequality of mesh steps. The maxima are maxima of norms, not random time suprema. The exponential estimate (R.43) follows from the displayed factorial bound.
- Equations (R.46)–(R.49) include both gate derivatives and all current terms. The envelope in (R.50) dominates the derivative recurrences with the correct source-step factors. In particular, (R.54) retains the terminal factor (1+ε Q_k); the current incoming field is not erroneously included in the past-time exponential. Convexity in (R.55), followed by the Gaussian-square exponential bound, proves integrability without independence between time coordinates.
- The same-array affine subtraction in (R.59), (R.62), and (R.65)–(R.66) keeps all nonlinear forcing terms. It does not silently replace the deterministic arrays by their actual affine baseline. The reverse-source forcing retains (h_j) even for unequal meshes. The choice (D_1=20H^8e^{HS}) dominates the feature/readout difference bounds; (10H^3D_1=200H^{11}e^{HS}) is exactly the derivative coefficient used in (R_0). The remaining summand (m_0(1+S)) covers the learned moment errors.
- The deterministic affine systems (R.69)–(R.73) follow from the differentiated source equations. Their subtraction produces the stated current-row dependence: α² uses past B²; α³ uses current A² and past B³; β³ uses past A³ in its affine part; β² uses current B³ and current A² through U. The expansions (R.79), (R.81), (R.83), (R.85), and (R.87) have their indicated step factors. In particular, the double-sum bound in (R.83) supplies S, not a mesh-count factor.
- The constants (D_2,D_3,C_T,E_3,C_U,E_2,K_*) are finite functions of ((a,B,S)). Because (K_*=2max{1,D_2,D_3,E_2,E_3}), it controls the sum β²+β³ in (R.89). The telescoping product in (R.92) closes the preceding-row estimate. The four-stage induction in R.9 establishes each new row before invoking its bound in the next stage; it is not a simultaneous bootstrap on unavailable current rows. The explicit current returns (R.93)–(R.94) agree with the derivative equations.

Accordingly (R.90) is a positive cap-, mesh-, control-, and geometry-independent threshold once the affine hypothesis is verified. No nonclassical response estimate is left unproved behind a label.

## Part G: geometry, arbitrary meshes, residual clock, and global bounds

The mixed-sign proof of (G.1) has determinant (D^2), trace at most 4, and (A^2+b^2gesum_i q_i^2). It therefore covers singular Γ. The Gaussian projection in (G.3) uses equal marginal variances, which the initialization supplies at each layer. Its orthogonal residual Gram is positive semidefinite, giving (G.4) and the iterated initial lower bound (G.5).

The strict positivity of η* is established by non-affineness at every positive Gaussian scale, continuity on compact scale intervals, and the positive limit (G.7). The regression stability argument (G.8) is valid for a non-Gaussian perturbed variable: standard deviation is 1-Lipschitz, the optimal slope is bounded by π, and evaluating that same affine predictor at the reference variable gives the claimed error bound.

On (Dle1), the forward and backward bounds (G.14) imply a hidden sum speed at most (1400a^3|C|) and readout speed at most (800a^3). Integration gives the (560000a^6s^2) bound, enlarged to (10^6a^6s^2). The Euler identity for (sum h_js_j) proves the identical estimate and rules out a discrete first overshoot without a small-step assumption. These bounds really verify the finite-array affine hypothesis with (B=12), rather than relying on a continuous affine solution alone.

I checked the activation-selection arithmetic. The first lower bound on a gives (a^6ge6.4cdot10^{19}lambda^{-3}), hence (D_Sle2.25cdot10^{-12}lambda) and (C_S^2le1.44cdot10^{-12}lambda), which imply (G.18) with ample slack. The second lower bound gives (615a^2D_Sle0.08856t_*). The feature Gram perturbation is bounded by (7.2cdot10^6a^6D), below the allowed quarter of initial coercivity. The hidden differential/update composition is bounded by (3(1400)^2a^6C_S^2<6cdot10^6a^6C_S^2).

The capped residual equation (G.23) correctly retains its potentially nonsymmetric hidden contribution. Its absolute norm, rather than an unjustified positivity assertion, is enough for the decay estimate. Stopping at total residual clock S and deriving the sharper bound S/2 excludes that stop. Bounded states and raw speeds then give finite strong endpoints and global fixed-cap continuation. This does not assume capped dynamics are gradient flows.

For each fixed cap and horizon, sufficiently fine physical Euler meshes have effective clock length below S by fixed-cap Euler convergence. Part R applies to those actual deterministic effective steps and frozen controls. Its uniform moment bounds pass by lower semicontinuity under strong mesh convergence. Thus (G.26) and the reference tails used in cap removal are justified with one threshold for all physical horizons.

## Part V: cap transfer, uniqueness, finite algorithms, and observations

The asymmetric gate inequality (V.10) is valid even for an uncut left side: first change the incoming argument, then split the remaining gate difference using the reference clip. It needs tails only of the reference incoming field. In backward substitution, an incoming error is multiplied by bounded actions and gate slopes; each new factor R multiplies a forward difference already controlled in the raw norm. This proves the single linear factor in (V.11). Residual feedback adds only bounded scalar-difference terms and does not produce another cap factor.

The reference tails from (V.7) produce (e^{-cR^2}), which survives the (e^{K_TR}) stability loss. Uniform Cauchy convergence of cap states and raw directions yields a strong (C^1) path. The same asymmetric estimate identifies its uncut field. It also proves uniqueness against a bounded-primal strong competitor without needing tails of that competitor. At a reached time the already exponentially small initial discrepancy remains small after a further linear-exponential loss, so the continuation claim is supported.

The fixed-cap Gaussian-probe estimates in V.3 include recomputed residuals, take their limits in the stated order, and justify expected derivative rows before V.4 proves pointwise absolute rows. Current transpose returns are included in (V.22). The finite primal event used there is independently established by exact update lengths and the primary fixed-program law in V.7; it does not depend on the velocity/source bounds being proved.

The appended velocity queries in V.5 require more than an observational continuity assertion, and the manuscript supplies the necessary nested truncation proof. The first input has derivative-row domination by (K(1+|P^1|)). This proves first-action moments before they are used to dominate the second-action derivatives. New forward sources are distinct formal arguments, with zero formal derivative in the opposite source group; their full same-family covariance remains present. Thus the passage does not invoke an unproved (L^p)-boundedness theorem for the initialized operators.

The deterministic velocity comparison (V.40) uses a bounded reference factor and tail split, with only one truncation factor M. Together with fixed-cap Euler estimates, coarse-node moment bounds, and fixed-transcript identification, it gives uniform-time finite capped velocity laws. The actual GD direction is evaluated at its preceding raw node, as required by raw interpolation; the proof does not substitute the field at the interpolated state. The vanishing random readout is handled by a same-initialization comparator at a fixed transcript.

True backward fields at capped states are explicitly observed via nested products and transpose actions in V.8. Their continuity and compact (L^2) time images provide the required uniform tails. Hence the true kernel blocks, including off-diagonals, are justified independently of the capped update kernel.

For uncut finite GF and GD, V.9 compares against a same-width cap reference on a cap-independent primal ball. Reference tail convergence is established at fixed cap. The first-exit argument excludes escape with probability tending to one once the fixed cap is large. In GD, the extra defect is the cap reference's within-step variation; it vanishes for η_n=n⁻². No width-uniform Lipschitz estimate for the uncut finite field is assumed.

The velocity cap-removal order in V.10 avoids multiplying an uncontrolled cap-dependent moment constant by the cap-removal error. It first uses compact tails of the uncut population velocity, then strong convergence of cap velocities, then empirical reference-tail convergence at fixed cap and M. Width, cap, and tail limits are taken in the stated order. This supports uniform-time and finitely many joint-time laws.

Finally, (V.57) supplies the missing path-space step: its integrated squared-speed bound controls the expected squared supremum error of observation-grid interpolation. Fixed-grid joint laws followed by grid refinement prove Wasserstein convergence of continuous paths. Uniform velocity laws imply second-moment and integrated squared-speed convergence. The action inequalities and rank unrolling support every fixed finite generated probe in both orientations. No growing Gaussian program or cross-width operator identification is used.

## Part N: all initial-motion assertions

The derivative-valid initialization extension V.I supplies the source formulas actually needed in N.2. Its first and second gate truncations have explicit integrable dominators, and covariance-square-root coupling handles singularity. Thus those formulas are not unsupported applications of the bounded-derivative theorem to unbounded products.

The top forward Gaussian has positive definite covariance because the augmented input Gram makes Q₁ positive definite and then Q₂ positive definite. The proof that S₃ is positive definite uses full support and the nonconstant derivative for every e>0. Fresh reverse Gaussian covariance gives S₂ and the conditional bottom covariance lower bounds. These prove all hidden parameter blocks and every bottom sample nonzero, including singular Γ and possible zero affine bottom motion.

The affine formulas (N.22)–(N.23) have the correct factors of a and the correct sample-dependent γ_j. The finite conditional covariance of (Q_n) in (N.26) is exact; its centered linear/quadratic cross covariance vanishes by Gaussian cubic moments. The normalized trace computations use fourth moments and the separate trace-probe/uniform-integrability argument, so expectations alone are not incorrectly taken as concentration. In particular, (N.35) gives the two terms ((1+1/n)τ_n(M_n^2)+τ_n(M_n)^2), yielding τ(R²)=3. The completed squares in (N.30) and (N.37) give positive lower bounds for each upper sample, without a symmetry assumption on the data.

I checked the nonlinear difference decompositions and the arithmetic in (N.41)–(N.50). The common error (2cdot10^8a^7e), under (ele(10^{10}a)^{-1}), is at most (a^6/50), smaller than half of both lower bounds in (N.39). The chosen activation satisfies that cutoff. Multiplication by φ'≥a preserves nonzero feature directions.

At time zero the hidden first velocities vanish and (C'(0)=3H). Strong multiplier continuity and action convergence give (b_i^ell(t)/t	o3β_i^ell), hence θ'_h(t)/t→9V and the displacement coefficient 9/2. The curve chain rule supplies every field's right initial second derivative without assuming a second ambient Fréchet derivative. For the projected kernel, the hidden-gradient contribution is (9t^2|V|^2), while the scalar feature-energy differential gives another (9t^2|V|^2). Their sum is the claimed coefficient 18. The nonzero direction has already been proved blockwise and samplewise.

## External-result audit and final assessment

I found no external advanced result whose missing proof leaves a verification obligation. The manuscript's nonclassical ingredients—adaptive Gaussian source laws, singular-query handling, common bounded actions with actual adjoints, controlled response bounds, product-query extensions, and cap transfer—are proved internally in the forms later invoked. Remaining uses are classical measure/probability and Hilbert-space facts such as Fubini, dominated convergence, Fatou, Cauchy–Schwarz, Gaussian integration by parts, and finite-dimensional spectral decomposition; the particularly consequential instances receive direct arguments in the text.

The main conclusions therefore survive this audit: one global canonical strong raw gradient flow with the stated uniqueness, joint compact-time GF/GD limits in the listed observations, a uniform nonlinear regression margin, nonzero initial hidden accelerations, and variation of the actual projected total kernel. No mathematical revision is required by this review.
