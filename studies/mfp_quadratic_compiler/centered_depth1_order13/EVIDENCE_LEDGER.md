# Evidence ledger: shallow normalized Hermite-2 audit

| ID | Status | Evidence | Claim impact |
|---|---|---|---|
| H2-1 | exact | `phi=(u^2-1)/sqrt(2)` has zero Gaussian mean and unit Gaussian variance. | Fixes the requested normalization. |
| H2-2 | exact | `b=sqrt(2)A, v=u^2` gives `b'=v-1, v'=2bv` and `F=E[b(v-1)]/2`. | Reduces the width limit to independent two-variable characteristics. |
| H2-3 | exact, two-route | Polynomial Lie derivatives and ordinary Taylor ODE coefficients agree through order 13; parity holds. | Accepts the derivative vector in `RESULTS.json`. |
| H2-4 | exact, two-route | Reversion/composition and `F'=K(F)` agree on `mu_0,...,mu_5`. | Accepts the six moments. |
| H2-5 | exact decisive violation | All 23 accessible square Hankel minors were enumerated; only `det H_2^+` is negative. | Disproves the Stieltjes property for this architecture. |
| H2-6 | exact coordinate reduction | `b^2-v+log(v)` is invariant and gives the quadrature in `RESULTS.md`. | Supplies a characteristic-level implicit solution, not a closed Gaussian-average `F` or `K`. |
| H2-7 | accepted negative search result | The exact Lie jet through order 81 supplies 40 moments; no algebraic OGF or P-recursive relation appeared in the frozen low-complexity discovery class. | Rules out only the declared bounded formula classes; it is not a no-closed-form theorem. |
