# Independent scientific review A of frozen packet P1

**Verdict: ACCEPT for the exact claims of this packet.** I found no unresolved required scientific correction after a complete semantic read of the assigned addition, dependencies, guide and edits, independent derivation of the critical implications, and a fresh standalone validation run. This is a scientific review of P1, not approval to promote it and not certification of the entire unchanged book.

Reviewer: `/root/scientific_p1_a`, 2026-09-11. I am distinct from the authors `/root`, `/root/reference`, `/root/response`, the assembler `/root`, selector `/root/selector`, and the other scientific reviewer. I did not author or repair the candidate. My report is original. I read only the neutral assignment, its frozen inputs, required skills and relevant skill references, and my own newly generated checking artifacts. I did not read the study README, author component originals, relevance decisions, prior reviews, other review findings, or live canonical sources. I did not contact the other reviewer. The sole coordinator exchange during review clarified that unavailable original-file provenance hashes are metadata, not supplied input bytes; it supplied no scientific finding or assessment.

I wrote only this report and the assigned scratch directory:

`/home/amir/Codes/PDE/data/generated/robust_learning_horizon/scientific_review_p1_a/`.

No input was altered. No training experiment, parameter sweep, network access, Git command, canonical edit, or promotion was performed.

## 1. Exact coverage and frozen identity

The manifest SHA-256 is exactly:

`3dc31cc04695cb3fd48741cbfcea8575a746f3182132e93c0ac609cd5ec6e089`.

Semantic coverage is complete over these ranges:

| Input | Read coverage |
|---|---|
| P1_SCIENTIFIC_ASSIGNMENT.md | All 70 lines |
| P1_MANIFEST.json | Entire JSON, all input/dependency/provenance entries |
| P1_ADDITION.md | All 1–1633: statement 1–205; reference/endpoint/activity/certificate 206–834; response proof 835–1326; raw-GD transfer 1327–1633 |
| P1_DEPENDENCIES.md | All 1–5014, including every supplied old proof body |
| P1_DOCS_README.md | All 1–273, including contextual references and the exact revised scope |
| P1_EDITS.json | All 1–39; each of five chapter replacements and two guide replacements, both old and new text |
| P1_CERTIFY_REFERENCE.py | All 1–58, also compared with the embedded code in the addition |
| P1_CERTIFY_TRANSFER.py | All 1–44 |
| validate_candidate.py | All 1–73 |

I also read `/etc/codex/skills/solve-math-rigorously/SKILL.md`, `/etc/codex/skills/investigate-conjectures/SKILL.md`, and the latter's complete `references/adversarial-audit.md`, `references/research-contract.md`, and `references/decisive-experiments.md`. The authorized computation was deterministic certificate/correspondence checking, not an empirical research experiment.

The first batched display was truncated: it showed the beginning of the addition through line 351 but omitted later material and portions of the other batched reads. I repaired coverage by reading addition ranges 340–740, 740–1140 and 1136–1633, rereading both complete certificate files, the complete validator and edits, and rereading the whole guide. A dependency display of 3380–3915 was truncated around its source-proof middle; I repaired it by rereading 3570–3699. The other complete dependency reads were 1–550, 550–1050, 1050–1650, 1650–2260, 2260–2810, 2810–3380, 3916–4495 and 4496–5014. These unions cover every assigned line; I did not treat a truncated tool response as a complete read.

The 5257-line global baseline, 6892-line global edition and 269-line guide baseline were completely processed as correspondence inputs. I checked their full bytes/hashes, exact declared replacement operations, appendix assembly, and preservation of the older complement. They were not subjected to an unrequested semantic certification of all unchanged chapter material. The supplied dependency bodies are the old substantive scientific scope.

All supplied input hashes match:

| Input | SHA-256 |
|---|---|
| P1_ADDITION.md | `741e9a8ee2dfd10db5638c82fc3fa6f35bde4297ab65edeae834f3ee9d001baf` |
| P1_GLOBAL_BASELINE.md | `1945ef5d407eafd534b32185e952fa3ed479605f3ffec09afd468b6266fd18d9` |
| P1_GLOBAL_EDITION.md | `d473d95ee27bc39d484185cea2689b5d3ceed448567a1527488f1003e24f1fa1` |
| P1_DOCS_README_BASELINE.md | `95b14c5a0430a783023d412d0103d8598a476963bad19180e2d4d0e2291bce3e` |
| P1_DOCS_README.md | `7824df11fe7fa2d89cf2c75dc32716438b20c815a18d1c3809a84a0785d9d34e` |
| P1_DEPENDENCIES.md | `6dab773038b451c74fcb3be082f92e4dbd3c1b3b6df2ab55c58a27b782d25b69` |
| P1_CERTIFY_REFERENCE.py | `f61103c86b278da16334f30e7af2961c41b4bcaae7062c5b8433fe4eef7a2036` |
| P1_CERTIFY_TRANSFER.py | `09da5455910c20ded0caf5dc979a956f43ea98b6039dc3ff98977ad6f6cada90` |
| P1_EDITS.json | `2bdc66ba6ac554bc29f60fdcf6b5ed6b442c69083fad52ec16ed38801e52e1e7` |
| validate_candidate.py | `a294bd8193b1393fa31ab634f505b83f08f6a95eb605b65ab57226f944b4295d` |
| P1_SCIENTIFIC_ASSIGNMENT.md | `aa59c19c5a27c21c7722492d365bddc3bfcc9f8bb0c87a7bc832d6858cbd7544` |

All eight dependency excerpt hashes and their exact declared line counts also match:

| Original source span | Excerpt SHA-256 |
|---|---|
| NOTATION 1–98 | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| global_nonlinear 1841–2452 | `c673a8feabc88b7fafd1d57eb9861823c628a00b29a93684dbfd47f04c1c9904` |
| global_nonlinear 3835–5257 | `b1d34b78bd50354ce2d036046a180a2beba415530472cbd32264684b0b378774` |
| special_data_limits 134–1349 | `b21046f7674455290f558ebb3da0a3c2f7e5ca113c71be4f1853f91a5ca624d5` |
| special_data_limits 3785–4326 | `8a8dbe6a31a51d3f73c999b75c75dcade76b3d533c8901a062c66587776cdd0a` |
| finite_dynamics 1–227 | `bd10bbff9d16f60c63d26b06e793510fa19958e1ec91dbdd85709caff7d96980` |
| finite_optimization_and_controls 1–345 | `06f3a5fc06ac766c9562cec9c774fb99e88b93b2ddfb4cf709c85d2de2fab887` |
| global_nonlinear 2923–3439 | `cf223a3eed88b3755d0379948316534fb44febc9fa1d6f0bb5d282ff8be4ac94` |

My separate checker additionally compares all three global excerpts directly with the exact specified slices of the supplied complete global baseline, checks both endpoints of each span, checks the complete notation hash, and checks that there are exactly eight unique matching spans. The validator alone does not check every one of those metadata conditions.

The manifest's author `source_components` hashes and whole-file hashes for the unavailable original special-data/finite-dynamics/finite-controls files are unverified provenance metadata. I identified this limitation to the coordinator before relying on anything outside the frozen bytes; the coordinator confirmed that these originals must not be opened. Their supplied excerpt bytes are verified and sufficient for the dependencies actually used. No necessary proof body is missing. The guide's external papers are contextual links expressly not imported as theorem dependencies; I did not use their conclusions to fill a gap.

## 2. Reconstructed mathematical contract and reference proof

The object is the actual two-hidden-layer tanh network with `z1=W1 u`, `z2=W2 h1`, `f=c^T h2/n`, `u=x/sqrt(2)`, small stored Gaussian readout variance `n^-2`, and unhalved mean loss. Its inverse-mobility metric is exactly `||dW1||F²/n+||dW2||F²+||dc||²/n`. A population rank `q tensor h` is `q E[h·]`, represented at width n by `qh^T/n`; the HS norm therefore corresponds to ordinary finite Frobenius norm. The initial Gaussian action need not be HS; only its learned increment is. This removes possible hidden normalization or optimizer changes.

For the equal-mass opposite-label reference, `h=(H2_1-H2_2)/2`, `b=<c,h>`. Direct differentiation in the row/HS metric gives the displayed hidden differential `J` and adjoint `J*`. The feature equation is `c_s=h`, `hidden_s=J*c`. The fixed-root scalar clock `j_X=sech²j` is globally defined, one-Lipschitz in X and at most linear in `(X,g)`. Bounded readout-supremum sets are closed in L2; the rank and backward differences make the integral map contract on a short interval. The polynomial feature-time bounds prevent finite escape. This uses an actual strong curve derivative, not a false global L2-valued Fréchet assertion.

I checked the B.1 specialization: two orthogonal normalized inputs, both tanh activations meeting the bounded derivative/Lipschitz derivative assumptions, first and middle variance constants one, Gaussian readout exponent beta=1, and all three sum-loss mobility constants `kappa=1/2`. This yields exactly the present mean-loss physical equation. Its fixed-program action construction, same-root stability, finite-flow mesh comparison, readout perturbation and observation claims have supplied proofs. The older arctangent fitting theorem and equal-label activity example are not used as tanh/opposite-label substitutes.

The swap/sign transformation maps `f(u)` to `-f(Pu)`. Enlarging the common generated program language by its swapped copies gives the required probability-space isometries intertwining the same initialized action and its adjoint. Uniqueness then yields the deterministic population symmetry `f1=b=-f2`; this does not assume a pathwise symmetry of finite initialized arrays. Oddness under `u -> -u` follows directly from the two odd activations and absence of biases.

The physical equations multiply the feature field by `2(1-b)`. The strong chain rules give `c_ss=JJ*c` and

`b_s=||h||²+||J*c||²=||theta_s||raw²`.

On each positive interval of `g=||c||`, differentiating the norm gives `g_s=b/g` and `g_ss>=0`, by Cauchy–Schwarz and the nonnegative hidden term. Since `g_s(0+)=sqrt(m)`, `g>=s sqrt(m)` excludes a later zero; hence `b_s>=m`. The initialized first-feature Gram is `q I`, and the actual initial Gaussian forward law gives independent second preactivations of variance q; thus `m=v/2`. The rational certificate proves `m>.1` and safely permits `m>=.1`.

Consequently b reaches 1 at one finite feature time `s_dagger<=10`. Bounded continuous `b_s` on that feature interval makes the physical clock integral diverge at its endpoint. Therefore there is no accidental finite physical interpolation time, no wrong clock sign and no extrapolation beyond the fitting segment. The residual satisfies `e_t=-2 b_s e`, so `e<=exp(-t/5)` and reference risk is at most `exp(-2t/5)`.

Integrating squared feature speed and applying Cauchy–Schwarz yields the actual raw-state endpoint estimate `||theta(s_dagger)-theta(s(t))||raw<=e/sqrt(m)`. The endpoint is independently specified by the autonomous feature equation stopped at b=1, including the initialized action/adjoint, and is not merely a new name for an assumed limit. On the straight state segment the predictor's gradient bound is

`sqrt(1+10[1+(2+sqrt(10))²])<17`.

This proves the whole-circle error `17 sqrt(10) exp(-t/5)`. The full first-row norm supplies input Lipschitz constant below 76. No training-direction projection is substituted for whole-circle control. The endpoint interpolates only the stated reference labels; uniqueness among all interpolants is correctly not claimed.

## 3. Reuse, source derivatives and explicit response tails

I inspected the complete supplied III.F conditioning/source/regularization proof and A.1 extension, including their hypotheses and zero-query treatment. For an adaptive query, conditioning successively on the transcript adds precisely the linear observation of the same Gaussian matrix. The conditional mean respects both forward and transpose constraints; its independent residual has the two orthogonal projections. The projection of the fresh output Gaussian onto finitely many old directions has vanishing normalized second moment. Gaussian integration by parts cancels the old forward regression terms and produces the expected named-source derivatives. Distinct orientations have independent Gaussian *sources*, while the answers retain their deterministic response terms.

The new tanh-clock source extension deals with its real extra difficulty: `j_g=sech²(j)/sech²(g)` is not globally bounded. Clipping only the fixed Gaussian root makes first derivatives bounded at each clipping level. The needed source derivative is instead `H_X=sech^4(j)<=1`, uniformly in the clipping level. The bounded readout and finite causal graph give deterministic bounds on all first source derivatives when earlier coefficients lie in compact sets. Covariance square-root coupling and this domination pass both values and expected derivatives through root-clip removal, including singular covariance. Same-array finite subtraction then identifies those values with the actual uncut program. No derivative in the root, no pseudoinverse continuity and no unproved finite-width higher-moment assertion is used.

The response recursion retains every named slot. It contains both the past derivative of the readout and the current term `beta_ka,kb=1_(a=b) E[c_k phi''(Z2_ka)]`. Learned rank contractions are included with the exact `gamma=h sigma/2` factors.

I recomputed the improved bounded-ball coefficients. The feature energy bound, HS mesh convergence and fixed-program Gram contractions place sufficiently fine unforced finite source meshes strictly within `M=7,C=4`, using the initialized finite action bound 3. At each fixed mesh sufficiently small fresh-root forcing remains inside this ball with width-independent slack; the readout supremum stays at most s because its update still uses bounded tanh outputs. There is no assumption that arbitrary forcing preserves the unforced energy identity.

For `d=x+a+z`, summing the three velocity differences gives column coefficients `56s+11/2`, `16s+5`, and 8. Thus `L(s)<=8+56s` and the Euler product is bounded by `exp(8S+28S²)=exp(2880)` at S=10. A reverse pulse produces immediate distance `h_j |epsilon| ||e||/2`; a forward pulse gives `h_j P |epsilon| ||e||` with `P=(M+1)S+1/2=161/2`, and the later delta bound has coefficient K=140.

The extraction order is essential and valid: fix mesh and nonzero forcing; take width to infinity jointly for forced/unforced programs and the fresh root; integrate by parts only in that independent local root with selected coefficients held fixed; divide by epsilon; finally send epsilon to zero using the separately proved coefficient/derivative continuity. This establishes individual named coefficients even for zero-variance slots. It does not infer a transverse derivative from a singular unforced value law.

Summing the two samples' past response bounds gives `2 S P K exp(2880)=225400 exp(2880)`. The learned reverse terms add at most `S C²=160`; current terms add at most 20. Thus `M_Q=225400 exp(2880)+180`. The forward remainder is at most `100(exp(2880)+1)`. Source variances are initially bounded by 1 and 16; on the limiting fitting segment the reverse variance improves to 10.

The common-space passage uses cross-program covariance isometries, not arbitrary marginal subsequences. Strong uniform-in-time L2 convergence of H and delta makes their Gaussian sources Cauchy; bounded remainders retain their pointwise bound at each deterministic time. L2-continuous approximations supply the time measurability used in Fubini. Gaussian exponential moments then prove the individual reference RMS tails. No bound on a maximum over inputs or Gaussian histories is inferred.

For the actual finite reference GF, finite energy gives the necessary raw bounds with its real random readout. Differentiating Q along this smooth finite flow gives a width-independent L2 time-Lipschitz bound using the readout coordinate bound. Positive-part cutoffs are Lipschitz; fixed-time second moments from B.1, followed by a time grid and grid refinement, transfer the tails uniformly in time. The weaker sigma=4 tail yields exactly `H exp(-R²/4096)` with `H=16(4+M_Q)` after the finite cutoff factor 2. Reference readout tails vanish at the final cutoff. This route supplies both error production and its stability transport.

## 4. Paired activity in both layers

The activity computation retains initial/current coordinates of the same neuron. I checked it separately from prediction convergence and from the older equal-label example.

At initialization let `U_a=h0 phi'(Y_a)`, `P_a=A0* U_a`, `V_a=phi'(g_a)²P_a`, and `R_a=q U_a+A0 V_a`. Conditioning the actual initialized matrix on its two forward calls gives

`P_a=sum_b p_ab tanh(g_b)+Gamma_a`,

where `p_ab=E[Y_b U_a]/q`, the Gaussian Gamma has covariance `C_ab=E[U_a U_b]`, and Gamma is independent of the first Gaussian root pair. This is the reused transpose law. C is positive definite: an almost-sure linear relation, continuity and Gaussian full support would force a linear relation between two independently varying nonconstant sech² functions.

Conditioning that same matrix again on its reverse calls yields

`A0 V_a=sum_b alpha_b Y_b+bar_d U_a+sigma_a gamma_a`.

Projection removes the deterministic root part of P from `E[P V_a^perp]`; the remaining pairing is `C_ba E[phi'(G)²]`, giving the stated response coefficient. The new Gaussian's variance is the norm squared of the orthogonal residual of V. It need not be independent of the other sample's new Gaussian. This argument keeps both reuse responses; deleting either is not an admissible simplification.

Conditional variance consequently gives `||V_a||²>=C_aa a0` and `||phi'(Y_a)R_a||²>=C_aa a0 r0`. Independence and oddness give `C_aa>=v r0/4>.03`. With `a0>.3,r0>.6`, both s² coefficient norms divided by four exceed .01. This proves nonzero coefficients for each reference input, stronger than only positivity of their average.

I checked the explicit remainder arithmetic: `||P_a||4<6`, `||R_a||4<8`, and the fixed-initial-field cutoff at R=10 has tail below .001. The successive estimates in R31 use no evolving coordinate tail. In R32 the activation Taylor term contributes `36/16=72/32`, giving `(116+5R)/32`. In R33 the additional middle/cross contribution is `55/32` and the final tanh Taylor term contributes `128/32`, giving `(415+10R)/32`. At `s<=.01`, the larger normalized error is below `.001+(515/32)10^-4<.003<.005`, leaving the lower bound `s²/200` in each layer.

The clock obeys `1-exp(-2t)<=s(t)<=2t`, so at physical `t_act=1/200` it lies between `199/20000` and `1/100`. Hence each reference averaged paired RMS is strictly greater than `1/2500000`. The relevant finite paired joint law is retained in fixed auxiliary programs and passes to GF by state comparison; it is not reconstructed from two separate marginals or from f alone.

## 5. Actual raw GD, arbitrary perturbed supports, risks and probabilities

The direct finite vector-field comparison uses the full first matrix and the exact joint cost `|u-v|+|y-z|`. Splitting each varying gate only against a *reference* backward factor yields one power of the cutoff R. There is no tail hypothesis on the actual perturbed-law network. I recomputed the conservative constants 313 and 3792 and the outer integrand sum 661180, which is below the chosen `10^6`; the total reference-tail coefficient is at most 676. The changed input vector in the first-row velocity is explicitly included.

On the initialization event, the actual reference mean loss is at most 25/16. Its raw block displacements through T=40 are each below `sqrt(40*25/16)<8`, so all reference state norms remain below 11. Its coordinate readout is at most 101. Thus stopping the raw-GD/reference distance at 1 legitimately puts both states and the GD preceding-node state in B=12. The raw velocity sum bound is exactly 4082. The finite predictor bound is 12 and its input Lipschitz bound is 1728.

Raw GD is Euler in stored weights, and the comparison reference is actual raw GF with the same three initial arrays. The latter has zero raw-field defect. The only actual-step discrepancy is at most `4082 eta` between the preceding raw state and its interpolant. Gronwall with `K=40*10^6` gives the stated stopped bound. With its right side below one, continuity rules out the stopping time. Width-independent crude raw-GD bounds also follow directly from increments; no discrete energy identity is assumed.

The source proof's meshes are fixed before the width limit and removed afterward. The resulting actual-GF reference conclusions are then inserted in the comparison with arbitrary actual GD. The sufficient condition `eta_k sqrt(n_k)->0` implies the vanishing actual step required here. The text correctly observes that this final comparison itself only uses eta tending to zero; it does not silently identify transformed Euler with raw GD or feed a growing transcript to a fixed-program theorem.

With `R=exp(2900)` and `delta=exp(-exp(3000))`, I checked `M_Q<exp(2893)`, `H<exp(2897)`, `K<exp(18)`, `1+R<exp(2901)`, and `R²/4096>exp(5791)`. Their substitution proves both cutoff and changed-law contributions below `exp(-100)<10^-18/4`. These are mathematical real constants, not floating-point computations of doubly exponential quantities. Both `Lrisk delta<1/256` and `8 B² delta<10^-18` hold. Thus the positive excess of the state discrepancy above `d0/2` tends to zero.

The whole-circle reference limit, the full-row Lipschitz bounds, and the exact state comparison give positive-excess predictor error at most 1/16; the deterministic reference contribution at T=40 is actually below .019. Replacing T by its preceding node costs a bounded speed times eta. Binary squared loss has joint Lipschitz constant `2(12+1)12³=44928`. Its reference-atom risk is at most `(1/16)²`; transport adds less than 1/256, establishing the 1/128 intermediate bound with slack relative to the required 1/4 event.

For activity, the squared-displacement integrand changes by at most `4(B+1)D_n` with state and `8B²|u-v|` with input. Hence its retained deterministic lower margin is greater than

`(1/2500000)²-26*10^-18-10^-18=1.59973*10^-13`,

strictly above both `1.59*10^-13` and the requested `10^-13`. Separate paired-observable reference convergence is present. The activity and risk/prediction events hold jointly by a finite union bound.

No part of the changed-law comparison assumes orthogonality, support cardinality, positive atom weights, a Gram inverse, or deterministic labels conditional on inputs for the perturbed law. Orthogonality is used only for the fixed reference. The proposed moved atom and spread arcs/label flips indeed have the stated transport costs and supply nonorthogonal and nonatomic admitted examples. The neighborhood is open and positive, although too small to be practically useful; it is not uniform-circle generalization.

For deterministic empirical laws, the triangle inequality gives `limsup W1(lambda_k,nu*)<delta`. For iid observations, fixed compact Borel partitions and each cell's variance `<=1/(4m_k)` give W1 convergence in probability. Every other event involves only the one fixed reference and initialization, so arbitrary relative sample/width growth is allowed. Strict margins establish the displayed probability-to-one events; convergence to a threshold alone is not being mistaken for such an event. No unique global perturbed-law flow, arbitrary-accuracy result for a fixed perturbed law, almost-sure joint limit, or uniform failure rate over laws is inferred.

## 6. Independent execution and source-level numerical audit

All shell commands ran from `/home/amir/Codes/PDE`. Reading commands were `cat` for the assignment, manifest, skills and required references, and `nl -ba FILE | sed -n 'LO,HIp'` for the exact semantic ranges listed in section 1. Complete numbered reads were also made of all three Python files and the edits. Initial hash inspection used Python `Path.read_bytes`, `hashlib.sha256`, and `json.loads` on precisely the manifest's frozen input mapping. These reads completed with exit status zero; the two display truncations were repaired as recorded above.

The exact standalone execution was:

```text
python studies/robust_learning_horizon/validate_candidate.py --inputs /home/amir/Codes/PDE/studies/robust_learning_horizon --output /home/amir/Codes/PDE/data/generated/robust_learning_horizon/scientific_review_p1_a/edition
```

The output directory was new; the validator enforces `exist_ok=False`. The invocation completed with exit code 0 on Python 3.10.12. Both subprocesses were launched with `/usr/bin/python`, from the new edition directory, against `edition/P1_CERTIFY_REFERENCE.py` and `edition/P1_CERTIFY_TRANSFER.py`. Both exited 0 with empty stderr. The reference output was exactly:

```text
[0.392108947877, 0.396376711612, 0.233120735618, 0.339792209687, 0.631761866359]
```

The transfer output reported PASS, transport coefficient 661180, risk Lipschitz 44928, raw speed 4082, response exponent 2880, and prefactor 225400. Complete stdout, stderr, source copies and validation metadata are preserved in the edition directory.

I independently wrote and ran the reviewer-owned correspondence/arithmetic check:

```text
python data/generated/robust_learning_horizon/scientific_review_p1_a/audit_packet.py
```

It exited 0 with PASS and wrote `packet_audit.json`. Its complete exact source is retained. Beyond independent input/excerpt/assembly checking, it uses only rational arithmetic and positive exponential Taylor lower sums to check the .019 endpoint bound, the 1/32 and 1/1024 margins, the gradient constant 17, Lipschitz constant 76, the activity clock and remainder inequalities, and the final activity margin. It does not execute a training trajectory.

The numerical proofs themselves were inspected, not accepted just because assertions passed. The 80-term positive exponential sum has a geometric upper remainder with ratio at most x/82 for all invoked arguments x<=18. Alternating arctangent bounds and the proved Machin identity bound pi and the density normalizer. On each of 1000 intervals of width 1/250, opposite endpoint choices give outward Gaussian/tanh/sech bounds; the two-sided missing Gaussian tail beyond 4 is below 1/10000. Summands are rounded outward exactly to denominator `10^12`, and all comparisons are rational. Printed floats are summaries, not evidence for a near-threshold inequality. The .624 and .633 substitutions have the correct monotonic directions relative to sqrt(q). The transfer exponential bounds use positive Taylor lower sums and integer comparisons, avoiding numerical overflow or underflow entirely.

A final output inventory was produced with Python `Path.rglob`, `read_bytes`, and SHA-256, saved as `output_hashes.json`. Important output identities are:

| Output relative to reviewer scratch | SHA-256 |
|---|---|
| audit_packet.py | `430fbd231b4c7b5799d7aba27872fb00f38048e363a44badcba96bdeebc0a1d2` |
| packet_audit.json | `585a7d788572c60361bc0edfeafac74218ea069a19d1f79c85d95611890292eb` |
| edition/validation.json | `b09bb2d50194c3450e425c83e80a04d34b37c10d769a4c50d616b69d937aea8e` |
| edition/P1_CERTIFY_REFERENCE.py.stdout | `ad40e8d8f73fed6d22cc9b68a19f7f9e6c3f2f59383d581e372ce0c976786900` |
| edition/P1_CERTIFY_TRANSFER.py.stdout | `97089ac3bf0eebb7a3b7058c4bc1e2e64c6f6af6321af84ff86569d9d0806df5` |

Both empty stderr files have SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. Generated global/guide documents match their frozen edition hashes. The output inventory also records every generated dependency document. The regenerated special-data file contains the two supplied excerpts, not the entire original chapter, and therefore correctly has a different hash from the unavailable whole original.

The validator's string replacements reproduce the complete chapter and guide. My checker verified all seven declared edits occur uniquely and that the only chapter terminal normalization removes its prior final newline before appending two newlines and the addition. The rest of the older complement is exactly preserved. The revised descriptions retain the local scope of C.4.1–4 and distinguish it from C.4.5's fixed-time finite-network transfer.

## 7. Adversarial outcomes, corrections and final component verdicts

| Attack | Result and consequence |
|---|---|
| Wrong loss factor, readout rescaling or optimizer substitution | Rejected by direct raw-gradient reconstruction and B.1's kappa=1/2 specialization. |
| Finite pathwise swap symmetry smuggled into the limit | Not used; symmetry is at the canonical action-law level with uniqueness. |
| Fitting extrapolated from initial conditioning only | Rejected: convexity of the readout norm gives b_s>=m along the complete feature curve. |
| Endpoint existence or whole-circle continuity assumed | Rejected: actual finite-feature endpoint, raw-state distance and full-row Lipschitz estimates are supplied. |
| Replaced transpose by a fresh Gaussian matrix | Rejected: conditioning retains the response terms in the activity and full source calculations. |
| Source derivative inferred from an unforced singular law | Rejected: fixed nonzero forcing precedes width and zero-forcing limits, with independent coefficient continuity. |
| Bounded RMS mistaken for Gaussian tails | Rejected: explicit fresh-root response sums supply a bounded remainder plus Gaussian source. |
| Improved forcing ball circularly assumes forced energy conservation | Rejected: unforced energy gives strict slack; fixed-mesh finite subtraction permits only sufficiently small forcing. |
| Fixed-program limit applied to the growing actual GD transcript | Not used; actual raw GD is compared directly with actual finite GF. |
| Tail or rank assumptions secretly imposed on perturbed supports | Not used; only fixed-reference individual tails enter the transport inequality. |
| Marginal activation laws substituted for paired displacement | Not used; paired initial/current tuples and separate observable convergence are retained. |
| Probability thresholds inferred without slack | Rejected by the explicit predictor/risk and activity margins. |
| Tiny neighborhood overstated as general useful generalization or causal feature advantage | Not claimed; practical scale and causal/accuracy limitations are explicit. |

Component verdicts: **accept** the exact statement and normalization; **accept** the reference existence, fitting, clock and endpoint; **accept** both opposite-label paired activity bounds and their explicit remainders; **accept** the fixed-root source extension, forcing order, response exponent and tail passage; **accept** the actual-GF/raw-GD comparison, support-independent transport, sample/width limit order and probability margins; **accept** the certificate arithmetic and frozen-edition correspondence.

Required corrections: **none identified**. Unresolved necessary mathematical dependencies: **none identified within the assigned supplied scope**. The provenance limitations above are explicit and do not conceal a missing theorem premise. No proof-assistant verification or finite-width quantitative rate is supplied, and the numerical scripts do not independently prove the analytic/probabilistic bridges; those were the subject of the semantic review.

Final verdict for manifest `3dc31cc04695cb3fd48741cbfcea8575a746f3182132e93c0ac609cd5ec6e089`: **ACCEPT**. This conclusion applies only to P1's stated fixed-reference, fixed-time, fixed-accuracy theorem and its precisely scoped auxiliary claims.
