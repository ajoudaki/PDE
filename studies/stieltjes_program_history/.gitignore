# Keep compact JSON/Markdown/text provenance from numerical runs, but exclude
# the large raw arrays and transient diagnostic streams.
numerics/**/runs/**/*.npz
numerics/**/runs/**/*.log
numerics/**/runs/**/*.csv

# Local interpreter caches.
**/__pycache__/
**/*.py[cod]
