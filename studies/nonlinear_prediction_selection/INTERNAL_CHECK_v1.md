# Coordinator reconstruction of candidate v1

Status: internally checked candidate; not established and not promotion-approved.
The independent mathematical reviews are separate and pending at this checkpoint.
Author/assembler: /root. Candidate SHA-256:
`e53cefcd3aa58c456d8cee49bc7bff8c6330bffb1f05b9d8baa3646074189404`.

The coordinator read the complete three frozen first-round routes after their
freeze, the complete second-round source, activity and slow-selection modules,
and all of CONTROL_TUBE_APPLICATION_NOTES.md. Exact original source coverage
and versions are in SOURCE_COVERAGE.md; subsequent complete reading also covered
the C.4 overview, global_nonlinear.md:3836–3980 and 2454–2461. The canonical
assembly preserves the proof bodies with documented scope narrowing, canonical
notation, removed author references, and resolved local bindings. Its producer
and exact input/output fingerprints are in assemble_candidate.py and
ASSEMBLY_MANIFEST_v1.json. No scientific result from another study was used.

Reconstruction concentrated on these nontrivial implications:

- The full Gaussian root box has probability exp(-O(r^2)); the clock-envelope
  complement at r^2 has probability exp(-Omega(r^4)), without an independence
  premise. The exact tanh clock therefore protects saturated rows. Sign jumps
  separate every nonantipodal finite feature list. The middle-rank Gram bound
  then gives actual trained conditioning and a nonzero hidden projected gradient.
- All controlled source sums retain their injection masses. The comparison uses
  m_p=|gamma_p|+|gamma*_p|, with sum m_p e_p equal to total control discrepancy.
  Zero/reference-zero coefficients and singular/current duplicate slots retain
  their named derivative conventions. The current alpha precedes current beta.
  The reference raw cap is anchored in bounded feature length, including the
  fresh-pulse/root-clip argument and differentiated raw-to-clock remainder.
- The raw comparison before the source bootstrap uses only reference query tails.
  The resulting source cap closes causally, with no physical-time constant.
  Reference prefixes plus a short append stay in the control tube only after
  charging the entire untruncated reference tail. Passing prefix and mesh limits
  never initializes fresh action or reverse sources at the fitted endpoint.
- Endpoint constrained Euler has bounded control mass and inherits the cap.
  A one-reference Gaussian-tail cutoff gives the Osgood modulus
  d sqrt(log(e/d)), whose reciprocal integral diverges. This proves strong
  completion and uniqueness; no infinite-dimensional Peano theorem or ambient
  twice differentiable vector field is invoked.
- Actual mixture Euler prefixes first approach the physical reference at fixed b
  using its tails. The post-b residual remainder is bounded by
  C h^2(|r|+epsilon)^2 along the actual affine update, using fourth query moments.
  Its discrete contraction telescopes to a bound on total anchor control mass.
  The inner/outer first-exit construction closes before a changed flow is assumed.
- Along reached paths the product w'Q is in L2 by L4/L4. All other gradient
  derivatives use bounded readout and actual action/rank rules. Thus B=G M^-1
  is absolutely continuous and the identity theta'=epsilon V+B r' is legitimate.
  Integration gives an error controlled by initial reference error and
  |r(b)|^2+epsilon|r(b)|+epsilon^2(t-b). Sending epsilon to zero at fixed b,
  then b to infinity, proves the original-time slow limit away from tau=0.
- The finite learning margin integrates the exact moving-field risk derivative.
  A fixed-endpoint-readout hidden contrast has derivative at least 5 kappa/16
  throughout a fixed episode. Cauchy–Schwarz gives an actual second-hidden
  activation displacement, rather than merely a hidden parameter displacement.
  The final episode is the smaller of the existence and activity intervals.
- Finite actual GF is compared to a deterministic finite program at each fixed
  positive epsilon. Only the proxy needs tails; its Gaussian tails dominate
  the cutoff amplification at that fixed finite horizon. The actual Gaussian
  readout is retained. A fixed-oracle cutoff induction, added after author
  checking, handles its vanishing perturbation without a width-dependent
  Lipschitz assertion. Paired reference/mixture fields use one union program
  on the same arrays, followed by the reference endpoint limit only in population.

The scalar constants in the activity module were checked directly, including
76/1216=1/16, residual at least 5/16 initially, the risk derivative lower bound
25 kappa/1024 on the retained episode, and the factor 30 T0^2 in the normalized
three-input hidden displacement. No numerical value for the new Gram minimum,
source radius, episode time, or positive final margins is claimed.

The upstream rational Gaussian certificate was independently rerun from its
complete standard-library source, and rerun in the standalone edition. Both
runs exited zero with identical output. See VERIFICATION.md and
STANDALONE_VALIDATION_v1.md. These checks certify inherited constants and
edition correspondence; they are not a formal verification of the new proof.

Independent relevance screening accepted assembly as a new C.4.9, with its
precise one-atom, finite-episode, second-hidden and width-first scope. Two fresh
complete mathematical reviews and a separate fresh integration review remain
required. Any required scientific correction will preserve this packet and
its original reports, produce a corrected packet, and reopen the fresh paired
review gate. No approval or established modification is inferred from this
internal check.
