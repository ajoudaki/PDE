# Complete independent adversarial review A — frozen candidate v1

**Verdict: PASS for the stated finite slow-time episode and its proposed scope edits.**

Reviewer: `/root/adversarial_a_v1`. Date: 2026-09-12. Candidate SHA-256:
`e53cefcd3aa58c456d8cee49bc7bff8c6330bffb1f05b9d8baa3646074189404`.

This verdict is about the theorem actually stated: an open one-added-atom
family, a common positive but possibly very small slow-time interval,
original-initialization population selection away from the original-time
initial layer, and finite GF capture with width first at each fixed positive
contamination. It is not a verdict establishing a final changed-law endpoint,
out-of-sample benefit, superiority to frozen features, a simultaneous
width/contamination limit, or a GD extension. None of those is required or
asserted by the proposed theorem.

## 1. Assignment, independence, and permitted actions

I received the neutral `REVIEW_ASSIGNMENT_v1.md` and the candidate fingerprint.
I started without an author discussion or a scientific summary of this
candidate. My identity is different from the coordinator/assembler, all three
named route authors, and the named relevance selector. I did not communicate
with reviewer B, read another review, inspect author checks or route drafts,
or delegate any scientific part of this review. This is my original full
report, not an incorporation of another reviewer's conclusions.

I read `AGENTS.md` and all of `RESEARCH_WORKFLOW.md`, applying their independent
isolated review scope. I did not read the study README, another study, Git
history, other tasks, relevance findings, or the live scientific chapters.
The source chapters are identified below only as provenance stated inside the
frozen packet. Links in the supplied guides were not followed. Metadata-only
Git checks showed concurrent changes and an empty staged list; I made no Git
mutation. The observed HEAD was
`f98be192b9b95002db4b3bef400c65bddc860e9f`.

I read and applied these required skills in full:

- `/etc/codex/skills/solve-math-rigorously/SKILL.md`;
- `/etc/codex/skills/investigate-conjectures/SKILL.md`;
- the latter's `references/research-contract.md`,
  `references/adversarial-audit.md`, and `references/evidence-ledger.md`.

This was a frozen proof audit and deterministic certificate verification.
There was no training experiment, new research campaign, external retrieval,
publication, or established-file edit. The only written locations were this
report and `data/generated/nonlinear_prediction_selection/adversarial_a_v1/`.

## 2. Actual complete read coverage and input integrity

Every line of the required frozen scientific inputs was read. In particular,
the dependency proofs were read rather than replaced by theorem summaries.
The actual coverage was:

| Frozen file | Complete coverage and reads |
|---|---|
| `REVIEW_ASSIGNMENT_v1.md` | Entire file, lines 1–96 |
| `CANONICAL_ADDITION_v1.md` | 1–575, 576–1150, 1151–1725, 1726–2300 |
| `PROPOSED_EDITS_v1.json` | Entire file, 1–16 |
| `PROPOSED_GUIDE_v1.md` | Entire file, 1–285 |
| `DEPENDENCIES_GLOBAL_v1.md` | 1–600, 601–1200, 1201–1800, 1801–2400, 2401–3000, 3001–3600, 3601–4200, 4201–4800, 4801–5400, 5401–6000, 6001–6600, 6601–7200, 7201–7800, 7801–8400, 8401–8901 |
| `DEPENDENCIES_GAUSSIAN_v1.md` | Entire 1–550 read, with the truncated middle repaired by a separate complete read of 220–370 |
| `DEPENDENCY_GUIDE_v1.md` | Entire file, 1–284 |
| `DEPENDENCY_NOTATION_v1.md` | Entire file, 1–98 |
| `DEPENDENCY_MANIFEST_v1.json` | Entire file, 1–44 |
| `REVIEW_MANIFEST_v1.json` | Entire file, 1–42 |
| `verify_reference_certificate.py` | Entire file, 1–58; also compared with the complete code printed in the global dependency |

The initial all-at-once Gaussian dependency output reported a middle
truncation. The repair covered the complete singular-query regularization
proof, the singular-support derivative convention, causal scalar feedback,
and overlapping text on both sides. No unresolved truncation remained. The
other scientific reads were bounded slices with contiguous endpoints.

The global dependency explicitly contains source ranges 1840–2461,
2462–3440, 3836–4947, and 5269–11439 of `docs/global_nonlinear.md`, whose
stated source hash is
`bda93ec446425c05f8c06ac3a67fa1505906dff309b74e13bab4effc1527bf05`.
The Gaussian dependency contains source lines 3785–4326 of
`docs/special_data_limits.md`, whose stated source hash is
`5b7b48aa5deab320042217a6f284002366a683bf0f7f0b63c05d8167c526a489`.
I verified the frozen files themselves, not unprovided complements of those
chapters. Unrelated unchanged guide claims were read as context; this is not
an unrestricted whole-book audit.

All review-manifest fingerprints matched. I computed a start fingerprint
record, rehashed every input after the mathematical reading and audit, and
asserted equality of the two records. The manifest's own hash was also
recorded. The results are:

| Input | Rechecked SHA-256 |
|---|---|
| `REVIEW_ASSIGNMENT_v1.md` | `57b550e6f4abf2a8d327e1f84e34a67ed5bdd49316f4fda34820c93f27af05c4` |
| `CANONICAL_ADDITION_v1.md` | `e53cefcd3aa58c456d8cee49bc7bff8c6330bffb1f05b9d8baa3646074189404` |
| `PROPOSED_EDITS_v1.json` | `0493be3c29d2499826320e3324dea54a77ec865b494e0db0e169a438d3c9c006` |
| `PROPOSED_GUIDE_v1.md` | `d5ecdfd35fbddd511d98dccd148a9a9e840f5a4c814658f930c5732bab218bc5` |
| `DEPENDENCIES_GLOBAL_v1.md` | `6ebdaf1a3b28bdd07244a7b8c1bb882a36c7c525c4170cd25af4d7661ab7d942` |
| `DEPENDENCIES_GAUSSIAN_v1.md` | `c95e358f6bb9741858e6293dabacf4fee01927a23cda1289cd3cd17e85a1ee77` |
| `DEPENDENCY_GUIDE_v1.md` | `5210ccd284ea79215d81c18f2fba93762538cfb1bb5b6b295c6e33e558225e1f` |
| `DEPENDENCY_NOTATION_v1.md` | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| `DEPENDENCY_MANIFEST_v1.json` | `75f8b2ae6716fb88d8f89306b78a104a2bffc62e1bf67bb64474b8298d5b415a` |
| `REVIEW_MANIFEST_v1.json` | `357a7b3bc9e805749fb8c5f3383400edfce0bdc56277861ce74987b0d65d67e7` |
| `verify_reference_certificate.py` | `07c51c139ebf66912ef7b730201dcc71581b11355dd29dbee6140bf2bba63118` |

The process-instruction files read have hashes
`7778b7073a02de3e94f8ae9aefe940ebc9eddea7467816662e5cce1020cd98ba`
for `AGENTS.md` and
`8b36d7dfc1ad881fcb6bfe32274a5e50c5125b33d68dbcf7c3937f7f6c21ce12`
for `RESEARCH_WORKFLOW.md`.

## 3. Exact contract and dependency audit

The finite normalization is consistent throughout. The first preactivation
is `W1 u` with `u=x/sqrt(2)`, the middle action is `W2 h1`, and the stored
readout enters as `c^T h2/n`. With mobilities `(n,1,n)`, the raw squared
metric is first Frobenius squared divided by n, middle Frobenius squared,
and readout Euclidean squared divided by n. Thus a middle population rank
has finite representative `delta h1^T/n`, and its population norm is the
Hilbert–Schmidt norm. The initialized action is bounded but is not asserted
Hilbert–Schmidt. The finite readout has standard deviation `1/n`, rather
than order one. The population initial zero readout is its limit.

Direct differentiation of the unhalved probability-mean square gives
`theta'=-2 integral r g_u dmu`. For the two reference atoms, each weight
one half removes that factor two, and their opposite residuals give
physical controls `(e_*,-e_*)`. Since feature velocity is
`(g_e1-g_e2)/2`, the feature clock is `ds/dt=2e_*`. The reference controls'
absolute mass is consequently the feature length, at most ten. For the
three-atom mixture the exact equations are the candidate's (29), including
the factors `(1-epsilon)` and `2epsilon`. These checks establish that the
slow equation concerns the stated original network and optimizer.

The following unchanged results are actual dependencies. Their included
proofs and the hypotheses used here were checked:

1. **III.F.1–III.F.7 and global A.1–A.3.** The conditioning formula is the
   orthogonal Gaussian projection onto the affine constraints from both
   matrix orientations. Adaptive queries are conditioned sequentially;
   the other residual Gaussian matrix factors remain independent.
   Conditional new Gaussian vectors give empirical weak and second-moment
   convergence at each fixed instruction. The source-response derivation
   uses Gaussian integration by parts against the uncentered input Gram.
   Regularization adds a distinct noise at each call, keeps the program
   finite while taking width, and removes the noise by covariance square
   roots and derivative envelopes. It makes no continuity claim for a
   pseudoinverse. The at-most-linear value extension and polynomial-envelope
   product derivative extension cover the fixed raw neural programs used
   here. The sharp action constant two follows from the contained Gaussian
   comparison/Poincare argument; it is not obtained from the weaker norm
   constant ten by assertion.

2. **III.F.7–III.F.10 and global A.4.** The generated language is countable,
   and finite unions preserve all same-array laws. Norm inequalities and
   finite transpose identities extend through the generated dense spans,
   constructing the bounded action and its actual adjoint. The HS rank
   identity gives the exact raw metric. Bounded-multiplier continuity and
   the strong curve chain rule justify hidden activations along curves.
   The fixed-weight scalar remainder argument separately proves continuous
   Frechet differentiability of scalar predictions. No ambient Frechet
   derivative for an L2-valued activation is imported.

3. **B.1, specialized to tanh, and C.4.5.1.** The same-root activation
   transform yields a genuinely Lipschitz clock equation on sets with
   bounded action and readout supremum. Its direct polynomial bounds give
   global finite-feature-time existence. Rank continuity upgrades the
   middle increment to HS regularity. The inverse scalar representation
   also proves uniqueness for the raw reference equation. The coordinate
   swap symmetry is a symmetry of the canonical action law, not a false
   symmetry of individual finite initialized arrays. With zero limiting
   readout, the reference first features have Gram `q I`, and the initial
   second preactivations are independent `N(0,q)`. The contained rational
   certificate gives `m>=1/10`. Convexity of the readout norm yields
   `b_s>=m`; the first `b=1` state is unique and occurs by feature time ten.
   Bounded `b_s` makes physical time diverge at that feature endpoint.
   Finally `||theta(s2)-theta(s1)||<=sqrt((s2-s1)(b(s2)-b(s1)))` gives the
   raw endpoint bound and the claimed exponential physical approach. These
   steps validate the candidate's endpoint initialization, norms, symmetries,
   and 76-Lipschitz whole-circle predictor.

4. **C.4.5.2 and C.4.7.3.** The former includes the clipped-root clock
   source construction, fresh-answer forcing, width-first/forcing-second
   coefficient extraction, and actual reference tails. The latter includes
   the exact raw named-source recurrences, temporary-cap pulse estimates,
   weighted old-source density estimates, differentiated raw-to-clock
   defects, and the causal source-cap closure. I checked these proof bodies
   to assess the candidate's accumulated-control specialization. In
   particular their source derivatives freeze residuals, covariances, and
   selected scalar coefficients, and retain separate names at singular
   covariances. The candidate must and does prove its stronger
   horizon-independent controlled estimate; the old fixed-T=40 statement
   alone would not suffice.

5. **C.4.6.3, especially S40–S44.** The envelope needed for endpoint
   conditioning comes from the auxiliary finite reference feature flow.
   Deleting one initialized column creates an independent cavity but keeps
   its entire trained increment and residual feedback. The difference
   estimate isolates forward forcing `||a_i||/sqrt(n)` and a reverse cavity
   Gaussian probe. It does not assert a small operator norm for the deleted
   column. The conditional Gaussian maximum bound follows from dyadic
   grids and an L2 parameter modulus of the cavity backward field. The
   cavity-good event is independent of the deleted column; the original
   good event is its subset. Width-first finite query laws and bounded
   truncation tests then transfer the finite supremum moment estimate to
   the countable canonical population query envelope. L2 continuity and
   Fubini give the simultaneous active clock bound. This proof establishes
   `||N||_p<=C_*sqrt(p)` jointly with the initial rows, without asserting
   that N is independent of those rows.

6. **C.4.1–C.4.3 and the reference/nearby-law finite comparison bodies.**
   The one-reference cutoff is a factor subtraction with one factor R,
   followed by bounded actions and the HS rank inequality. Only the
   reference backward factors need tails. Fixed finite-program oracles
   retain same-array pairings and allow the actual small readout to be
   restored by RMS cutoff comparison. Time and input nets are taken after
   the fixed-program width limit. These arguments support the candidate's
   finite capture proof, but do not themselves grant its new long horizon.

Other included response, sampling-context, GD, and local-law statements were
read. Their stronger or different conclusions are not premises silently
substituted for any of the six dependencies above. No indispensable scientific
input was missing from the frozen packet.

## 4. Adversarial examination of accumulated source control

**Attack: an L1 control perturbation can have tiny or zero individual
coefficients, a genuinely new input, or an arbitrarily long physical
history. A raw L2 bound cannot by itself control its query tails.**

The proof addresses all four features in a way that preserves the source
mass. Its dominating measure is `(|a|_1+|a_*|_1)dt`, whose total mass is
at most `2L_*+q`. Flat parts of physical time carry no updates. The arrays
`m_p=|gamma_p|+|bargamma_p|` have bounded total mass and each control
discrepancy has normalized size `e_p<=1`, with
`sum m_p e_p=q_disc<=q`. This is normalization by common mass, never by a
possibly zero reference coefficient. A new direction has `bargamma=0` and
is therefore included. A slot with both coefficients zero is removed or
retained with zero past influence. Cancellation inside an integration cell
can only reduce the discrete mass and discrepancy, and causes no invalid
division.

I checked (CT12)–(CT16) by differentiating their local scalar expressions.
The direct reverse-source injection enters the lower update exactly once
with coefficient `gamma_p`. A forward source has a direct current gate
derivative even if that step has zero control, but every influence on a
later state is then zero. Current c and w depend only on earlier times,
so a current upper input has only its own direct current source. This
justifies the single distinguished diagonal, rather than a sum of order
the number of current queries. Old passive calls that were never consumed
by an update have no descendants and no later formal derivative. Singular
covariance does not merge formal names.

**Attack: the reference anchor or nearby cap may presume the tail it is
supposed to prove.** The reference clock equation has bounded first-feature
clock derivatives and deterministic bounded readout. Its raw/action
subtractions therefore give a finite Lipschitz amplification over feature
length at most ten, including a forced-answer program. At each fixed graph
the root clip is removed chronologically, with a deterministic envelope
for the named source derivatives. The fresh Gaussian insertion obeys
`partial_z V^q=q partial_slot V^q`; pairing with z, taking width first,
then q to zero proves the individual clock coefficient bound. This bound
is uniform in mesh, although zero-forcing continuity is asserted only for
each fixed graph. That distinction is sufficient.

The independently existing feature flow supplies tails before raw Euler
is compared with it. For the raw-to-clock step I checked the cancellation
in (CT22): `F'(w)phi'(w)=1` removes the first-order term. Differentiating
the integral remainder produces the candidate's (CT23), with the only
remaining exponential `exp(2h|b|)`, not `exp(2|w|)` in a uniform estimate.
At the direct injection step, division by `|bargamma_p|=h_s/2` leaves
`O(h_s)`. Later terms have bounded fixed moments from the past temporary
cap, and `sum h_k^2<=L_*h_max` gives a summable normalized defect.

The transformed lower pulse has bounded clock gates and deterministic
linear propagation. Subtracting its clock counterpart uses a pointwise
bounded normalized clock pulse, L2-small gate differences, old beta-row
differences, and the summed defect. Taking expected feature derivatives
and then subtracting the upper pulse equations gives (CT25). Current alpha
is constructed before current beta; the latter's right side uses only
earlier capped rows. Thus the first-failure argument is causal and supplies
the reference raw cap rather than presuming it.

Under a temporary nearby cap, (CT27) really is a Gaussian plus a bounded
remainder: bounded first features control the response and learned-memory
rows. Jensen uses a weighted sum of Gaussian absolute values, not a maximum
over history. It supplies all fixed moments of the pulse amplification.
The old alpha and beta density bounds retain `|gamma_p|`, while the full
upper derivative row sums have deterministic pointwise bounds.

Raw proximity (CT34) is obtained before the nearby cap: subtract updates
as in (CT33), putting all coefficient changes in a bounded-gradient term
and using tails only for the two reference-axis terms. Its amplification
is exponential in R and its source tail Gaussian in R, so the stated
cutoff choice makes the error tend to zero with q.

I expanded the weighted comparison separately. In the lower pulse equation
the forcing consists of the injection, update coefficients, outside gate,
Q, D-row, and inside old-gate differences. The exact learned D-row
subtraction is (CT38). After division by `m_p` the injection costs `e_p`;
the eventual source sum costs `sum m_p e_p`, not a count of slots. The
interpolation calculation `1/12=(1/11)/2+(10/11)/24` confirms its field
exponent. Bounded gate differences give exponent at least `1/6`, so the
weaker common `1/16` is valid. Three L12 factors give an L4 forcing, and
the random amplification has an L4 bound, giving the required L2 product.
Minkowski is taken before old-source summation, avoiding a maximum of
query differences.

For the upper equations the direct current impulses cancel. Summing their
differences over source indices uses the pointwise barred row bound and
the density `|F_iq|<=C_B m_q`. Exchanging the two finite memory sums costs
only the total mass. This gives (CT44), hence (CT36), with no unknown
current row on its right. With `B=B_*+1`, sufficiently small q leaves
slack one half. Finally `Q=zeta+J`, `|J|<=D_B`, implies the claimed
Gaussian RMS tails without independence between zeta and J.

**Outcome:** the uniform source cap closes for the declared controlled
programs. The proof does not equate bounded raw norms with query tails,
discard the reference history, refresh a source at restart, or invoke a
probability supremum over random finite-network feedback controls.

## 5. Endpoint conditioning and finite learning margins

**Attack: a query envelope depending on the first roots might leave only
degenerate endpoint features, or positivity might fail uniformly in alpha.**

The protected-row argument uses only a union bound, so dependence is
harmless. For p chosen of order R squared, the moment bound gives a tail
`Pr(N>R)<=exp(-R^2/(e^2 C_*^2))`. The Gaussian box about `r v` has
probability at least a constant times `exp(-O(r^2))`, whereas
`Pr(N>r^2)<=exp(-c r^4)`. Their intersection is therefore positive for all
large r. On that event both nonzero coordinates of v make the first roots
large, and the exponential growth of `F'` dominates the permitted clock
displacement `5r^2`. The displayed bound (2.4) follows by first preventing
exit from `[g_a-1,g_a+1]`, then applying the minimum derivative on that
interval.

Consequently first features on these events approach `sign(v dot u_i)`.
An almost-sure linear dependence would force the corresponding sign
identity for every v avoiding the finite perpendicular lines. Crossing
one line gives its coefficient zero. For the actual list
`(e1,e2,u_alpha)`, all three directions are distinct modulo sign,
uniformly over the closed alpha interval. This is the nonantipodal
distinct-direction situation used by the argument; repeated or antipodal
directions would not have this independence conclusion and are not used.

The endpoint readout is nonzero because it fits the first anchor.
`sech^2 Z2(u)>0` almost surely, so every endpoint backward field delta(u)
is nonzero in L2. Applying the first-feature Gram inequality at each
upper-layer coordinate and integrating gives (2.7) exactly in HS norm.
This does not require A to be injective. The first-feature Gram and the
nonzero delta norms vary continuously in alpha. Compactness gives the
strictly positive common kappa in (2.8).

The projected added gradient is the combination with coefficients
`t=(-beta1,-beta2,1)`. Since `|t|>=1`, its middle block alone has squared
norm at least kappa. This proves conditioning and a nonzero hidden
component without identifying hidden-parameter motion with hidden-value
motion. The latter is proved separately.

**Attack: arbitrary L2 products might invalidate endpoint continuity.**
The forward and upper backward subtractions in (3.1) keep the fixed
bounded endpoint c in the changed-gate term. The lower changed gate
multiplies only the fixed endpoint Q in L4. Its boundedness and L2
smallness give the L4 gate bound (3.2), hence the square-root raw modulus.
The projector estimate then follows from ordinary two-by-two inverse
perturbation. General gradient continuity comes from fixed-factor
bounded-multiplier continuity, not from an ambient Hessian.

**Attack: the hidden margin could merely measure the already learned
reference, or risk improvement could be an infinitesimal Taylor claim.**
The contrast (4.1) freezes the endpoint readout and endpoint t, so its
gradient at the endpoint is exactly `(d_H,0)`. Its derivative in the
constrained field is `-2r_dagger ||d_H||^2`, at least `5kappa/8`.
The same fixed-endpoint continuity estimate controls this derivative on
a raw neighborhood. The first-exit speed argument keeps the actual
constrained solution in that neighborhood for the chosen positive time.
This is an existing-path argument with a uniform derivative modulus,
rather than extending the initial Taylor term without an error bound.

I checked the numerical factors in the margins. On the selected ball,
`-r>=5/32` and `||Pi g||^2>=kappa/4`. The exact risk derivative is
`-4r^2 ||Pi g||^2`, giving
`a=25kappa tau0/1024`. The contrast derivative remains at least
`gamma_O=5kappa/16`. Since `||c_dagger||^2<=10` and
`J2=(sum_i ||Delta H_i^2||^2)/3`, Cauchy–Schwarz gives
`|Delta O|^2<=30T0^2 J2`. Hence
`j=gamma_O^2 tau0^2/(30T0^2)>0` has the correct normalization.
Here T0 is B's coefficient bound, not a physical horizon.

**Outcome:** conditioning and both positive finite-episode margins hold
uniformly over NS1. The activity measures upper hidden values relative to
the reference endpoint on the same carrier. The nonlinear forward fields
and projection continue to evolve. No stronger feature-superiority or
out-of-sample interpretation is proved or needed.

## 6. Construction, continuation, and singular selection

**Attack: continuity alone does not construct an infinite-dimensional ODE,
and a fitted endpoint does not grant restart existence.**
Unit C constructs rather than invokes existence. It appends explicit
constrained Euler steps to longer and finer original reference prefixes.
Their total appended control is bounded by `A tau0`, their displacement
by bounded speed times tau0, and the omitted full reference suffix has
vanishing absolute control mass. These bounds put each full program
strictly inside the source tube before applying its tails. The common
carrier is obtained from finite unions and a countable dense parameter
family, followed by continuity. No source is restarted at the endpoint.

The Gaussian cutoff gives the one-reference modulus
`omega(z)=z sqrt(log(e/z))`. Its reciprocal has divergent integral at
zero. Integrating the scalar comparison through
`sqrt(log(e/Z))` gives (13) on its explicitly stated small-error branch.
It follows that the appended Euler families are Cauchy, uniformly in
the parameter box. Continuity passes their integral equations to a
strong C1 solution. Soft positive-part tails pass through L2 convergence,
and bounded readout passes by an almost-everywhere subsequence. Comparing
any other strong solution to this tail-bearing solution proves uniqueness
without imposing a tail hypothesis on the competitor. Thus no ambient
local Lipschitz assumption or compactness-to-uniqueness inference is used.

**Attack: products and B' in the residual identity may need an unavailable
ambient C2 theorem.** Along a reached controlled curve, the controls give
`||w'||_4<=C m(t)`, HS and L2 speed bounds, and pointwise `|c'|<=m(t)`.
All forward and upper backward derivatives in (24) are therefore strongly
L2 integrable. The only new lower-gradient product is `w' Q`; Holder
controls it by the two L4 norms. Coordinatewise absolutely continuous
representatives and Fubini prove the integral identities, not just formal
derivatives. Thus anchor G is raw absolutely continuous with
`||G'||<=C m`, finite M and its inverse are absolutely continuous, and
`||B'||<=C m`. The product rule applied later to B r has the hypotheses
it needs. Joint L4 query continuity follows from raw L2 convergence and
uniform higher moments; it also justifies the endpoint strong hidden
derivative statement.

**Attack: actual-mixture continuation could be assumed before the residual
estimate used to establish it, or a long training phase could be inserted.**
The fixed-b prefix exists first as a finite raw Euler recursion. Direct
bounded-gate estimates give b-dependent raw and readout bounds without a
mixture cap. One-reference comparison to the already existing reference
then gives (32a); scalar residual comparison gives actual integrated
control closeness (32b). Thus a small raw displacement is not used as a
substitute for small control distance.

After b the proof uses inner and outer stopping regions. An affine Euler
segment is itself a prefix with a shorter last update, so the admitted
source theorem applies at its queried points. Its fixed node velocity has
an L4 row bound of order `|r_k|+epsilon`. Differentiating the anchor
gradients along this affine segment, using that bound and current query
L4 bounds, proves the residual expansion (32c). Its remainder is
`C h_k^2 (|r_k|+epsilon)^2`. The positive anchor Gram makes the linear
factor contractive; bounded residuals and a sufficiently small step absorb
the square remainder. Telescoping (32d) gives the integrated residual
bound with no accumulated `O(hT)` error.

The total deviation from the still-running physical reference is bounded
by the prefix discrepancy, the entire reference suffix mass, the small
prefix residual, and `C tau0`. The same estimate controls raw
displacement. Choosing b large, the contamination and mesh small, and a
fixed positive tau0 small makes a first exit impossible. Only after this
discrete argument are meshes completed at each fixed epsilon on its
finite horizon. The resulting path starts from the original state and
uses the same mixture throughout. The reference-prefix constructions are
proof approximations, not a physical pretraining or reset in an actual run.

**Attack: the formal tangential force might not determine a uniform
1/epsilon limit.** The exact equations imply
`theta'=epsilon V_p(theta)+B(theta)r'`. Multiplication by
`B=G M^-1` shows the normal component cancels with the correct sign.
The residual differential inequality gives an exponentially decaying
initial term plus `O(epsilon)`. Since `||B'||<=C(|r|+epsilon)`, the
integral of `||B' r||` is bounded by the right side of (35). Integration
by parts is consequently valid in the raw Hilbert space and yields (36).

On the shifted slow clock `t=b+tau/epsilon`, all errors in the resulting
integral equation have limsup bounded by `C exp(-b/5)` at fixed b.
The one-reference Osgood comparison with the already constructed
constrained path gives (38), uniformly in p and tau. The common tau0
does not shrink as b increases; only the required contamination threshold
and proof mesh do. One may therefore take epsilon to zero at fixed b
and then b to infinity. At unshifted time use
`sigma=tau-epsilon b`; bounded constrained speed costs `O(epsilon b)`.
For every fixed positive tau_min this sigma is nonnegative for small
epsilon. This proves NS5 in its actual stated order and explains why
tau=0 must be excluded.

**Outcome:** construction, uniqueness, original-mixture continuation,
raw derivative identities, and the uniform slow-limit statement are
proved. The analysis does not extend a finite-order response expansion
to a long horizon or require a finite changed-law endpoint.

## 7. Actual finite GF and the same-array observable bridge

**Attack: a population slow limit might concern a substituted initialization,
or a fixed-program theorem might be applied to a growing GF transcript.**
The finite raw energy identity makes finite GF global and bounds its
displacement on each fixed horizon. Bounded tanh gives the separate
readout supremum bound. Those estimates hold with the actual nonzero
Gaussian readout. Its maximum tends to zero by
`2n exp(-n^2 eta^2/2)`, and its normalized RMS tends to zero as well.

At a fixed positive epsilon, T=tau0/epsilon is finite. Unit D first freezes
a finite population Euler program's scalar controls and runs a proxy on
the actual Gaussian arrays. Expanding learned ranks gives a finite
same-matrix program. Causal contraction convergence, at-most-linear
coordinate values, and fixed-oracle tail cutoffs identify it. The small
initial readout is retained in that proxy; comparison to the zero limiting
readout oracle is a fixed-graph argument with cutoffs, not a
width-dependent finite-dimensional Lipschitz assertion.

The actual GF/proxy subtraction needs only proxy tails. A changed gate
times a proxy field is split at R; direct bounded-action subtraction
handles Q itself, and the middle difference uses the exact HS rank norm.
The residual coefficient discrepancy tends to zero at each fixed finite
mesh. Interpolation costs `O(h_max)`. Proxy tails at intermediate times
can be transferred through soft cutoffs using finite interpolation nets
and Q's L2 continuity on the bounded-readout raw region. There is no
assertion that arbitrary actual finite GF queries already have Gaussian
tails.

The comparison has amplification `C_T exp(C_T R)` and Gaussian proxy
remainder `exp(-aR^2)`. At each fixed positive epsilon one chooses a
finite cutoff for the desired error, then a finite sufficiently fine
mesh, and takes width at those choices. Equivalently one takes the
fixed-cutoff/fixed-mesh width limsup and removes the approximations in
that order. The constants may depend on T and epsilon; no rate uniform
as epsilon vanishes is asserted. Strong population Euler completion
identifies the unique path from Unit C.

On bounded raw balls, predictions and hidden values are uniformly
Lipschitz in raw distance. Full first-row bounds give input moduli for
the entire circle, and parameter speed gives the time modulus. Finite
input/time nets therefore produce the claimed
`C([0,T] x S1)` prediction convergence. This step uses the full first
row, not only its training projections.

For NS9 the finite union of reference and mixture proxies uses exactly
the same initialized first, middle, and readout arrays. The joint
finite-program law identifies mixed same-layer activation products.
Normalized hidden-norm comparison and Cauchy–Schwarz transfer squared
distances to the two actual finite flows. The reference is captured only
at this separately fixed finite T. Its population endpoint is introduced
afterwards, when epsilon tends to zero and T tends to infinity.
This gives precisely the paired endpoint-versus-constrained hidden
quantity, with factor `1/(3n)` at finite width and `1/3` in the population.

Finally squared loss at the added atom is continuous on the common
bounded prediction region. The positive deterministic risk and hidden
margins therefore survive with thresholds a/2 and j/2 with probability
tending to one in the displayed iterated order. Independence of the
several error events is unnecessary; a finite union bound suffices.

**Outcome:** the capture statements concern actual GF, its true stored
readout and true matrix transpose, whole-circle predictions, and joint
same-array hidden observations. They do not hide a simultaneous limit or
assume any finite-width training endpoint.

## 8. Exact certificate execution and deterministic scope checks

The certificate was run independently, not inferred from the old displayed
output. The execution environment was:

```text
Python: 3.10.12 (main, Aug 31 2026, 10:18:17) [GCC 11.4.0]
Executable: /usr/bin/python
Platform: Linux-5.15.0-151-generic-x86_64-with-glibc2.35
Working directory:
  /home/amir/Codes/PDE/data/generated/nonlinear_prediction_selection/adversarial_a_v1
Command:
  /usr/bin/python /home/amir/Codes/PDE/studies/nonlinear_prediction_selection/verify_reference_certificate.py
External dependencies: none
Seed: none; exact rational arithmetic
Exit status: 0
Stderr: empty
Stdout:
[0.392108947877, 0.396376711612, 0.233120735618, 0.339792209687, 0.631761866359]
```

The complete script was read. All asserted comparisons are rational/integer
comparisons; conversion to float is only for the printed summary. The
exponential lower and upper bounds have a positive geometric-tail
denominator on every used argument, including eighteen. The alternating
arctangent bounds give outward pi bounds. Positive and negative rounding
use floor and its sign-reversed ceiling, so every summand is rounded in the
required direction. The tanh and sech rational transforms have the
monotonicity required for the lower/upper quadrature. The omitted Gaussian
tail is included for q's upper bound. Thus the observed numbers certify
the weaker exact inequalities used by the reference proof, rather than
being floating-point estimates substituted for them.

An independent text comparison returned `Certificate matches contained
code: True`: removing only the two provenance comments from the supplied
script gives exactly the code printed in the dependency proof. No script
body was edited.

I also compared the two full supplied guides. The only changes are the
C.4.9 addition to the Global nonlinear learning table row and the same
scope statement in the future-development section. The old chapter scope
string in `PROPOSED_EDITS_v1.json` occurs exactly once in the frozen global
dependency. Its requested append is the reviewed candidate, and its new
fragment agrees with the C.4.9 heading. The guide explicitly limits the
new conclusion to a finite episode, width-first GF capture, and added-atom
risk. It does not turn this into out-of-sample risk or changed-law endpoint
control. The notation contract and maintained code are unchanged.

Retained deterministic evidence in the assigned scratch directory:

- `input_hashes_start.json` and `input_hashes_end.json`;
- `certificate_environment.json`;
- `certificate_stdout.txt`, `certificate_stderr.txt`, and
  `certificate_exit_status.txt`.

## 9. Component verdicts, objections, and completion

| Component | Verdict | Decisive reason |
|---|---|---|
| Exact finite model, initialization, metric, loss and clocks | PASS | Direct normalization and physical-control checks agree |
| Common Gaussian carrier and actual adjoint | PASS | Complete fixed-program, singular-query, density and adjunction proofs supplied |
| Accumulated-control named-source cap | PASS | Common source masses, independent reference anchor and causal weighted bootstrap close |
| Endpoint conditioning uniform over NS1 | PASS | Protected Gaussian rows, nonzero gates, tensor Gram inequality and compactness |
| Strong constrained construction and uniqueness | PASS | Admissible full-history Euler completion and one-reference Osgood uniqueness |
| Actual-mixture continuation | PASS | Prefix control closeness and discrete residual contraction precede completion |
| Raw derivatives and residual identity | PASS | Reached L4 products and strong absolute-continuity rules justify B' and integration by parts |
| Uniform slow selection and initial layer | PASS | Exact residual identity with epsilon-first-at-fixed-b, then b-to-infinity comparison |
| Added-risk and paired upper-hidden margins | PASS | Exact risk derivative and fixed-endpoint hidden contrast retain uniform signs on a finite interval |
| Actual finite GF and whole-circle/same-array observations | PASS | Fixed finite proxies, one-reference tails and explicit iterated limit order |
| Canonical assembly and proposed scope | PASS | No needed author artifact, missing dependency, normalization change or broader promoted claim found |
| Exact rational certificate | PASS | Independent execution, exact comparisons, unchanged body, exit zero |

No unresolved mathematical objection or required correction remains for
the stated target. The proof's constants are existential and can be extremely
large or small; the statement makes that limitation explicit. The
one-atom family has nonempty interior in its two declared parameters, rather
than being an open neighborhood in the space of all probability laws. The
strictly positive hidden margin certifies actual upper hidden adaptation;
it does not prove that this adaptation is necessary for the risk gain.
These are limits of the proved result, not missing bridges to its stated
conclusion.

All assigned scientific lines and dependency proof bodies were read,
the sole truncation was repaired, the prescribed code was independently
executed, and the complete frozen fingerprint set was rechecked unchanged.
The report preserves the original isolated audit. No established files or
Git state were changed. This completes reviewer A's assignment for candidate
v1; any changed scientific packet requires a fresh review under the workflow.
