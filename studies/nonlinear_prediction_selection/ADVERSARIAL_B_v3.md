# Complete isolated adversarial review B — candidate v3

**Verdict: PASS for the exact frozen addition and proposed scope edits.**

Reviewer: `/root/adversarial_b_v3`. Review date: 2026-09-12.
Candidate SHA-256: `c858c7b41d90b490871450b8bf7494afe8c6cd7f39bebd3f1faa89f3604a5879`.

I found no unresolved mathematical objection or required correction to the
stated theorem. This verdict concerns the finite positive slow-time episode,
the specified compact family with nonempty interior, and the width-first
finite-GF conclusions. It does not certify a changed-law final endpoint,
simultaneous width/contamination limit, raw-GD extension, or out-of-sample
risk guarantee. Those are not claims of this candidate.

## 1. Assignment, independence, scope, and actual coverage

I received only the neutral complete review assignment, the frozen candidate
fingerprint, the allowed-input instructions, and my owned output paths. I am
distinct from the coordinator, the three route authors, the relevance selector,
all prior reviewer identities listed in the assignment, and the other current
reviewer. I did not read an author conversation, another reviewer's findings,
the study README, earlier candidates, correction records, author components,
other studies, task histories, or Git history. I did not delegate this review
or exchange scientific findings with another reviewer. This is my original
complete report, rather than a coordinator's reconstruction.

I read `AGENTS.md` and the complete `RESEARCH_WORKFLOW.md`, following the
independent isolated review exception to ordinary author startup. I read the
complete required skills:

- `/etc/codex/skills/solve-math-rigorously/SKILL.md`;
- `/etc/codex/skills/investigate-conjectures/SKILL.md`;
- the latter's `research-contract.md`, `adversarial-audit.md`,
  `evidence-ledger.md`, and `decisive-experiments.md` references.

The only scientific inputs were the following frozen files in
`studies/nonlinear_prediction_selection/`. Every scientific line, including
the supplied proof bodies, was actually read. The exact coverage is:

| Input | Complete coverage | Reading evidence and repaired truncations |
|---|---:|---|
| `REVIEW_ASSIGNMENT_v3.md` | 1–97 | Complete initial read. |
| `CANONICAL_ADDITION_v3.md` | 1–2329 | Consecutive reads 1–400, 401–900, 901–1500, 1501–1950, 1951–2329. All theorem, source, conditioning, continuation, derivative, finite-capture, and completion text included. |
| `PROPOSED_EDITS_v3.json` | 1–16 | Complete read; reread with the review manifest after an initial combined output was truncated. |
| `PROPOSED_GUIDE_v1.md` | 1–285 | Reads 1–145, 146–215, 216–285; complete unchanged context and both new C.4.9 descriptions. |
| `DEPENDENCIES_GLOBAL_v1.md` | 1–8901 | Initial large reads were truncated and not counted as complete. Repaired by 1–250, 251–500, 501–750, 751–1000, and the additional overlapping 801–950 read; the last overlap repairs the missing part of the 751–1000 output. Then complete consecutive reads 1001–1350, 1351–1700, 1701–2050, 2051–2400, 2401–2750, 2751–3100, 3101–3450, 3451–3800, 3801–4150, 4151–4500, 4501–4850, 4851–5200, 5201–5550, 5551–5900, 5901–6250, 6251–6600, 6601–6950, 6951–7300, 7301–7650, 7651–8000, 8001–8350, 8351–8700, 8701–8901. |
| `DEPENDENCIES_GAUSSIAN_v1.md` | 1–550 | Read 1–280; the truncated adaptive-conditioning/source transition was repaired by 120–220. Read 281–420 and 421–550 completely. |
| `DEPENDENCY_GUIDE_v1.md` | 1–284 | Reads 1–145, 146–215, 216–284, completely. |
| `DEPENDENCY_NOTATION_v1.md` | 1–98 | Complete read. |
| `DEPENDENCY_MANIFEST_v1.json` | 1–44 | Complete read. |
| `REVIEW_MANIFEST_v3.json` | 1–42 | Initial combined output was truncated; complete separate reread repaired it. |
| `verify_reference_certificate.py` | 1–58 | Entire script read, independently executed, and compared with the complete embedded dependency program. |

The global dependency excerpt explicitly contains source ranges 1840–2461,
2462–3440, 3836–4947, and 5269–11439 from its named established source. The
Gaussian excerpt contains source lines 3785–4326. I read these supplied bodies,
not just their theorem statements. I did not follow their links to material
outside the frozen set. No indispensable scientific input was missing.

The unchanged proofs actually used are: global A.1–A.4's fixed-program value
and neural-product extensions, sharp initialized action bound and scalar chain
rules; B.1's same-root clock construction and reference finite-width bridge;
C.4.1–C.4.3's exact full-row equations, one-reference comparison and fixed
oracle/norm conventions; C.4.5.1's reference feature endpoint, fitting,
symmetries, quantitative convergence and rational certificate; C.4.5.2's
fixed-graph clipping, forcing, source extraction and tail passage;
C.4.6.3.S40–S44's canonical reference envelope, together with its complete
finite cavity and Gaussian chaining proof; and C.4.7's explicitly included
source recursions, differentiated raw-to-clock defect, raw/HS comparison,
completion and finite approximation arguments. Gaussian III.F.1–III.F.10
provide the finite law, singular-query convention, generated carrier, true
adjoints, HS identities and scalar/curve differentiation. III.F.11 was also
read; its fixed-cap existence conclusion is not substituted for the new
uncut continuation.

The broader local-depth, tangent, statistical, and nonlinear-remainder
material included in those excerpts was read in full. Its claims beyond the
dependency chain above are context; this review is not an unrestricted audit
of all chapters mentioned in the guide.

I wrote only this report and files in
`data/generated/nonlinear_prediction_selection/adversarial_b_v3/`. I performed
no Git operation, established-file edit, training experiment, or publication.

## 2. Contract and normalization audit

The finite object is exactly the bias-free two-hidden-layer tanh network
with input `u=x/sqrt(2)`, full first matrix, middle sum without an added
normalization, and stored readout pairing divided by `n`. Entry variances
are `(1,1/n,1/n^2)` and stored-weight mobilities are `(n,1,n)`. The unhalved
mean square, not a half loss or sum loss, is used throughout.

Direct finite differentiation gives the raw metric

`||dw||_F^2/n + ||dW2||_F^2 + ||dc||_2^2/n`

and the population rank `delta tensor H1`, represented finitely by
`delta h1^T/n`. Its population HS norm and finite Frobenius norm are the
products of their population L2 or finite RMS factor norms. Thus the middle
metric is not accidentally divided by `n`, and only the learned middle
increment is HS. The first-row block retains both independent Gaussian
coordinates. It is not a projection-only state.

The raw prediction gradient in NS2/CT1 has the correct three blocks.
The middle factor uses the true adjoint in the preceding lower query,
and the `u` factor remains in the row gradient. Independent Gaussian
*source groups* in III.F do not replace the reused action and adjoint by
independent answers: the response corrections CT13–CT16 remain present.

For the two reference atoms, the loss weights cancel the factor two to give
the physical controls `(e_*,-e_*)`. The feature equation uses half these
signed gradient columns, so `ds/dt=2e_*` and the total absolute physical
reference control mass is precisely the feature length. The loss energy
and the feature identity `b_s=||theta_s||_raw^2` consequently have the
normalizations used in the endpoint proof.

The one-added-atom rectangle contains only inputs nonparallel to the two
reference axes, and has nonempty interior in both angle and label. Uniform
constants on its closed rectangle prove the advertised open-family claim
by restriction to the interior; the text does not claim openness in the
space of all Borel probability laws. Its labels are admitted for every
fixed `Y>=1` without changing the reference labels.

The finite Gaussian readout remains present. Its maximum satisfies
`P(max |c0,j|>eta)<=2n exp(-n^2 eta^2/2)` and its RMS vanishes. Zero population
readout and auxiliary zero-readout source programs therefore have the proper
roles; they are not finite-network resets. Verdict on model correspondence:
**PASS**.

## 3. Probability, carrier, and unchanged reference dependencies

I checked the conditional matrix formula III.F.6 against both constraints
`WV=Y`, `W^T U=Q`. Its homogeneous subspace is
`P_(U-perp) K P_(V-perp)`, and the displayed mean is its orthogonal
minimum-norm complement. Conditioning is performed sequentially on
transcript-measurable inputs, which justifies its use for adaptive calls.
The finite-rank projection of fresh Gaussian noise has normalized expected
square `rank(U)/n`; removal at each fixed program is valid. The source
integration-by-parts calculation cancels the old least-squares forward
piece and produces the displayed response, including both orientations.

The singular-query proof adds distinct independent query noise before
taking a fixed-program width limit, establishes positive Schur complements,
and then removes that noise. Continuity of finite covariance square roots,
rather than pseudoinverses, carries the scalar recursion and expected
derivatives to rank loss. Distinct formal names must remain distinct there;
only contracted corrections are invariant under equivalent expressions on
a singular support. Candidate A respects this distinction.

The countable generated-language construction passes finite linear
identities and norm inequalities to the dense span and then completes it.
The same finite transpose pairing gives actual Hilbert adjunction.
Real coefficients and required continuous at-most-linear instructions are
obtained by fixed approximations before width passage. There is no new
arbitrary Gaussian direction or law-dependent extension of the action in
the proposed constrained equation. In particular, the common-carrier
comparison is meaningful in raw HS/L2 norm; it never subtracts a finite
matrix from an operator on another carrier.

For the reference endpoint, the clock field is Lipschitz on the complete
bounded-readout clock/HS/L2 class. Its direct readout, rank, and clock
bounds prevent finite-feature-time escape. The swap/sign symmetry is a
law symmetry commuting with the generated action construction and unique
integral equation; no finite-array exchange symmetry is used. If
`g=||c||`, then `g_ss>=0`, `g_s(0+)=sqrt(m)`, and `||h||>=g_s` prove
`b_s>=m>=1/10`. The resulting first `b=1` time is unique and at most ten.
Bounded `b_s` makes the physical integral clock diverge at that endpoint.
Thus the reference remains unfitted at every finite physical time and
converges exponentially in raw norm as claimed. The endpoint norm,
whole-circle prediction, and input-Lipschitz bounds follow from the raw
path-length inequality and the explicit prediction gradient.

I also checked the stronger source envelope actually used in B.2. In the
supplied C.4.6 proof, deletion removes one *initialized column* while
retaining the whole trained cavity flow, residual feedback, and learned
middle increment. Conditioning is on the cavity-good event, which does
not depend on the deleted column; the original good event is only a
subset. The forward deleted-column error costs its norm divided by
`sqrt(n)` and its reverse action costs a scalar Gaussian cavity query
divided by `sqrt(n)`. The clock comparison therefore yields a small
same-array response without a falsely small operator difference.
The learned transpose term is bounded coordinatewise by its integrated
rank formula. The conditional two-parameter Gaussian chaining has finite
dyadic sums, giving `C sqrt(p)` for query values, not their input
derivatives. Applied to the auxiliary feature equation through length ten,
the fixed-mesh joint width limits pass finite maxima on a countable dense
set and then monotone convergence gives `N#`. Strong L2 continuity and
Fubini give its required deterministic-input and active-clock meanings.
No independence between `N#` and the first roots is needed or asserted.

These dependencies supply what the candidate consumes. I did not replace
their proofs by a general appeal to a tensor-program, concentration, or
ODE theorem. Dependency verdict: **PASS for the used statements**.

## 4. Attack on A: integrated-control source transport

The strongest obstruction is that bounded raw norms alone cannot control
tails of an adapted Gaussian query or every named response coefficient.
The candidate does not make that inference. Its proof has a separate raw
proximity step, a reference clock coefficient bound, and a causal response
comparison, which I checked as follows.

The dominating clock CT5 has total mass at most `2L_*+q`. Flat pieces
have zero control and zero update. Exact integrated coefficients obey
`sum m_p<=2L_*+q`, `q_disc<=q`, and
`sum m_p e_p=q_disc`. Normalizing a pulse by this *common* source mass
works when either actual or reference control is zero; there is no
division by the reference residual or an atom's minimum weight. It also
works for control sign changes inside a cell because only integrated
coefficients enter its Euler recursion.

CT13–CT16 are the differentiated finite source expressions with all
deterministic coefficients frozen. I checked the derivative of both
rank expansions and all three upper terms: derivative of the readout,
derivative of the current upper preactivation, and the product combining
them. At a passive current slot only its one direct forward source gives
the diagonal `E[c phi''(Z)]`. Identical inputs do not create extra
diagonals. A zero update coefficient has zero lower injection; its upper
pulse can affect its own current observation but cannot enter subsequent
states or a later F coefficient. Old unused passive calls can therefore
be discarded; actual training history cannot, and is retained.

For the reference anchor, feature-clock Euler has bounded clock-feature
derivatives even at Gaussian roots. A complete-answer pulse of size `qz`
changes the subsequent state by its source mass times a mesh-uniform
clock amplification. The extraction order is essential: fixed graph and
nonzero forcing, width limit, then zero forcing. The supplement proves
the required expected-source-derivative continuity by root clipping,
deterministic fixed-graph derivative envelopes, and chronological
covariance-square-root coupling. In that scalar representation the fresh
root occurs only as `slot+qz` and is independent of the centered source
groups. Consequently `E[z V^q]=q E[partial_slot V^q]` is legitimate even
for a zero-variance or repeated slot. An unforced value law alone would
not give that coefficient. The proof does not rely on it doing so.

The raw-to-clock transfer cancels the large `F'(w)` term before estimating
the defect. From CT22, differentiating the integral produces exactly the
two CT23 terms `b^2 partial_p w` and `|b| partial_p b`, with the displayed
`exp(2h|b|)(1+h|b|)` factor. Capped Gaussian queries and normalized lower
pulse moments control these factors. At its injection step, division by
`h_s/2` leaves an `O(h_s)` defect; later terms sum using
`sum h_k^2<=L_* h_max`. The transformed pulse propagation CT24 has
bounded deterministic gates and D rows, avoiding the unbounded raw Q
multiplier. Its clock comparison pulse is pointwise bounded. Thus raw
field proximity and the summed differentiated defect give CT25 with
only earlier unknown beta rows. Fixing `B_cl+1` before choosing the mesh
closes this first cap. This is uniform in physical horizon because its
clock length is bounded.

Under a temporary cap for a general controlled program, the raw direct
bounds are valid independently of that cap: summing `mass * preceding
readout` gives `||K||_HS<=L^2/2`. CT27 is Gaussian plus a bounded sum,
not a claim that the adapted query itself is Gaussian. Jensen over the
deterministic source masses yields CT28 without temporal independence
or a random maximum of the query history. CT15 then gives the common
all-fixed-moment envelope for normalized lower pulses. The upper causal
equations give pointwise derivative row bounds and the old-source
density estimates in CT30–CT31.

Raw proximity CT34 precedes the new cap: in CT33 only the unchanged
reference controls multiply the gradient difference, so only the two
reference-axis tails are needed. Its Gaussian tail beats the
linear-in-cutoff stability exponential. This independently supplies
`eta<=Phi(q)` and avoids a circular raw-to-source bootstrap.

For the beta comparison I checked the exact decomposition CT38; its
changed coefficient multiplies the barred contraction and its unchanged
actual coefficient multiplies the contraction difference. The lower
forcing terms are exhausted by injection, coefficient, two gates, query,
and D-row differences. Interpolation from L2 and L24 gives L12 exponent
`1/11`, since `1/12=(1/11)/2+(10/11)/24`; the weakened exponent `1/16`
is valid for small errors. At most three L12 factors give L4 forcing,
and the L4 integrating factor gives the required L2 bound. Minkowski
is used before summing past deterministic D coefficients, so no random
maximum of all past gate differences is assumed. The injection costs
sum as `sum m_p e_p=q_disc`.

CT43's upper subtraction is exact. Its barred derivative rows are
pointwise bounded, while F has density `C_B m_q`; hence summing the
upper source indices and exchanging the finite mass sums gives CT44.
Discrete Gronwall gives CT36, and current alpha is constructed before
current beta. At the first possible beta failure, every query moment
used belongs to earlier capped rows or the new forward row, whose F
coefficients are already known. The final choice `B=B_*+1`, followed
by small `delta`, strictly improves the cap. None of the constants
requires a lower bound on nonzero controls, a minimum angle, covariance
rank, or number of source slots.

Finally, current-input continuity of expected rows follows from the
`(1+|w|)` gate difference against the controlled pulse moments and the
pointwise upper row bounds. It extends the deterministic row supremum
from countably many inputs; it does not assume a random continuous
Gaussian-query supremum. The bounded remainder plus scalar Gaussian
tail then yields CT7, with a reduced exponent after taking a square
root. **A verdict: PASS.**

## 5. Attack on B: conditioning and actual added-data learning

A possible failure is that the trained first-row law need not retain a
density or be independent of its query envelope. The protected-root
argument needs neither. For each fixed vector v with both coordinates
nonzero, the event `|g-rv|_infty<=1` has probability at least a Gaussian
`exp(-O(r^2))` quantity, while `P(N>r^2)` is `exp(-Omega(r^4))`.
Their intersection therefore has positive probability for every
sufficiently large r, regardless of dependence. On it, the endpoint
clock displacement is at most `5r^2`. The minimum of `F'=cosh^2` on
the unit interval around each large root exceeds that displacement,
first confining the row to that interval and then giving the stated
exponentially small difference from the root. Thus for every direction
avoiding the finitely many perpendicular lines, the limiting tanh signs
must satisfy any putative linear feature relation. Crossing only one
of those lines forces its coefficient to vanish. Distinct nonantipodal
inputs are essential here and hold throughout NS1.

The upper readout is nonzero because an anchor prediction equals one.
Since each upper gate is strictly positive almost surely, each `delta_i`
is nonzero. Applying the first-feature Gram inequality pointwise in
the upper coordinate and integrating proves 2.7 for middle gradient
blocks; no injectivity of the trained action is assumed. Continuity of
the Gram and the delta norms on the compact angle interval gives the
strict uniform kappa in 2.8. This also conditions the anchor Gram.

Projection subtracts only the two anchor columns, so the three-input
coefficient vector has last entry one. The middle Gram bound therefore
gives `||d_alpha,K||^2>=kappa` despite that subtraction, which is stronger
than merely nonzero full gradient. The displayed upper gradient and
coefficient bounds are finite with the stated endpoint norms.

The endpoint input Lipschitz bound and the coordinate-swap symmetry give
`|f_dagger(u_alpha)|<=1/16`, hence the residual lies between `-11/16`
and `-5/16`. In the raw continuity estimates the endpoint readout is
kept as the bounded factor. The sole remaining lower gate difference
is paired with the fixed endpoint L4 query; the bounded-gate L4
estimate gives the square-root modulus 3.3. The inverse/projector
identities therefore preserve the gap on a true raw neighborhood,
without pretending that every nearby readout is L-infinity close.

I attacked the activity claim by allowing all observable motion to be
readout motion or inherited reference learning. The frozen-readout
contrast 4.1 excludes both explanations. Its derivative at the endpoint
in the actual projected field is
`-2r_dagger ||d_alpha,H||^2>=5kappa/8`, and its scalar strong-curve
gradient is justified by the supplied chain rule. The explicit
square-root derivative modulus and first-exit radius keep that sign
for one fixed positive episode. Cauchy–Schwarz gives
`|Delta O|^2<=30T_0^2 J_2`, with the factor three coming from the
declared average over three inputs, and the factor ten from the
endpoint readout norm squared. Hence a positive hidden parameter block
is converted into a positive *upper hidden activation* measurement.

Likewise the exact nonlinear risk derivative is
`(r^2)'=-4r^2 ||Pi g||^2`. On the chosen ball its magnitude is at
least `25kappa/1024`, and integration gives B.5's positive risk margin.
This is the unweighted added-atom risk, not an epsilon-scaled mixture
gain. All features and the projector evolve throughout; no initial
Taylor term is used as the finite trajectory. The time and both margins
are independent of epsilon and width. **B verdict: PASS.**

## 6. Attack on C: actual continuation and the singular limit

The main existence risk is using a tail theorem for an already existing
changed-law path to prove that very path exists. Here the controlled
source theorem is a finite-program theorem and comes first. The
one-reference gradient bound uses a bounded comparison readout and
Gaussian comparison query tails only. Optimizing its cutoff gives
`omega(z)=z sqrt(log(e/z))`, whose reciprocal is nonintegrable at
zero. The displayed explicit Osgood bound follows by differentiating
`sqrt(log(e/Z))`; it proves uniqueness and Cauchy completion without
an ambient Lipschitz assumption.

For NS3, reference prefixes end at later and later physical times,
their raw Euler terminal states approach the explicitly constructed
endpoint, and the omitted reference *suffix* control mass tends to
zero. Appended constrained Euler coefficients are computed from the
current state, the fixed atom, and an ordinary conditioned two-by-two
inverse. Their total mass and raw displacement fit strictly inside
the source and raw neighborhoods before any source conclusion is
applied. Comparing two such programs gives a uniform Cauchy family
on one countable generated carrier, extended to all parameters by
the uniform estimate. Continuous fields pass the integral equation;
positive-part tails and the readout bound pass through the strong
limit. The constructed path supplies tails for uniqueness against
any competing strong raw solution on the same carrier. Anchor
preservation is exactly `G^* Pi=0`, followed by the scalar chain rule.
No unknown changed-law path enters the coefficients.

For the actual original mixture, the proof first fixes a finite b.
Every finite Euler recursion exists, and direct bounded-gate sums
give bounds depending on b. Comparison against the *existing
reference* proves 32a without a changed-program cap. Prediction
continuity then gives the integrated *control* discrepancy 32b;
raw closeness alone is not substituted for it. These finite histories
now satisfy the source tube.

The post-b stopped Euler argument is also constructive. Each affine
step is a partial last controlled update, so its recomputed queries
are covered by the same finite-program theorem. The node row velocity
has L4 norm `C(|r_k|+epsilon)`. Along the affine segment, current
queries have L4 bounds and the strong gate/product calculation gives
the derivative of each anchor gradient in raw norm. A second scalar
integration thus gives 32c, including the factor
`h_k^2(|r_k|+epsilon)^2`. This is an along-step estimate, not a
bounded ambient Hessian assertion.

With `4kappa I<=M_k<=M_max I`, the small-step matrix contraction
absorbs the remainder to produce 32d. Telescoping its norm
inequality controls `sum h_k |r_k|` directly; it does not leave
an unbounded `O(hT)` remainder on the epsilon-inverse horizon.
The entire integrated discrepancy is bounded by the initial
prefix error, the complete reference tail, the small residual at b,
and `C tau_0`. These strict inner margins contradict the first
stopping exit. Thus the finite histories through
`b+tau_0/epsilon` exist and have uniform source bounds before
Euler completion. For separately fixed positive epsilon this is
a finite physical interval, so completion and one-reference
uniqueness apply legitimately. The reference is never stopped or
restarted on the actual physical schedule.

I checked all differentiations needed for the residual identity.
For a reached controlled curve with total rate `m(t)`, the row
velocity is L4-integrable and the pointwise readout derivative is
bounded by `m(t)`. The upper derivatives in 24 are consequently
L2-integrable. The only new lower product in differentiating a
gradient is `(w' dot v) Q(v)`; L4 times L4 puts it in L2 with
norm `C m(t)`. Coordinatewise absolute continuity and Fubini
justify the formulas and their integral identities for integrable
controls. Finite-dimensional matrix differentiation then gives
`||B'||<=C m(t)`. This suffices for the Hilbert-valued product
rule in 36, without invoking C2 regularity of the raw vector field.

The factors and sign of the residual identity check directly:

`theta'=-(1-epsilon)Gr-2epsilon v`,
`r'=-(1-epsilon)Mr-2epsilon G^*v`,
`B=GM^-1`, `V=-2(I-BG^*)v`,

so `epsilon V+B r'` is exactly `theta'`. Pairing the residual
equation with its direction gives 30 and hence 35. Over
`t-b<=tau_0/epsilon`, the quadratic integrated term is bounded
by `C(|r(b)|^2+epsilon|r(b)|+epsilon)`. The boundary terms are
bounded by `C(|r(b)|+epsilon)`. Thus the perturbed integral
equation 37 has an error vanishing after epsilon tends to zero
at fixed b and then b tends to infinity. Comparison with NS3
uses the constrained path's tails and the same Osgood modulus.

Finally `sigma=tau-epsilon b` removes the proof time shift with
cost `C epsilon b` for `tau>=tau_->0`. The constants and episode
can be fixed for every sufficiently large b, so this ordered
argument proves the single uniform NS5 limit. At tau zero the
original Gaussian state is not the endpoint; its exclusion is
necessary and explicitly made. Uniform forward-state bounds
give the whole-circle and finite named hidden conclusions.
**C verdict: PASS.**

## 7. Attack on D: finite GF, whole-circle norms, and pairing

I checked whether the growing physical horizon had been passed
through a fixed-horizon or fixed-program width theorem. It has
not. Epsilon and the law are fixed first, making
`T=tau_0/epsilon` a finite number. All proxy meshes, cutoffs,
and observation nets are fixed before width tends to infinity;
their sizes may depend on this fixed epsilon. No simultaneous
rate is extracted.

Actual finite GF is global by smooth finite-dimensional local
existence and raw energy dissipation. On a fixed finite horizon
the displacement is bounded by `sqrt(T L_n(0))`, the middle
operator by its initialized norm plus its Frobenius increment,
and the readout coordinate derivative by `2sqrt(L_n(0))`.
These are legitimate width-independent bounds on the stated
initial high-probability events. They are used for stability,
not as a source-tail theorem.

At a fixed population Euler graph, the finite proxy uses the
actual initialized arrays and both directions of the same
middle matrix. Its population coefficients are causally
determined proof approximants. The fixed-program result and
finite rank contractions identify its law; a one-reference
cutoff induction includes its actual small Gaussian readout.
This induction first fixes the cutoff against oracle L2
fields and uses their continuous positive-part second moments,
then removes it. It does not require width-uniform Lipschitz
constants for arbitrary finite parameter balls.

The distinction between hard and soft RMS tails in D.2 is
correct. The soft norm is one-Lipschitz, its squared value
is a valid continuous quadratic observation, and
`tau_(R,n)<=2 sigma_(R/2,n)` gives the hard-tail estimate.
Extra fixed interpolation nodes and the uniform forward/Q
L2 time comparison extend these estimates along each affine
proxy segment. Gaussian population tails then beat
`exp(C_T R)` in the actual/proxy comparison. Choosing the
cutoff first and the mesh second, before the width limit,
gives arbitrarily small same-carrier raw error. It is not
necessary or valid to remove the cutoff at a fixed positive
mesh; the candidate explicitly avoids that order.

The finite input-Lipschitz constants have exactly the factors
`||c||/sqrt(n)`, `||W2||_op`, and `||W1||_F/sqrt(n)`.
Finite time and circle nets therefore upgrade scalar proxy
limits to the advertised entire-circle uniform norm. The
forward hidden RMS comparisons pass the finite named
observations. Lower backward observations use the same fixed
oracle cutoff argument when their gate factors change.

For NS9 the reference and mixture proxy programs are united
on the *same* initialized arrays, including readout. The
fixed-program joint same-layer law identifies the mixed
activation products. Same-carrier RMS approximation and
Cauchy–Schwarz pass these products to the actual two flows.
Only after this fixed-positive-epsilon width limit is the
population reference allowed to tend to its endpoint as
epsilon tends to zero. Thus neither a finite trained endpoint
nor an arbitrary coupling between separate hidden marginal
laws is being assumed. The limiting hidden statistic is the
one in NS7, which measures post-reference added-law adaptation.

On the uniformly bounded prediction range the added squared
loss is continuous. The strict population margins and the
finite union bound give the joint probability statement for
gain at least `a/2` and hidden statistic at least `j/2` in
exactly the displayed iterated order. **D verdict: PASS.**

## 8. Exact computation and scope-edit evidence

The required deterministic certificate was run afresh, once, without
changing its source. There was no training simulation or parameter search.

Working directory: `/home/amir/Codes/PDE`.

Exact certificate command:

```text
/usr/bin/python studies/nonlinear_prediction_selection/verify_reference_certificate.py
```

Environment: Python `3.10.12 (main, Aug 31 2026, 10:18:17) [GCC 11.4.0]`;
platform `Linux-5.15.0-151-generic-x86_64-with-glibc2.35`.
Elapsed time recorded by the wrapper: approximately 3.29 seconds.
Exit status: **0**. Stderr: empty. Actual stdout:

```text
[0.392108947877, 0.396376711612, 0.233120735618, 0.339792209687, 0.631761866359]
```

The assertions use `fractions.Fraction`, not these printed decimals.
I checked the series upper remainder ratio, the alternating arctangent
bounds and Machin identity, the density bounds, the Gaussian tail addition,
and outward rounding to denominator `10^12`. Increasing tanh and decreasing
sech expressions are bounded at the appropriate endpoints. The inequalities
`.624^2<.39` and `.633^2>.4` make the substituted scale bounds valid.
Thus the exact output certifies the weaker endpoint constants used by the
proof; it is not evidence for an unexecuted neural trajectory.

The saved complete execution record is
`data/generated/nonlinear_prediction_selection/adversarial_b_v3/certificate_run.json`.
I additionally checked with standard-library Python that the script, after
its two provenance-comment lines, is exactly the Python certificate embedded
in the supplied dependency body. That check passed.

The exact proposed edits were checked against frozen material, without
reading or changing the live destinations. The old overview sentence occurs
exactly once in the frozen global excerpt; the replacement retains it and
appends only the C.4.9 scope sentence. The proposed guide equals the frozen
guide after deleting its two identical C.4.9 additions: one addition to the
global-chapter table row and one scope paragraph. The candidate heading
produces the fragment
`c49-nonlinear-prediction-selection-during-a-finite-added-data-episode`,
and both new guide links and the new overview link use it. The append file
is exactly `CANONICAL_ADDITION_v3.md`; notation and code are listed unchanged.
The new guide states the finite episode, added-atom risk, hidden adaptation,
and width-first GF capture, and excludes a changed-law final endpoint and
out-of-sample guarantee. It does not enlarge the theorem's scope.

These deterministic scope checks exited zero and are recorded in
`data/generated/nonlinear_prediction_selection/adversarial_b_v3/scope_checks.json`.
All new quantities needed by NS1–NS9 are defined in the candidate or the
included canonical dependencies. Its references to the scalar-gradient
argument in A.4 refer to the supplied chapter's A.4, and its clock/source
references have their full proof bodies in the packet. Local equation-number
reuse is explicitly declared and does not change a normalization. No
coefficient, proof, or endpoint definition depends on an author component,
historical report, generated training array, or another study.

Scope-edit verdict: **PASS**. Validation of the eventual live assembled
edition remains a separate integration step; this review checks the exact
frozen additions and supplied destination context.

## 9. Rechecked fingerprints and completion evidence

All supplied manifest hashes matched at the initial fingerprint check before
certificate execution. All eleven input files were hashed again after the
reads, computation, and scope checks; they were unchanged. The review
manifest's own hash was also recorded and rechecked. A final post-report
hash check is retained in the scratch evidence.

| File | SHA-256, unchanged from initial to final check |
|---|---|
| `REVIEW_ASSIGNMENT_v3.md` | `7c31e8ba586e28769352704c70c8b98bfdfbf32924a9b9de7daee23cc4204cec` |
| `CANONICAL_ADDITION_v3.md` | `c858c7b41d90b490871450b8bf7494afe8c6cd7f39bebd3f1faa89f3604a5879` |
| `PROPOSED_EDITS_v3.json` | `46e1c92ed0d6c78940c8a7f60a966ca357e1883317ba8bb3cc977493da40e73f` |
| `PROPOSED_GUIDE_v1.md` | `d5ecdfd35fbddd511d98dccd148a9a9e840f5a4c814658f930c5732bab218bc5` |
| `DEPENDENCIES_GLOBAL_v1.md` | `6ebdaf1a3b28bdd07244a7b8c1bb882a36c7c525c4170cd25af4d7661ab7d942` |
| `DEPENDENCIES_GAUSSIAN_v1.md` | `c95e358f6bb9741858e6293dabacf4fee01927a23cda1289cd3cd17e85a1ee77` |
| `DEPENDENCY_GUIDE_v1.md` | `5210ccd284ea79215d81c18f2fba93762538cfb1bb5b6b295c6e33e558225e1f` |
| `DEPENDENCY_NOTATION_v1.md` | `199bb0786f632198a071d220544dd61ad54b24643f07f8232254c75b6f81500b` |
| `DEPENDENCY_MANIFEST_v1.json` | `75f8b2ae6716fb88d8f89306b78a104a2bffc62e1bf67bb64474b8298d5b415a` |
| `REVIEW_MANIFEST_v3.json` | `394c052a06f241d36dd9b502dff26b121796323787c2608a50e6b5f9bb61608e` |
| `verify_reference_certificate.py` | `07c51c139ebf66912ef7b730201dcc71581b11355dd29dbee6140bf2bba63118` |

The initial and final hash records are `initial_hashes.json` and
`final_hashes.json` in the owned scratch directory. The post-report check
also records the report's own fingerprint, allowing this original report
to be preserved unchanged.

| Component | Verdict | Unresolved objection |
|---|---|---|
| Architecture, metric, loss, physical clock and actual finite readout | PASS | None |
| Included Gaussian/action, scalar-chain and reference dependencies as used | PASS | None |
| A: common-mass named-source transport, raw anchor, all-history cap | PASS | None |
| B: endpoint conditioning, uniform added-risk and paired hidden margins | PASS | None |
| C: construction, uniqueness, actual-mixture continuation, derivatives and slow limit | PASS | None |
| D: actual finite GF, whole-circle prediction and same-array observations | PASS | None |
| Exact rational certificate and correspondence to embedded source | PASS | None |
| Exact new scope text, definitions and local navigation | PASS | None |

Every target obligation was addressed against the stated model and quantifiers.
No necessary bridge remains conditional on an unproved continuation, tail,
ambient C2, finite-endpoint, or simultaneous-limit assertion. The verdict is
therefore **PASS**, with no required corrections. It is a mathematical review
of this fixed package, not permission to change established material or a
substitute for the separate integration and user-approval stages.
