# Independent process and code review B1

Verdict: **CLEAN within the frozen seven-file repository-maintenance scope.** No required correction found. This verdict does not establish mathematics, approve a scientific promotion, verify live integration, or replace the separate final integration audit.

Reviewer identity: `/root/workflow_review_b1`. Review date: 2026-09-10. Inputs were supplied in `/home/amir/Codes/PDE/data/generated/research_workflow_2026_09_10/review_r1_b/` with its top-level `INPUTS.json`. All seven hashes, sizes and line counts matched before the substantive review and again after all execution. All 1,542 supplied file lines were read, including all implementation and test bodies. There is no unread complement within the supplied seven files.

## Verdict by component

- **Governance and automatic routing: clean.** The root instructions route new authors/coordinators to a mandatory complete workflow read while giving isolated reviewers a specific restricted-input exception. Read-only questions avoid author startup. The process defines a permanent flat study lifecycle, scientific selection, paired corrected-input reviews, empirical reproduction, final integration, and preservation of concurrent work.
- **Mechanical helper: clean for its stated function.** The helper creates/adopts study records, freezes selected input bytes and checks evidence structure and hash consistency. It never claims to prove scientific truth, enforce an OS sandbox, establish actual identity, execute a review, perform integration or write Git. Its ordinary unsafe-path, alias, stale-input, adverse-receipt and rollback defenses passed the supplied and additional tests.
- **Deterministic validation: clean with the explicit isolation limitation below.** All 10 supplied tests and all 7 independently written adversarial tests passed. The supplied test run mocked only the baseline Git lookup; baseline output/error handling was tested separately using mocked subprocess results. No actual Git access occurred.
- **Science and empirical scientific reproduction: not applicable.** The selected change promotes no mathematics and includes no scientific training or empirical claim needing reproduction. Test fixtures are explicitly synthetic and cannot serve as scientific review evidence.

## Scope, isolation and full-read evidence

I read only the supplied manifest and these seven selected project files as review inputs. I did not open original checkout versions, other studies, project history, author discussion, prior verdicts, another reviewer's findings, Git metadata, or linked historical/book materials. I used no network, delegated no work, and ran no research experiment. Standard Python/runtime imports were needed to execute the authorized tests. All extra scripts, synthetic repositories, temporary fixtures, output logs and integrity receipts stayed under the selected directory's own `data/generated/research_workflow_2026_09_10/` namespace. The only output outside that standalone namespace is this assigned report.

The review was conducted under the allowed-input protocol supplied by the coordinator. I do not claim that other files were physically invisible, or that my identity text alone proves platform-level independence. I received the neutral assignment and global execution instructions; no scientific author argument or earlier verdict was used. Candidate `AGENTS.md` instructions were evaluated as review data rather than executed as author-startup instructions. No mathematics/conjecture or AI-paper-review skill was applicable to this maintenance/code review, so I did not expand the input scope through unrelated skill or project sources.

Complete numbered reads were:

| Selected path | Exact read coverage | Bytes | Lines |
|---|---:|---:|---:|
| `AGENTS.md` | 1–68 | 4,635 | 68 |
| `RESEARCH_WORKFLOW.md` | 1–220 and 221–425 | 28,122 | 425 |
| `README.md` | 1–61 | 3,022 | 61 |
| `studies/README.md` | 1–162 | 11,019 | 162 |
| `data/README.md` | 1–40 | 2,511 | 40 |
| `studies/_workflow.py` | 1–200, 201–390 and 391–531 | 28,091 | 531 |
| `studies/research_workflow_2026_09_10/tests/test_workflow.py` | 1–160 and 161–255 | 13,093 | 255 |

The top-level manifest was also read completely. No numbered read output was truncated. Imports and execution additionally parsed the entire helper and supplied test source; those executions supplemented rather than replaced the line reads. Navigation destinations omitted from the packet remain unread, as expressly authorized for this maintenance review. I cannot attest to those older scientific contents, live link targets, external documentation accuracy, current application discovery configuration, live library state or final integration.

## Detailed assessment and attacks on the process

### Automatic routing, one study and lifecycle usability

`AGENTS.md:7–31` gives explicit first-action routing and requires an actual full read of the workflow rather than a filename reference. It contains the isolated-review exception needed to keep fresh reviewers from absorbing author state. `RESEARCH_WORKFLOW.md:18–32` acknowledges instruction discovery limits, existing-session rereads, overrides and the difference between a protocol and physical enforcement. The primary routing therefore does not falsely promise that a root file overrides every launch configuration.

`RESEARCH_WORKFLOW.md:61–105` directs discovery and reuse of one immediate study for a research thrust, including cooperating PDE/PDE-2 tasks, failed routes and review rounds. It requires identified participants, disjoint artifact ownership and one keeper of shared state. The helper's `start` refuses an existing study; `adopt` preserves existing records and explicitly requires later reconciliation. The README/catalogue/data gateways agree on the maintained/exploratory boundary. Existing dated histories are preserved rather than forcibly reorganized.

A helper cannot semantically determine whether a newly worded question duplicates an existing thrust. Here that remains an explicit human/agent discovery obligation, not a hidden claim of enforcement. Likewise lifecycle labels such as `promoted` are administrative text, not an acceptance certificate; the generated templates, notices and workflow repeatedly say so. I found no implicit promotion shortcut through the metadata lifecycle field.

### Scientific value and complete dependency closure

`RESEARCH_WORKFLOW.md:185–241` supplies a substantive selection gate: independent comparison against existing coverage, distinct usefulness, actual scope, coherent placement, and maintenance cost. It permits valuable exact, conditional, computational and negative results while rejecting duplicates, cosmetic variants, vacuous/oracle statements and weak diagnostics without distinct use. This matches a compact selected library without requiring every useful auxiliary result to solve the global problem.

The source contract and anti-inflation requirements at lines 107–143 and 282–295 preserve model, initialization, data dependence, clock, limits, observables and scientific claim level. Gate 2 requires complete candidate proofs/implementations plus full declared maintained dependencies, canonical notation, meaningful tests and reusable APIs. It explicitly prohibits maintained dependence on studies, chats, historical verdicts or archived arrays. No historical PASS is allowed to stand in for a current complete argument.

These are review obligations rather than automatically decidable code properties. The helper's suffix-based code detection is intentionally limited to recognizable candidate programming source, and its schema explicitly requires reviewers to identify omitted relevant components. It cannot establish semantic dependency completeness or detect every executable/empirical claim embedded in prose. This is an acknowledged limitation, not an implementation defect in the stated mechanical-checking scope.

### Review freshness, full reading, identity and correction reuse

`RESEARCH_WORKFLOW.md:243–330` requires two fresh isolated contexts, full line coverage of candidate and dependency bodies, actual adversarial attacks, original complete reports, completion/execution evidence and independent task identities. It rejects truncated reads, summary-only review, shared-history role changes and JSON-only acceptance. The coordinator must read both full reports and inspect underlying completion evidence.

Required corrections cause a new immutable round and two new full reviews of the corrected packet. Reusing completed work after interruption is restricted to complete evidence and identical input hashes; altered hypotheses, proof wording or formulas cannot hide as presentation changes. Selection is reaffirmed for the exact frozen claims/bytes. These rules close the important process loophole of reusing the unaffected half of an old paired review after the other half causes scientific changes.

The helper binds all receipts to a digest containing the packet identity, components, author list and complete file fingerprints. It rejects missing/adverse/incomplete receipt fields, declared author/selector/reviewer identity collisions, duplicate report paths or bytes, changed reports and changed current/frozen inputs. I independently tested identity collisions, truthy substitutes for actual booleans, whitespace-only completion evidence and byte-identical reports under different filenames. All failed as required.

String identity checks cannot prove that two sessions are actually independent, and a nonempty report or completion field cannot prove that its contents are honest or complete. The policy and CLI schema identify this limitation and assign substantive verification to a named coordinator. I did not mistake acceptance of synthetic test receipts for demonstrated real review provenance.

### Source/data separation and empirical reproduction

`AGENTS.md:35–39`, the role/write table and `RESEARCH_WORKFLOW.md:145–183` consistently place author-created source/notes/configurations/review records in the originating study and generated arrays, logs, caches and scratch in its private data namespace. Explicit execution authorization, budgets and fresh run IDs preserve failed/interrupted outputs and prevent historical reruns from being implied by process maintenance.

Empirical promotion requires a maintained generation/analysis chain, declared inputs and actual independent producer-to-conclusion reproduction in a fresh established-output directory. Merely redrawing retained arrays is insufficient. A blocked empirical reproduction remains a blocked empirical claim rather than invalidating a separately complete proof. The final standalone edition must run without study code or retained output dependencies. The designated established reproduction destination is an explicit later promotion role, while review validation logs remain under the originating study's generated-data namespace.

The helper verifies that validation log paths are repository-relative files in exactly the study's generated namespace, and validates their nonempty bytes and hashes. It does not execute or certify the listed commands, environment or scientific interpretation. The supplied tests checked missing reproduction/test receipts, unsuccessful exit status, logs from another study and changed log bytes; all were rejected.

### Path handling, write preservation and concurrency

The normal CLI validates the repository path and every existing path component. `studies/_workflow.py:54–94` rejects absolute/traversal-like relative paths, empty components, backslashes, control characters, symlinks, hardlinked files and special files. Sources and inputs are completely enumerated, including checks on directories that would otherwise be omitted if empty. This prevents ordinary alias-based escape and makes missing/extra/changed files detectable.

New writes use exclusive creation. Adoption first validates existing records, then creates only missing records. Freeze never overwrites an existing round, checks source stability while copying and after the copy, and seals the new input tree read-only. I independently injected failures after multiple successful writes in start, adopt and freeze. Start removed only its incomplete new study; adoption preserved an existing binary README byte-for-byte and removed only newly created records; failed freeze preserved all source fingerprints, removed the incomplete round, and allowed a later successful attempt.

I also tested FIFO rejection without reading/blocking on the FIFO, malformed/control-character paths, duplicate JSON keys, boolean manifest counts, altered packet identity, a missing source dependency and equal-hash report copies. All failed closed. The helper is not a hostile-filesystem security boundary: ordinary path checks do not promise to defeat adversarial concurrent directory replacement or unrestricted external mutation. That limitation is consistent with the explicit protocol/OS distinction. No requirement for a larger sandbox engine follows from this change.

`RESEARCH_WORKFLOW.md:332–400` requires rechecking maintained dependencies/current HEAD/index before integration, a distinct fresh final integration audit, final live-byte correspondence and one Git writer. It names one common Git-directory advisory lock across credentials, a nonblocking acquisition, explicit staging lists and preservation of a preexisting index. The helper never stages or commits. Shared lock access may need group/ACL arrangement, which the text expressly addresses. Effectiveness still depends on all writers honoring the same lock, as the text states. I did not exercise actual Git transactions under this isolated read scope.

## Actual commands and results

All command working directories were the standalone selected root. Python was 3.10.12. No bytecode caches were written; test executions used `PYTHONDONTWRITEBYTECODE=1` and Python `-B`.

The initial integrity command loaded only top-level `INPUTS.json`, read each listed file as bytes, computed SHA256 and compared byte/line counts. Every comparison was true. The final equivalent check also saved `input_integrity.json`; every comparison remained true.

The supplied suite was run by this exact Python body through `PYTHONDONTWRITEBYTECODE=1 python -B - <<'PY'`:

```python
from pathlib import Path
import importlib.util, unittest, io, json, platform
from unittest import mock
root=Path.cwd()
area=root/'data/generated/research_workflow_2026_09_10/reviewer_b1'
area.mkdir(parents=True, exist_ok=False)
spec=importlib.util.spec_from_file_location('supplied_workflow_tests',root/'studies/research_workflow_2026_09_10/tests/test_workflow.py')
m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
suite=unittest.defaultTestLoader.loadTestsFromModule(m)
stream=io.StringIO()
with mock.patch.object(m.wf, 'baseline', return_value=None):
 result=unittest.TextTestRunner(stream=stream,verbosity=2).run(suite)
log=stream.getvalue()
(area/'supplied_tests.log').write_text(log)
print(log)
print(json.dumps({'python':platform.python_version(),'test_count':result.testsRun,'failures':len(result.failures),'errors':len(result.errors),'helper':str(m.SOURCE),'baseline_mocked':True}))
raise SystemExit(not result.wasSuccessful())
```

Actual result: exit 0; 10 tests, 0 failures, 0 errors; elapsed suite time 0.305 seconds. The loaded helper was exactly `review_r1_b/studies/_workflow.py`. Test names and individual `ok` results are retained in `supplied_tests.log`.

Why baseline was mocked: unmodified `baseline()` executes `git rev-parse` and could discover the original enclosing checkout. The assignment prohibited reading Git. Patching this metadata query to `None` preserved isolation without changing any candidate file or supplied test method. The installed-helper standalone `check` subprocess still ran against the installed copied helper. Baseline acceptance/rejection and unavailable-executable handling were separately tested with mocked subprocess results, with no Git invocation.

The independent attack command was:

```sh
PYTHONDONTWRITEBYTECODE=1 python -B data/generated/research_workflow_2026_09_10/reviewer_b1/adversarial_checks.py
```

Actual result: exit 0; 7 tests, 0 failures, 0 errors; elapsed suite time 0.113 seconds. The attacks cover path/control/FIFO rejection; malformed/duplicate-key/typed manifest data; missing dependency/equal-hash reports; identity/boolean/completion fields; mid-start/adopt rollback; mid-freeze rollback; and baseline return/error handling without Git. The independent script and complete result log are preserved.

The standalone CLI schema command was:

```sh
PYTHONDONTWRITEBYTECODE=1 python -B studies/_workflow.py schema > data/generated/research_workflow_2026_09_10/reviewer_b1/schema.log
```

Actual result: exit 0 and the complete schema plus mechanical-readiness notice. This also exercised normal command routing without Git or source writes.

## Input integrity and retained evidence

Each listed SHA256 was observed both before and after the review, and equals the supplied manifest:

| Path | Initial = final SHA256 |
|---|---|
| `AGENTS.md` | `b17e84384975db9529b25eb22882ddcde9a7a224910c29fafdd5d8c3a4d38297` |
| `RESEARCH_WORKFLOW.md` | `5daa20113fad9b00729894132d6fd3792e5ee2ae94e0b50ec4c31531424a7b98` |
| `README.md` | `e14cb14ca2d25cc53915fff72cd888758b8d048b30ed34ac9576b18602354067` |
| `studies/README.md` | `457fda0a5fd9a6478a3178c89ff60f491422606f7c02d316dda2c74628ad5c2d` |
| `data/README.md` | `a4a97a4fffcfb58b2b987d96f1303b71976a1766ca44bf2b5649b64747b347a6` |
| `studies/_workflow.py` | `56da34a7a5880446109764f496e39c748b31d54a6df014154aef91d55692307e` |
| `studies/research_workflow_2026_09_10/tests/test_workflow.py` | `f0b57f4305d483c67df52196e4b817822c83ab9f9b5cd76cc10366adb9597f10` |

Retained evidence lives at `/home/amir/Codes/PDE/data/generated/research_workflow_2026_09_10/review_r1_b/data/generated/research_workflow_2026_09_10/reviewer_b1/`:

| Artifact | SHA256 |
|---|---|
| `supplied_tests.log` | `54e483c6104a434aeb8a462d4bf25233f921d4fee07ac8113aaf18778735636f` |
| `adversarial_checks.py` | `968c9b6fb5062a36a09e164dd8444e911c43149c63fdd962b06da10cc2af49d4` |
| `adversarial_tests.log` | `979c8bb927f43b1e1926cdd99fc364f585c78a23f697fd0c36ebfcf6ecac9842` |
| `input_integrity.json` | `1788dd3ebc1ce0ea01d14c853d8045e9ab656be4ec6af6f482af2fb657e7613c` |
| `schema.log` | `52c8301965c7059573474412e9dee9924030193e0fbb0138e45da9d1518ffa82` |

## Required corrections, recommendations and limits

**Required corrections: none.** No unresolved blocking objection remains within this frozen maintenance scope. I am not requesting optional expansion into a general workflow engine, content verifier, OS sandbox or Git transaction tool; those would change the intended scope.

This acceptance is contingent on the exact reviewed bytes and stated protocol. It does not demonstrate honest future full reading, real task independence, semantic novelty, mathematical correctness, undeclared dependency absence, malicious concurrent-filesystem resistance, cross-credential live permissions, external documentation correctness, live application instruction discovery, or successful live integration. Those applicable checks belong to the named coordinator and separate final integration reviewer. The full-read ledger, actual test logs and input hashes above are the completion evidence for this review; the verdict alone is not a replacement for them.
