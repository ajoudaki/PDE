# Fresh adversarial review: L=2, three-input partial result

Date: 2026-09-08. Review performed from the actual five current mathematical files, with no experiments and no canonical-file edits by this reviewer. This is a new mathematical check, not reliance on older reviews. The principal focus was initialization geometry, exact small-amplitude slope, necessary state/time scales, and the conditional nonlinear clock. I also checked the source/reference statements and the actual older initialized-action and Gaussian-conditioning dependencies needed to interpret their scope.

## Verdict

**CORRECT PARTIAL CLAIMS at the five hashes recorded below. This is explicitly NOT PASS for the full requested theorem.**

No unresolved mathematical gap was found in the stated initialization bounds, sharp worst-case scale, exact Gaussian leading slope, excursion/time obstructions, or conditional scalar-clock implication. The report correctly identifies the full global strong population/GF/raw-GD theorem with a polynomial activation threshold as unproved. It does not mistake the stationary zero-amplitude example, necessary divergent fitting time, failure of an affine comparison, or a nonvanishing comparator field defect for a counterexample to a positive-amplitude theorem with an amplitude-dependent rate.

One precise wording error was reported during review and has been corrected in the final source file: a bounded L2 ball is uniformly integrable in L1, whereas its squared magnitudes need not be uniformly integrable. The corrected statement now says exactly the latter and identifies the missing uniform L2 tail bound.

## Checks

1. **Initialized positivity and explicit constant.** Each tensor `u_i tensor v_ij tensor v_ik` has norm one, annihilates the other two tensor powers, and pairs with its own tensor power by at least `delta(2-delta)`. Summing the three squared dual inequalities gives the stated factor `1/3`. Gaussian integration by parts gives `E[atan(G)H_3(G)]=1-2m`; the Hermite normalization in the document therefore yields precisely the covariance `theta^2 b_3^2 Gamma^{circ3}`. The second-layer common marginal variance permits Gaussian regression with a common first-chaos coefficient at least `a`, so the PSD lower bound passes to Q2 with factor `a^2`. The elementary bound on `m-1/2`, including its probability and numerical factors, is correct. Neither step assumes nonsingularity of the input Gram.

2. **Matching Q2 worst-case scale.** For the nearly collinear triple, the vector `(1,-2c,1)` annihilates the input linear combination. The first centered second difference has L2 bound `sqrt(3)(1-c^2)`, and the scaling correction has the stated bound, giving `C_0=2 sqrt(3)+2+pi`. In the upper Gaussian layer, `2(M-cB_0)` has exactly the preceding feature-combination variance. The difference variance obeys `Var(D)<=1-c^2`. Gaussianity, rather than any independence of M, D and B0, gives `||D^2||_2=sqrt(3) Var(D)`. These facts yield the displayed nonlinear second difference and then `||v.h^2||_2<=2 theta C_0(1-c)`. The Rayleigh quotient includes the correct denominator `2+4c^2`. Choosing `c=1-delta` satisfies all three closed separation inequalities for `0<delta<=1/4`. Thus the two-sided `Theta(theta^2 delta^2)` claim has absolute constants, uniformly for `theta<=1/2`, in every fixed input dimension at least two. The additional fixed-depth initialization recurrence is also valid: Lipschitz contraction controls the same pair difference at subsequent initialized layers and the mixture identity increases the coefficient by one.

3. **Exact initial slope.** The common-sector eigenvalue of Q1 is exactly `theta^2 eta`. The exchangeable second-layer Gaussian can be coupled as `alpha_theta X_i+theta sqrt(eta/3)G`; the orthogonal-sector coefficient tends to one. Boundedness and Lipschitz continuity of arctangent justify the claimed L2 limit of the scaled feature sum. Its two limiting components are independent and centered, each with variance `3 eta`. Hence `y^T Q2 y=6 eta theta^2+o(theta^2)` and `||H_0||^2=(2/3)eta theta^2+o(theta^2)` are exact. The initial loss-derivative and exponential-rate constants follow with the specified loss normalization. These calculations refute only a uniform positive amplitude-independent rate, not a rate depending on theta.

4. **Joint raw distance and necessary fitting time.** At loss at most `3/8`, residual Cauchy--Schwarz forces the sample prediction sum to be at least `3/2`. The exact equilateral cancellation then yields `||C||(1+a||A||)>=1/(pi theta)`. Writing `x=||C||` and `y=||A-A_0||_HS`, the actual direct-sum raw metric gives `x^2+y^2<=R^2`. Therefore `x(3+y)<=3R+R^2/2`, with no unjustified separate saturation of the two blocks. Solving this quadratic gives `R>=sqrt(9+2/(pi theta))-3`. For any existing true strong GF reaching the target, energy and Cauchy--Schwarz give `R^2<=3T/2`; the final time inequality and `liminf theta T>=4/(3pi)` are correct. The strong-population existence condition and distinction from the finite nonzero random readout are explicit. The time obstruction also legitimately excludes an amplitude-independent exponential estimate with any fixed finite prefactor that would force bounded fitting time.

5. **Conditional scalar clock.** On the assumed canonical strong ascent trajectory, `J'=||grad_hidden J||^2+||H||^2` and `c'=J/c`. The derivative of `J/c` is nonnegative by Cauchy--Schwarz; its initial limit is `||H_0||`. Thus `J'>=||H_0||^2`, proving the finite ascent-horizon and path-length bounds if the trajectory continues. The physical identity additionally requires equal predictions; the companion explicitly requires that the canonical construction or uniqueness justify this symmetry. With these premises, `d/dt log L=-6J'` proves the displayed decay constant. Strong continuation through the first hit gives bounded gradient on the compact ascent interval, hence `1-J(s)<=M(s_*-s)` and divergence of the inverse physical clock. This is a valid conditional global parametrization, not an unproved continuation claim disguised as one.

6. **Source and reference scope.** The source derivative formulas retain both past-response families, the actual current return, formal frozen-coefficient derivatives and singular covariance conventions. They agree with the actual finite-query source construction in the older `sources/L3_LOCAL_COMPLETE_PROOF.md`, which I read at its source/conditioning sections rather than consulting old reviews. The conditional moment and derivative envelopes use marginal Gaussian moments, Volterra bounds and Jensen in time; they do not assume independence over time. Their production bounds do not close the coefficient premises, and the document explicitly says so. The initialized operator bound used in the geometric and reference arguments is supported by the older `AFFINE_CERTIFICATE.md` Section 1, also read directly. For the nonlinear frozen-bottom reference, the invariant `BB^*-C tensor C=P_E`, Gram lower bound, factor bound and finite-dimensional continuation are consistent. Its rank-one equilateral states give the stated nonzero bottom-force limit, and the restored top-nonlinearity terms have vanishing error by the given operator bounds. The force comparison is correctly restricted to fields at reference states and is not promoted to a statement that actual trajectories differ or fail to fit.

## Final reviewed SHA256

Directory: `/home/amir/Codes/PDE/studies/mean_field_peeling/three_sample_odd_activation_depth2`.

| File | SHA256 |
|---|---|
| REPORT.md | `25cfd1e93b619b6c5aabc674cab7170199024ac7a3cb700365a23048e62d341f` |
| CONTRACT.md | `e4acafb4b6dee57ab867bd3947a92b6b25d994c51e62bbfaeaaccebede6c113d` |
| GEOMETRY_AND_NECESSARY_SCALES.md | `37a6c0bf1715ab5a182ecfd9a6a3213cddf9cb4a0ae95fd441a5a6a7d46f61e6` |
| SOURCE_AND_CONTINUATION.md | `4c3b8c54893a417ce210f73eaae2b8e71a71eac8f3205f5fdc32da9480c2a568` |
| NONLINEAR_REFERENCE.md | `778793eb7a832f16dadc5b5746e9b705c02ca08a74ab9e3f81748962a958a422` |

The original requested threshold theorem remains unproved. This review certifies only the mathematical partial claims and their stated conditional/negative scope.
